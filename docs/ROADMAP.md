# Garden Tracker — Roadmap

> **Purpose:** Single source of truth for *where we are* and *what's next*. Updated at the end of every session by Claude. Read this at session start (after CLAUDE.md and VISION.md) to know exactly where to pick up.
>
> **Conventions:**
> - 🟢 Active / in progress
> - 🟡 Planned, ready to execute (clean-pass plan exists, awaits user greenlight to build)
> - 🔵 Designed but not yet planned (decisions made; needs plan-audit before build)
> - 🟣 Awaiting design / Phase 2-7 work
> - ⏸️ Parked — blocked or deferred (with WHY noted)
> - ✅ Shipped

---

## 1. Current focus

**As of 2026-06-11 (latest — Photo Import comprehensive cleanup, Dispatch-spawned) — 2 strict bugs + PO-chrome mislabel + dead-path removal ✅ shipped `e552799`.** Pre-tester sweep of the Photo Import path (Add Plant → Photo Import + Add Seed Packet → Photo Import). **Two strict bugs:** (1) multi-photo crop queue silently dropped every second photo — `ImageCropModal` fires `onConfirm` THEN `onClose` on Apply and `BatchAddSeed` popped the crop queue in both (single pop now lives in `onClose`); (2) the hero page never restored `defaultProfileType` from storage, so Add Plant → Photo Import (and the PO→hero roundtrip) with type Permanent saved as seed — wrong `profile_type`, `is_permanent_planting:false`, bogus care templates + harvest task. **PO-chrome mislabel:** hero page hardcoded `source:"purchase_order"`, so photo imports rendered the PO review chrome ("Step 1: Import Review", PO field labels) + wrote a bogus `source_po_ref` journal artifact in addPlantMode; `source` now threaded through `PendingPhotoHeroData`. **Image-handling parity:** cropped captures posted uncompressed natural-res base64 to `/api/seed/extract` (every sibling path compresses) — compression moved to crop-confirm. **Dead-path removal (NORTH_STAR "No duplicate paths"):** `handleSaveAll` (113-line direct DB save ignoring `addPlantMode`) + `processFilesWithGeminiAndRedirect` + `processOrderConfirmation` + their states/JSX + unused `onSuccess` prop (5 callsites) deleted — which makes **`/vault/import/photos/page.tsx` provably orphaned (escalated; Syd greenlit deletion same day → removed in `6250025`)**. Capture CTAs `bg-emerald`(=500)→emerald-600/700 per VISION §8 split (BatchAddSeed + BatchAddSupply, 2-of-2 siblings). Audit + matrix: [docs/plans/photo_import_cleanup_2026-06-11.md](plans/photo_import_cleanup_2026-06-11.md). Brief caveat: the Dispatch queue memory (`project_gt_queued_chapters`) was unreadable from this machine — findings independently re-derived from code; reconciliation ask in §5. See §5 entry.

**As of 2026-06-11 (prior — Form Add/Edit parity sweep, Dispatch-spawned) — form primitives converge across all Add/Edit flows ✅ shipped `650a1e6`** (+ Syd-greenlit follow-up `33a9758` un-gating the editGrow "Number of plants" field for seasonal plantings — the E1 escalation from that sweep, closed). Systemic sweep (not per-finding nitpicks) locking the form-primitive set per VISION §8 locks + NORTH_STAR "No duplicate paths": Save buttons converge on the emerald-600/700 CTA token (editGrow "Edit Plant" + AddPlantManualModal were `bg-emerald-900 hover:opacity-90` — the FAB-saga ADJACENT park from `8b744cb` now closed; "Add packet"→"Add Packet" casing; "Save Changes"/"Saving..."→"Save"/"Saving…"); 8 form-level error sites converge on `<FormError>` (EditPacketModal / Edit Plant Profile / editGrow / AddPlantManualModal / PlantingForm / QuickLogModal / BatchAddSeed ×2); `SubmitLoadingOverlay` added to the 4 modals that had no submit feedback (EditPacketModal / Edit Profile / editGrow / AddPlantManualModal). **Queued status-default findings verified ALREADY RESOLVED** (all 6 `grow_instances` inserts hardcode `status:"growing"`; enum collapsed by `67a6344`) — no code. **Escalated to Syd, not shipped:** un-gate "Number of plants" in editGrow for seasonal instances (collected at Add for both types, editable only for permanent — recommend un-gating; one-line change). Parity matrix + ADJACENT register: [docs/plans/form_parity_sweep_2026-06-11.md](plans/form_parity_sweep_2026-06-11.md). See §5 entry.

---

*Prior 2026-06-11 (pre-tester hardening bundle, Dispatch-spawned — S1 unauthenticated `/api/invite` deleted + feedback device-context metadata (migration `20260611000000` applied) + Garden Tracker tab title `9c8e582` + Vercel Web Analytics follow-up `58ee57f`; bundle fully closed — full content in §5 entry) + 2026-06-11 (branded 404 + error-page hardening, Dispatch-spawned — branded `not-found.tsx` + `RouteErrorFallback` hardening (no raw `error.message`, digest reference line) + 13 route-level error surfaces converged + `global-error.tsx` inline-styles rebuild `1c615ab` — full content in §5 entry) + 2026-06-10 (Seasonal/Permanent toggle sweep, Dispatch-spawned — legacy type toggle removed from ALL Add Plant creation flows `c29ed34`; opt-in `deriveProfileType` lifecycle→profile_type derivation; parked follow-up: PO/photo-imported permanents' grow instance keeps `is_permanent_planting:false` pending a reconciliation decision — full content in §5 entry) + 2026-06-10 (pre-tester onboarding copy ship, Dispatch-spawned — G1/G2/G3 onboarding fixes + Sanctuary-era vocabulary sweep `0b3b288`; audit follow-ups G4 zone-helper link / G5 step-1 deep-link / G6 Quick Start re-entry still open, undecided; Syd dogfooding the new-account flow — feedback feeds the [onboarding persona audit](plans/onboarding_persona_audit_2026-06-10.md) §6 monitor list) + 2026-06-10 (Sprint 4 Chunk B execution, Dispatch-spawned — profile display split + pill+detail + responsive Propagation + anchor nav + couldn't-find UX `af67c59`, the Sprint 4 TESTER-READY milestone; next per plan-doc §4: C6 auto-naming, then Chunk D) + 2026-06-09 (`Groups follow-up ship` chat — Groups hardened + extended `c7fe6f6` + migration `20260609120000`: tabs scaling fix, three assignment doors, auto-journal on group change, multi-select FAB cleanup, annual/perennial relocation, Manage-modal hang fix, delete-group reassign flow, ONE-group-per-plant lock) + 2026-05-30 (variety presentation reversal chat — `dc173ef` stacked-italic-no-parens canonical + VISION §8 lock) + 2026-05-30 (`Sprint 5 polish bundle` chat — `332f0d6` SHOULD #21 overlay fix + PacketVaultView casing leak + #19/#20 VERIFIED CLOSED; SHOULD bucket 100%) + 2026-05-29 (`Sprint 4 close-out + doc-hygiene` chat shipped 2026-05-30 — Sprint 4 fully shipped: Established plants `ff4ac2e` + AddPlantModal Manual symmetry `f77fe75` + FAB cohesion sweep `9dc8c2d` + SHOULD #16 `dc63fa9` + SHOULD #17 `e684331` + SHOULD #18 caught from `7242cb6` pre-sprint) + 2026-05-29 (`sprint-3-ship-b-b4-polish` chat — Sprint 3 Ship B fully shipped: B1 `f5c7c76` schema + B2 `04ef744` unified Garden + GroupTabs + B3 `cb6f050` Manage Groups modal + B4 `0b73ec7` onboarding tooltip + ICON_MAP gap + REDESIGN doc Zones→Groups rename) + 2026-05-28 (`ia-ship-a-plants-tab` chat — IA Ship A `9142aad` + `34e8948`: Plants tab top-level route + `+Add Variety` FAB chip + AddVarietyForm extraction) + earlier 2026-05-28 (`plant_instance_phase_a` chat — Plant instance Phase A status foundation `67a6344` + same-day un-archive UI follow-up `f29ae24`) lead-lines archived per §1 2-paragraph cap — full content preserved in §5 + §6.*

*Earlier 2026-05-28 (`phase2-followup-polish-4items` chat — Phase 2 follow-up polish bundle `88ba080`) + 2026-05-28 (`grow-now-phase-2-zone-enrichment` chat — per-profile zone-window AI enrichment `dcfdbe4`) + 2026-05-27 (`gt-plant-profiles-to-library` chat — Vault sub-tab "Plant Profiles" → "Library" surgical rename) + 2026-05-27 (`gt-seed-propagation-context` chat — Vault Propagate / Save Seeds contextual empty-state `39d5faa`) + 2026-05-27 (`gt-empty-cell-convention` chat — em-dash empty-cell lock + app-wide sweep `29f37a1` + `0bc4342`) + 2026-05-27 (`gt-icon-style-convention` chat — chrome vs content icon-style split lock `cea6c71`) + 2026-05-27 (`fab-tree-route-to-modal-conversion-cluster` chat — From Vault in-menu overlay `bde3745`) + 2026-05-27 (`gt-title-case-split` chat — casing-policy reversal + app-wide sweep `3ee1063` + `5b5617c` + `cb6a203`) + 2026-05-26 (`continue-from-prev-chat-fab-form-submit-saga` chat — FAB-form submit pattern standardization saga across 5 commits `fc1463d` + `36d0ffb` + `003e3b4` + `8d6562f` + `ba9319f`) + 2026-05-26 (`continue-from-stalled-fab-modal-consistency` chat — modal-consistency cluster) lead-lines archived per §1 2-paragraph cap — full content preserved in §6 entries.*

**As of 2026-05-25 (latest — `gt-casing-theme3-a1` chat) — 3 ships:** casing-cohesion sweep (`a78dbd6`) + §3.12 theme #3 Phase A.1 page-label renames complete (`b669a2a` + `8310070`). **`a78dbd6`** = 22 Title Case → sentence case edits across 7 files in the FAB tree (4 of 6 named modals — QuickAddSeed / QuickAddSupply / QuickLogModal / PurchaseOrderImport — plus 3 same-pattern siblings via Pass 3 sibling sweep — EditJournalModal / CareScheduleManager / BatchAddSeed); NewTaskModal + AddPlantModal CLEAN (already sentence case, "Active Garden" preserved as proper-noun page label); Volume pills (Full / Partial / Low / Empty) preserved as single-word categorical enum tokens; mid-execution amendment caught aria-label cohesion ("Vendor / Nursery" ×2 in QuickAddSeed flipped lowercase to match visible label). **`b669a2a` + `8310070`** = 4 plain-language page-label renames per §3.12 catalog theme #3 P0 Phase A.1: Vendor Scorecard → "Vendor ratings" (Settings menu + Vendors page h1); Monthly pulse → "Monthly view" + Annual roadmap → "Yearly view" (Home subtitle + Home buttons + Schedule view-toggle; Option 3 calendar-app-convention pair locked by Syd); Plantable → "Plant this month" + matching title/aria-labels on calendar/page.tsx (verb-choice driven by data-source audit — Syd's *"ready to sow? like plant by seed?"* challenge caught what would have been a wrong-narrowed label; audit found `plant_profiles` data source includes ALL profile types (perennials, established plants, anything with `planting_window`), not seed-only; broad-verb "Plant" covers sow + transplant + plant-out paths). Drilldown modal h2 "{plantType} – Seed inventory" preserved (widget-title-vs-drilldown asymmetry captured as ADJACENT for separate data-shape chunk). Phase A.2 (Vault + Shed; URL `/vault/...` + sidebar nav + 50+ files; needs redirect strategy) deferred to dedicated chats. **Drops from Phase A scope** (zero user-facing strings in src/): Polyculture / True leaves / Hardening off / Grow instance (purely internal type/component name). **Defers to §8 settings IA chat** (dev-gated since `4a8b04c`): Repair hero / Extract cache. NORTH_STAR cite: "Take mental load OFF the user" (jargon decode → plain action language) + "No duplicate paths" (consistent casing across siblings IS the cohesion expression) + data-honest framing (verb matches the actual data shape, not a narrower interpretation). Tests 434/434 across all 3 commits; build clean; concurrent-fetch 0/0 pre-commit each. Worktree Preview MCP skipped per documented `.env.local`-absent condition (functionally auth-blocked: AuthGuard redirects before FAB modals + Home + Schedule + Settings + Calendar render); Syd phone-verifies on prod (dogfood plan for all 3 commits surfaced in chat).

---

**Earlier 2026-05-20 (`intelligent-pare-f7abd4` chat) — BUGS U7 ✅ FIXED (Path Y refactor, 5 commits `bee5338` + `6652a25` + `b9cb40b` + `c812a5a` + `d49e1ae` on top of revert `143a07f`).** All 5 add-button picks now use the in-place horizontal slide that Add Plant uses today. Menu container stays mounted through the entire FAB flow; only inner sub-screen content swaps via `animate-submenu-slide-forward` / `animate-submenu-slide-back`. Form bodies extracted as `<TaskForm>` / `<JournalEntryForm>` / `<SupplyForm>` / `<SeedPacketForm>` named exports (same file as parent modal, no new files). Internal `screen` state renamed to `step` in QuickAddSupply + QuickAddSeed to avoid collision with menu's outer `screen`. **Standalone modal shells preserved verbatim** for non-FAB callers — OnboardingContext.openTaskForOnboarding (Observe Hillside) → NewTaskModal; calendar `?openTask=1` deep-link → NewTaskModal; OnboardingDock CTA + VaultPageContent autoload → QuickAddSeed; journal "+Entry" → QuickLogModal; QuickLog "+ Add New Supply" cross-modal → QuickAddSupply; garden My Plants empty-state CTA + menu "Add plant" sub-screen → AddPlantModal; vault Plant Again + vault/shed/[id] + ShedView + VaultShedWing all unchanged. 5 add-button-mounting pages updated (home / garden / calendar / journal / vault page) — drop 4 `onAdd*` props on `<UniversalAddMenu>` + drop `onBackToMenu={backToMenu}` on the 4 standalone-modal mounts (kept on AddPlantModal mount; `backToMenu` stays in context). Two accepted regressions per Q1 Option B locked at greenlight: (a) cross-modal "+ Add New Supply" closes menu and does NOT auto-resume on journal sub-screen; (b) pages don't auto-refresh after in-menu successful add (page-side refetch hooks only fire on standalone-modal mount path). Followed prior misfire `e7d59d7` (choreographed close-then-open coordination), reverted via `143a07f` before Path Y landed. **4-pass plan-audit terminated clean** with 2 mid-execution amendments re-audited inline (Q1 Option B simplification + AddPlantModal discovery preserving `backToMenu` in context). Tests 439/439; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition; user phone-verifies on prod. ROADMAP §6 FAB transition standard entry amended same-commit (all 5 picks now classified as submenus, not just Add Plant). Plan file at `C:\Users\marsh\.claude\plans\intelligent-pare-f7abd4.md`.

---

*Older dated lead-line paragraphs (2026-05-18 `trusting-proskuriakova-8e23c2` §3.12 #5 ship `9bad88f` + 2026-05-17 `mystifying-lichterman-55e122` T4 admin-inbox + T1+T2 bundled ship `e50a667` + T3 lock `0c3b655` + 2026-05-14 and earlier + 2026-05-16 entries + 2026-05-17 ecstatic-rubin / competent-blackwell / binary-valley / flamboyant-wilson / distracted-wilson / flamboyant-lamport paragraphs) archived — full content preserved in §6 entries. §1 lead-line cap = 2 paragraphs (locked 2026-05-16, see §7).*

### Just shipped this session

🟢 **`6250025` (Orphaned `/vault/import/photos` route deleted — Dispatch-spawned 2026-06-11, closes the `e552799` E1 ask)** — `chore(photo-import): delete orphaned /vault/import/photos route + dead localStorage pipeline`. Executes Syd's same-day greenlight on E1 from [docs/plans/photo_import_cleanup_2026-06-11.md](plans/photo_import_cleanup_2026-06-11.md) §5: `src/app/vault/import/photos/page.tsx` deleted (478 lines; its only feeder — the `handleSaveAll` dead path — was removed in `e552799`, leaving it provably unreachable; NORTH_STAR "No duplicate paths" — a second photo-import pipeline one rewire away from resurfacing as a divergent surface). **Companion cleanup (declared amendment, re-audited):** the `pendingPhotoImport` localStorage helper block in [reviewImportStorage.ts](../src/lib/reviewImportStorage.ts) (key + 2 types + get/set/clear, 40 lines) had the deleted page as its only consumer and no remaining writer — removed with it. **Zero-reference gate verified before deletion:** app-wide grep — every live `src/` reference targets the **kept** `/vault/import/photos/hero` step (the live find-hero flow, untouched; builds fine with `photos/` as a pathless segment); zero unit/e2e references to the route or helpers; the storage key referenced nowhere else (no logout-sweep). Immaterial residue noted: pre-`e552799` `garden-pending-photo-import` localStorage entries on devices stay inert (transient data, writer already dead). 2 files, −518 lines, no additions. Tests **623/623**; build clean ×2 (re-run after rebase onto `6119c54`); route manifest confirms `/vault/import/photos` gone + `/vault/import/photos/hero` present; auto-push tier (no schema/RLS/auth/data-loss). **Uncovered work:** dated audit docs ([IMPORT_FLOW_AUDIT.md](IMPORT_FLOW_AUDIT.md), [IMPORT_BY_LINK_VS_IMAGE.md](IMPORT_BY_LINK_VS_IMAGE.md)) still describe the deleted pipeline — left as historical artifacts (the cleanup plan doc supersedes them); flag if either is still treated as live reference. **Syd re-test:** none needed — the route was unreachable from the UI; Photo Import end-to-end path unchanged (the `e552799` re-test above still covers it).

🟢 **`e552799` (Photo Import comprehensive cleanup — Dispatch-spawned 2026-06-11)** — `fix(photo-import): cleanup sweep — crop-queue photo drop + permanent-type drop + PO-chrome mislabel + dead-path removal`. Pre-tester sweep; full audit matrix + NORTH_STAR citations in [docs/plans/photo_import_cleanup_2026-06-11.md](plans/photo_import_cleanup_2026-06-11.md). **F-I (data loss):** [ImageCropModal](../src/components/ImageCropModal.tsx) fires `onConfirm` THEN `onClose` on Apply; [BatchAddSeed](../src/components/BatchAddSeed.tsx) popped `cropQueue` in BOTH → every second photo in a multi-photo batch silently dropped (cropped A queued, B's entry popped uncropped + blob URL revoked, C shown next). Single pop now lives in `onClose`; sibling sweep confirmed the 3 other ImageCropModal callers are single-image/idempotent — unaffected. **F-G:** hero page ([/vault/import/photos/hero](../src/app/vault/import/photos/hero/page.tsx)) read `pending.addPlantMode` but never `pending.defaultProfileType` → Permanent-type photo imports (and the PO→hero roundtrip) saved `profile_type:"seed"`, `is_permanent_planting:false`, plus care templates + harvest task meant for seasonal. Ref now restored from storage. **F-F:** hero page hardcoded `source:"purchase_order"` → photo imports rendered PO review chrome ("Step 1: Import Review" h1, PO field labels/visibility splits) and addPlantMode saves wrote a bogus `source_po_ref` alongside the correct `image_file_path`. `source` threaded through `PendingPhotoHeroData` ("photo" from BatchAddSeed; `importSource` from review-import's `handleGoToHeroPhotos`; missing = pre-deploy batch → keeps "purchase_order"). Review-import's auto-hero effect verified to NOT re-fetch on the flip (items carry `hero_image_url` + `imageBase64`). **F-B:** crop output is a natural-resolution canvas blob; `processOneItem` posted it uncompressed to `/api/seed/extract` while every sibling AI-image path compresses — compression moved to crop-confirm (`compressImage` 1200px/0.85; also shrinks localStorage quota pressure). **F-A (NORTH_STAR "No duplicate paths"):** dead code deleted — `handleSaveAll` (113-line direct DB save that ignored `addPlantMode`), `processFilesWithGeminiAndRedirect` (only feeder of `setPendingPhotoImport`), `processOrderConfirmation`, their states/JSX, and the unused `onSuccess` prop (5 page callsites). Consequence: `/vault/import/photos/page.tsx` is **provably orphaned** (zero live feeders) — left in place, fate escalated (ask below). **F-C:** Capture CTAs `bg-emerald` (=emerald-500 STATE shade) → `bg-emerald-600 hover:bg-emerald-700` per VISION §8 emerald split in BatchAddSeed + [BatchAddSupply](../src/components/BatchAddSupply.tsx) (2-of-2 siblings; progress-bar fills stay `bg-emerald` per semantic carve-out). **F-H:** `processOneItem` stale `access_token` closure (deps fix). Verified already-consistent, no change: F13 `imageLoads` hero verification, F19 multi-item warning, placeholder-hero fallback (matches `buildPlantProfileInsertPayload` canon), AddPlantModal/review-import compression. Tests **623/623**; build clean ×2 (re-run after rebase onto `33a9758`); e2e grep clean (no label changes); auto-push tier (no schema/RLS/auth/data-loss). **ADJACENT (logged, not fixed):** shed/review-import "Save All" + QuickAddSupply capture also `bg-emerald` — same token class, supply-side surfaces, fold into a future pass. **Asks for Syd:** (1) `/vault/import/photos/page.tsx` fate — **(A) Recommended: delete the route next ship** (the crop→inline-extract flow superseded it; keeping a second pipeline invites "duplicate paths" drift) vs (B) resurrect as a no-crop bulk path **[RESOLVED 2026-06-11: Syd greenlit (A); shipped `6250025`]**; (2) reconciliation — the Dispatch queue memory (`project_gt_queued_chapters` 5-item Photo Import chapter + hero-finder entry) was unreadable from this machine; findings were independently re-derived from code — please check the queue list against F-A…F-I and flag anything not covered. **Deferred:** hero-finder architecture items (BUGS.md #6 "2B" unvalidated hero URLs via `background-hero-for-profile`; IMPORT_FLOW_AUDIT 0b defer-hero-to-post-import) — M-sized, already registered, out of cleanup scope. **Syd re-test:** Add Plant → Photo Import → pick type Permanent → add 3+ photos (crop each) → all 3 appear in queue → Save to Vault → hero step → review shows "Import Review" (no "Step 1", no PO labels) → save → plant lands in My Plants (not Active Garden) with no harvest task.

🟢 **`236dc1e` + `02ff1d8` (Sprint 3 — growing instance → standalone `/garden/grow/[id]` page + `docs/NORTH_STAR.md` — Dispatch-spawned 2026-06-10)** — the 4th/last detail surface reaches real-page parity with Library/Packet/Shed (NORTH_STAR "No duplicate paths" + "Take mental load OFF the user"). Reuses [`GrowInstanceModal`](../src/components/GrowInstanceModal.tsx) via a new `variant="page"` prop (default; modal branch now dormant, gated-not-deleted); new [`/garden/grow/[id]/page.tsx`](../src/app/garden/grow/[id]/page.tsx) wrapper owns the unframed Back chip (§8), filter-aware swipe (`SwipeScope += "instances"`; GardenView snapshots the filtered+sorted order; Q4 fallback `status='growing'`/`sown_date` desc), and the HarvestModal host (refetch-in-place via `reloadKey`). Every entry point now navigates to the real page (Q2): Garden grid/list, Calendar care deep-link, Packet "Used in Plantings" (`from=packet`), Library profile peek (`from=library`, was a read-only modal — now the editable page). Garden page drops the modal render; legacy `?grow=` deep-links redirect to the page. Doc hygiene first: [`docs/NORTH_STAR.md`](NORTH_STAR.md) consolidates the principle-set + added to CLAUDE.md required-reading. 14 code files + doc; Pass 1 bidirectional matrix clean; tests **578/578**; build clean; e2e grep clean; auto-pushed (no schema/RLS/auth/data-loss). **Resolves the swipe-on-modal decision parked at `d274b3d`.** Full detail + dogfood path in §5.

🟢 **`f1d046a` (Groups refetch staleness fix + batch-move header copy — Syd dogfood 2026-06-09)** — `fix(groups): propagate group-membership mutations to Garden page refetch + pluralize batch-move header`. **Bug:** reassign-on-delete wrote the DB (plant page showed the new group) but Garden's destination tab read "0 items." **Root cause:** [ManageGroupsModal](src/components/ManageGroupsModal.tsx) `onMutated` only bumped [GroupTabs](src/components/GroupTabs.tsx)' internal tab-list refetch; the page-level `refetchTrigger` that reloads GardenView's batches+memberships never fired — and both Manage and the plant modal (Door 2) live on `/garden` via searchParams, so no remount rescues the cache. **Fix:** GroupTabs `onMutated` prop (forwards to parent trigger; tab-only fallback preserved), GrowInstanceModal `onGroupChanged` after successful `saveGroup`, garden page wires both into the existing `refetchTrigger` (Door 3 already bumped it). Closes the "Door 2 refreshes on next load" trade-off flagged at `c7fe6f6` — group changes now reflect live. Vault-profile modal mount intentionally unwired (cross-route nav remounts Garden = fresh fetch). Header copy: "Move 2 to…" → "Move 2 plants to…" (pluralized; casing kept to the sheet's existing register — flagged for the sheet-wide casing question, see §6/c7fe6f6 parks). Tests 554/554; build clean; string sweep 0 stale refs; auto-push tier. **Syd re-test:** Manage → trash a group with plants → reassign to a destination → destination tab shows the plant immediately; plant page → change group → Garden tab reflects without reload.

🟢 **`f0c881d` (Groups Door 1 parity fix — Plant-from-Packet + Vault Confirm Planting — Syd dogfood gap 2026-06-09)** — `fix(groups): add Group field to Plant-from-Packet + Vault Confirm Planting flows — sibling-sweep gap from c7fe6f6`. Syd dogfood caught the Groups field missing on the Plant-from-Packet flow ("Planting" form) — a sibling-sweep miss in `c7fe6f6`, which swept pill/action-sheet/AddPlant-variant siblings but not all `grow_instances` *insert sites*. Full insert-site sweep this time (5 sites): AddPlantModal ×2 ✅ already covered · [PlantingForm.tsx](src/components/PlantingForm.tsx) ❌→fixed (Syd's screenshot path; field between Location and Sow method, batch-wide like its shared-field peers) · [VaultPageContent.tsx](src/app/vault/VaultPageContent.tsx) Confirm Planting modal ❌→fixed (field before Sow method; reset + fetch on modal open) · `review-import` addPlantMode — **flagged, deferred (decision below)** · `completeSowTask.ts` — system-side, intentionally no field (new plants default NO GROUP per opt-in lock). **Structural fix for the drift class:** extracted shared [`GroupSelectField`](src/components/GroupSelectField.tsx) (single-select chip + search + inline-create, `compact` register variant) and retrofitted AddPlantModal onto it — ONE implementation across all three forms, per-form drift impossible. Both new sites wire through `setInstanceGroup()` (priorGroups `[]`) → per-plant auto-journal "Added to {Group}". Tests 554/554; build clean; e2e grep 0 references to the field; auto-push eligible (Phase 4 clean + no schema + no RLS + no data-loss) per CLAUDE.md 2026-06-07 tier. **Syd re-test:** Vault → tap packet → Plant → confirm Group field sits between Location and Sow method → pick/create a group → Confirm planting → Journal shows compact "Added to {Group}"; repeat via Vault batch-select → Confirm Planting modal.

🟢 **`c7fe6f6` (Groups follow-up ship — tabs fix + 3 doors + auto-journal + FAB cleanup + annual/perennial relocation + hang fix + delete-reassign — Dispatch-spawned 2026-06-09)** — `feat(groups): tabs scaling fix + 3 assignment doors + auto-journal + FAB cleanup + annual/perennial relocation + Manage-modal hang fix + delete-group reassign`. 15 files (13 changed + `createJournalEntry.ts` + migration `20260609120000_journal_entry_type_group_change.sql`, applied to remote via `supabase db push` BEFORE code push per constraint-ordering). **Single-group membership locked** (Syd 2026-06-09; Door 1 reconciled multi→single; `setInstanceGroup()` clears prior rows so legacy multi-memberships collapse lazily). All 4 assignment routes funnel through `setInstanceGroup()` → auto-journal "Added to X" / "Moved A → B" / "Removed from X" (`group_change` entry_type, compact info-note render in all 3 journal views). GroupTabs restructured (Manage pinned outside scroll strip + overflow fade — fixes 4+ break). Batch FAB sheet = Move to group · Journal · End batch · Delete (`assignSelectedToGroup` replaces `moveSelectedToPermanentPlants`/`moveSelectedToGrowingGarden` on GardenViewHandle). Perennial pill removed (grid + list); `profile_type` Seasonal/Permanent toggle added to Library Edit Plant Profile modal. Manage modal: `busy` lock + 10s `withTimeout` + reconcile-on-finally + optimistic add + `min-w-[5.5rem]` Add button + `aliveRef` unmount guard; delete-with-plants → reassign panel (radiogroup: other groups + No group/Unassigned) → journaled per-plant reassign → `deleteGroup`. 4-pass plan-audit at `.claude/plans/sprint2_groups_feature.md` (incl. bidirectional matrix on Manage modal add/timeout/late-success + close-mid-save). Tests **554/554** (7 new: setInstanceGroup language quartet + fetchGroupAssignments + reassign-panel flow); build clean; e2e swept 0 stale assertions; NO Preview MCP per brief. Trade-offs flagged at ship: instance `is_permanent_planting` no longer user-editable from Garden (display-only); Door 2 group change refreshes Garden tab on next load (no live refetch). Merge-groups deferred v2. See §1 dogfood path + §6 2026-06-09 decision entry.

🟢 **`877c1c7` (FAB-tree polish bundle close — QuickAddSeed + QuickAddSupply chip Title Case sweep — `fab_tree_polish_bundle` spawn 2026-05-31)** — `polish(casing): QuickAddSeed + QuickAddSupply chooser chips — Sprint 5 MUST #7 coverage gap close`. Closes the FAB-tree polish bundle queued from Syd's 2026-05-25 dogfood (4 items). **Phase 1 audit found 3 of 4 items already shipped:** items 1+2 via `7c0decf` (Back-arrow z-stack overlap fix on page-local-state modals + sub-screen restore via new `openMenuOnScreen` context API + 15 callsite fixes); item 4 via `b1f4ce7` (Tier 1 — `handleLoadPlantProfilePicture` → `handleSaveToVault` + button label "Load plant profile picture" → "Save to Vault" + loading label "Preparing…" → "Saving…"). Items 1+2+4 closed as no-op with shipped-SHA cross-ref. **Item 3 inverted per §8 + sibling sweep widened scope.** Brief paraphrased "make Title Case → sentence case" based on stale (pre-2026-05-27) casing rule; current VISION §8 lock (2026-05-27) sets modal h2 / page h1 / chip labels = Title Case (with `Photo Import` as explicit chip-label example). All existing `Photo Import` headers already correct. The actual drift = 8 stale-sentence-case chip labels across both Quick* choosers: Manual entry / Link import / Purchase order / Photo import × 2 files = 8 strings. Sprint 5 MUST #7 sweep (`7db44c5`) missed all of them — same coverage-gap shape as `332f0d6` PacketVaultView "Add a Packet" leak + `3e4c02a` V5.2 empty-state CTA sweep. **Cohesion math at audit:** fixing only the 2 brief-named "Photo import" chips would have degraded in-chooser cohesion (1-of-4 Title Case mix worse than 0-of-4); all 8 strings together is the cohesion-preserving move. Syd locked Q2=A (full sweep) via plain-English ask at end of Phase 1. **Code:** 8 source strings flipped (QuickAddSeed.tsx:627/635/647/660 + QuickAddSupply.tsx:403/411/420/430) + 4 e2e references flipped per CLAUDE.md 2026-05-27 e2e-scope sub-rule (synthetic-user.authenticated.spec.ts:57 + vault-add-seed.authenticated.spec.ts:43-47+61-62) + 3 stale "still sentence case in source per sweep miss" comments removed. **4-pass plan-audit terminated clean iter 1** (Pass 1 factual via grep verification of all 12 file:line cites; Pass 1 bidirectional state-transition matrix SKIP-with-reason — string-replacement only, no modal/menu state-machine touched; Pass 2 concerns hunt + persona walk all 5 pass; Pass 3 sibling sweep with vocabulary-breadth grep across stale + Title Case variants of all 4 strings, zero residual drift; Pass 4 lock hygiene clean — VISION §8 closes coverage gap, §10/§11 untouched). 4 files changed, +12/-16. Tests 527/527; build clean. **NO Preview MCP** per chat HARD RULE + `feedback_no_screenshots_for_verification.md` (Syd phone dogfood = visual verification). Concurrent-fetch pre-push: 0 ahead / 0 behind. Plan file `.claude/plans/fab_tree_polish_bundle.md` pre-existing from 2026-05-26 (Amendment 1 closed via `7c0decf`); this ship closes the Tier 2/3 residual via Phase 1 audit redirect. **Syd dogfood path:** FAB → Add Seed Packet → confirm chooser chips read `Manual Entry` / `Link Import` / `Purchase Order` / `Photo Import` Title Case; FAB → Add to Shed → confirm same 4 chips Title Case. **Out of scope (parked):** Item 5 from original 2026-05-26 plan (multi-link "+" for shed link import) — separate chat, ~4-10h feature work per the original triage.

🟢 **`dc173ef` (Variety presentation reversal — stacked italic across Library + Packets + Garden — this chat 2026-05-30)** — `polish(variety): stack variety beneath plant name (italic, no parens) across Library + Packets + Garden — Syd reversal 2026-05-30 + lock VISION §8 variety presentation`. Syd-driven canonical reversal of variety rendering on all inventory list surfaces; replaces the prior implicit inline `Name (Variety)` parens-wrapped pattern (last canonicalized 2026-05-27 "Variety presentation standardization across list views" chapter, Sprint 5 SHOULD #19 VERIFIED CLOSED 2026-05-30 in polish bundle `332f0d6`) with stacked-italic-no-parens render. **The reversal:** plant name renders on its own line (existing register: bold gallery h3, semibold list-row), variety renders on the next line in italic + no parens + muted color register (gallery `text-black/60`, list/table `text-neutral-600`). Conditional render: variety line only appears when `variety_name?.trim()` is truthy. **Code:** 7 visible variety-rendering sites flipped across 3 files — Library/Plants gallery card h3 ([SeedVaultView.tsx:1008-1017](src/components/SeedVaultView.tsx:1008)) + list-row span ([SeedVaultView.tsx:1080-1085](src/components/SeedVaultView.tsx:1080)); Packets gallery card h3 ([PacketVaultView.tsx:702-712](src/components/PacketVaultView.tsx:702)) + mobile list-row ([PacketVaultView.tsx:787-794](src/components/PacketVaultView.tsx:787)) + desktop table cell ([PacketVaultView.tsx:846-855](src/components/PacketVaultView.tsx:846)); Garden gallery card h3 ([GardenView.tsx:1181-1191](src/components/GardenView.tsx:1181)) + list-row title ([GardenView.tsx:1302-1317](src/components/GardenView.tsx:1302)). Helper `formatBatchDisplayName` at [GardenView.tsx:229](src/components/GardenView.tsx:229) preserved as-is — still used inline at 4 non-visual callsites (aria-label at L1261, toast at L641, modal body at L928, delete-confirm prose at L975); only the visible list-row callsite at L1304 replaced with two-element stacked render. **Tokens anchored:** gallery cards = `block font-normal italic text-black/60 truncate text-center w-full` (extends existing gallery card variety register from `2e9df79`); list rows + desktop table = `block text-sm font-normal italic text-neutral-600 truncate` (anchors VISION §8 info-note primitive `text-sm text-neutral-600 italic`). **VISION §8 amendment** (bundled into same commit per Syd's single-commit instruction): new subsection "Variety presentation — stacked italic" inserted between "Default sort by use case (discovery vs lookup)" and "Profile-tab contextual add actions". Captures rule + decision criterion + tokens + Syd's verbatim rationale (*"I like the variety under plant type because it's easier to skim and read. These pages get incredibly long so being able to easily skim the variety OR the name was nice."*) + the principle named explicitly (**independent skimmability of two axes**) + persona walk all 5 pass + explicit supersedes acknowledgment + generalizes-beyond-4-surfaces framing. **Why supersedes-acknowledged, not silent drift:** the prior inline standardization was canonical for ~3 days (2026-05-27 lock → 2026-05-30 reversal) and was VERIFIED CLOSED on the same day this chat reversed it. Reversal captured per CLAUDE.md authority-precedence — prior ship work landed inline-uniform pattern as intermediate state; this amendment flips the target. **4-pass plan-audit terminated clean iter 1 + 1 amendment re-audit** (Syd folded "independent skimmability" rationale into VISION text mid-Phase-2; re-audit verified amendment text matches §8 voice, generalization scope correct, Syd verbatim preserved, supersedes acknowledgment intact). Pass 1 factual unchanged across amendment (same 7 sites, 3 files, helper untouched); Pass 2 hunt across 9 categories incl. persona walk all 5 pass + cohesion-by-aggregation anchors named for every micro-decision (gallery variety register ↔ `2e9df79`; list/table variety register ↔ VISION §8 info-note primitive; helper preservation criterion = visual vs non-visual callsite); Pass 3 sibling sweep across `formatBatchDisplayName` callsites (1 visible flips, 4 non-visual preserved) + ShedView confirmed no variety field + VaultPacketWing/VaultPageContent delegate rendering to PacketVaultView/SeedVaultView (already covered) + MyPlants/ActiveGarden merged into GardenView per Sprint 3 B2 `04ef744` (already covered) + e2e/ vocabulary-breadth grep on `variety|Variety|\(Cherokee` returned 0 stale assertions; Pass 4 lock hygiene clean — VISION §10 untouched; §11 untouched; supersedes the 2026-05-27 inline standardization chapter explicitly per the amendment text. 4 files changed (3 src/components/*.tsx + 1 docs/VISION.md), +95/-18 net. Tests 527/527 (50 test files; 0 new test impact — verified via grep on `(Cherokee|(Brandywine|formatBatchDisplayName|profile_variety_name` returned no matches in src/ tests, and e2e/ grep on `variety|Variety` returned no matches). Build clean (Windows OneDrive sync race required one clean-rebuild retry; Vercel unaffected — known infra-only failure mode). **NO Preview MCP** per chat HARD RULE + CLAUDE.md lock 2026-05-27 (tests + build + git diff sufficient; Syd dogfood verifies visual on phone). Concurrent-fetch pre-commit: 0 ahead / 0 behind. **Syd dogfood path:** (a) Vault → Packets tab → grid mode → confirm every packet card shows `Name` line + italic `Variety` line beneath (no parens); (b) Vault → Packets tab → list mode (mobile) → confirm every row shows name line + italic variety line + vendor sub-line stacked; (c) Vault → Packets desktop table (sm:block) → confirm Variety column cell stacks name + italic variety; (d) Plants tab (Library) → confirm gallery cards show stacked name + italic variety; (e) Plants tab → list mode → confirm rows stack name + italic variety; (f) Garden tab → gallery view → confirm batch cards stack name + italic variety (extends prior `2e9df79`); (g) Garden tab → list view → confirm rows stack name line + italic variety line below the badges row. Out of scope (parked): Shed (supplies have no variety field — confirmed); Calendar widget pills (separate chapter in flight per spawn-time note); modal body / aria-label / toast inline `Name (Variety)` rendering (intentionally preserved per VISION §8 amendment — body-copy-equivalent, no skimming axis).

🟢 **`332f0d6` (Sprint 5 polish bundle — SHOULD #21 overlay + PacketVaultView casing leak — prior chat 2026-05-30)** — `polish(sprint-5): SHOULD #21 + PacketVaultView casing leak — embed overlay positioning + Add a Packet Title Case`. Closes Sprint 5 SHOULD bucket as 100% complete via single polish ship + verification of #19+#20. **SHOULD #21** Add-plant Manual loading-overlay coverage bug — root cause: `SubmitLoadingOverlay`'s `absolute inset-0` ([SubmitLoadingOverlay.tsx:14](src/components/SubmitLoadingOverlay.tsx:14)) lacked deterministic positioned ancestor in embedded form path. Standalone modals work (AddPlantModal wrapper at [AddPlantModal.tsx:1125](src/components/AddPlantModal.tsx:1125) is `relative` so overlay covers full shell), but the embedded path since 2026-05-26 `ba9319f` Path Y FAB-form submit saga renders content inside [UniversalAddMenu.tsx](src/components/UniversalAddMenu.tsx) sub-screen wrappers that had `${slideClass} flex-1 min-h-0 flex flex-col` with no `relative`. With transform-bearing slide animations creating inconsistent containing-block behavior across browsers, the overlay scoped to the body scroll area only — leaving header + footer interactive during submit (Syd dogfood: "top half only; bottom appears interactive"). **Fix:** added `relative` to ALL 10 sub-screen wrapper divs uniformly (main + add-plant + add-plant-manual + add-plant-established + add-plant-from-vault + seed + shed + task + journal + variety). Establishes deterministic positioning context. Pass 3 sibling sweep BLOCKING-aligned — same bug shape across all 6 embedded form types (TaskForm task / JournalEntryForm journal / SeedPacketForm seed / SupplyForm shed / AddVarietyForm variety / AddPlantModal-embedded add-plant-manual + add-plant-established) fixed in one change. Cohesion-by-aggregation: uniform addition keeps all 10 wrappers identical-shape (no 2-pattern drift introduced); future-proofs against any new overlay-bearing form added to a different sub-screen. **PacketVaultView casing leak** folded into same commit: [PacketVaultView.tsx:618](src/components/PacketVaultView.tsx:618) empty-state CTA "Add a packet" → "Add a Packet" per VISION §8 Title Case (AP-style: verb `Add` first-word capitalized, article `a` lowercase mid-string, noun `Packet` last-word capitalized). Sibling alignment to V5.2 sweep canonical (`3e4c02a` Sprint 5 MUST #14 onboarding pass 2026-05-30 flipped SeedVaultView "Add a Packet" / "Add a Plant" + ShedView "Add a Supply" + Home/shopping-list "Add an Item" but missed this PacketVaultView site — single coverage gap caught in close-out audit). 9bad88f wordform preserved; only casing register flips per Q2=A precedence (VISION §8 wins over 9bad88f sentence-case canonical per CLAUDE.md authority precedence). **SHOULD #19 + #20 VERIFIED CLOSED (no code change):** both queue entries lagged ship cadence per gt_v1_scope.md §2.6 (queue file snapshots Syd's mental queue but lags ship cadence). #19 variety presentation — list-view `Name (Variety)` inline pattern already unified across 3 list-view surfaces (Packets mobile list [PacketVaultView.tsx:788](src/components/PacketVaultView.tsx:788) `font-semibold text-sm text-neutral-900` plain inline + Garden list [GardenView.tsx:1300](src/components/GardenView.tsx:1300) same classes via `formatBatchDisplayName(name, variety)` helper; Active Garden + My Plants merged into GardenView via Sprint 3 Ship B B2 `04ef744`). #20 Shed-vs-Garden list text styling drift — row wrapper + title font + subtitle font all identical post-`c851b3d` 2026-05-28 (`flex items-center gap-3 px-3 py-2 min-h-[44px] hover:bg-gray-50 transition-colors`); remaining `text-[11px]` chip-register (Shed) vs `text-xs` sentence-register (Garden sown/harvest paragraph) difference is intentional content-shape preservation documented in c851b3d commit message; Garden emerald-tinted thumbnail is semantic active-growing signal, not drift. Both marked CLOSED via prior-ship verification; doc-trail catch-up here. **v1 % post-bundle:** 15 / 18 = 83% (catch-up: +1 for MUST #9 `8897fe8` shipped 2026-05-30 prior to this bundle, was not yet incorporated into §5.2 at MUST #14 close-out). **Polish %:** 7 / 8 = 87.5% (SHOULD bucket = 7/7 = 100% complete). All Claude-implementable Sprint 5 work is now CLOSED; remaining v1 = L1 PITR decision + L2 restore drill + L5 retention dashboard check (all Syd dashboard actions, ~40 min her time total). **4-pass plan-audit terminated clean iter 1** (Pass 1 factual + bidirectional state-transition matrix SKIP-with-reason — pure CSS-class additions + single text replacement, refactor preserves state-machine shape verbatim per CLAUDE.md skip condition; Pass 2 concerns hunt across 12 categories + persona walk all 5 pass — overlay full coverage closes Walter/Sam mis-tap risk during submit, Title Case "Add a Packet" reads polished for Maya/Sydney; Pass 3 sibling sweep BLOCKING-aligned uniform 10-wrapper fix + V5.2 casing peer alignment; Pass 4 lock hygiene clean — VISION §10/§11 untouched, ROADMAP §6 recent decisions unaffected). 2 code files (`UniversalAddMenu.tsx` + `PacketVaultView.tsx`), +11/-11 net. Tests 527/527; build clean. **NO Preview MCP** per chat HARD RULE + CLAUDE.md lock 2026-05-27 (tests + build + git diff sufficient; Syd dogfood verifies visual). Concurrent-fetch pre-commit: 0 ahead / 0 behind. **Syd dogfood path:** (a) Vault → Packets tab → if Library is empty, tap "Add a Packet" empty-state CTA → confirm Title Case render; (b) FAB → Add Plant → Manual entry → fill form (name + optional variety) → tap submit → confirm loading overlay covers BOTH header AND footer (back-arrow + Cancel + Add Planting buttons can't be tapped while spinner shows); (c) repeat (b) for Established Plant chip; (d) FAB → Add Seed Packet → choose Manual → fill + submit → confirm overlay full-coverage; (e) FAB → Add to Shed → choose Manual → fill + submit; (f) FAB → Add Task → fill + submit; (g) FAB → Add Journal → fill + submit; (h) FAB → Add Variety → fill + submit. Out of scope (parked): theme #7 raw-card empty-state primitive canonicalization (pre-existing park, not v1-blocking); broader perennial-pill decision (still parked from Sprint 3 B4, awaiting mixed-annuals+perennials Syd dogfood signal).

🟢 **`3e4c02a` (Sprint 5 MUST #14 — Onboarding pass — verification + V5.1 + V5.2 — this chat 2026-05-30)** — `feat(onboarding): Sprint 5 MUST #14 — empty-state cohesion pass (V5.1 em-dash drift + V5.2 §8 CTA Title-Case sweep)` — Closes the verification-only onboarding pass per gt_v1_scope.md §2.1 row 14 ("Not net-new feature work; verification + gap-close pass against §1.7 criteria"). **V1-V4 verified clean:** 5 surface empty-states walked (Home / Library / Garden / Vault Packets / Vault Shed — all on 9bad88f canonical 3-part frame except Garden which had drifted); 3 Add Plant paths verified per Sprint 4 closure (ff4ac2e + 9dc8c2d + f77fe75); zone setting discoverable Settings → Profile + ties to onboardingCtx.reportAction("zone_set"); Manage Groups tooltip B4 `0b73ec7` verified live (localStorage `gt-groups-manage-tooltip-seen`, SSR-safe, dismisses on Close-X OR Manage tap). **V5.1 — Garden em-dash misapplication drift fix:** [GardenView.tsx:1057](src/components/GardenView.tsx:1057) + [L1082](src/components/GardenView.tsx:1082) dropped "— X —" em-dash bracketing on empty-state titles; aligned to ROADMAP §6 9bad88f canonical (sentence-case title + period, matching SeedVaultView/ShedView peers). VISION §8 em-dash convention is for missing-data CELL display in profile/table/detail views, NOT title decoration — comment cleanup removes the misleading §8 reference. **V5.2 — §8 CTA Title-Case sweep:** SeedVaultView ×2 ("Add a packet"/"Add a plant" → Title Case), ShedView ×1 ("Add a supply" → Title Case), Home ×2 ("Add item" → "Add an Item"), shopping-list ×1 ("Add item" → "Add an Item"). GardenView CTAs already Title Case. **Doc-vs-doc resolution (Q2=A precedence call):** VISION §8 (locked 2026-05-27) wins over ROADMAP §6 9bad88f Decision #2 "Add a {noun}" sentence-case (locked 2026-05-18) per CLAUDE.md authority precedence (VISION wins on locked design decisions). 7db44c5 (MUST #7 sweep) hit Calendar/error-fallback/Settings buttons but missed empty-state CTAs — this ship closes that coverage gap. 9bad88f "Add a {noun}" wordform stays canonical; only casing register flips. **Brief-vs-plan-doc reconciliation at chat open:** brief expanded MUST #14 scope to net-new tooltips on Add Plant FAB / Library FAB / Calendar + first-time Add-Plant defaults; conflicted with plan doc §2.1 explicit "Not net-new feature work" + VISION §3 disclosure-by-data + §10 empty-by-default LOCK. Reconciled via plain-English ask; Syd locked (A) verification-only scope. Memory note `feedback_verify_brief_vs_locked_plan.md` governance working as designed. **4-pass plan-audit terminated clean iter 1 + 1 amendment re-audit** (V5.2 bundling per Q1=B + Q2=A locks). 5 files (`src/app/page.tsx` + `src/app/shopping-list/page.tsx` + `src/components/GardenView.tsx` + `src/components/SeedVaultView.tsx` + `src/components/ShedView.tsx`), +13/-10 net. Tests 527/527 + build clean. **NO Preview MCP** per chat HARD RULE + CLAUDE.md lock 2026-05-27 (Syd dogfood verifies visual on phone). Concurrent-fetch validation pre-push: 0 ahead / 0 behind. e2e/ swept 0 stale assertions on flipped strings. Syd dogfood path: (a) Garden empty (no batches) → confirm "Your garden is empty." (no em-dashes, period); empty Group → "No plants in this group yet."; (b) Vault Packets/Shed empty CTAs read "Add a Packet" / "Add a Supply"; Plants Library empty → "Add a Plant"; Home + shopping-list empty → "Add an Item"; (c) clear `gt-groups-manage-tooltip-seen` → reload → Garden tab → tooltip fires once, dismisses on Close-X/Manage, doesn't re-fire. **Out of scope (parked):** theme #7 raw-card empty-state primitive canonicalization (pre-existing park, not v1-blocking); Sprint 5 #7+#8 §5 doc-trail catch-up (pre-existing doc-trail debt from those chats; v1% recompute here includes the catch-up math regardless).

🟢 **`34e8948` (IA Ship A part 2 — `+Add Variety` chip + AddVarietyForm extraction — `ia-ship-a-plants-tab` chat)** — `feat(fab): +Add Variety chip + AddVarietyForm extraction (IA Ship A part 2)`. Closes the lone Library exception to the FAB-as-universal-add cohesion bar. AddVarietyForm extracted as a named export from `AddVarietyModal.tsx` following the Path Y form-extraction pattern locked 2026-05-20 (`bee5338` SeedPacketForm / SupplyForm / TaskForm / JournalEntryForm). Embedded inline as a UniversalAddMenu "variety" sub-screen with back-arrow header, no focus trap / escape / scroll lock (parent menu owns those single-source-of-truth). Standalone AddVarietyModal preserved as thin wrapper for backward-compat (no callers in tree post-Ship-A). FAB chip placement: TOP of main menu (above Add Seed Packet) anchored in IA discussion §1.3 ordering — encyclopedia adds → inventory adds → instance adds → supply adds. Icon: `ICON_MAP.Plant`. Copy aligned with new IA: header "Add Plant Variety" → "Add Variety" (matches chip Title Case), sub-copy references "your plant encyclopedia" not legacy Vault, submit CTA "Add to Vault" → "Save to Plants" (encyclopedia is /plants now, not /vault). Inline Vault toolbar button + `addVarietyOpen` state + standalone modal mount removed from VaultPageContent.tsx — FAB chip is single source of truth. `UniversalAddModal` + `UniversalAddMenuScreenTarget` + `UniversalAddMenuScreen` unions widened with `"variety"` (additive; existing exhaustive consumers unaffected per Pass-1 verification). 4 files, +155/-114 net. Tests 444/444; build clean. **NO Preview MCP** per chat HARD RULE — Syd phone dogfood verifies visual. Plan + 4-pass audit at `.claude/plans/ia_ship_a_plants_tab.md` §§2(e), 2(f), 4. Syd dogfood path: open FAB → confirm "Add Variety" chip is TOP of menu → tap → confirm form embeds in-place with back arrow → fill plant name → tap Save to Plants → confirm menu closes; visit `/plants` → confirm Library no longer has inline `+Add Variety` button in toolbar.

🟢 **`9142aad` (IA Ship A part 1 — `/plants` top-level route + 6-tab nav — `ia-ship-a-plants-tab` chat)** — `feat(plants): add /plants top-level route + 6-tab nav (IA Ship A part 1)`. Library promoted from a Vault sub-tab to its own top-level Plants tab. Bottom nav + Sidebar expand to 6 entries: Home / Garden / Plants / Vault / Calendar / Journal. Vault collapses to Packets + Shed only. **Implementation: pathname-aware delegation, NOT a full VaultLibraryWing extraction.** VaultPageContent gains `isPlantsSurface` constant from `usePathname()`. On `/plants` the tab switcher hides and `viewMode` is forced to `"grid"` (Library single-mode). On `/vault` the Library tab button is gone and default-landing-tab shifts to Packets (`?tab=list`) — previously Library. Legacy URL surface (`/vault?tab=grid|active|plants`) redirects to `/plants` at mount, preserving other query params (sow, status, etc.). Detail route `/vault/[id]` stays canonical per IA discussion Q5 lock — defers rename to a separate URL-cohesion chapter to avoid 30+ deep-link breakage. SproutIcon added to `navItems.tsx` in the existing inline 24×24 stroke-currentColor family per VISION §8 chrome icon convention (sibling to HomeIcon/LeafIcon/VaultIcon/CalendarIcon/JournalIcon). Wing extraction (peer to VaultPacketWing + VaultShedWing) deferred to a parked refactor (§4 entry) — adds zero user-visible value beyond what pathname-routing achieves; structural refactor risk not worth the trade for this ship. 3 files, +142/-75 net. Tests 444/444; build clean. **NO Preview MCP** per chat HARD RULE — Syd phone dogfood verifies visual. Plan + 4-pass audit at `.claude/plans/ia_ship_a_plants_tab.md` §§1-4. Syd dogfood path: bottom nav shows 6 tabs → tap Plants → lands on `/plants` with Library gallery → tap Vault → lands on `/vault?tab=list` (Packets) → type `/vault?tab=grid` manually → confirms redirect to `/plants` → /vault/[id] deep links still resolve to detail pages.

🟢 **`88ba080` (Phase 2 follow-up polish bundle — 4 items + sibling fix — `phase2-followup-polish-4items` chat)** — `polish(phase2-followup): zone provenance + viability + retrofit awareness — 4 items from Syd dogfood`. Closes 4 trust/UX gaps Syd flagged when dogfooding the same-day Phase 2 zone-aware enrichment ship (`dcfdbe4`). **Item 1 — Generated for Zone X label:** new `plant_profiles.planting_window_zone` text column (additive idempotent migration `20260528120000`, applied via `supabase db push --linked`) persists the USDA zone the AI used. `enrichProfileFromName.ts` now writes `enrichData.zoneUsed → updates.planting_window_zone` alongside the existing planting_window write. `vault/[id]/page.tsx:203` select string extended to include the new column. `VaultProfileAboutTab` renders `Generated for Zone {N}.` italic info-note below the How to Grow card's dl grid (anchored to VISION §8 info-note primitive `text-sm text-neutral-600 italic`, sentence case). Lazy-populated — existing profiles stay NULL until re-enrichment via ✨ Fill Blanks. **Item 2 — Zone input validation:** verified zero code change needed — Settings → Profile already uses `<select>` dropdown restricted to USDA 1a-13b (`page.tsx:269` ZONES array, `page.tsx:396-405` `<select>` element). The "21b" speech-to-text dogfood report doesn't reproduce on current code. Documented as confirmed-already-shipped. **Item 3 — Plant viability warning:** extended `researchVariety.ts:77-79` zoneClause prompt with explicit viability case ("if this plant CANNOT survive outdoor growing in Zone N year-round, return the exact string 'Not viable in Zone N — indoor / greenhouse only'"). UI in `VaultProfileAboutTab` detects `planting_window?.trim().startsWith("Not viable in Zone")` and renders italic warning info-note ABOVE the How to Grow dl grid: "This plant won't survive outdoor growing in Zone {N}. Consider growing indoors or in a greenhouse." **Item 4 — Zone-change retrofit awareness toast:** `saveGardenSettings` in `settings/profile/page.tsx` detects zone delta vs `lastSavedSettings.planting_zone`; on successful upsert, sets `zoneChangeNotice` state holding `{oldZone, newZone}`. Persistent dismissable info-note renders below the Saved! indicator: italic neutral-600 prose inside `bg-neutral-50 border border-neutral-200 rounded-lg p-3` container with `ICON_MAP.Close` dismiss button (44×44 tap target). Copy: "Your zone changed from Zone {old} to Zone {new}. Existing plant profiles still use the old zone. To update them, open each profile and tap ✨ Fill Blanks." v1 awareness-only per chat brief — gated to `previousZone && newZone && previousZone !== newZone` (skips first-time-set null → "5a" since there are no existing profiles to retrofit). **Sibling fix folded in (Pass 3 ADJACENT):** `vault/[id]/page.tsx:203` select string was missing `seed_propagation_context` since the 2026-05-27 Propagate / Save Seeds ship (`39d5faa`) — silent UX regression where the rendered italic caveat at `VaultProfileAboutTab:109-111 + :187-189` evaluated to falsy because the column wasn't fetched. Added in same select-string edit alongside `planting_window_zone`. **4-pass plan-audit terminated clean iter 1 (Pass 2 re-run iter 2 after folding the ADJACENT into plan).** Pass 1 factual + bidirectional state-transition matrix SKIP-with-reason (no modal/menu lifecycle touched; persisting a column + rendering it; no axes to trace). Pass 2 concerns hunt across 8 categories incl. persona walk all 5 pass + cohesion-by-aggregation anchors named for every micro-decision (info-note primitive ↔ VISION §8; retrofit notice copy register ↔ helper-text patterns; AI prompt viability phrasing as explicitly-named new pattern; column naming `planting_window_zone` ↔ existing `planting_window` parent). Pass 3 sibling sweep across info-note rendering peers (VaultProfileAboutTab:110 + :188) + Settings zone-input surface + enrichProfileFromName updates-builder pattern + vocab-breadth grep across `zone | window | provenance | generated_for | retrofit | refresh | regenerate`. Pass 4 lock hygiene clean — VISION §10 don't-touch untouched; §11 Phase 3 multi-zone Schedule rebuild explicitly deferred; no state-machine surface touched; no React contexts modified. 6 src files (`settings/profile/page.tsx` + `vault/[id]/VaultProfileAboutTab.tsx` + `vault/[id]/page.tsx` + `lib/enrichProfileFromName.ts` + `lib/researchVariety.ts` + `types/garden.ts`) + 1 new migration (`supabase/migrations/20260528120000_plant_profiles_planting_window_zone.sql`), +48/-3 net. Tests 444/444; build clean. **NO Preview MCP** per chat HARD RULE + CLAUDE.md lock 2026-05-27 (tests + build + git diff sufficient; Syd dogfood verifies visual). Migration applied to remote (additive + idempotent; code-tier per CLAUDE.md SQL migrations subsection). Concurrent-fetch validation 0/0 pre-push. Plan + audit at `.claude/plans/phase2_followup_polish_4items.md`. BUGS Post-Launch #1 ticket annotated with follow-up-portion closure. Out of scope: Phase 2.5 (zone-aware library schema); Phase 3 (multi-zone Schedule rebuild); plant-instance redesign; Care tab; multi-link "+" backend; custom date dropdown; FAB "Add to Library"; AddPlantManualModal Packets tab; automatic batch retrofit (deferred to Phase 2.5+).

🟢 **`353b249` (Library gallery 2-col → 3-col corrective ship — same chat, post-Q1 revision)** — `fix(library): swap Library "photo" mode from 2-col big-cards to 3-col gallery — peer-matching cohesion`. Corrective ship to `a51fbd7`: original Q1=A had Library binary toggle gallery ↔ list with `"photo"` rendering as 2-col big-cards (orphan — no peer surface has 2-col) and `"condensed"` (the peer-matching 3-col pattern) dropped. **Wrong direction.** Syd caught pre-dogfood + clarified: Library's "condensed" view IS the 3-col gallery matching peers (PacketVaultView/ShedView/MyPlantsView/ActiveGardenView all use `grid-cols-3 gap-2`); Library's "photo" view was the 2-col big-card orphan. Q1 revised to A': rename Library "condensed" → "gallery" semantically (peer-matching), DROP the 2-col big-card mode, keep list view. **Net: Library binary toggle = gallery (3-col, peer-matching) ↔ list.** This ship swaps the `gridDisplayStyle === "photo"` JSX branch from 2-col big-cards to 3-col gallery (= recovered pre-`a51fbd7` condensed JSX with Item 3 mt-auto-removed + Item 4 hide-own gate re-applied). Persistence migration unchanged — persisted-`"condensed"` users still auto-migrate to `"photo"` which NOW semantically IS the 3-col gallery they originally chose (perfect outcome); persisted-`"photo"` (was 2-col big cards) users get the 3-col gallery per drop-big-card-mode lock. **4-pass plan-audit clean iter 1** on as-written corrective change. 1 file (src/components/SeedVaultView.tsx), +10/-11. Tests 444/444; build clean. NO Preview MCP per HARD RULE. **Lesson:** in the framework-level ask (Q1), names like "photo" vs "condensed" carry implicit assumptions about which is canonical — clarifying which JSX visually = peer gallery before locking is a future audit-step. Captured for sibling-sweep methodology backlog.

🟢 **`a51fbd7` (Inventory polish bundle — 6 items + discovery-vs-lookup sort framework — `inventory-polish-bundle-5items` chat)** — `polish(inventory): bundle 6 items + lock discovery-vs-lookup sort framework (VISION §8)` — Closes Syd's 2026-05-27 dogfood batch after gallery card standardization (fb5d9a0). All 6 items shipped as one cohesive bundle per Q3=A greenlight. **(1) Default-view-gallery across 5 inventory surfaces** — first-open default flips from list/condensed to gallery on Library, Packets, Shed, Active Garden (My Plants already gallery). Persistence preserved via sessionStorage / useSessionStorage — only fresh-state users see the new default; persisted-`"list"` users keep their preference. **(2a) Packets default sort = variety asc + OOS-bottom** — sort comparator gains `is_archived || qty_status <= 0` tiebreaker that pushes OOS rows to bottom when sortBy === "variety" regardless of asc/desc direction (Q2=A trigger condition). Other sortBy values respect literal sort. **(2b) Shed default sort = name asc** — VaultShedWing wrapper (embedded path) + ShedView internal default both flipped. No OOS handling (supply_profiles has no stock column). **(3) Gallery card top-anchoring** — removed `mt-auto` from third metadata row on SeedVaultView photo cards, ShedView grid cards, MyPlantsView grid cards. Cards with less content stack from top; whitespace goes to bottom. Refines fb5d9a0 lock; doesn't break standardization. **(4) Owner pill foreign-only** — added `user.id !== currentUserId` gate to OwnerBadge call sites across PacketVaultView (grid + list), ShedView (grid + list), ActiveGardenView (grid + list — list site was a Pass 3 sibling-sweep miss caught during execution), SeedVaultView (gallery + condensed branches — also a Pass 3 sibling-sweep miss caught during execution). MyPlants already used this pattern via FAM badge (different visual primitive, deferred for future cohesion pass). **(5) Library list view + toggle reshape** — replaced `"condensed"` mode with proper list view (Q1=A locked). Library binary toggle is now gallery ↔ list, matching the 4 peer surfaces. Persisted-`"condensed"` users auto-migrate to gallery via sessionStorage migration. List row format mirrors Packets list pattern (10x10 thumb + name + variety/packet-count subtitle). Toolbar toggle uses same SVG 3-line list icon as garden/page.tsx pattern for cohesion. **Framework lock — VISION §8 "Default sort by use case (discovery vs lookup)":** discovery surfaces (Library, Active Garden, My Plants) = most-recent first; lookup surfaces (Packets, Shed) = alphabetical by name; lookup + OOS-equivalent concept → OOS at bottom regardless of direction. Pairs with CLAUDE.md quick-lookup callout (same pattern as other 2026-05-27 convention locks). Library + My Plants currently default to alphabetical despite being discovery surfaces — gap noted as parked-for-future-polish; framework is source-of-truth even when current state hasn't caught up. **4-pass plan-audit terminated clean iter 1 + 1 amendment re-audit** (Shed extension added mid-Phase-1 per Syd's lookup-surface framework lock). Two mid-execution Pass 3 sibling-sweep misses caught + applied inline (ActiveGardenView list-view OwnerBadge site at L1216 + SeedVaultView's 2 OwnerBadge sites at L1207 + L1289). 13 files, +140/-112. Tests 444/444; build clean. **NO Preview MCP** per chat HARD RULE + CLAUDE.md lock 2026-05-27 (tests + build + git diff sufficient; Syd dogfood verifies visual). Concurrent-fetch validation 0/0 pre-push. Plan + audit at `.claude/plans/inventory_polish_bundle_5items.md`. Syd dogfood path: visit `/vault` → confirm Library opens to gallery by default → toggle to list → confirm rows match Packets/Shed style → toggle back → switch to Packets → confirm sort defaults to alphabetical → confirm OOS rows at bottom → switch to Shed → confirm gallery default + alphabetical sort → switch to `/garden?tab=active` → confirm gallery default → switch to family view → confirm own-items have no pill, foreign items show pill. Out-of-scope (parked): owner-pill visual cohesion (OwnerBadge emerald vs FAM violet — separate cohesion polish), Library + My Plants discovery-default gap (framework lock points to most-recent but current code is alphabetical), Calendar + Journal OwnerBadge sites (out of inventory scope; same hide-own gate would apply).

🟢 **`dcfdbe4` (Phase 2 per-profile zone-window AI enrichment — `grow-now-phase-2-zone-enrichment` chat)** — `feat(enrich): Phase 2 — per-profile zone-window via AI (BUGS Post-Launch #1 capability gap)` — Closes the capability gap behind Phase 1's honest-framing stopgap (feb2e32). Server-side `user_settings.planting_zone` read added to `/api/seed/enrich-from-name` route; passed as 5th optional `userZone` param to `researchVariety()` (now in `src/lib/researchVariety.ts:65`); appends a `zoneClause` template to the Gemini prompt instructing zone-specific window generation in Jan-Dec month-abbreviation format (preserves downstream `plantingWindow.ts:getDefaultSowMonthsForZone10b` parser compatibility). Library-cache skipped for non-10b users (`global_plant_library` lacks a zone column; cost-bounded by existing AI-fallback path). 10b users + unset-zone users: UNCHANGED behavior. New `zoneUsed` field on the API response; 3 new `logEvent("enrich", ...)` breadcrumbs in `enrichProfileFromName` (zone_requested / zone_window_generated / zone_fallback) for `/settings/developer/debug-log` diagnosability. 4 caller modals (AddVarietyModal, AddPlantModal, QuickAddSeed, PlantingForm) wire `useUserPlantingZone()` hook + pass `userZone` option (client-side for logging only; server is authoritative). **4-pass plan-audit terminated clean iter 1.** Pass 1 factual including verifying `extract/route.ts:171` re-exports `researchVariety` from `@/lib/researchVariety` (so the surgical edit point covers all 3 production callers + admin scripts pass `undefined` for backward compat); bidirectional state-transition matrix SKIP-with-reason (stateless background fetch — no modal/menu lifecycle). Pass 2 concerns hunt + persona walk all 5 pass + cohesion-by-aggregation anchors named for every micro-decision (logEvent format ↔ existing scope pattern; response field shape ↔ existing snake/camel mix; prompt phrasing as explicitly-named new pattern). Pass 3 sibling sweep across 3 researchVariety callers + vocab-breadth grep `zone|window|usda|hardiness|climate|sow|planting`. Pass 4 lock hygiene clean — VISION §10 don't-touch untouched (no UI surface changed); §11 Phase 3 multi-zone Schedule rebuild explicitly deferred (this ship is Phase 2 only). 9 files, +218/-5. Tests 444/444 (was 438; +6 new zone-aware tests using new `makeSupabaseWithZone` per-table mock factory). Build clean. **NO Preview MCP** per chat HARD RULE (data-layer change, not browser-observable; tests + build + git diff sufficient). Concurrent-fetch validation 0/0 pre-commit. Plan + audit at `.claude/plans/grow_now_phase_2_zone_enrichment.md`. Out-of-scope (parked): Phase 3 Schedule rebuild stays parked; `fill-blanks-for-profile` route (transparently benefits via internal `enrich-from-name` fetch); backfill scripts (admin context, no user-zone meaning); vendor-URL extraction (`extract/route.ts:441` — zone-agnostic). BUGS Post-Launch #1 ticket annotated with capability-portion closure; remains open until Phase 3 closes the cross-profile aggregate-schedule view portion.

🟢 **`feb2e32` (Phase 1 zone-aware framing stopgap on grow-now surfaces — `grow-now-zone-investigation` chat)** — `polish(schedule): zone-aware framing stopgap for non-10b users` — Closes the deception gap that has sat in BUGS.md Post-Launch #1 since session start: `plantingWindow.ts` + `zone10b_schedule.ts` are explicitly hardcoded to Zone 10b (verbatim TODOs at [zone10b_schedule.ts:9](src/data/zone10b_schedule.ts:9) + [plantingWindow.ts:3](src/lib/plantingWindow.ts:3)) while `user_settings.planting_zone` is stored + onboarding-gated + Settings-editable but never read by any scheduling logic. For non-10b users this surfaces as misleading "plant in March" advice that contradicts their microclimate (VISION §6 Theme 3 "Micro-climate timing untrusted" failure mode — sister's verbatim: *"doesn't feel that the app is reliable for what to plant when"*). Real fix is Phase 2 (per-profile AI-enriched windows) + Phase 3 (multi-zone dataset OR frost-date computation); deferred to dedicated chats. This ship is honest-framing only. **Surfaces:** (a) Schedule page header at [schedule/page.tsx:53](src/app/schedule/page.tsx:53) — when user zone is set and ≠ "10b", header swaps from "Zone 10b reference guide — when to start indoors or plant outside. Not your vault." to "Showing Zone 10b reference — your zone is {N}. Recommendations may not match your microclimate yet. (Coming soon: zone-aware schedule.)"; 10b users + users with no zone set see existing copy. (b) Calendar "Plant this month" widget at [calendar/page.tsx:990-1037](src/app/calendar/page.tsx:990) — info-note appended to footer of expanded panel when user zone ≠ "10b": "Showing plants with empty windows defaulted from Zone 10b. Edit a plant's profile to set a custom window." Info-note primitive per VISION §8 (`text-sm text-neutral-600 italic`, sentence case, no fill/border/icon — matches the lock from `39d5faa` 2026-05-27). New shared hook [src/hooks/useUserPlantingZone.ts](src/hooks/useUserPlantingZone.ts) reads `user_settings.planting_zone` (sibling fetch pattern to [useOnboarding.ts:91](src/hooks/useOnboarding.ts:91)) + exports `isReferenceZone()` helper (returns true for "10b" or null; null = no advisory shown so we don't surface claims we can't substantiate). **Persona walk Pass 2** — Walter / Sam / Aria / Maya (non-10b) now see honest framing instead of misleading advice; Sydney (10b) unchanged. Closes the 3-of-5-persona failure on the prior state. **Pass 3 sibling-sweep ADJACENT logged:** 3 child schedule view headers (ActionNowView L31 + MonthlyPulseView L38 + AnnualRoadmapView L55) still carry "Zone 10b reference:" prefix. Not contradictory with the new parent advisory (both honestly say "Zone 10b data"), just redundant for non-10b users. Out of scope per Syd's locked scope (in-scope = parent header only); flagged for future cohesion polish. **Pass 4 lock hygiene clean:** VISION §10 "Plantable widget separation" preserved (info-note nests inside existing expanded panel — not merged elsewhere); no §11 parked decisions touched; no state-machine surface; no contexts. **Bidirectional state-transition matrix:** SKIP-with-reason — pure inline-rendered copy + helper-call addition; no modal/menu lifecycle change. **E2E-scope sub-rule clean:** no e2e assertions reference the Schedule page header or Calendar plantable note text (verified by grep); zero spec updates required. 3 files (1 new hook, 2 edited), +70/-1. Tests 438/438; build clean. **NO Preview MCP** per chat HARD RULE (2026-05-27 lock + Syd's explicit prompt re-statement); tests + build + git diff sufficient. Syd dogfood path on prod: `/schedule` → confirm header reads zone-aware advisory (since prod user zone ≠ 10b? need to check; if user IS in 10b on prod, change zone temporarily via Settings → Profile to verify swap, then revert) → `/calendar` → expand "Plant this month" widget → confirm info-note appears at footer. Concurrent-fetch validation pre-push: 0 ahead / 0 behind (validation #16 of the rule running clean). Plan + audit in-chat (XS scope, inline plan per CLAUDE.md threshold).

🟢 **`c851b3d` (Garden list views adopt Packets+Shed condensed format — `garden-list-condensed-format` chat)** — `polish(garden): list views adopt Packets+Shed condensed format` — Active Garden + My Plants list views migrated to share the condensed wrapper pattern already used by Seed Packets + Shed list views. **Format change only — DATA preserved verbatim.** Outer container becomes `rounded-xl border border-black/10 bg-white overflow-hidden` (single border on the whole list, not per-row). Inner `<ul className="divide-y divide-black/5">` replaces `space-y-4` (rows share a subtle divider instead of being separated by 16px gap). Row container becomes `flex items-center gap-3 px-3 py-2 min-h-[44px] hover:bg-gray-50 transition-colors` (was `flex items-start justify-between gap-3` + p-4 card + shadow). Thumbnails 12→10 (rounded-xl→rounded-lg on My Plants). Title `font-medium text-black/90` (Active Garden) / `font-semibold text-neutral-900` (My Plants) → both `font-semibold text-sm text-neutral-900`. My Plants variety subtitle `text-sm` → `text-xs`; meta chip-row `text-xs` → `text-[11px]` to match Shed's chip register exactly. Active Garden's sown/harvest paragraph stays `text-xs` (sentence register, not chip register — content-shape preserved). Per-row highlight + bulk-select ring moves to inner element as `ring-inset ring-2 ring-emerald-500 bg-emerald-50/80` (was `ring-2 ring-emerald-500 ring-offset-2` on `<li>` — ring-offset incompatible with divided list). Bubble `mt-1` dropped (items-center handles alignment). My Plants inline `boxShadow` style removed (card shadow goes with the card pattern). **DATA preserved:** Active Garden progress bar (h-2 unchanged), sown/harvest paragraph + seed/sprout/plant/harvest counts, planting_method_badge, location chip, owner badge, "Add journal entry" pen icon, Link href, bulkMode handling, long-press → bulk-select bubble, scroll-into-view `highlightBatchRef`. My Plants planted-ago + care/journal counts, Perennial chip, FAM chip, "Add journal entry" pen icon, `getLongPressHandlers` wiring, `batchSelectMode` bubble, `householdViewMode` family-view gating. **Anchors cited:** [PacketVaultView.tsx:744-803](src/components/PacketVaultView.tsx:744) + [ShedView.tsx:387-467](src/components/ShedView.tsx:387) — both reference patterns verbatim; chose Shed's `px-3 py-2` over Packets' `px-3 py-3` because both targets have multi-line content (title + subtitle + meta) matching Shed's content shape. Selection ring anchored to Shed's `bg-emerald-50/80 ring-inset ring-2 ring-emerald-500` (preferred over Packets' `border-2` which would double up against the divider). **4-pass plan-audit terminated CLEAN iteration 1** with one pre-build plan revision: initial draft slimmed the Active Garden progress bar `h-2 → h-1.5` but reverted at Pass 2 because the bar has no anchor in reference patterns + counts as "DATA" not wrapper-shape per the "format change only" framing. Pass 1 factual: every cite verified ([PlantImage.tsx:22](src/components/PlantImage.tsx:22) size="sm"=w-10 h-10; [PlantPlaceholderIcon.tsx:4](src/components/PlantPlaceholderIcon.tsx:4) size="sm"=w-10 h-10; ICON_MAP.Edit + OwnerBadge + PlantImage + Link + formatBatchDisplayName imports + all hook bindings already in scope). Bidirectional state-transition tracing Pass 1 subcategory: **SKIP-with-reason** documented — list-view migration preserves state-machine shape verbatim (no modal/menu lifecycle touched; click handlers and long-press behaviors preserved literally; matches CLAUDE.md skip condition "refactors that preserve state-machine shape verbatim"). Pass 2 concerns hunt + cohesion-by-aggregation: 8 micro-decisions named with anchors (row padding `px-3 py-2` → Shed; thumb dimensions → both peers; divider → both peers; container → both peers; selection ring → Shed; hover → both peers; title font → both peers; subtitle → both peers; meta row register → Shed) + persona walk all 5 pass (Maya scan-the-list faster / Sydney cohesion / Walter 44×44 targets preserved + long-press preserved / Aria less heavy register / Sam empty-state behavior unchanged). Pass 3 sibling sweep: Packets + Shed peer pattern verified; JournalView gallery/list ADJACENT logged for future polish (separate chapter). Pass 4 lock hygiene: VISION §10 don't-touch — Active Garden gallery (grid view) untouched per "the bug was clipping, not the view itself" lock; §11 parked "Cross-view consistency: Vault grid / Garden gallery / My Plants list" partially closed (list-view portion now cohesive across all four surfaces); ROADMAP §6 + VISION §4 + §8 untouched. **E2E-scope sub-rule clean:** zero visible labels changed (CSS class swaps only); grep of `e2e/` for "My Plants" / "Active Garden" returned zero hits. 2 files, +24/-16. Tests 438/438; build clean. **NO Preview MCP** per chat HARD RULE locked 2026-05-27 (tests + build + git diff is sufficient; visual verification falls to Syd dogfood). Concurrent-fetch validation pre-push: 0 ahead / 0 behind. Plan + audit at `.claude/plans/garden_list_condensed_format.md`. Syd dogfood path: visit `/garden` → switch both Active Garden + My Plants to list view → confirm rhythm matches Library / Packets / Shed list views.

🟢 **`b6e4b04` ("Back to X" labels collapsed to "Back" — iOS-standard pattern)** — `polish(nav): collapse all "Back to X" labels to "Back" — iOS-standard pattern`. (Lead-line for prior 4 close-outs already archived per the natural roll-forward; this is the first batch entry that surfaced after `9ab4dab` was logged. Captured here as part of the rolling 🟢 list for this chat's roll-forward.)

🟢 **`a17f40a` (Vault profile-detail back-button source-aware)** — `fix(vault): profile-detail back-button is source-aware — Library / Packets / Shed`.

🟢 **`fc338c2` (Packets list↔grid toggle + Library photo-card active outline parity)** — `polish(vault): Packets list↔grid toggle + Library photo-card active outline parity`.

🟢 **`9ab4dab` (Plant Profile card polish — remove inventory dot + packet count)** — `polish(vault): remove inventory dot + packet count from Plant Profile cards` — Closes Syd's 2026-05-26 dogfood (re-flagged 2026-05-27): the small color-indicator dot + packet count digit on each Plant Profile card mixed signals with the green "actively growing" outline ("0 inventory" + green outline read as contradictory). Card title row simplifies from `<name + HealthDot + count>` to `<name>` only across both grid card variants (photo + condensed) in [src/components/SeedVaultView.tsx](src/components/SeedVaultView.tsx). Now-orphaned `HealthDot` + `getHealthColor` helpers deleted per no-dead-code rule. `getCardBorderClass` untouched — green active-growing outline preserved (governed by `seed.status === "active"`; `packet_count` still flows through as DATA input for the out-of-stock dashed border, just not displayed). Packet inventory display stays scoped to the Seed Packets tab via `PacketVaultView` (different component, untouched). 1 file, +1/-29. Tests 438/438; build clean. **Pass 3 ADJACENT finding logged for future polish:** photo-card variant of Plant Profile cards (lines ~1252-1280) doesn't apply `getCardBorderClass` (hardcoded `border border-black/5 hover:border-emerald-500/40` instead) — only condensed variant (lines 1341+1354) renders the active green outline via the helper. Pre-existing asymmetry; default `gridDisplayStyle="condensed"` means users see the outline normally, but explicit photo-card toggle currently doesn't. Separate polish item. Preview MCP at 412×915: dev server reached AuthGuard login redirect on `/vault` (auth-blocked per documented condition; no `.env.local` session); no console errors after fresh dev rebuild. User phone-verifies on prod (Vault → Plant Profiles tab → confirm cards show variety + green outline when actively growing, no color dot, no count number).

🟢 **`bde3745` (From Vault → in-menu overlay — `fab-tree-route-to-modal-conversion-cluster` chat)** — `feat(fab-tree): From Vault → in-menu overlay (close slide-up-vestigial)` — Closes Syd's "From Vault menu slides up and is diff than our other menus" dogfood feedback structurally. PlantingFlowModal bottom-sheet (z-60, `animate-modal-slide-up`, `sm:max-w-2xl` iPad+) converted to in-menu overlay mounted inside UniversalAddMenu's flex-col panel as new `"add-plant-from-vault"` sub-screen. Mirrors AddPlantModal C1 embedded-mount pattern from `ba9319f` (2026-05-26). **Scope:** `UniversalAddMenuScreen` union extension (+ matching `UniversalAddMenuScreenTarget`); new screen branch with menu-owned header (Back arrow `goBackToAddPlant` + "Planting" title + w-11 spacer); "From Vault" chip handler swap from `onClose() + onAddPlantFromVault()` to `setScreenDirection("forward") + setScreen("add-plant-from-vault")` (matches Manual entry chip shape verbatim); drop `onAddPlantFromVault` prop from `UniversalAddMenuProps` + all 5 page mounts (Home / Calendar / Garden / Journal / Vault); drop `plantingFromVaultOpen` state + dynamic import + `PlantingFlowModal` mount from each page; **delete `src/components/PlantingFlowModal.tsx`** (no remaining consumers — verified by grep); drop its preload line from FAB-menu-open warm-up effect. **Q2=B OVERRIDE locked in:** the bottom-sheet decision from `.cursor/plans/fab_tree_modal_consistency.plan.md` Item 1 (2026-05-26 morning, shipped via `aa4c116`) is overridden per user explicit reaffirm — AddPlantModal C1 embedded mount (2026-05-26 evening, `ba9319f`) proved the in-menu pattern works for complex forms with secondary modals + sticky bottom-bars, making in-menu the canonical FAB chip shape. Trade-off heard + accepted: iPad-portrait viewport tightens from `sm:max-w-2xl` (~672px) to menu's `max-w-md` (448px); peer screens (seed/shed/task/journal/add-plant-manual) all use same width so this is consistent with sibling pattern. **Accepted regression (Path Y Q1=Option B precedent):** pages don't auto-refresh after in-menu successful add — Garden + Vault need manual refresh to see fresh data. Same shape as 5 sibling in-menu paths; functionally important enough on Garden/Vault to warrant a `notifyPlantingSuccess` follow-up if user flags it. **`/vault/plant` page route preserved verbatim** for non-FAB callers (Calendar Plantable Plant button, Vault toolbar batch-select with `?ids=`). PlantingForm internal logic unchanged. **Pass 1-4 audit terminated clean.** Pass 1 factual + new bidirectional state-transition matrix (CLAUDE.md provisional rule from `789688a`) applied across 4 axes (Open/Close, Forward/Back, Mount/Unmount on 5 page mounts, Suspense/Lazy resolution) — surfaced C3 (celebration/onSaved ordering) which was verified clean at [PlantingForm.tsx:456-465](src/components/PlantingForm.tsx:456) (800ms `setTimeout` fires `setShowSeedlingCelebration(false)` + `onSaved?.()` together so menu close cascades after celebration completes). Pass 2 concerns hunt + persona walk: C2 (PlantingForm modal-mode footer has Confirm planting only, no Cancel button — adjacent to FAB-tree cohesion since SeedPacketForm/SupplyForm/TaskForm/JournalEntryForm all have own Cancel; deferred since reshaping requires both page-mode + modal-mode work, ADJACENT for future polish) + C8 (iPad `max-w-md` cramp accepted) parked. Pass 3 sibling sweep clean — `add-plant-manual` is the closest analog and every shape matches verbatim. Pass 4 lock hygiene clean — Q2=B override explicitly documented in plan file + this ROADMAP §6 entry; VISION §10 don't-touch + §11 parked decisions untouched. **Concurrent-fetch validation #14** pre-push 0/0 clean. Tests 438/438; TypeScript clean on diff (pre-existing test errors unrelated); local Next build hit unrelated `/api/developer/backfill-cache` page-data-collection error due to dev server holding `.next` state on Windows — confirmed not caused by diff (TypeScript clean + tests clean + Preview MCP console clean post-HMR). Preview MCP auth-blocked from FAB-tree paths per documented condition; user phone-verifies on prod. 7 files (5 page mounts + UniversalAddMenu + PlantingFlowModal deletion), +30 / -206 net. **First real-world test of CLAUDE.md provisional bidirectional state-transition tracing rule (`789688a`)** — matrix surfaced C3 (celebration timing trace) that pure Pass 2 concerns hunt may have missed; rule running as designed. **Also first real-world test of orchestrator-spawn-prompt stale-memory detection (Step 0 search)** — both Chapter A + B framings were stale per current code state (`aa4c116` + `7242cb6` already shipped 2026-05-26); surfaced before plan-of-record locked; user explicitly reaffirmed Chapter A as override + closed Chapter B as no-op. Plan file at `.claude/plans/fab_from_vault_in_menu_conversion.md`.

🟢 **`ba9319f` (Commit 4 — AddPlantModal C1 embedded mount + PlantingForm cohesion — `continue-from-prev-chat-fab-form-submit-saga` chat)** — `refactor(forms): Commit 4 — AddPlantModal C1 embedded mount + PlantingForm cohesion` — Closes the FAB-tree polish saga. AddPlantModal grows `embedded?: boolean` prop; when true, renders the 3-section triplet directly into the menu panel without standalone backdrop + panel wrappers; useFocusTrap + useEscapeKey + useBodyScrollLock all gated on `!embedded` (menu owns these single-source-of-truth concerns when embedded). enrichmentFailed secondary modal stays as-is (z-[60] fixed inset-0, positioning DOM-parent-independent). UniversalAddMenuScreen union grows `"add-plant-manual"`; Manual entry chip handler changed from `onClose() + onAddPlantManual()` (closes menu + opens standalone) to `setScreenDirection("forward") + setScreen("add-plant-manual")` (slides in-place matching the 4 other FAB chip sub-screens). 5 page mounts drop `onAddPlantManual={openPlant}` wiring; 4 of 5 also drop now-unused `openPlant` destructure (garden/page.tsx retains for empty-state). PlantingForm bundled verify-only tweaks: `border-black/10` → `border-neutral-200`, `bg-emerald-500 hover:bg-emerald-600` → `bg-emerald-600 hover:bg-emerald-700` on Confirm Planting button (no shell change; single-button bar shape preserved). VISION §11 Save/Cancel parked decision CLOSES; `.cursor/plans/fab_addplantmodal_extraction.plan.md` parked plan CLOSES. 8 files, +42/-25. Tests 438/438; build clean. Preview MCP auth-blocked from FAB-tree paths (login redirect) — real-device verification needed.

🟢 **`8d6562f` (Commit 3 — Batch + PurchaseOrder standalone modals fixed-bar standardization — `continue-from-prev-chat-fab-form-submit-saga` chat)** — `refactor(forms): Commit 3 — Batch + PurchaseOrder standalone modals fixed-bar standardization` — Migrates the 3 standalone FAB-reachable multi-step modals. PurchaseOrderImport: amendment from plan — single-step modal (review on separate page after redirect), not multi-step; footer = Cancel + Extract; Extract disabled until `!previewUrl || isExtracting`; Close-X dropped from header (cohesion). BatchAddSupply: 2-step (capture/extracting); footer = Cancel always + "Extract & Review (N)" on capture only (disabled until readyCount > 0); Cancel disabled during extracting. BatchAddSeed: 3-step (capture/extracting/review) + success state; per-step footer (capture: + "Review pending (N)"; extracting: Cancel only; review: + "Load Plant Profile Picture"; success: single Done); header gains back-arrow on review step (→ setStep("capture")) preserving previous in-body Back-button functionality. All 3 outer panels: `overflow-y-auto p-6` → `flex flex-col overflow-hidden`. Save-button color: 5 inline `bg-emerald` (= emerald-500) Saves migrate to `bg-emerald-600 hover:bg-emerald-700`. 3 files, +213/-187. Tests 438/438; build clean.

🟢 **`003e3b4` (Commit 2b — SeedPacketForm + SupplyForm fixed-bar standardization — `continue-from-prev-chat-fab-form-submit-saga` chat)** — `refactor(forms): Commit 2b — SeedPacketForm + SupplyForm fixed-bar standardization` — Migrates the 2 multi-step in-menu forms. SeedPacketForm: 3-step (choose/manual/link) with 2 sub-modes on manual; two `<form>`s carry `id="seed-link-form"` + `id="seed-manual-form"` so footer Save buttons use HTML5 `form="<id>"` attribute to trigger submit from outside the form DOM tree (preserves Enter-key submit). Save-for-later (new-mode only) moves inline next to its helper text; disable tightened to `submitting || !plantName.trim()`. Per-step footer: choose = Cancel only (full-width single-button matching main-screen anchor); link = Cancel + Import (disabled until importUrl); manual link-mode = Cancel + "Add to vault"; manual new-mode = Cancel + "Add to vault (with packet)". SupplyForm: 3-step (choose/link/form) + Edit mode; `<form id="supply-form">` + footer Save via `form="supply-form"`. Both standalone shells: `p-6 max-h-[85vh] overflow-y-auto` → `max-h-[85vh] flex flex-col overflow-hidden animate-modal-slide-up`. UniversalAddMenu seed + shed sub-screens drop the `<div className="flex-1 min-h-0 overflow-y-auto px-6 py-6">` wrapper (forms own the shell now). Plan-of-record amendment captured mid-execute: "Back + Save" → "Cancel + Save" per cohesion-by-aggregation with 2a anchors; re-audit Pass 1-4 clean. 3 files, +258/-214. Tests 438/438; build clean.

🟢 **`f79a8aa` (Debug-log 8-layer instrumentation — `continue-from-prev-chat-stalled-logger-coverage` chat)** — `feat(debug-log): 8-layer in-app instrumentation for diagnostic breadcrumbs` — Additive logging across 8 layers so user-reported bugs can be diagnosed from the `/settings/developer/debug-log` copy-paste alone (no screenshot-of-text round trips). 19 files (2 NEW: `src/lib/debugLog.ts` + `debugLog.test.ts`; 17 EDITED). **Layers:** (1) `logEvent(scope, action, payload?)` helper — bracket-prefix `[scope] action {payload}` matches `apiErrorLog.ts` sibling shape (Pass 3 cohesion anchor). (2) Buffer cap 50 → 500 in `debugLogBuffer.ts` (test parametrized via `__test__.MAX_ENTRIES`). (3) Boot in `DebugLogInit` + auth events (`getSession`/`onAuthStateChange`/`signOut`) in `AuthContext` + SW lifecycle (`register/registered/update_found/controller_changed/register_error`) in `ServiceWorkerRegistration` (previously-silent SW catch now logged). (4) Network online/offline + offline-queue replay (`replay_start/replay_ok/replay_retry/replay_done` with counts) in `OfflineIndicator`. (5) Navigation `[nav] enter {path}` via `usePathname` in `DebugLogInit`. (6) Onboarding `action/complete/dismiss` in `useOnboarding`. (7) Supabase mutations — 5 helpers in `supabaseWithOffline.ts` (insert/update/insert_many/upsert/delete) wrap with `start + ok/error/queued` including `table, op, ms, online, error.message`. (8) ErrorBoundary catch + `useToast.showErrorToast` + 6 canonical FAB-flow form envelopes (`add_plant` / `add_seed` / `add_supply` / `add_journal` / `add_task` / `harvest`) with submit/success/error tagging. **Privacy contract (load-bearing):** payload SHAPE only — counts, ids, table names, op kinds, durations, error.message. Never plant names, notes, journal text, supply names, or any user content. `logEvent` wraps `console.log` in try/catch so logger can never crash the app. **Exclusions** (signal/noise): no read queries, no render cycles, no mouse events, no success toasts, no calendar cache hits, no per-asset SW cache events. **Pass 3 CONCERN** captured (codebase has bracket `[scope]` vs colon `scope:` console.error split in lib helpers; new `logEvent` joins bracket family per handoff substance; colon-notation peers unchanged — see §4 new parked entry for future cohesion pass + ADJACENT items). **Pass 4 lock hygiene clean** — no VISION §10 don't-touch brushed; HarvestModal parked end-state realization decision (§11) preserved (envelope addition only). Dev tooling under `useDeveloperUnlock` — all 5 personas pass by exclusion. Tests 434 → 438 (+4 from `debugLog.test.ts`). Build clean. Concurrent-fetch 0/0 pre-commit. **Plan rebuilt this chat** after previous chat's plan file lost (never committed — previous chat stalled on AskUserQuestion before AskUserQuestion-forbidden rule shipped in `f4c2075`). Plan file at `.claude/plans/gt-debug-log-instrumentation.md` (committed for audit trail).

🟢 **`6691d20` (Calendar section-header casing fix — `gt-casing-theme3-a1` chat)** — `feat(plain-language): Calendar "Upcoming Tasks" → "Upcoming tasks"` — Section header on Calendar page (above the upcoming task list, shown when no specific date is selected). Caught in Syd's dogfood after Phase A.1 + casing sweep. **Pass 3 sibling cohesion:** aligns with the OTHER conditional branch of the same `<h2>` at L1226 ("Tasks for {date}") which already uses sentence case — the Title Case "Upcoming Tasks" was the outlier within its own conditional render. 1 file (src/app/calendar/page.tsx:1233), +1/-1. Tests 434/434; build clean. Companion "Back to Settings" rename in the same dogfood batch HELD pending Syd's call — Pass 3 sweep found 21 "Back to [PageName]" peer sites all Title Case; page-label preservation lock from `006dd69` Item 2 applies; flipping just the vendors page would create a new outlier + preempt Phase A.2's full page-label rename pass.

🟢 **`1548280` (§3.12 theme #3 Phase A.1 #4 followup 2 — `gt-casing-theme3-a1` chat)** — `feat(plain-language): §3.12 theme #3 Phase A.1 — dedup time-frame on Plantable title` — Second one-line followup to `8310070` + `8261304`: drops the `{month}` interpolation from the widget title. **Plant this month ({count})** is the final wording. Drops the month interpolation since the calendar page header above already provides the time context (month-nav widget shows the month label). **Pass 3 sibling-sweep cohesion WIN:** the new wording matches the canonical peer pattern already in the codebase — `Plant this month ({count})` appears verbatim at [VaultPacketWing.tsx:565](src/app/vault/components/VaultPacketWing.tsx:565) + [VaultPacketWing.tsx:815](src/app/vault/components/VaultPacketWing.tsx:815) + [VaultPageContent.tsx:1140](src/app/vault/VaultPageContent.tsx:1140). The Calendar widget was the outlier; this rename brings it into perfect alignment with 3 peer sites. Exactly the kind of sibling-pattern cohesion win the Pass 3 sweep catches. Aria-labels at L1028+L1029 ("See plants for this month in Vault") preserved as-is — already in plain temporal form, cohesive with new visible-label framing. Semantic accuracy preserved: when user navs to a future/past month, the calendar nav header provides the disambiguating context. 1 file (src/app/calendar/page.tsx:999), +1/-1. Tests 434/434; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition.

🟢 **`8261304` (§3.12 theme #3 Phase A.1 #4 followup — `gt-casing-theme3-a1` chat)** — `feat(plain-language): §3.12 theme #3 Phase A.1 — shorten Plantable title per Syd redirect` — One-line followup to `8310070`: shortens widget title from "Ready to plant in {month}" → **"Plant this month"** per Syd's mid-chat redirect ("also okay with 'Plant in' to remove 'ready to'"). Verb-led action framing instead of state-flavored framing; matches her plain-terse preference. Aria-labels at L1028+L1029 ("See plants for this month in Vault") preserved as-is — full-sentence framing reads natural in screen-reader context. Semantic correctness preserved from `8310070`: broad verb "plant" still covers all 3 paths (sow / transplant / plant out) since `plant_profiles` data source includes all profile types. 1 file (src/app/calendar/page.tsx:999), +1/-1. Tests 434/434; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition.

🟢 **`8310070` (§3.12 theme #3 Phase A.1 #4 of 4 — `gt-casing-theme3-a1` chat)** — `feat(plain-language): §3.12 theme #3 Phase A.1 — Plantable widget rename (4 of 4)` — Plantable widget on Calendar page renamed to "Plant this month" + matching title attr + aria-label updates ("See plants for this month in Vault"). 1 file (src/app/calendar/page.tsx L999 + L1028 + L1029), 3 line-edits. **Plantable verb-choice driven by data-source audit:** Syd flagged sharp semantic concern *"ready to sow? like plant by seed? or transplant?"* mid-plan. Audit found `plant_profiles` data source includes ALL profile types (perennials, established plants, anything with `planting_window`), not seed-only; field name (`planting_window`) + function names (`isPlantableInMonth*`) + existing aria-labels all already lean "plant" not "sow." Concrete misfire prevented: apple-tree profile with `planting_window="feb-apr"` + `status="in_stock"` would have rendered "Ready to sow in March" (wrong). Broad-verb "Plant" covers all 3 paths (sow seeds / transplant seedlings / plant out perennial starts). Drilldown modal h2 "{plantType} – Seed inventory" preserved as-is (widget-title-vs-drilldown asymmetry captured as ADJACENT for separate data-shape chunk). Tests 434/434; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition. Plan + audit done in-chat (XS scope; data-source verification was the load-bearing audit step).

🟢 **`b669a2a` (§3.12 theme #3 Phase A.1 #1-3 of 4 — `gt-casing-theme3-a1` chat)** — `feat(plain-language): §3.12 theme #3 Phase A.1 — 3 of 4 page-label renames` — 3 plain-language page-label renames per §3.12 catalog theme #3 P0. (1) Vendor Scorecard → "Vendor ratings" — Settings menu + Vendors page h1; reuses "ratings" from existing subtitle "Average packet ratings by vendor"; Walter-plain. (2) Monthly pulse → "Monthly view" — Home subtitle + Home button + Schedule view-toggle. (3) Annual roadmap → "Yearly view" — Home subtitle + Home button + Schedule view-toggle. Option 3 (calendar-app convention: Monthly view / Yearly view pair) chosen by Syd over Option 1 (maximally plain: "This month" / "Year ahead") and Option 2 (descriptive: "This month's plantings" / "Year's plantings"). 4 files (src/app/page.tsx + src/app/schedule/page.tsx + src/app/settings/page.tsx + src/app/settings/vendors/page.tsx), 7 line-edits (7 insertions / 7 deletions). URL params (`?view=heatmap` / `?view=roadmap`) preserved as programmatic identifiers; internal component name `MonthlyPulseView.tsx` preserved (not user-facing). Tests 434/434; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition. Concurrent-fetch 0/0 pre-commit.

🟢 **`a78dbd6` (FAB-tree casing-cohesion sweep — `gt-casing-theme3-a1` chat)** — `feat(casing-sweep): FAB-tree labels + headings → sentence case + 3 sibling files` — 22 Title Case → sentence case edits across 7 files. **Scope-1 (6 named modals):** QuickAddSeed (Plant name / Variety / cultivar / Vendor / nursery ×2 visible + aria-labels ×2), QuickAddSupply (Product name / Soil amendment option / Application rate / Usage instructions), QuickLogModal (QUICK_ACTIONS labels Pot up / Plant out / Cold stratify; Supply used SearchableMultiSelect label; "+ Add new supply" emptyStateAction; Take photo button), PurchaseOrderImport (Purchase order import h2 + Upload from files button). **Scope-2 (Pass 3 ADJACENT same-pattern siblings):** EditJournalModal mirror (5 fixes), CareScheduleManager soil_amendment enum (1 fix), BatchAddSeed "Upload from Files" button (1 fix). **NewTaskModal + AddPlantModal CLEAN** (already sentence case from prior ships; "Active Garden" preserved as proper-noun page label per today's `006dd69` Item 2 lock). **Volume pills** (Full / Partial / Low / Empty) preserved as-is per single-word categorical enum-token rule. **Mid-execution amendment + re-audit:** aria-label="Vendor / Nursery" ×2 occurrences (L700+L865 of QuickAddSeed) flipped to lowercase to maintain visible↔aria cohesion after visible-label rename. NORTH_STAR cite: "No duplicate paths" + "Take mental load OFF the user." Cross-surface scope-check pattern validated by today's earlier `006dd69` ship — would have shipped sibling-asymmetric otherwise. Tests 434/434; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition. Concurrent-fetch 0/0 pre-commit.

🟢 **`9bad88f` (§3.12 theme #5 — `trusting-proskuriakova-8e23c2` chat)** — `feat(empty-state): §3.12 #5 — guiding-not-scolding copy across 17 surfaces` — Per-surface empty-state copy rewrite across Home / Vault / Garden / Calendar / Journal / Shed / Shopping-list. 18 files total: 17 code shipped in `9bad88f` (full diff: `src/app/page.tsx`, `src/components/SeedVaultView.tsx`, `src/components/PacketVaultView.tsx`, 4× Vault profile tabs, `vault/tags/page.tsx`, `vault/history/page.tsx`, `vault/shed/[id]/page.tsx`, `src/components/ActiveGardenView.tsx`, `src/components/MyPlantsView.tsx`, `src/components/GrowInstanceModal.tsx`, `src/app/calendar/page.tsx`, `src/app/journal/page.tsx`, `src/components/ShedView.tsx`, `src/app/shopping-list/page.tsx`) + 1 doc-trail (this recovery commit). ~22 distinct copy strings touched (~30 edit operations counting collapsed `isNewUser` variants). **3-part frame anchored everywhere:** purpose-describing title / plain next-step body / verb-led CTA "Add a {noun}" (standardized over "Add your first {noun}" — works for mature-state users too — and "Add {noun}" — less personal/inviting). **Walter-plain swaps:** "long-living plants" replaces "permanent plants"/"perennial plants" jargon; drops "FAB," "active batches," "Vault import," "Map your hillside," "Plant from the Seed Vault," "polyculture," "Tap + to..." instructions where the CTA is already visible; "library" softens "Vault"/"Shed" in body copy. **Aria-light:** "fruit trees, bushes" → "trees, bushes, or anything else you keep year after year" (full outdoor-sweep is theme #2). **Sam-day-1:** every "No X yet" cliff title replaced with invitation framing; singular CTAs avoid paradox-of-choice. **`isNewUser` branches collapsed** in V1/V2/S1/G1 where the new copy reads correctly for both first-time + returning users (small code complexity reduction; behavior preserved). **J1 day-rotating prompts preserved unchanged** (already on-voice). **VISION §10 empty-by-default LOCK preserved verbatim** — this ship refines COPY ON existing empty states; state-default unchanged, OnboardingDock untouched, no new notifications/tasks-by-default. **All 5 personas pass Pass 2 walk.** **4-pass plan-audit terminated clean at iteration 2 on Pass 2:** Pass 1 (factual: every line reference + import + handler + branching logic verified via Read) clean iter 1; Pass 2 iter 1 surfaced 7 concerns A-G (CTA-collision risk on G2 / V4 branching preservation / `isNewUser` collapse safety / H1↔SL1 mirroring / G4 secondary empty-state preservation / per-surface persona walk / cohesion-by-aggregation anchors), all resolved inline; Pass 2 iter 2 re-passed clean on revised plan; Pass 3 vocabulary-breadth sibling sweep clean iter 1 across 5 grep families (`empty`/`Empty` 47 files narrowed to user-facing; `No \w+ yet` 20 surfaces; `haven't` alt phrasings; `first \w+ to`/`Get started`/`Welcome`/`Start by` alt phrasings; audit-time vocab `scolding`/`cliff`) — **BLOCKING** aligned (CTA verb "Add a {noun}" cohesion anchor across all 6 EmptyStateCard sites + all raw-card sites), **ADJACENT** captured to §4 (4 raw-card empty states use `bg-white rounded-xl border ... p-8 text-center` instead of canonical `EmptyStateCard` — primitive-canonicalization belongs to §3.12 theme #7 cross-view consistency), **CONCERN** logged (page-label renames "Vault"/"Shed"/"Plantable" remain theme #3 territory; body softened only); Pass 4 lock hygiene clean iter 1 (VISION §10 no-touch verified; §11 9 parked items all unbrushed — only sharing-UX resolved 2026-05-17 unrelated; ROADMAP §3.12 themes #2/#3/#7/#8 boundaries respected — #2 light-touch only, #3 body-level only, #7 raw-card primitive parked, #8 N/A). Persona walk all 5 pass (Sam day-1 cliffs fixed; Walter jargon dropped; Aria outdoor light-touch; Maya copy stays short; Sydney 3-part frame is the cohesion anchor). Tests 405/405; build clean. Preview MCP skipped per documented `.env.local`-absent worktree condition (functionally auth-blocked equivalent — AuthGuard redirects before any empty-state surface mounts). User phone-verifies on prod after this doc-recovery push. **Concurrent-chat fetch (Phase 2 gate item 9) validation #7:** 0 ahead, 0 behind at plan-lock time (verified pre-commit on the §3.12 #5 ship). Plan file at `C:\Users\marsh\.claude\plans\trusting-proskuriakova-8e23c2.md`. **Doc-trail recovery context:** the original `trusting-proskuriakova-8e23c2` chat experienced ENOSPC during the Phase 5 ROADMAP write that truncated its local `docs/ROADMAP.md` to 0 bytes; code commit `9bad88f` had already landed on origin/main and is unaffected. This separate recovery commit (`naughty-kalam-54efba` worktree, 2026-05-18) replays the §1/§3.12/§4/§5/§6 doc edits from the intact 26 KB plan file as source of truth.

🟢 **`bc253be` (§3.12-tester T4 — `mystifying-lichterman-55e122` chat)** — `feat(feedback): §3.12-tester T4 — admin feedback inbox dev-gated page` — Admin feedback inbox at `/settings/developer/feedback-inbox`. Consumes T1+T2 (`e50a667`) voice + debug-log columns; replaces Dashboard SQL Editor flow. **Q1 security model = Option B security-definer Postgres function** (locked at Phase 1 greenlight): `admin_list_feedback()` bypasses `user_feedback` "users SELECT own only" RLS only when caller's `auth.uid()` is in new `developer_users` allowlist table; raises 42501 otherwise. Page renders friendly "Not authorized — add your user_id to developer_users" with paste-able INSERT for the user. **One-time setup** post-deploy: user runs `INSERT INTO public.developer_users (user_id) VALUES (auth.uid())` once in Dashboard SQL Editor while signed in. **Autonomous design decisions** (AUTO MODE): `developer_users` table not hardcoded UUID list (extensible + future hook for BUGS U27); 200-row server-side cap; public URLs (bucket already public, matches `/settings/feedback` pattern); no new icon (Settings menu is text-only); debug log collapsed per-row (toggle-to-expand). **Migration `20260518000000_admin_feedback_inbox.sql`** — additive only (`CREATE TABLE IF NOT EXISTS` + `CREATE OR REPLACE FUNCTION` + `GRANT EXECUTE`). User applies on prod via Dashboard SQL Editor (worktree's supabase CLI not linked; same flow as T1+T2). 5 files, +533/-1: NEW migration + NEW page + NEW page.test (7 tests) + EDIT settings/page.tsx (+1 DEV_ITEMS entry) + EDIT settings/page.test.tsx. Tests 432 → 439 (+7). Lint clean (no new warnings on new files). Build clean (`/settings/developer/feedback-inbox` = 3.54 kB). **4-pass plan-audit terminated clean at iteration 1** with Pass 2 revision applied mid-audit (plpgsql + RAISE EXCEPTION instead of returning empty rows for not-authorized case; re-passed clean on revised plan). **Pass 3 vocabulary-breadth sibling sweep** clean across 8 vocab families (admin/dev gating, security-definer functions, RPC patterns, storage public URL, `<audio>`, debug log display, filter dropdown, opt-in toggle); 6 BLOCKING alignments matched verbatim, 3 ADJACENT logged (table-vs-card dev-page list shape; developer_users as future U27 hook; mark-as-read workflow), 0 CONCERN. **Pass 4 lock hygiene clean** (VISION §10 no-touch, §11 no parked-brush, §3.12-pre `useDeveloperUnlock` pattern aligned, §3.12-tester T1+T2 columns consumed as designed, BUGS U27 untouched). **Persona walk:** dev-only surface; 4/5 personas (Maya/Sydney/Walter/Aria/Sam) pass by exclusion (never tap version 7 times); user (developer-persona) sees full surface with refresh + filter + audio playback + screenshot + debug log toggle. **Preview MCP skipped per documented condition + doubly auth-blocked beyond the standard skip rationale** (page gated by `useDeveloperUnlock` + needs applied migration + needs developer_users INSERT). **Concurrent-chat fetch validation #6:** 0 ahead, 0 behind at commit time. Plan file at `C:\Users\marsh\.claude\plans\mystifying-lichterman-55e122.md`.

🟢 **`e50a667` (§3.12-tester T1 + T2 bundled — `vigorous-nightingale-039e05` chat)** — `feat(feedback): §3.12-tester T1 + T2 — voice memo + debug-log opt-in attach` — Voice recorder + debug-log opt-in toggle on FeedbackModal. **T1 voice:** new `useVoiceRecorder` hook anchors on `useDesktopPhotoCapture` stream-cleanup pattern; MediaRecorder MIME fallback chain (`audio/webm;codecs=opus` → `audio/mp4;codecs=mp4a.40.2` → `audio/webm` → `audio/mp4` → `""` default) handles Chrome/Android + iOS Safari; 60s auto-stop via setInterval; native `<audio controls>` playback + Re-record button always visible (Walter-safe); plain-language `NotAllowedError` recovery copy. **T2 debug-log:** opt-in checkbox (SettingsSuccessSoundToggle shell verbatim) defaults OFF; ephemeral per-session state (NOT localStorage — privacy-conscious); when ON, renders scrollable read-only textarea with `formatEntriesForCopy(getEntries())` preview; plain-language disclosure copy ("Attaches recent technical messages from your browser. Helps us figure out what went wrong. You can review the text below before sending."). **Cohesion:** new `Mic` entry in `ICON_MAP` ([styleDictionary.tsx](src/lib/styleDictionary.tsx)) using `iconProps` defaults (stroke 1.2, PRIMARY_GREEN); record-button idle shell anchored on existing "Attach screenshot" dashed-border button; recording-state container uses emerald-500 border + red pulse dot (universal recording-app convention, Tailwind `bg-red-500` not a new design token); copy frame mirrors screenshot section ("Voice memo recorded (0:18)"). **Migration:** `20260517120000_user_feedback_voice_and_debug_log.sql` adds nullable `voice_path` + `debug_log_text` columns. User applies on prod via Supabase Dashboard SQL Editor. Voice blob upload to existing `journal-photos` bucket at `{user_id}/feedback-voice-{uuid}.{webm|m4a}` — RLS already path-prefix-only. **Pass 3 vocabulary-breadth sibling sweep:** 8 vocab families grepped (audio/media-recording, blob-upload, toggle/preference, console-capture, sensitivity, access-gate, camera-stream-cleanup, ICON_MAP); 6 BLOCKING alignments captured + matched; 3 ADJACENT logged (voice memo copy frame as future-sibling anchor, privacy-disclosure copy frame as future-sibling anchor, MIME fallback may evolve as iOS Safari adds Opus). **Pass 4 lock-hygiene clean** (VISION §10 no-touch, §11 modal-placement audit untouched, §4 Principle 9 aligned, T3 lock honored verbatim, §3.12 #4 visible-button-fallback aligned, §3.12-pre dev-gating non-conflicting, migration-naming guard followed). **Persona walk:** all 5 pass both features; Walter is the most-served persona by T1. 5 files: NEW migration, NEW useVoiceRecorder hook (+189), NEW `feedbackModalAttachments.regression.test.ts` (+146, 27 assertions across 6 describe groups), EDIT FeedbackModal.tsx (+132 net), EDIT styleDictionary.tsx (+14). Tests 405 → 432 (+27). Lint clean (no new warnings from new files). Build clean. Preview MCP skipped per documented `.env.local`-absent worktree condition; user phone-verifies on prod (verification path in commit body). **Concurrent-chat fetch (Phase 2 gate item 9):** 0 ahead, 0 behind at commit time — validation #5 of the rule running clean. Plan file at `C:\Users\marsh\.claude\plans\vigorous-nightingale-039e05.md`. 4-pass plan-audit terminated clean at iteration 1.

🟢 **`0c3b655` (§3.12-tester T3 lock — `flamboyant-lamport-f5cc74` chat, doc-only)** — §3.12-tester T3 (Tools-bar feedback button visibility scope) ✅ Locked Option A — public, stays as-is. ZERO code change (feedback button already public to all authed users; no gating to add or remove). Doc-only updates: §3.12-tester T3 status flipped 🔵 → ✅ + Option A rationale + persona-walk findings + Pass 3 vocabulary-breadth sibling-sweep result; T1 + T2 status entries amended with public-context constraints locked (Walter/Sam-safe voice UX with visible record/stop + 30-60s cap + plain-language mic permission for T1; debug-log defaults OFF + opt-in toggle + content preview + plain-language disclosure for T2); §1 lead-line refreshed (demoted `flamboyant-wilson-34fc0c` §3.12-pre paragraph to archive note per 2-paragraph cap, inserted new T3-close paragraph); §1 Next session focus refreshed (T3 row removed; T1/T2 promoted as next-ready chats); §6 new 2026-05-17 entry with full decision log. **Pass 1+2+3 audit inline (XS scope, doc-only):** Pass 1 verified 3 feedback button sites (AuthGuard:195 loading-shell + AuthGuard:297 post-auth mobile + Sidebar:162 desktop) + verified auth page pre-sign-in shows zero feedback (all sites inside `{!isAuthPage}` blocks); Pass 2 persona walk on all 3 options (Maya / Sydney / Walter / Aria / Sam) + app-store-surface concerns + privacy + downstream T1/T2 scoping; Pass 3 vocabulary-breadth sibling sweep confirmed `useDeveloperUnlock` sole access-gate pattern in repo (no tester/beta/allowlist concept exists). **Concurrent-chat collision caught + reconciled via Phase 2 gate item 9:** 2 commits (`efe588d` + `42ed0b8`) landed on origin/main during my audit; fast-forward rebase clean (no edits to merge — no local commits yet); re-audit on rebased state confirmed Pass 1-3 findings still hold. **Validation #4** of the Phase-2-gate-item-9 rule running clean (precedent: `flamboyant-wilson`, `distracted-wilson`, both this week). 1 file edited (`docs/ROADMAP.md`). Tests N/A (no code). Doc-only push tier per CLAUDE.md.

🟢 **`efe588d` (§3.12 theme #4 visible-button fallback — `distracted-wilson-616865` chat)** — 3 calendar surfaces updated to lower button-fallback breakpoint from `lg:`/`xl:` to `md:` (iPad-portrait+, ≥768px): month-nav arrows at [calendar/page.tsx:976+985](src/app/calendar/page.tsx:976), singleton row Snooze/Complete wrapper at [:2442](src/app/calendar/page.tsx:2442), consolidated overdue Snooze-all/Complete-all at [:2242+2253+2254](src/app/calendar/page.tsx:2242). Aligns with verified Vault/Shed `md:` anchor for cohesion. Walter persona (locked 2026-05-17, iPad-primary, swipe-excluded) now sees visible buttons on iPad — primary exclusion fixed. Phone-portrait users keep swipe-first per VISION §4 Principle 9 spirit. **2 in-code comments updated** to reference the new breakpoint + persona-accessibility narrowing rationale (line 2151-2155 ConsolidatedOverdueHeader docstring; line 2357-2361 CalendarTaskRow swipe comment). **`calendarBatchSelect.regression.test.ts` updated:** 4 assertion strings flipped `lg:` → `md:` (singleton row line 354; consolidated count assertion line 422; chevron negative-check line 434) + 2 describe/comment blocks reworded for accuracy. **VISION.md:** §4 Principle 9 worked-example narrowed with persona-accessibility refinement; §11 row-primitive parked entry footnoted with the `md:` refinement (does not resolve the bubbles-vs-list density question separately). **Pass 4 lock-hygiene** caught 3 brushed locked decisions, surfaced via Rule A ask, user picked Recommended Option A. **Pass 3 vocabulary-breadth grep** (rule locked 2026-05-17 in `flamboyant-wilson`) covered swipe/drag/pan/pointer/long-press/gesture-lib/pinch families — found 6 surfaces total, of which 2 were already at `md:` anchor (Vault profile + Shed product nav) and 1 is intrinsic UX (image crop). **Audit also surfaced:** user-prompt's "Garden drag-to-move" + "Journal swipe-delete" do NOT exist in codebase (both pages use long-press for bulk-select, theme #10 territory). Honest correction logged in plan file. **Concurrent-chat collision caught + reconciled** via fast-forward rebase before code edits (2 doc-only commits `33955b4` + `3396d03` landed mid-execute). 4 files, ~10 line-edits (calendar/page.tsx) + ~10 test-assertion edits + 2 doc edits + 1 ROADMAP entry. Tests 405/405; build clean. Preview MCP visual check at 412×915 + 768×1024 pending pre-push. Plan file at `C:\Users\marsh\.claude\plans\continue-from-prev-chat-lazy-valiant.md`.

🟢 **`4a8b04c` (§3.12-pre dev-tools gating — `flamboyant-wilson-34fc0c` chat)** — Settings page hides Developer / Import logs / Extract cache when `DeveloperUnlockContext.isUnlocked === false`; each of the 5 dev sub-pages adds the canonical scraper-audit-style "Developer tools require unlock" gate (matching [admin/scraper-audit/page.tsx:68](src/app/admin/scraper-audit/page.tsx:68) verbatim). Resolves persona-audit P0 theme #1 (APP-STORE BLOCKER). Sam-on-day-1 sees clean 6-item menu. Implementation = existing `useDeveloperUnlock` tap-count gate (entry gesture = tap App Version 7× on Profile bottom). **Major Amendment 2:** initial Q1 Rule A picked env-var allowlist (Recommended); Pass 3 sibling-sweep missed existing `DeveloperUnlockContext` (regex covered env-var vocabulary only); discovered pre-commit during stale-mock grep; surfaced via second Rule A ask; reverted entirely (`devAccess.ts` + test deleted) + reworked to canonical pattern. 7 files, +100/-8. Tests 405/405; build clean. Preview MCP skipped per `.env.local`-absent worktree condition. ADJACENT finding captured to BUGS U27 (4 `/api/developer/*` server-side routes have no dev gate — defense-in-depth follow-up).

🟢 **`fdc295b` (Sharing-purpose chat closeout — 10 decisions + PERSONAS.md)** — Non-build design chat (`competent-blackwell-5de2a2`) Phase 5 captures shipped. All 6 open-territory items from VISION §11 "Sharing UX / purpose narrowing" RESOLVED + 4 bonus locks (public boundary, sharing data model, persona system, app-store distribution context). **New file:** `docs/PERSONAS.md` (5 personas — Maya / Sydney / Walter / Aria / Sam — for plan-audit Pass 2 + Pass 3 persona walks). **VISION.md:** §1 boundaries (public-distribution line); §3 first paragraph + Layer 2 attribution + Layer 3 internal group-mode structure; §9 new Public social-media stream 🕐 row + Level 3 trade row updated with two-shape note; §10 new no-activity-feed don't-touch; §11 sharing-UX entry replaced with resolved summary. **ROADMAP.md:** §6 comprehensive entry + §3.5 trade sub-item expanded + new §3.11 chunk "Trusted-circle infrastructure" 🟣 + §1 lead-line refresh (demoted 2026-05-16 frosty-wright paragraph to archive). **CLAUDE.md:** "What this project is" refined to name app-store distribution + Pass 2 + Pass 3 gain persona-walk subcategory + Required-reading mentions PERSONAS.md. **Concurrent-chat collision caught mid-Phase-5:** `5ca4bae` + `d61fff0` (ecstatic-rubin-5f30c8) landed between my plan-lock and pre-push; stash + pull --rebase + pop stash + surgical conflict resolution on §1 + §6 (both ships preserved, no work lost). 4 files, +294/-11. Tests 404/404 (sanity — no code). Doc-only push tier. Classifier-bypass via `permissions.allow` worked clean (parallel chat's P4 open-verification confirmed). Plan file at `C:\Users\marsh\.claude\plans\continue-from-prev-chat-frolicking-token.md`.

🟢 **`2cc313a` (Doc batch — close prior-chat gaps + capture new calendar feedback)** — Three doc-only captures: (1) **CLAUDE.md push-tiers** — new `Push-classifier caveat (locked 2026-05-13)` paragraph naming the auto-mode classifier behavior that gates every main push regardless of file type; convention locked to "Reply `push`" concrete-phrasing not letter codes ("A/B"). Extends the doc-only tier rule, doesn't contradict it. (2) **VISION §12** — two new 2026-05-13 bullets at top: first covers R1-R5 ship (`884cf9d` + `4b333ea`) with R3 audit-rec variance preserved + R5 §11 brush handling + audit-pass framing correction (`60d0836`); second captures 2026-05-13 calendar testing feedback as "table for later" (Item A swipe-to-complete confirm refinement; Item B task card structure proposal). (3) **ROADMAP §3.9** — added items #9 (swipe-confirm) + #10 (task card restructure) with `(new 2026-05-13)` label suffix to disambiguate from §4's `(2026-05-12 #N)` parenthetical labels referencing original-batch parked items. Cross-refs to `cea21e0`, `f77507a`, VISION §11 row-primitive parked decision, BUGS U13 hierarchy pattern. Plus inline fix to §3.9 count line (was "(7 items)", listed 1-8, now "(10 items)" listing 1-10) — amendment caught + re-passed clean. 3 files, +8/-4. 387/387 tests pass. Audit looped to termination at Pass 2 of pass-type 2 (min 2 pass-types met; no Pass 3 trigger).

🟢 **`3de1c2c` (Chunk 3.9 #2 + #4 batch)** — Two coordinated XS visual fixes per WORKFLOW §"Batching small fixes." **(1) #2 Calendar month-nav arrows:** added `hidden xl:flex` to both `← →` buttons in `src/app/calendar/page.tsx:976+985`. Arrows now hide below `xl:1280px` (phone + iPad-landscape); desktop keeps arrows. Phone/iPad nav stays via existing horizontal swipe gesture on the wrapping div. Matches B1 + B2 breakpoint family. **(2) #4 Garden card "Add journal entry" trigger icon:** swapped `JournalCareHands` → `Edit` (pen with motion) in 2 sites in `src/components/ActiveGardenView.tsx` (lines 1094, 1242). Resolves visual duplication where the trigger button shared its icon with the `care` entry-type marker on journal entries via [journal/page.tsx:230]. Scope A locked: trigger only; `JournalCareHands` stays in ICON_MAP and continues to mark `care` entries on display. **Mis-triage caught:** ROADMAP §3.9 #4 was originally captured as "Bag (shopping list) icon swap" in the mobile header — actual scope all along was the Garden trigger duplicate. 2 files, 4 line-edits. 387/387 tests; clean build. Pre-push Preview MCP check attempted at 412×915; dev server env-blocked in worktree (no .env.local) → skipped per rule `aad2e56`'s documented condition (functionally auth-blocked equivalent). 3-pass plan-audit terminated clean.

🟢 **`6d76c20` (Calendar expand-all toggle — chunk 3.9 #1)** — Chevron button next to "Upcoming Tasks" header toggles every collapsed section open/closed in one motion. 200ms ease-out arrow rotation per VISION §8 transition standard. Smooth content height animation via `grid-template-rows 0fr → 1fr` (no JS height measurement; arbitrary content height). Applies to both master and per-section toggles for consistency. Master state derived from existing `expandedDateGroups` Set; no new state shape. Hidden when no sections have content. Built on `a7dadb7` default-collapse. 1 file, +46 -4 lines. Pre-push mobile-viewport check skipped (auth-blocked per the rule's skip condition).

🟢 **`79836f4` (FAB UX fix batch — U22+U23)** — Two fixes to the Universal Add Menu: (1) Android back button now closes the menu via `useModalBackClose` wired on Home/Garden/Journal/Calendar (Vault already correct). Skip-pop infrastructure was already half-built in 4 files; this completes the wire-up. (2) Menu container switched from absolute-center (`top-1/2 -translate-y-1/2 max-h-[85vh]`) to flex-wrapper with `max-h-[85svh]` — fixes Chrome Android cutoff where 2 of 5 options + Cancel were hidden behind OS nav bar. 5 files, +10 -1 lines.

🟢 **`fb67e10` (U21 fix)** — hides webkit native search cancel button on journal search input via `[&::-webkit-search-cancel-button]:appearance-none` Tailwind arbitrary variant. Removes the Android Chrome / desktop Chrome duplicate X (grey custom + blue native → grey custom only). iOS Safari unchanged (no native button rendered there). 1-line CSS, 387/387 tests, clean build.

🟢 **`ed5441c` (Debug log page)** — `/settings/developer/debug-log` captures last 50 console.log/warn/error messages this session; Copy-all button writes to clipboard so user can paste captured output into bug reports without screenshots. Tooling, not user-facing. Plus temp `console.log` instrumentation in journal page for flicker-bug investigation (to be removed after diagnosis).

🟢 **`7482c59` (Journal search)** — search field on journal page, multi-token AND across note + type + plant name + variety + date, all three view modes. **Remaining follow-up:** journal page flicker / FAB cutoff (under investigation via the debug log). U21 follow-up shipped above in `fb67e10`.

### Verified clean

🟢 **`4a8b04c` (§3.12-pre dev-tools gating)** — user phone-verified 2026-05-17 on prod. All 4 steps clean: (a) default Settings shows 6-item menu (Developer / Import logs / Extract cache hidden); (b) tap App Version 7× on Profile → return to Settings → 9-item menu with dev tools visible; (c) all 5 dev sub-pages render normally while unlocked; (d) sign out + sign in fresh → gate re-engages, 6-item menu returns. APP-STORE BLOCKER (persona-audit P0 theme #1) confirmed resolved. Sam-on-day-1 path now clean.

🟢 **`11eaf37` (U25 — MyPlants "Add journal entry" icon)** — user phone-verified 2026-05-17 on prod: My Plants list pen icon (`ICON_MAP.Edit`) matches Active Garden gallery + list. Cross-surface cohesion within Garden tabs confirmed. (Vault profile sites still parked per ROADMAP §4 "Cross-surface 'Add journal entry' icon cohesion — Vault profile tabs.") Confirmed working.

🟢 **`a7dadb7` (Calendar default-collapse)** — both phone screenshots from user (2026-05-12) show "Overdue (42 tasks)" / "Overdue (41 tasks)" collapsed at top with Upcoming Tasks expanded below. Confirmed working.

### Still TBD verification

- **`f79a8aa` Debug-log instrumentation** — open `/settings/developer/debug-log` after Vercel deploy; trigger boot / auth / nav / 1 add-flow / 1 error toast / optional airplane-mode toggle; confirm structured `[scope] action {payload}` entries appear and that `Copy all` output contains zero user content (no plant names, notes, varieties, journal text). Dogfood verification path written in commit body + chat close-out.
- **`6691d20` Calendar section-header casing fix** — h2 above the upcoming task list now reads "Upcoming tasks" (was "Upcoming Tasks") when no specific date is selected. Sibling branch "Tasks for {date}" unchanged (already sentence case).
- **`8310070` + `8261304` + `1548280` §3.12 theme #3 Phase A.1 #4 (Plantable rename, final wording)** — Calendar page widget reads **"Plant this month ({count})"** (was "Plantable in {month}"; iterated from "Ready to plant in {month}" → "Plant in {month}" → final "Plant this month ({count})" via 2 Syd mid-chat redirects + final dedup that aligns with canonical peer pattern in VaultPacketWing + VaultPageContent); ∞ button title + aria-label both read "See plants for this month in Vault"; drilldown modal h2 still reads "{plantType} – Seed inventory" (intentional, preexisting asymmetry parked).
- **`b669a2a` §3.12 theme #3 Phase A.1 #1-3 (Vendor ratings / Monthly view / Yearly view)** — (a) Settings menu reads "Vendor ratings" + Vendors page h1 reads "Vendor ratings"; (b) Home page "When to plant (by zone)" row reads "Action now · Monthly view · Yearly view" subtitle + matching button labels; (c) Schedule page view-toggle reads same pair.
- **`a78dbd6` (casing-cohesion sweep)** — phone + desktop verify per the 6-flow dogfood plan surfaced in chat: FAB → Add seed packet (manual entry + link-to-existing paths confirm sentence-case labels); FAB → Add to shed (Soil amendment option + sentence-case labels); FAB → Add journal (3 changed quick-action buttons + Supply used + Take photo + "+ Add new supply"); FAB → Purchase order chooser; Edit Journal modal (sibling mirror of QuickLog QUICK_ACTIONS). No regressions expected on New Task + Add Plant modals.
- **`efe588d` §3.12 theme #4 (visible-button fallback at `md:`)** — needs phone + iPad verification: (a) **phone portrait (<768px)**: Calendar singleton row + consolidated overdue header still swipe-only (no visible Snooze/Complete buttons; swipe still works); month nav still arrow-free; (b) **iPad portrait (≥768px)**: Calendar singleton row + consolidated overdue header now show Snooze/Complete buttons inline; month nav shows ← → arrows; swipe still works on the same surfaces (regression check). Implicitly re-verifies `cea21e0` + `f77507a` swipe gestures — if buttons render correctly AND swipe still fires, swipe surfaces are confirmed working.
- **`3de1c2c` chunk 3.9 #2** (calendar arrows hide on phone) — **superseded by this ship**: arrows now visible at `md:` (≥768px) instead of `xl:` (≥1280px). The original "hide arrows on phone" rationale (phone-portrait, where swipe is the primary nav) still holds — but the breakpoint shifted to serve iPad-portrait users (Walter persona). Verify alongside §3.12 theme #4 above.
- **`8624c8d` B2** (desktop ≥1280px two-column) — user hasn't been on desktop browser yet
- **`e61ffdc` B1** (desktop ≥1280px sidebar) — user hasn't been on desktop browser yet
- **`cea21e0` + `f77507a` phone swipe** (singleton + consolidated row swipe gestures) — will be implicitly re-verified by §3.12 theme #4 verification above (the new ship does not touch swipe handlers; only button visibility). If swipe regresses, that's a finding.

### New feedback batch (2026-05-12) — triaged

**🔵 Current build (chunk 3.9 — see §3.9):**
1. Calendar "Upcoming Tasks" master expand-all / collapse-all toggle w/ smooth arrow swap + smooth content animation
2. Phone calendar month-nav arrows redundant w/ swipe — hide on phone, keep desktop *(aesthetic — needs user input)*
3. Garden card spacing fix *(aesthetic — user needs to point at what feels off)*
4. Bag (shopping list) icon swap *(aesthetic — needs user pick from options)*
5. Journal gallery card format: structured header + description + variety/location tags *(aesthetic — discuss structure)*
6. Journal search
7. Plant profile section — no-AI-data fallback *(needs current-behavior look + empty-state copy decision)*

**🟣 Future (added to §4 parked):**
- Welcome instructions — ⚠️ conflicts with VISION §10 "empty-by-default IS the onboarding"; needs clarification before triage (#8)
- Gallery vs table view purposes — Phase 5 page-goals discussion (#9)
- Journal growing-indicator tags (flowering, first leaves, etc.) — Phase 3 IA tag schema (#10)
- Vault filter by flower color / perennial / fruit-vs-veggie — Phase 3 IA plant metadata (#11)
- Harvest calculator (season stats, weights, first/last logged) — memory plane / Failure Mode #2; aligns w/ chunk 3.6 plant profile depth (#12)

### Next session focus

**§3.12-tester chunk ✅ CLOSED 2026-05-17.** All 4 items shipped/locked: T1 + T2 bundled ship (`e50a667`), T3 locked Option A doc-only (`0c3b655`), T4 admin inbox shipped (`bc253be`). Pending verifications: T1+T2 prod migration apply + phone verification; T4 prod migration apply + one-time developer_users INSERT + admin inbox phone verification.

**§3.12-pre ✅ SHIPPED + VERIFIED 2026-05-17 (`4a8b04c`, `flamboyant-wilson-34fc0c` chat).** User phone-verified clean on prod. APP-STORE BLOCKER resolved.

**Next chat ready: §3.12 theme #3 Phase A.2 (Vault + Shed page-label renames + back-link family casing decision).** Phase A.1 ✅ COMPLETE 2026-05-25 (`b669a2a` + `8310070` + `8261304` + `1548280` + `6691d20`, `gt-casing-theme3-a1` chat) — 4 page-label renames + 1 Calendar section-header casing fix shipped: Vendor Scorecard → "Vendor ratings"; Monthly pulse → "Monthly view"; Annual roadmap → "Yearly view"; Plantable → "Plant this month" (verb-choice data-source-audited, dedup aligned with VaultPacketWing+VaultPageContent canonical peer); "Upcoming Tasks" → "Upcoming tasks" Calendar section header. Polyculture / True leaves / Hardening off / Grow instance dropped from scope (zero user-facing strings); Repair hero / Extract cache deferred to §8 settings IA chat (dev-gated). **Phase A.2 scope (must bundle):** **(a)** Vault rename (URL `/vault/...` + sidebar nav + remaining tab "Seed Vault" — Plant Profiles tab already renamed to Library 2026-05-27 — + "Add to vault" copy + ~50 files) — XL effort, needs redirect strategy so existing bookmarks/links don't break. **(b)** Shed rename (URL `/shed` + `/vault/shed/[id]` + tab + "Add to shed" copy + ~10 files) — L effort. **(c) Back-link family casing decision** — 21 `Back to [PageName]` peer sites all Title Case today; deferred from `gt-casing-theme3-a1` chat because flipping the case standalone would create a 1-of-21 outlier and the new Vault/Shed destination labels are unknown until A.2 ships. Decision is binary: all 21 stay Title Case (proper-noun page labels) OR all 21 go sentence case (treat as common-noun destinations) — make the call with new destination labels in view. **(d)** Tab labels inside Vault page — partial close 2026-05-27: Plant Profiles → Library shipped surgically (see §6 `gt-plant-profiles-to-library` entry); remaining "Seed Vault" → "Packets" cohesion sweep is Phase 6 territory, bundle if natural. **Theme #3 Phase B** = body copy + tooltip + secondary-surface plain-language pass; bundles cleanly with theme #8 (settings IA) since both are Phase-4-navigation work and the Phase A rename map informs the settings group labels.

**Deferred — Chat A (aesthetic-decision sweep on 9 SURFACE items)** — see plan file `fresh-chat-purpose-vision-sorted-clover.md`. Now sequenced AFTER §3.12 catalog ships (T4 → #5 → #3 → #2/#6 → #8). Original guidance preserved: persona-audit didn't invalidate items; added context (U12 reinforced as Walter P0; icon density sits inside theme #3).

**Persona-audit kickoff ✅ CLOSED 2026-05-17** (`binary-valley`). 8 surfaces × 5 personas walked at skim depth; 10 cross-cutting themes surfaced; new §3.12-pre + §3.12 chunks landed. Plan file `C:\Users\marsh\.claude\plans\continue-from-prev-chat-binary-valley.md` retained.

**Deferred — Chat A (aesthetic-decision sweep):** 9 SURFACE items walked individually per Rule C. See plan file `fresh-chat-purpose-vision-sorted-clover.md`. **Now runs after §3.12-pre dev-tools gating ships.** Persona-audit findings did not invalidate Chat A items but added context to weigh them (esp. U12 two-+Entry-paths now reinforced as Walter P0 friction; icon-density now sits inside theme #3 plain-language audit). U25 fix (originally Chat A) shipped on Garden tabs via parallel-chat `11eaf37`; 3 Vault profile sites parked.

**Per-theme planning chats spin off §3.12 catalog as user prioritizes:**
- **#2 (outdoor-garden assumption sweep)** + **#6 (FAB / tab disclosure-by-data)** = same family; build together; M-L effort
- **#3 (plain-language audit)** + **#8 (settings IA)** = Phase 4 navigation audit work; L effort. **#3 Phase A.1 ✅ SHIPPED 2026-05-25** (`b669a2a` + `8310070`). **#3 Phase A.2** (Vault + Shed renames) = dedicated chats next.
- **#4 (visible-button fallback for swipe)** = ✅ SHIPPED 2026-05-17 (`efe588d`, `distracted-wilson-616865` chat; awaits phone + iPad verification)
- **#5 (empty-state cliff)** = ✅ SHIPPED 2026-05-18 (`9bad88f`, `trusting-proskuriakova-8e23c2` chat; awaits phone verification)
- **#7 (cross-view consistency)** = already VISION §11 parked, queued for design pass; M effort. **+1 new ADJACENT line item parked 2026-05-18** (raw-card empty-state primitive canonicalization — see §4).
- **#9 (memory plane gaps)** = chunk 3.6 territory
- **#10 (touch target sweep)** = independent audit + targeted fix; S effort

**Other open items (deferred to dedicated chats — see audit's updated 4-chat sequence):**
- **U12** (two "+Entry" paths on journal) — Chat A (now Walter P0 friction confirmed)
- **App-wide icon density** — Chat A (sits inside §3.12 theme #3)
- **HarvestModal end-state realization** — Chat A (Maya impact confirmed via §3.12 Garden surface)
- **Garden card spacing (3.9 #3)** — Chat A
- **B3 (FAB → popover)** — Chat C (chunk 3.2 resumption)
- **§3.10 R6-R8 + R9 + D1/D2 + Finding F/G wording fixes + 3 missing destinations + Vault profile aria-label cohesion** — Chat D
- **Journal flicker bug from `7482c59`** — needs U-entry capture in Chat D (currently floats with no destination)

**Optional setup at session start:**
- Read `docs/CLAUDE_CODE_SETUP.md` and (if you want stronger enforcement) enable the `UserPromptSubmit` hook by creating `.claude/settings.json` at the main repo root.

See §3 for the full ranked queue.

---

## 2. Design phase status

The full design framework — what was outlined as "what a design phase usually covers."

| Phase | What it answers | Status |
|---|---|---|
| **Phase 1 — Strategy / Vision** | Who is this for, why does it exist, what's it for | ✅ Closed 2026-05-08. Vision statement v4 locked. See VISION.md. |
| **Phase 2 — Jobs-to-be-done** | What is the user trying to accomplish per surface | 🟣 Pending. ~30-60 min focused session. Best done before designing new pages from scratch. |
| **Phase 3 — Information Architecture** | Data model, entity relationships, mental model | 🟣 Pending. **Required before:** beds-as-first-class implementation, lifecycle batch tracking work, plant database moderation. |
| **Phase 4 — Navigation & sitemap** | Top sections, paths between them, audit current navigation | 🟣 Pending. |
| **Phase 5 — Page goals & flows** | Per-page purpose statements, primary actions | 🟣 Pending. **Best for:** redesigning pages that the user has flagged as confusing (plant profile, home page, etc.) |
| **Phase 6 — Design system completion** | Typography, spacing, button hierarchy, modal patterns, component conventions | 🟣 Pending. Some tokens already in VISION.md §8. |
| **Phase 7 — Edge cases & accessibility** | Empty states, errors, offline, screen readers, mobile vs desktop | 🟣 Pending. |

**Important:** We don't have to do all phases sequentially before building. We pick up a phase when the build work surfaces a question that phase needs to answer.

---

## 3. Build chunks (epics)

Major work items, ranked by recommended order. Each has a status, brief scope, and dependencies.

### 3.1 ✅ Calendar fatigue batch — SHIPPED 2026-05-08

**Status:** Built, tested (340/340), pushed to `main` (commit `2697f04`), Vercel deployed. Awaiting user prod-verification.

**What shipped:**
1. **Day header visual treatment** — per-date headers now `text-sm font-semibold text-black/85` + `bg-emerald-50/40` tint. Reads as section dividers without competing with content.
2. **Overdue consolidation** — repeated overdue tasks (e.g. daily copper fungicide × 14 days) collapse into a single summary row showing `{count} overdue · oldest {date}` with chevron-expand. Group key = `(title, plant_profile_id, grow_instance_id, user_id)`. Singletons render flat (no fake nesting). Group key includes `user_id` so household members' identical tasks don't merge in family view.
3. **"Select all in group" button** on consolidated rows — reuses existing long-press batch-select flow (selectMode + selectedIds + batchMenuOpen). No parallel checkbox UI introduced.

**Plus (folded in):** calendar grid adjacent-month pad cells switch from `bg-black/[0.02]` to `bg-neutral-100` for clean "not part of this month" cue.

**Deferred from original plan:** Undo toast on bulk action. New UI pattern; deserves its own decision in a future design-system pass.

**Captured signals:** Row primitive ("bubbles vs. list") flagged by user → parked in VISION.md §11. Re-evaluate after prod verification.

**Source:** VISION.md §7 (Calendar surface), §11 (Calendar task fatigue parked decision).

---

### 3.2 🟢 Phase 4 + 5 Desktop Layout Pass

**Status:** Active. Milestone B1 shipped 2026-05-11 (`e61ffdc`); B2–B5 queued.

**Frame:** Bundles three entangled VISION.md §11 items as one coherent pass because they share architecture (sidebar width → main column width → right column → modal anchoring):
1. Desktop calendar oversized + empty horizontal space
2. Bottom nav feels mobile-y on desktop
3. Phone-style modals/sheets centered on wide screens

**Locked decisions (A1–A7 + S1–S5, 2026-05-11):**
- Breakpoint: **`xl:` (1280px)** — iPad-landscape stays on mobile-style layout.
- Sidebar appears on **every page** in this pass; right-column treatment is **Calendar-only**. Other pages stay single-column main until Phase 5 designs their layouts.
- Sidebar: 240px expanded ↔ 64px collapsed, chevron toggle at top-right, state persisted to localStorage. Active state = emerald left-edge accent bar + `bg-emerald-50` tint. Icons identical to BottomNav (reuse via new shared `navItems.tsx` module). Small "Garden Tracker" wordmark in emerald-700 at top.
- Top header at `xl:`: slim — keeps cloud-sync, page title, household toggle. Shopping list / Help / Settings / Feedback move to sidebar footer.
- FAB stays viewport-anchored (no offset by sidebar). FAB → popover treatment is B3.
- Modals on desktop: B4 will convert phone bottom-sheets to centered modals with backdrop (capped width).

**Milestone breakdown:**

| # | Milestone | Scope | Status | Effort |
|---|---|---|---|---|
| **B1** | App shell + sidebar nav at `xl:` | New `Sidebar.tsx` + shared `navItems.tsx`; `AuthGuard.tsx` wraps shell in `xl:flex` w/ sidebar; `BottomNav.tsx` gains `xl:hidden`. | ✅ Shipped 2026-05-11 (`e61ffdc`) | M |
| **B2** | Calendar two-column | Calendar page wraps grid + task list in `xl:flex xl:gap-6` two-column layout. Left col 640px sticky at `top-12`; right col `xl:flex-1 xl:max-w-[720px]`. Mobile unchanged. | ✅ Shipped 2026-05-11 (`8624c8d`) | S |
| **B3** | FAB menu → popover at `xl:` | FAB add menu becomes anchored popover at `xl:` (not centered card). Mobile unchanged. | 🟡 Next (after B2 prod verification) | S |
| **B4** | Modals/sheets desktop treatment | NewTaskModal, QuickLogModal, snooze sheet, batch sheets get `xl:` centered-modal-with-backdrop variant. Width cap. Mobile = unchanged. New design-token sanity check needed (backdrop opacity, modal rounding, matches FAB transition language). | 🔵 | M |
| **B5** | Polish + cross-page audit | Walk all five pages at `xl:`; flag hardcoded `max-w-*` containers that read off-center in the sidebar-offset main column; capture Phase 5 questions for other pages' eventual right-column content. | 🔵 | S |

**Risks tracked:**
- Sidebar at `xl:` only — if iPad-landscape users want it, easy follow-up to lower the breakpoint.
- Right column on Calendar reuses the existing dense task list; density-fatigue may resurface in the narrower column. Follow-up if so (VISION.md §11 row-primitive park is the candidate fix).
- Modal centered-with-backdrop at `xl:` is a new pattern in this codebase — B4 will need a quick design-token decision before build.

**Out of scope (don't expand):**
- Phase 5 page-goals work for Home / Vault / Garden / Journal right-column content.
- Beds-as-first-class (Phase 3) — see §3.4.
- Floating-months grid with fade-slide transitions — confirmed ornamental for a utility app; rejected.

**Source:** VISION.md §11 (parked desktop layout, FAB modal placement), §12 (recent signals 2026-05-08 + 2026-05-11).

---

### 3.3 🔵 FAB consistency batch

**Status:** Decisions surfaced from prior session, awaits user input on canonical choices, then plan-audit-build.

**Scope (3 changes bundled):**
1. FAB main button vs. submenu icon style consistency — canonical stroke weight; whether FAB main is intentionally heavier or matches submenu
2. ~~Save/Cancel button consistency across modals~~ ✅ **CLOSED 2026-05-26** via FAB-form submit pattern standardization saga (`fc1463d` → `ba9319f`). Locked: 3-section flex-col shell + Cancel-left + Save-right + bg-emerald-600 + border-t border-neutral-200 + disable-until-valid. See §6 2026-05-26 entry.
3. Cross-view consistency — Vault Library grid / Garden Active Garden gallery / Garden My Plants list — which is canonical, what should match

**Why second:** Builds momentum on cohesion (Failure Mode #4) without major scope.

**Effort:** S. Each item is small.

**Dependencies:** User decisions on each choice.

**Source:** VISION.md §11 (open decisions).

---

### 3.4 🟣 Beds-as-first-class implementation (Phase 3 IA work)

**Status:** Vision-level decision locked (VISION.md §8). Implementation requires Phase 3 IA session before build.

**Scope:**
- Phase 3 design: data model for `bed` entity, migration plan for existing location strings, page design for bed detail page, plan for updating dependent surfaces
- After Phase 3 design lands: build (M-sized engineering work)

**Why third:** Foundation for many other things — soft-blocks the lifecycle batch tracking (3.5), unblocks soil testing (🕐), unblocks future bed layout (🕐), provides destinations for batch-split workflows.

**Effort:** Phase 3 design (~30-60 min session) + M-sized build (split data model, migration, UI updates).

**Dependencies:** Phase 3 IA design must come first.

**Source:** VISION.md §8 (beds-as-first-class), §11 (Phase 3 deferred items).

---

### 3.5 🟣 Lifecycle / batch-tracking improvements (Failure Mode #1)

**Status:** Major theme. Multiple connected workflows. Needs Phase 3 IA work first (beds), possibly Phase 5 page goals work for the seed-starting flow.

**Scope (multi-part):**
- Growing-batch concept (status, germination rate, batch operations)
- Up-potting workflow (transition tray → individual pots)
- Splitting batch to multiple destinations (3 to bed A, 2 to bed B, etc.)
- Easy cull / kill workflow
- Transfer / give away / trade between users (within trusted circle, with full history). **Two-shape model locked 2026-05-17 (§6):** Shape 1 (direct give) from inventory cards (long-press plant/packet → pick recipient from browse-mode group members → confirm); Shape 2 (broadcast offer) from board-mode group surface (Post-an-Offer → claim). Both auto-log to Journal as transfer entries; recipient's inventory increments, sender's decrements. Cross-references new chunk §3.11 (trusted-circle infrastructure).
- Re-finding plants from a 6-week-old batch when ready to harvest

**Why fourth:** Closes the biggest single failure mode. But scope is large; likely multiple commits and sessions.

**Effort:** L. Probably broken into smaller chunks during planning.

**Dependencies:** Beds-as-first-class (3.4) recommended first; possibly Phase 5 page goals for the seed-starting flow.

**Source:** VISION.md §6 (Failure Mode #1), §5 (Lifecycle paths).

---

### 3.6 🟣 Plant profile depth (Failure Mode #2)

**Status:** Surfaced as a failure mode but specific design work pending.

**Scope:**
- Establishment care vs. regular care templates (currently set up manually each time)
- Deeper growing data on profile page (pending more sister feedback)
- Profile-to-history connection clarity (the "where do I see growing history" disorientation)
- **Tree-specific data fields (raised 2026-05-16):** chill hours (variety-level on `plant_profiles`) + rootstock (instance-level on `grow_instances`). See §4 parked entries for full context. Both fit "deeper growing data" sub-scope. Attachment-point split (variety vs instance) is a Phase 3 IA decision input — variety data describes the cultivar; rootstock describes the specific specimen and what it's grafted onto.

**Why fifth:** Real user pain (sister specifically), but needs Phase 5 page goals work to scope properly.

**Effort:** M (after Phase 5 design).

**Dependencies:** Phase 5 page goals work for the plant profile page. Phase 3 IA work for attachment-point decisions (which fields belong on `plant_profiles` vs `grow_instances`).

**Source:** VISION.md §6 (Failure Mode #2). Tree-data sub-items raised by user 2026-05-16.

---

### 3.7 🟣 Cross-view cohesion / disorientation (Failure Mode #4)

**Status:** Spans multiple surfaces. Partially addressed by 3.3 cross-view consistency batch. Larger work pending.

**Scope:**
- Audit visual language across surfaces (typography, button treatments, card patterns, terminology)
- Audit IA — clear paths between Plant Profile ↔ Growing Instance ↔ Tasks ↔ Journal ↔ Harvest
- Resolve mental-model mismatches (e.g. "where is the plant's history?")

**Why sixth:** This is mostly "fix as we go" rather than one big project. The 3.3 batch addresses surface inconsistency; the IA work is partly addressed by 3.4 + Phase 4 navigation; mental-model fixes will surface as Phase 5 page goals get defined.

**Effort:** Distributed across many sessions.

**Dependencies:** Phase 4 (navigation), Phase 5 (page goals), Phase 6 (design system) all contribute.

**Source:** VISION.md §6 (Failure Mode #4).

---

### 3.8 🟣 Micro-climate triggers (Failure Mode #3)

**Status:** ✅ in scope per VISION.md §9, but requires new infrastructure. Major feature work.

**Scope:**
- User location capture (zip code, hardiness zone, possibly geo)
- Weather data integration (Open-Meteo or similar, free tier)
- Frost-date data + zone-based recommendations
- Weather alert system (alerts not currently built)
- User observation layering for personalized microclimate over time

**Why seventh:** Foundational for fixing sister's "untrusted timing" complaint, but a big lift. Build only after lifecycle and profile gaps are addressed (those have higher daily impact today).

**Effort:** L. Multi-commit feature.

**Dependencies:** Alerts/notifications infrastructure (also not built yet).

**Source:** VISION.md §6 (Failure Mode #3), §9 (micro-climate triggers ✅).

---

### 3.9 🔵 Bug/feedback batch — 2026-05-12

**Status:** Triaged from user feedback batch 2026-05-12 (items 1-8) + 2026-05-13 calendar testing additions (items 9-10). **Shipped:** 1 (`6d76c20`), 2 (`3de1c2c`), 4 (`3de1c2c`), 6 (`7482c59`). **Parked to §4:** 3 (garden card spacing). **Needs user input before plan-audit:** 5 (journal gallery card format = U13), 7 (plant profile no-AI-data fallback), 8 (two +Entry paths = U12), 9 (swipe-confirm interaction model), 10 (task card structure). Will continue shipping in plan-audit-build cycles as each aesthetic decision lands.

**Scope (10 items: 1-8 from 2026-05-12 batch + 9-10 from 2026-05-13 calendar-testing additions):**

1. **Calendar "Upcoming Tasks" master expand-all / collapse-all toggle.** ✅ **SHIPPED 2026-05-12 (`6d76c20`).** Chevron button next to "Upcoming Tasks" header (only when no date selected and at least one section has content). Tap → all sections expand + arrow rotates 180° (200ms ease-out, per VISION §8). Tap again → all collapse + arrow rotates back. Smooth content height animation on per-section expand/collapse via `grid-template-rows 0fr → 1fr` technique (no JS measurement). Applies to both master and per-section toggles for consistency. Awaiting user prod verification.
2. **Phone calendar month-nav arrows.** ✅ **SHIPPED 2026-05-12 (`3de1c2c`).** `← May 2026 →` arrows now hide below `xl:1280px` (phone + iPad-landscape) via `hidden xl:flex` Tailwind classes on both buttons in `src/app/calendar/page.tsx:976+985`. Desktop ≥1280px keeps arrows. Phone/iPad nav stays via existing swipe gesture on the wrapping div. Matches B1 + B2 breakpoint family. Awaiting user phone verification.
3. **Garden card spacing.** 🟡 **PARKED 2026-05-12 → see §4 Parked.** User identified empty space below short plant names during 2026-05-12 decision session. Root cause diagnosed: `min-h-[1.75rem]` on h3 in `ActiveGardenView.tsx:1075` reserves 28px for 2-line names — 14px visible gap when name is 1 line. 3 fix options surfaced (remove min-h / reduce to `min-h-[1rem]` / move space via `justify-end`) — user opted to think through the alignment-vs-tightness trade-off in its own batch rather than bundle with #2 + #4. Full options + trade-offs preserved in §4.
4. **Garden card "Add journal entry" trigger icon swap.** ✅ **SHIPPED 2026-05-12 (`3de1c2c`).** *Originally mis-triaged 2026-05-12 as "Bag (shopping list) icon swap" referring to the mobile header — actual scope all along was the Garden plant card trigger button.* `JournalCareHands` was rendered both as the trigger icon (on every Garden plant card in gallery + list views) AND as the `care` entry-type marker on journal entries ([journal/page.tsx:230](src/app/journal/page.tsx:230)). Result: trigger button visually duplicated one of the entry-types it produces. Swapped to `ICON_MAP.Edit` (pen with motion) in 2 sites in `src/components/ActiveGardenView.tsx` (lines 1094, 1242). **Scope A locked:** trigger only; `JournalCareHands` stays in ICON_MAP and continues to mark `care` entries on display. Scope B (also retag `care` marker) and Scope C (full entry-type vocabulary audit) deferred — the secondary cohesion question (Harvest basket + JournalCareHands cupped curve reading as same family) noted for future Phase 6 design-system pass. Awaiting user phone verification.
5. **Journal gallery card format.** **= BUGS.md U13 (pre-existing, re-flagged 2026-05-12).** User wants structured layout: clear header w/ relevant info (date + type) + description body + variety/location tags. Currently variety pill is mingled with description visually. Effort: S/M (~1-2h per U13). **Aesthetic discussion:** confirm structure — `[date | type icon+label]` header, description below, variety + location pills at bottom? Or different shape?
6. **Journal search.** ✅ **SHIPPED 2026-05-12 (`7482c59`).** Search input above view-toggle row in journal sticky header. Multi-token AND across note, entry_type (underscores stripped), plant name + variety, formatted date. All three view modes (table/gallery/timeline). Awaiting user prod verification. **Follow-up bugs from ship (BUGS.md):** U21 (two clear-X buttons on Android Chrome) ✅ **Fixed 2026-05-12 (`fb67e10`)** — webkit native cancel button hidden via Tailwind arbitrary variant.
7. **Plant profile no-AI-data fallback.** When AI returns no data for a plant profile section, the section should show a graceful empty state rather than nothing/broken UI. Effort: S. **Needs:** look at current behavior to see what happens today, then aesthetic decision on empty-state copy (retry button? "No info yet" + add-manually CTA? Encyclopedia stub?).
8. **Two "+Entry" paths on journal page = BUGS.md U12 (pre-existing, re-flagged 2026-05-12).** User re-surfaced after `7482c59` ship. UX coherence decision: keep both with differentiation, or remove one. Effort: Decision + ~30 min. Surfaced 2026-05-07; user has not yet made the call.
9. **Swipe-to-complete should require checkmark confirmation, not autocomplete on swipe (new 2026-05-13).** Refines `cea21e0` (2026-05-08 singleton row swipe) and `f77507a` (2026-05-10 consolidated row swipe) shipped behavior — both currently autocommit past 100px threshold + 8px direction lock. User flagged 2026-05-13 during calendar testing: "user has to hit check mark so it doesnt autocheck as complete for accidental swipe." VISION §11 swipe-batch resolution direction (2026-05-08) named "left=complete, right=snooze" but didn't specify confirm-vs-autocommit interaction model — refinement of locked behavior, not contradiction. **Aesthetic — needs user input on interaction model:** swipe reveals checkmark (user taps to confirm)? swipe-then-confirm-via-modal? rubber-band-back if no checkmark tap within N seconds? Cross-ref: VISION §11 swipe-batch parked-resolution; BUGS U13 sister concern (hierarchy on cards). Effort: S/M (touches `useRowSwipe` hook + `CalendarTaskRow` + `ConsolidatedOverdueHeader` + swipe test cases in 387/387 suite).
10. **Restructure calendar task cards — primary TO_DO line, plant/variety secondary, conditional date (new 2026-05-13).** User flagged 2026-05-13 during calendar testing: "lets structure the tasks cards more clearly. TO_DO task, plant/variety. we may want to remove date on the card, unless its in the overdue section(?)" Related to VISION §11 "Calendar task row primitive — bubbles vs. list" (parked 2026-05-08; same surface, internal-structure variant of the same density/clarity concern) AND BUGS U13 "Journal entry cards — visual hierarchy poor" (re-flagged 2026-05-12; same hierarchy pattern, now echoed for Calendar tasks). **Aesthetic — needs structure proposal + per-element treatment input:** primary line wording (TO_DO verb only? "Water Tomato — Sungold"?); plant/variety placement (secondary line? pill on first line?); date conditional rule (hide except in Overdue? hide except for tasks >7 days out? always show but demote visually?). Cross-ref: VISION §11 row-primitive parked decision; BUGS U13 hierarchy pattern; sister to #9 (both shape the Calendar task row primitive). Effort: M (touches `CalendarTaskRow` markup + likely day-section grouping logic).

**Dependencies:**
- Item 1 depends on `a7dadb7` (already shipped). No blockers.
- Item 6 (Journal search) — read existing journal page to find where search input fits; likely no schema change.
- Item 7 — read existing AI-data flow on profile page to understand current state.
- Aesthetic items (2, 3, 4, 5) blocked on user input only.

**Verification strategy:** standard plan-audit-build per item or grouped (3-5 batch per WORKFLOW.md). User prod-verifies on phone after each ship.

**Source:** User feedback batch 2026-05-12. See VISION.md §12 for verbatim signal.

---

### 3.10 🔵 Doc hygiene cleanup — 2026-05-12 audit findings

**Status:** Triaged from user-delivered audit pasted 2026-05-12 (separate audit pass, possibly run in another session). All findings cross-referenced against existing ROADMAP/VISION/BUGS captures — most are new. **R1-R5 strict drift batch shipped 2026-05-13** as a single doc-only commit; R6-R8 structural items still need scope decisions; D1/D2 need user input.

**Strict fixes — ✅ shipped 2026-05-13 (no aesthetic decisions, single doc commit):**

- **R1** — `CLAUDE.md` line 81: test count `329/329` → `387/387`. Verified via `npm run test:run` 2026-05-12. ✅ **Shipped 2026-05-13**
- **R2** — `docs/WORKFLOW.md` line 3: "Seed Vault" → "Garden Tracker" (rename happened pre-VISION-v4); bump stamp to 2026-05-12. ✅ **Shipped 2026-05-13**
- **R3** — `docs/BUGS.md`: U1 ("Set up your zone" widget) + U3 (variety dropdown alphabetical) are silently fixed in code. Move to "Resolved (Closed)" with verification notes. Verified: no zone widget in `src/app/page.tsx`; `src/components/QuickAddSeed.tsx:213` uses `localeCompare` for variety sort. ✅ **Shipped 2026-05-13** (variance from audit: applied inline `✅ Fixed (verified date)` annotations on U1/U3 rows in-place, matching the in-file U21-U23 convention rather than physically moving rows to the Resolved table).
- **R4** — `docs/BUGS.md` stamp: "Last updated: 2026-05-07" → 2026-05-12 (file contains U21-U23 from this week). ✅ **Shipped 2026-05-13**
- **R5** — `docs/VISION.md` §11 icon density: "~25+ icons" → "42 icons" (actual count in `ICON_MAP`). Recommendation direction unchanged. ✅ **Shipped 2026-05-13** (factual count only — decision substance preserved verbatim, see §6 2026-05-13 lock-hygiene note).

**Structural fixes — 🔵 needs scoping (user input on shape):**

- **R6** — `docs/PROJECT_STATUS.md` is pre-VISION-v4 (claims "Seed Vault" identity, Phase 0-9 taxonomy, 329 tests, stamped 2026-04-06) but cited as canonical by `WORKFLOW.md:215` + `BACKLOG.md:7`. **Decision needed:** banner at top "ARCHIVED"? rename `PROJECT_STATUS.archive.md`? move to `docs/archive/`? Update the 2 citations to point at ROADMAP.md regardless.
- **R7** — `docs/WORKFLOW.md` missing 5 procedural rules locked 2026-05-11/12 (Plan-audit 3-pass, Pre-push visual check, Feedback-batch triage Step 0, PM/feature-creep, Roadmap maintenance). All in CLAUDE.md but absent from WORKFLOW.md. **Decision needed:** thin pointer (one-line refs to CLAUDE.md sections) vs full duplication (single source ambiguity risk). Recommend pointer approach.
- **R8** — `docs/BACKLOG.md:7` references `.cursor/plans/remaining_steps_roadmap.plan.md` (pre-Claude-Code Cursor IDE era, incompatible Phase taxonomy). Remove citation.
- **R9 — Migration-naming convention fix + audit existing files for duplicate timestamp prefixes (locked 2026-05-16).** Bug surfaced during U24 Phase B diagnosis: two migration files share the exact timestamp prefix `20250330000000` (`_journal_entry_type_prune.sql` + `_user_settings_onboarding_complete.sql`). Supabase migration ordering uses the prefix; collisions create undefined order and risk silent migration skip on prod via `supabase db push` or Dashboard application. The U24 root cause `ended_at` may have come from the same shape — needs investigation. **Recommended fix path:** (a) audit `supabase/migrations/` for ALL duplicate-timestamp files via `ls supabase/migrations/ | cut -c1-14 | sort | uniq -d`; (b) rename collisions to use unique `YYYYMMDDHHMMSS` 14-digit timestamps (e.g. `20250330120000_user_settings_onboarding_complete.sql`); (c) lock convention in CLAUDE.md "Things to never do without explicit user permission" — never create a migration with a timestamp prefix that collides with an existing file; (d) for legacy files that were already applied to prod under the original names, renaming local files is fine as long as Supabase tracking doesn't depend on the filename (CLI uses the prefix; Dashboard applies whatever SQL gets pasted). Effort: S (~30 min — audit + renames + CLAUDE.md rule). Cross-ref: this fix prevents future U24-class silent failures.

**Future / parked — 🟣 in §4:**

- **D1** — ~30 legacy docs in `docs/` folder (AI_FLOWS.md, AI_PROCESS_AUDIT.md, FULL_APP_AUDIT.md, AUDIT-2025-02.md, etc.) of unclear provenance — none referenced by canonical docs but sit alongside them. **Recommended approach:** bulk-move pre-VISION-v4 docs to `docs/archive/` so `docs/` shows only active+canonical. Full content audit is correct long-term but high cost — defer.
- **D2** — `.claude/settings.json` doesn't exist at repo root despite `docs/CLAUDE_CODE_SETUP.md` describing the optional `UserPromptSubmit` hook for the rules-card injection. User-optional; surface whether to enable.

**Cross-reference / low-risk:**

- BUGS.md timestamp stale (covered by R4).
- ROADMAP §1 says "Journal flicker bug repro still pending" — confirm whether still next action.
- VISION §11 Calendar task fatigue parking framing could tighten now that chunk 3.9 #1 (expand-all toggle) shipped (`6d76c20`).

**Effort:** R1-R5 are 5 doc edits (~20 min total). R6-R8 + D1 need user decisions before execution.

**Source:** User-delivered audit document 2026-05-12. Full audit preserved in this session's transcript for reference.

---

### 3.11 🟣 Trusted-circle infrastructure (sharing UX build)

**Status:** Vision-level design locked 2026-05-17 in `competent-blackwell-5de2a2` chat. Scope is a major theme — likely multi-chunk; needs further planning before any build greenlight.

**Scope (high-level, from VISION §3 Layer 3 expanded structure + §6 2026-05-17):**
- **Groups model** — circle = collection of groups; each group has members + mode (browse OR board) + name
- **Default group** — "Friends & Family" browse-mode auto-created on first circle member added
- **Multi-group membership** — a person can be in multiple groups across modes
- **Browse-mode surface** — destination page showing circle members' opted-in inventories; long-press item → "Give / trade" or "Request" depending on whose inventory you're viewing
- **Board-mode surface** — group feed of asks + offers; Post-an-Ask + Post-an-Offer + Browse flows (A/B/C from §6 2026-05-17)
- **Per-category privacy opt-in** — user controls what categories (seed packets / active plants / harvests) are shared per group
- **Trade flow** — two shapes both auto-log to Journal: (a) direct-give from inventory card (long-press → pick recipient → confirm); (b) broadcast offer from board (post → claim → completes)
- **Notifications** — direct interactions push (default-on); ambient activity pull-only; household opt-in light push; no activity feed
- **Household attribution** — data captured by default; filter view surfaces "who did what" on demand
- **Invite mechanics** — how do users invite each other into groups (parked sub-decision — needs separate design pass)

**Why this chunk:** Closes (a)-(f) open-territory items from VISION §11 Sharing UX entry (now resolved). Enables Level 3 trade ✅ scope. Differentiator-protecting (community without becoming social media).

**Effort:** L (large). Multiple sub-chunks during planning.

**Dependencies:**
- §3.5 batch-tracking adds richer browse depth ("10 tomatoes, 4 planted, 6 in pots") but MVP sharing CAN ship before §3.5 lands (aggregate counts work for first version)
- May inform §3.4 beds-as-first-class (bed-level sharing implications — communal plots in board-mode groups could share at bed level)
- Phase 3 IA work for group + member data model

**Open sub-decisions still needed (carry from §6 2026-05-17 bucket b):**
- Invite mechanics UX (needs separate design pass)
- Multi-responder coordination on board-claim (first-come vs owner-approve)
- Notifications build path (the WHAT is locked; the HOW isn't)

**Source:** VISION §3 Layer 3 expanded structure + §9 Level 3 row + §10 no-activity-feed + §11 sharing-UX resolved entry. ROADMAP §6 2026-05-17 sharing-purpose-chat closeout entry (this chat).

---

### 3.12-pre ✅ App-store ship-readiness preflight — gate developer tools (shipped 2026-05-17 `4a8b04c`)

**Status:** ✅ Shipped 2026-05-17 (`flamboyant-wilson-34fc0c` chat, `4a8b04c`). Awaiting user phone-verification on prod.

**Implementation chosen at plan time = existing `useDeveloperUnlock` tap-count gate** (per `binary-valley` Rule A Q1 list, ultimately chosen after Amendment 2 — see §6 2026-05-17 `flamboyant-wilson` entry for full Pass-3-sibling-sweep miss + revert log). Settings page menu gates on `isUnlocked`; each of 5 dev sub-pages renders the canonical scraper-audit-style "Developer tools require unlock" message + Go to Settings link when locked. Aligns with existing pattern at [admin/scraper-audit/page.tsx:68](src/app/admin/scraper-audit/page.tsx:68); no new mechanism introduced. Entry gesture = tap App Version 7× on Profile (existing).

**Surfaced by:** Persona-audit P0 theme #1 of 10. Verified during plan-mode via [src/app/settings/page.tsx:7-17](src/app/settings/page.tsx) — `SETTINGS_ITEMS` array rendered Developer / Import logs / Extract cache to every user with no gate.

**Scope (shipped):** 5 pages now gated (initial scope said "4 sub-pages under /settings/developer/*" — actual count was 3 under that route + 2 standalone = 5 total):
- Developer (fill-in-blanks, repair hero, cache, trash) — [/settings/developer](src/app/settings/developer/page.tsx)
- Debug log — [/settings/developer/debug-log](src/app/settings/developer/debug-log/page.tsx)
- API Usage — [/settings/developer/usage](src/app/settings/developer/usage/page.tsx)
- Import logs — [/settings/import-logs](src/app/settings/import-logs/page.tsx)
- Extract cache — [/settings/extract-cache](src/app/settings/extract-cache/page.tsx)

Walter / Aria / Sam all confused at first sight; Sam-on-day-1 was the explicit APP-STORE BLOCKER signal ("dev tools on first run = app is unfinished / not for me"). Maya merely intrigued (not blocking but messy).

**Why pre-§3.12:** Mechanical fix, isolated from all persona-design work. Ships independently. Unblocks public-distribution context locked 2026-05-17 (VISION §1, §3). The other 9 persona-audit themes need design discussion; this one didn't.

**Implementation alternatives considered (at plan time):**
- Build-flag gate (`process.env.NEXT_PUBLIC_DEV_TOOLS === 'true'` hides in prod build) — REJECTED, user uses dev tools on prod against real data
- Role-gated menu (user role flag in Supabase) — REJECTED, schema migration overkill for S effort
- Env-var user-ID allowlist (`NEXT_PUBLIC_DEV_USER_IDS`) — initially CHOSEN via Q1 Rule A, then REVERTED via Amendment 2 when Pass 3 miss surfaced existing `useDeveloperUnlock` already covered this
- Tap-count-to-unlock (gesture-revealed) — FINAL CHOICE via Amendment 2; already half-built in codebase

**Files shipped:** [src/app/settings/page.tsx](src/app/settings/page.tsx) (split `USER_ITEMS` / `DEV_ITEMS` + `isUnlocked` gate) + [src/app/settings/page.test.tsx](src/app/settings/page.test.tsx) (two cases: locked + unlocked) + 5 dev pages each adding the `if (!isUnlocked) return <unlock message>` gate.

**Verification:** Phone-verify on prod after Vercel auto-deploy: (1) Settings shows 6-item menu by default; (2) tap App Version 7× on Profile bottom → return to Settings → 9-item menu; (3) bookmark `/settings/developer` while locked → "Developer tools require unlock" message + Go to Settings link. Persona walk in audit log confirmed all 5 personas + user pass.

**Dependencies:** None. Independent of §3.12 catalog.

**Effort actual:** S — 7 files, +100/-8 lines, ~45 min including Amendment 2 revert + re-test.

**Source:** Persona-audit P0 theme #1; ROADMAP §6 2026-05-17 `binary-valley` + `flamboyant-wilson` entries.

**ADJACENT finding captured:** [/api/developer/*](src/app/api/developer) server-side routes (4 handlers) accept any authed user — captured as BUGS U27 for follow-up security-hardening ship (per Q2 Rule A lock in `binary-valley`).

---

### 3.12 🟣 Persona-audit findings catalog — 2026-05-17

**Status:** Catalog form (triage list, not build chunk). Locked 2026-05-17 (`binary-valley` persona-audit chat, Rule A ask). Per-theme planning chats spin off this catalog as user prioritizes; **do not build directly from §3.12** — promote items into their own chunks.

**Method:** Each of 8 surfaces (Home / Vault / Garden / Calendar / Journal / Shed / Settings / FAB+modals) walked through 5 personas (Maya / Sydney / Walter / Aria / Sam). Top 2-3 concerns per cell. Findings tagged P0 (app-store blocker) / P1 (high) / P2 (medium) / P3 (low). Full 8 × 5 walk preserved in plan file `C:\Users\marsh\.claude\plans\continue-from-prev-chat-binary-valley.md`.

**Cross-cutting themes (highest leverage):**

1. **P0 — Developer tools gating** — Settings exposes dev tools to all users; APP-STORE BLOCKER. **Split into §3.12-pre chunk (sibling above), ships first** independent of §3.12 catalog.
2. **P0 — Outdoor-garden assumption sweep** — Aria + Sam excluded by default framing across Home (frost alerts, planting schedule), Vault ("Seed Vault" tab), Garden ("Active Garden" / beds), Calendar (planting calendar), Shed ("Shed" naming), FAB (Add Seed / Add Supply outdoor-framed). Per VISION §3 disclosure-by-data: these should appear gracefully on data, not be default visible.
3. **P0 — Plain-language audit** — **Phase A.1 ✅ SHIPPED 2026-05-25** (`b669a2a` + `8310070`, `gt-casing-theme3-a1` chat). 4 renames: Vendor Scorecard → "Vendor ratings"; Monthly pulse → "Monthly view"; Annual roadmap → "Yearly view"; Plantable → "Plant this month" (verb-choice driven by data-source audit). **Dropped from scope** after audit found zero user-facing strings: Polyculture / True leaves / Hardening off / Grow instance (purely internal type/component name). **Deferred to §8 settings IA chat:** Repair hero + Extract cache (dev-gated since `4a8b04c`). **Phase A.2** (Vault + Shed renames; URL + sidebar nav + 50+ files; needs redirect strategy) = separate dedicated chats next.
4. **P0 — Visible-button fallback for swipe-only gestures** — ✅ SHIPPED 2026-05-17 (`distracted-wilson-616865` chat). Audit + targeted fix on 3 calendar surfaces (month-nav arrows, singleton row Snooze/Complete, consolidated overdue Snooze-all/Complete-all): button-fallback breakpoints lowered from `lg:` / `xl:` to `md:` (iPad-portrait+). Aligns with existing Vault/Shed `md:` anchor; phone-portrait keeps swipe-first per Principle 9 spirit. VISION §4 Principle 9 worked-example narrowed + §11 row-primitive parked-direction footnoted. **Audit also surfaced:** user-prompt's "Garden drag-to-move" + "Journal swipe-delete" do NOT exist in codebase (both pages use long-press for bulk-select, theme #10 not #4); image crop drag (ImageCropModal) is intrinsic to UX, no fallback possible.
5. **P0 — Empty-state cliff audit** — ✅ SHIPPED 2026-05-18 (`9bad88f`, `trusting-proskuriakova-8e23c2` chat). Sam day-1 risk addressed via per-surface copy rewrite: 17 code files / ~22 distinct strings / 3-part frame anchored (purpose-describing title / plain next-step body / verb-led CTA "Add a {noun}"). 6 EmptyStateCard sites + 14 raw-card / inline `<p>` empty states across Home / Vault main / Vault profile / Vault sub-pages / Garden / Calendar / Journal / Shed / Shopping-list. VISION §10 empty-by-default LOCK preserved (refines COPY ON existing empty states; state-default unchanged). Walter jargon dropped ("permanent"/"perennial" body / "Vault import" / "active batches" / "Map your hillside" / "FAB" / "Plant from the Seed Vault" / "polyculture"). "Long-living plants" replaces "permanent plants" (Walter-plain term). "Library" softens "Vault"/"Shed" in body (page labels preserved for theme #3 Phase A). All 5 personas pass Pass 2 walk. 4-pass plan-audit terminated clean at iteration 2 on Pass 2. **ADJACENT finding captured to §4:** 4 raw-card empty states use `bg-white rounded-xl border ... p-8 text-center` shape instead of canonical `EmptyStateCard` primitive — primitive-canonicalization belongs to theme #7.
6. **P1 — Disclosure-by-data on FAB Menu + tabs** — 6-option FAB always; 3-tab Vault always; 2-tab Garden always. Sam paradox of choice; Aria sees 3-4 irrelevant always. Needs explicit per-option / per-tab disclosure rule. Phase 5 design candidate.
7. **P1 — Cross-view consistency** — Sydney's primary pain. Vault grid + Garden gallery + My Plants list visual incoherence; row-primitive bubbles-vs-list; icon-cohesion (multiple parked). Already at VISION §11 + queued for design pass.
8. **P1 — Settings information architecture** — 10+ flat items, no grouping, mixed user/dev surfaces. Walter loses navigation; Sam overwhelmed. Independent of dev-tools gating (#1). Phase 4 navigation work.
9. **P1 — Memory-plane gaps** — Maya's primary value gap. Multi-season comparison, variety eval, year-over-year, structured journal tagging — partial-build. Chunk 3.6 territory.
10. **P1 — Touch target sweep** — Walter; iPad-first audit across ALL surfaces: long-press handles, count badges, expand-all chevron, supply rows, alert dismissal, insight dismissal, sub-screen back buttons. Mechanical fix shape once enumerated.

**Per-surface findings:** see plan file `C:\Users\marsh\.claude\plans\continue-from-prev-chat-binary-valley.md` for full 8 × 5 walk with prioritized findings.

**Dependencies:**
- #1 (dev-tools gating) = independent + mechanical = §3.12-pre (above), ships first.
- #2 (outdoor-garden assumptions) + #6 (disclosure-by-data on FAB/tabs) = same family; build together.
- #3 (plain-language) + #8 (settings IA) = Phase 4 navigation audit work. **Phase A.1 ✅ SHIPPED 2026-05-25** (`b669a2a` + `8310070`); Phase A.2 (Vault + Shed) = dedicated chats next.
- #4 (visible-button fallback) = ✅ SHIPPED 2026-05-17 (`efe588d`, `distracted-wilson-616865` chat; awaits phone + iPad verification).
- #5 (empty-state cliff) = ✅ SHIPPED 2026-05-18 (`9bad88f`, `trusting-proskuriakova-8e23c2` chat; awaits phone verification).
- #7 (cross-view consistency) = already VISION §11 parked, queued for design pass.
- #9 (memory plane) = chunk 3.6 territory.
- #10 (touch targets) = independent audit + targeted fix.

**Effort:** Catalog itself = none (this entry). Per-theme planning chats vary; #1 = S, #4 + #10 = S each, #5 = M, #2 + #6 = M-L, #3 + #8 = L (Phase 4 work).

**Source:** Persona-audit chat (`binary-valley`, 2026-05-17). Method + persona system locked 2026-05-17 sharing-purpose chat (VISION §3 + PERSONAS.md). App-store distribution context VISION §1 boundary line + §3 first paragraph.

---

### 3.12-tester ✅ Tester feedback enhancements — 2026-05-17 (CLOSED)

**Status:** All 4 items closed. T1 + T2 ✅ SHIPPED bundled 2026-05-17 (`e50a667`, `vigorous-nightingale-039e05` chat). T3 ✅ Locked 2026-05-17 (Option A — public stays as-is; `flamboyant-lamport-f5cc74` chat, doc-only). T4 ✅ SHIPPED 2026-05-17 (`bc253be`, `mystifying-lichterman-55e122` chat).

**Context:** Current Tools-bar feedback flow ([AuthGuard.tsx:297](src/components/AuthGuard.tsx:297) + [AuthGuard.tsx:195](src/components/AuthGuard.tsx:195) loading-shell mobile top bar + [Sidebar.tsx:162](src/components/Sidebar.tsx:162) desktop sidebar footer → [FeedbackModal.tsx](src/components/FeedbackModal.tsx)) captures: `message`, `category` (bug/feature/question/other), optional screenshot, `page_url`, `user_id`, `user_email` — saves to supabase `user_feedback` table (RLS-protected; users can only insert their own rows). **No voice. No debug log today.** Debug log lives separately at [/settings/developer/debug-log](src/app/settings/developer/debug-log/page.tsx) (now dev-gated post-`4a8b04c`) — manual navigate + Copy All + paste flow only. **Auth page (pre-sign-in) shows zero feedback buttons** — all 3 sites are inside `{!isAuthPage && ...}` blocks.

**Items:**

- **T3 ✅ Locked 2026-05-17 — Tools-bar button visibility scope = public (Option A).** Decision rationale: app-store distribution locked 2026-05-17 (VISION §1, §3) inverts the calculus — public users are exactly who we want feedback from on launch; gating cuts that off. Feedback button ≠ developer tool (§3.12-pre's `4a8b04c` resolved destructive admin actions visible to fresh users; feedback is the opposite — user-to-developer communication, common public-app pattern). Persona walk: 3/5 personas neutral-to-positive on public; 5/5 lose if gated. Sensitive payload pieces (voice, debug log) get gated at attach-time per T1/T2 below, not at channel-level here. **Pass 3 sibling-sweep with vocabulary-breadth rule (CLAUDE.md 2026-05-17):** `useDeveloperUnlock` confirmed as sole access-gate pattern in repo (9 consumers, all dev-tools); no tester/beta/allowlist concept exists. Option B would reuse the existing pattern cleanly but silence public bug-reporters; Option C would invent a new mechanism with zero peers. **Decision shape:** Option A — public, no code change. Zero new gating code. ADJACENT finding: BUGS U27 (`/api/developer/*` server-side gating) unrelated — feedback writes via Supabase RLS, not via an API route. Three Rule-A alternatives surfaced (A/B/C); user picked Recommended (A) without modification. **Aesthetic flags addressed:** entry copy stays "Send feedback" (no rename); icon stays `FeedbackIcon`; no menu re-anchor. **Persona-walk findings carried forward into T1/T2 scoping** — see below.

- **T1 ✅ SHIPPED 2026-05-17 (`e50a667`, `vigorous-nightingale-039e05` chat) — Voice recorder attach in FeedbackModal.** Implementation: new `useVoiceRecorder` hook (`src/hooks/useVoiceRecorder.ts`, +189 lines) — MediaRecorder MIME fallback chain (webm/opus → mp4/aac → default) handles Chrome/Android + iOS Safari; 60s auto-stop via setInterval timer; state machine `idle → recording → recorded`; stream cleanup on unmount anchored on `useDesktopPhotoCapture` pattern. FeedbackModal renders visible Record button (with `ICON_MAP.Mic`, dashed-border shell matching "Attach screenshot"), Stop button during recording (emerald solid + red pulse dot), native `<audio controls>` playback + Re-record button when `recorded`. Plain-language permission-denied recovery copy: "Microphone access denied. You can still type your feedback or attach a screenshot." All 5 persona-walk: pass. Walter benefits most. New `Mic` entry in `ICON_MAP` ([styleDictionary.tsx:484+](src/lib/styleDictionary.tsx)) using `iconProps` defaults. Voice blob uploads to existing `journal-photos` bucket at `{user_id}/feedback-voice-{uuid}.{webm|m4a}` (RLS path-prefix-only, no new policy). New `voice_path text` column on `user_feedback` via migration `20260517120000_user_feedback_voice_and_debug_log.sql`. **All T3 Option A locks honored verbatim:** visible record/stop (no gestures), 60s cap (within 30-60 range), plain-language mic permission, retake always visible, no auto-start. User picks the in-plan defaults verbatim via single-message greenlight (no per-item Rule A churn — auto-mode override accepted).

- **T2 ✅ SHIPPED 2026-05-17 (`e50a667`, `vigorous-nightingale-039e05` chat) — Debug-log opt-in auto-attach on feedback submit.** Implementation: opt-in toggle in FeedbackModal (verbatim SettingsSuccessSoundToggle peer-sr-only + emerald-500 track shell) defaults OFF; state local to modal (`useState(false)`, NOT localStorage — per-session privacy default). When ON, scrollable read-only textarea renders `formatEntriesForCopy(getEntries())` from existing `debugLogBuffer.ts` infrastructure (no buffer changes). Empty-log case: "(No console messages captured yet.)" placeholder. Plain-language disclosure copy: "Attaches recent technical messages from your browser. Helps us figure out what went wrong. You can review the text below before sending." On submit: text stored inline in new `debug_log_text text` column on `user_feedback` (capped at ~12.5 KB max per MAX_ENTRIES × char-budget; well under pgsql text-column overhead). All 5 persona-walk: pass. Toggle resets to OFF on modal close. **All T3 Option A locks honored verbatim:** default OFF, opt-in toggle, content preview visible, plain-language disclosure. Console-content sensitivity sweep clean (no `password|token|api_key|secret` in `debugLogBuffer.ts` source; captured log = partial user IDs + error stacks + Supabase error codes, no credentials).

- **T4 ✅ SHIPPED 2026-05-17 (`bc253be`, `mystifying-lichterman-55e122` chat) — Feedback inbox admin page at `/settings/developer/feedback-inbox`.** Un-parked at user greenlight 2026-05-17 (per the recommended ship order: T4 → #5 → #3 → #2/#6 → #8). **Q1 security model locked Option B = security-definer Postgres function** `admin_list_feedback()` bypasses `user_feedback` "users SELECT own only" RLS when caller's `auth.uid()` is in new `developer_users` allowlist table; raises 42501 'Not authorized' otherwise. Page renders friendly "Not authorized — add your user_id to developer_users" guidance with paste-able INSERT for the user. Page features: list (newest first, 200-row cap), filter dropdown (category) + has-voice/has-debug-log toggles, per-row render of created_at + category badge + user_email + message + page_url + screenshot (public URL) + native `<audio controls>` (public URL, `preload="none"`) + collapsible debug-log textarea. Refresh button. All UI primitives anchored on existing patterns ([scraper-audit/page.tsx:68](src/app/admin/scraper-audit/page.tsx:68) gate copy, [/settings/feedback/page.tsx:66](src/app/settings/feedback/page.tsx:66) row primitive, [FeedbackModal.tsx](src/components/FeedbackModal.tsx) audio shape from T1 ship, [styleDictionary.tsx](src/lib/styleDictionary.tsx) filter dropdown shell, [SettingsSuccessSoundToggle.tsx](src/components/SettingsSuccessSoundToggle.tsx) toggle shape from T2 ship). **One-time setup post-deploy:** user runs `INSERT INTO public.developer_users (user_id) VALUES (auth.uid())` once in Supabase Dashboard SQL Editor while signed in as herself. **Migration `20260518000000_admin_feedback_inbox.sql`** (additive only — `CREATE TABLE IF NOT EXISTS developer_users` + `CREATE OR REPLACE FUNCTION admin_list_feedback()` + `GRANT EXECUTE`). User applies on prod via Dashboard SQL Editor; worktree's supabase CLI not linked (link state in gitignored `supabase/.temp/`); same Dashboard-SQL-Editor pattern as T1+T2's `20260517120000`. **Architecture decisions (made autonomously per AUTO MODE dispatch):**
  - **`developer_users` allowlist table** (not hardcoded UUID list, not `app_metadata` flag, not API route): extensible for future devs, portable, RLS-locked (no policies → only the security-definer function reads it), function is sole reader.
  - **plpgsql + RAISE EXCEPTION** for not-authorized case (clean distinction empty-table vs not-allowed; Pass 2 revision applied mid-audit).
  - **200-row server-side cap** (no pagination yet; revisit if needed).
  - **Public URLs not signed URLs** (bucket already public per `20250204100000`; matches existing `/settings/feedback/page.tsx:81` pattern).
  - **No new ICON_MAP entry** (Settings menu is text-only).
  - **Debug log collapsed per-row** (toggle-to-expand; matches FeedbackModal T2 preview shape).

  **Pass 3 sibling sweep (vocabulary breadth):** 8 vocab families grepped — admin/dev gating (`useDeveloperUnlock` 13 files; sole pattern), security-definer functions (13 migrations; `STABLE + SET search_path = ''` shape adopted), `developer_users|dev_users|admin_users` (none exist — genuinely new), `supabase.rpc(` (9 sites; signature adopted), `getPublicUrl` (10 sites; adopted), `<audio` (1 site = FeedbackModal T1 ship; pattern adopted), `debugLogBuffer` (T2 ship consumers; this ship reads persisted `debug_log_text` column, not live buffer — vocabulary-distinct ADJACENT logged), list-with-filter dev pages (scraper-audit table vs /settings/feedback cards — picked cards; CONCERN logged for future cohesion pass). All BLOCKING alignments matched verbatim. **3 ADJACENT findings logged:**
  1. Two co-existing list patterns in dev pages (table at scraper-audit vs cards at /settings/feedback + this ship). Future dev-page-cohesion pass should unify (Phase 5+).
  2. `developer_users` table is a primitive that BUGS U27 (`/api/developer/*` server-side gating) can reuse. Saves rebuilding the gate.
  3. No "mark feedback as addressed / read" workflow. Future enhancement.

  **Pass 4 lock hygiene clean** (VISION §10 no-touch — dev surface, no default-state change; §11 no parked-decision brush; §4 Principle 3 disclosure-by-data aligned; ROADMAP §6 §3.12-pre `useDeveloperUnlock` pattern aligned; §3.12-tester T1+T2 columns consumed; BUGS U27 untouched — uses Supabase RPC not API route). **Persona walk:** dev-only surface; 4/5 personas (Maya/Sydney/Walter/Aria/Sam) pass by exclusion. User (developer-persona) gets full surface. 5 files (+533/-1). Tests 432 → 439 (+7). Lint clean. Build clean. Preview MCP skipped (`.env.local`-absent + page gated + needs migration + needs `developer_users` INSERT = doubly auth-blocked beyond standard skip rationale). 4-pass plan-audit terminated clean at iteration 1 on as-written plan. Concurrent-chat fetch validation #6 clean. Plan file at `C:\Users\marsh\.claude\plans\mystifying-lichterman-55e122.md`.

**Dependencies:** T3 ✅ done. T1 ✅ shipped. T2 ✅ shipped. T4 ✅ shipped. Chunk fully closed pending the 3 verification steps (T1+T2 migration apply + phone verify; T4 migration apply + one-time INSERT + admin inbox phone verify).

**Why 🔵 not feature creep:** Per CLAUDE.md "Counter-cases (don't flag)" — internal tooling counter-case. The Tools-bar IS the tester-feedback infrastructure; expanding it is finishing the tooling, not new product scope. **T3 Option A reframe:** the channel is public on app-store distribution, but the EXPANSION work (voice, debug log) is still internal-tooling counter-case because the infrastructure serves the dev-feedback loop. Public users benefit from the channel; testers (and developer) benefit most from the richer payload.

**Source:** `gifted-wright-4184cb` chat 2026-05-17 user feedback batch + `flamboyant-lamport-f5cc74` chat 2026-05-17 T3 design lock (Option A via Rule A).

---

### Later (🕐 long-term aspirational, in priority-ish order)

- **Pest / illness ID camera + Q&A + treatment recs.** Likely paid tier. Differentiator.
- **Soil test result tracking.** Pairs with beds-as-first-class.
- **Garden bed layout / spatial design.** After beds-as-first-class lands.
- **Encyclopedia entries integrated with data.** Pest/variety/condition reference, surfaced contextually.
- **Engagement / push content.** Articles, seasonal guides. Off-season retention.
- **Contextual tips on relevant screens.** Inline micro-tips.
- **Plant ID camera.** Only if accuracy is meaningfully better than existing tools.
- **Generic data import (CSV / webhooks).** For users with hardware sensors.

See VISION.md §9 for full status of each.

---

## 4. Parked / blocked items

Items deferred with the reason for the parking. Re-surface when conditions change.

- **`/plants` (Library) empty-state Filter/Search bar ungate (parked 2026-06-01, ADJACENT from Ship 2a `a47331b`)** — Ship 2a Finding 9 ungated the **Packets**-list toolbar so search+filter render even when the vault is empty (matching the canonical Shed pattern). The **Library/grid** toolbar on `/plants` carries the *same* `vaultHasSeeds` empty-gate at [VaultPageContent.tsx:1031](src/app/vault/VaultPageContent.tsx:1031) (`viewMode === "grid" && vaultHasSeeds`), so an empty Library still hides its Filter/Search bar — a residual Library↔Packets↔Shed inconsistency. Fix is a 1-line guard removal (drop `vaultHasSeeds &&`), identical to the Packets change. **Why parked (deferred-to-testing-signal):** `/plants` empty state is the primary Sam-class first-launch onboarding touchpoint ("add your first seed"); rendering a Filter/Search bar over an empty onboarding card silently changes onboarding chrome, which we declined to do inside a cohesion ship. **Re-surface when:** a trusted-circle tester flags the empty-Library-vs-empty-Packets/Shed inconsistency (then ungate for full parity), OR it gets folded into the VaultLibraryWing extraction park below (natural co-ship). Until a tester signals, the asymmetry stays — Library's onboarding moment is intact by design.
- **`/plants` sessionStorage namespace separation + `getNavSection` `/plants` branch (parked 2026-05-29, post `c45d6c6` hotfix)** — `c45d6c6` shipped a 4-line `if (!isPlantsSurface)` guard on the `vault-view-mode` restore in [VaultPageContent.tsx:418-426](src/app/vault/VaultPageContent.tsx:418), patching the load-bearing case where stale `"list"` from a prior `/vault?tab=list` visit flipped `/plants` to Packets rendering on mount. Underlying architectural fragility remains: (a) `/plants` and `/vault` share the same `sessionStorage` namespace (`vault-view-mode`, `vault-status-filter-*`, `vault-grid-style`, `vault-search`, `vault-sort`) per the unified key list at [navSectionClear.ts:10-24](src/lib/navSectionClear.ts:10) — any future shared-state field added without a matching `isPlantsSurface` guard could re-introduce the same bug class; (b) [`getNavSection("/plants")`](src/lib/navSectionClear.ts:38) returns `"home"` via the catch-all default (no `/plants` branch) — cross-section clear silently no-ops between `/` and `/plants` and between `/plants` and `/vault` in one direction. The clean end-state is two changes: split `/plants` sessionStorage into its own namespace (`plants-view-mode` etc., wired through `useEffect` write+read on the plants surface), AND add `if (pathname.startsWith("/plants")) return "plants"` to `getNavSection` with a matching `PLANTS_KEYS` array + `clearPlantsFilters()` consumer in the cross-section clear path. Estimated effort: 1-2h structural + ~10-15 site sweep for any other reader of the vault keys. **Why parked:** today's guard makes the bug class no longer load-bearing — fix is durable for the current code shape. Adds zero user-visible value beyond the guard; pure architectural cleanup. Pairs naturally with the existing VaultLibraryWing extraction park (both clean up the `/plants` ↔ `/vault` co-housing). **Re-surface when:** VaultLibraryWing extraction ships (natural co-ship — the Wing extraction will lift sessionStorage out of VaultPageContent anyway), OR when a future shared-state field is about to be added and the guard discipline starts feeling fragile, OR as a planned Phase 6 design-system cohesion pass.
- **VaultLibraryWing extraction (parked 2026-05-28, post IA Ship A `9142aad` + `34e8948`)** — Library "grid" rendering logic stays embedded in `VaultPageContent.tsx` post-Ship-A via pathname-aware delegation; `/plants` mounts VaultPageContent with `isPlantsSurface=true` and the same component serves both surfaces. The cleaner end-state is a `VaultLibraryWing` peer to `VaultPacketWing` + `VaultShedWing` (own context provider + toolbar + content + modals components), mounted directly from `src/app/plants/page.tsx`. Estimated effort: 3-4h structural refactor (shared search query needs to lift up to page-level OR collapse, gridFilters peer to listFilters, batch select state, AddVarietyForm wiring, refine modal mount). **Why parked:** adds zero user-visible value — the user gets the same Library on `/plants` either way. Carries non-trivial regression risk in state-interlock paths (sessionStorage migration, sow filter URL handling, batch-select-mode reset on tab switch). Worth a dedicated cohesion ship when not under feature pressure. **Re-surface when:** Ship B (Garden merge + Groups) is shipping and the surface-aware logic in VaultPageContent starts feeling like a wart, OR when adding a new Library-only feature that the pathname-aware shape can't cleanly carry, OR as a planned Phase 6 design-system pass cohesion item.
- **Debug-log instrumentation — ADJACENT expansion (parked 2026-05-25 post-`f79a8aa`)** — 4 form-envelope expansion candidates + 1 codebase cohesion pass logged during the 8-layer instrumentation ship's Pass 3 sibling sweep. Re-surface when a future bug report needs deeper coverage OR as a follow-up consolidation ship. Not blocking — current 6 canonical FAB-flow add-paths cover the highest-diagnostic-value forms.
  - **BatchAddSeed.handleSaveAll** ([BatchAddSeed.tsx:564](src/components/BatchAddSeed.tsx:564)) — multi-stage save (photo-extract → review → save). Form envelope not added in `f79a8aa` because handler is multi-step rather than single-form save. Coverage useful for diagnosing photo-import seed-add path failures.
  - **BatchAddSupply.handleExtractAll** ([BatchAddSupply.tsx:232](src/components/BatchAddSupply.tsx:232)) — photo-extract + route flow. Hands off to `/shed/review-import` rather than saving directly. Coverage useful for diagnosing extract-API or routing failures.
  - **Edit-flow envelopes** — `EditJournalModal`, `EditPacketModal`, `GrowInstanceModal`. Add paths covered in `f79a8aa` (`add_journal` / `add_seed` / via plant edit-modal mode); explicit edit-path envelopes not. Diagnostic value lower than adds but symmetric for completeness.
  - **BatchLogSheet.handleSubmit** ([BatchLogSheet.tsx:287](src/components/BatchLogSheet.tsx:287)) — multi-row save with 5 distinct `showErrorToast` call-sites already shipped. Could benefit from envelope to thread the multi-row save context (which step failed, how many rows succeeded).
  - **`console.error` format standardization (codebase-wide cohesion pass)** — Pass 3 CONCERN captured in `f79a8aa`. Codebase has bracket `[scope]` family ([apiErrorLog.ts:7](src/lib/apiErrorLog.ts:7), [reviewImportStorage.ts:112](src/lib/reviewImportStorage.ts:112); 2 uses) AND colon `scope:` family (cascadeOnGrowEnd.ts:18, completeSowTask.ts:132+165+193, generateCareTasks.ts:69+291+378+457, revertProfileStatus.ts:57, cascadeOnProfileDelete.ts:30+97, cascadeOnPacketDelete.ts:70, ActiveGardenView.tsx:765, MyPlantsView.tsx:493, BatchLogSheet.tsx:287, HarvestModal.tsx:113 left intact, GrowInstanceModal.tsx:421; 14+ uses). New `logEvent` joins bracket family per handoff substance; existing colon-notation peers unchanged this ship. Future cohesion pass could standardize all 16+ sites on `[scope] action {payload}` shape.

- **Garden card spacing (chunk 3.9 #3, parked 2026-05-12)** — User flagged empty space below short plant names. Root cause: `min-h-[1.75rem]` on the h3 at [ActiveGardenView.tsx:1075](src/components/ActiveGardenView.tsx:1075) reserves 28px for 2-line names → 14px gap visible when name is 1 line. The min-h is there to align the "Sown..." date at the same y-coordinate across cards in a grid row (so name slot is uniform regardless of 1 vs 2 lines). Three fix options surfaced during 2026-05-12 discussion:
  - **Option 1 — Remove `min-h-[1.75rem]` entirely.** Short names tighten; date sits directly under name. Trade-off: dates won't align across cards in a grid row when name lengths differ.
  - **Option 2 — Reduce to `min-h-[1rem]` (16px).** Compromise. ~2px gap remains for short names; partial date alignment preserved.
  - **Option 3 — Add `justify-end` (or `justify-center`) to the text section wrapper at line 1074.** Keep min-h, push name+date toward the button row. Empty space moves to TOP under image (reads as padding). Date alignment preserved. Side effect: names sit at varying vertical positions within cards.
  - User opted to defer the alignment-vs-tightness call rather than batch it with #2 + #4. Re-surface when ready to choose; trade-offs are already mapped.
- **Calendar task fatigue (broader than the batch in §3.1)** — Today/Week default window, lazy-load older completions, and other approaches beyond what's in the batch. Parked until the §3.1 batch ships and we see how it feels.
- **Cross-view consistency** between Vault grid / Garden gallery / My Plants list (covered partly by §3.3 FAB consistency batch).
- **Too many places to edit a plant's image** (in `BACKLOG.md`). Audit + consolidate.
- **Sister's additional feedback** — pending. User said she'd get more.
- **Plant database moderation philosophy** — Phase 3 deferred. Concern about user pollution / inappropriate entries. Hybrid (curated canonical + private user extensions + suggestion queue) recommended.
- **Growing instance representation detail** — Phase 3. Now that beds are first-class, the exact relationship needs design.
- **Welcome instructions (2026-05-12 #8)** — User clarified 2026-05-12: "practically nonexistent. that'll be a whole work chunk at the end." So this is its own future chunk, sized as a major build-out, planned for late in the roadmap (after Phase 3/4/5 design + core lifecycle/profile/cohesion work). **Does not conflict** with VISION §10 empty-by-default — welcome instructions are an additive guidance layer (copy, tooltips, first-run walkthrough) ON TOP OF the empty-by-default state, not a replacement for it. Will be promoted to a chunk in §3 when scoping begins.
- **Gallery vs table view purposes (2026-05-12 #9)** — User asked "discuss purposes of gallery vs table view." Phase 5 page-goals work. Belongs in design-phase session when we pick up Phase 5.
- **Journal growing-indicator tags (2026-05-12 #10)** — Tag a journal entry with growing milestones (flowering, first leaves, lost leaves, fruit set, etc.) so user can search/filter to see historical patterns year-over-year. Phase 3 IA work (needs tag schema design). Aligns with VISION memory plane / "what works for me" library. M-sized feature after Phase 3 lands.
- **Vault filter by flower color / perennial vs annual / fruit vs veggie (2026-05-12 #11)** — Improve vault search w/ plant-metadata filters. Phase 3 IA work (needs metadata schema on plant profiles). M-sized feature after Phase 3 lands.
- **Harvest calculator (2026-05-12 #12)** — Per-plant report: total weight harvested, first/last log dates in a growing season, derived stats. Memory plane work; aligns w/ Failure Mode #2 (profile depth) and §3.6 plant profile depth chunk. M-sized after profile work is scoped.
- **Chill hours on plant profiles (2026-05-16, sub-item of §3.6)** — User raised 2026-05-16: "i need chill hours added to our profiles for trees." Adds a variety-level field on `plant_profiles` for required chill hours (e.g. "Snow Queen nectarine = 200-300 hrs"). Used by gardener to match variety against local zone's average chill accumulation. Effort: S-M (1 schema migration + UI on Vault profile + display logic). Likely also wants integration with micro-climate triggers (Failure Mode #3) eventually for "this variety won't perform at your location" alerts — but that's a later extension. **Dependencies:** Phase 3 IA decision on schema (likely straightforward — variety-level data fits `plant_profiles`); Phase 5 page goals for where on profile UI it sits. **Cross-ref:** chunk 3.6 sub-item; sister's feedback (VISION §11 parked) may reinforce this when received; relates to Failure Mode #2 (profile shallow for growing).
- **Cross-surface "Add journal entry" icon cohesion — Vault profile tabs (parked 2026-05-16)** — Surfaced by Pass 2 iteration 1 audit during U25 fix. App-wide, the action "Add journal entry" has 6 sites across 4 files with 3 distinct visual treatments: (a) Garden tabs (ActiveGardenView + MyPlantsView after U25) use `ICON_MAP.Edit` (pen-with-motion) at `w-5 h-5`, icon-only; (b) [VaultProfileJournalTab.tsx:51](src/app/vault/[id]/VaultProfileJournalTab.tsx:51) + [:67](src/app/vault/[id]/VaultProfileJournalTab.tsx:67) use `ICON_MAP.Journal` (lined paper) at `w-4 h-4` with the text label "Add journal" — labeled CTA primitive; (c) [VaultProfilePlantingsTab.tsx:133](src/app/vault/[id]/VaultProfilePlantingsTab.tsx:133) uses `ICON_MAP.Journal` at `w-4 h-4`, icon-only. U25's user-reported bug was the cross-surface mismatch *within Garden tabs* only — that's closed. The Vault sites raise a separate aesthetic question per VISION §4 principle 2: should Vault inherit Garden's `ICON_MAP.Edit` for app-wide cohesion of the same action, OR keep `ICON_MAP.Journal` because Vault profile context lacks the `care`-entry-type-marker collision that `3de1c2c` was originally resolving? Plus a size-standardization question (`w-5 h-5` vs `w-4 h-4`). User parked 2026-05-16 via Rule A ask; recommended option taken (park rather than bundle into U25). Re-surface during the cross-surface icon-cohesion pass (likely Phase 6 design-system pass, or sooner if a new Add-journal site is added and the question becomes blocking).
- **Rootstock tracking on grow instances (2026-05-16, sub-item of §3.6)** — User raised 2026-05-16: "i want to be able to track rootstocks for trees." Adds an instance-level field on `grow_instances` for rootstock identifier (e.g. "M27 dwarf apple rootstock"). Specific to grafted fruit trees; rootstock determines size, vigor, hardiness, disease resistance independent of the scion variety. **Pre-existing capture migrated from `docs/FEEDBACK_AND_FEATURE_NOTES.md:67`** (legacy pre-VISION-v4 doc, see §3.10 D1 archive question) where the prior recommendation was: workaround via variety/name line OR growing notes for now; first-class fields later. User's 2026-05-16 ask promotes "later" to "park canonically." Effort: S (1 optional field on `grow_instances` migration + UI on Grow Instance Modal + Vault Plantings handler). **Dependencies:** Phase 3 IA decision on `grow_instances` schema extensibility — connects to beds-as-first-class architectural work since both extend the grow-instance schema. Likely bundled with that Phase 3 build. **Cross-ref:** chunk 3.6 sub-item; legacy doc at FEEDBACK_AND_FEATURE_NOTES.md:67 should be marked superseded.
- **FAB-menu add-entry friction (`ux-fab-menu-add-entry-friction`, parked 2026-05-18)** — User signal verbatim: *"i need to address friction in the fab menu on the garden vault. specifically with adding entries. well need a session later to work on them if we havent already."* Frame: friction with the add-entry flows accessed FROM the FAB menu (Add Seed / Add Plant / Add Supply / Add Task / Add Journal — exact set TBD by audit) once a FAB option is tapped. **Distinct from §3.12 theme #6** (which addresses *which* FAB options appear / disclosure-by-data on the menu shell); this is *what happens after* the user taps an option — modal flow, field count, required vs optional, plain-language labels, persona-fit for Walter / Sam. **Cross-ref:** parallel chat `admiring-carson-81435d` (FAB audit) surfaced 21 friction items F1-F21 across the FAB menu + post-tap flows + adjacent surfaces (Edit Plant Profile, Plant Again, Tasks lifecycle, Calendar UI); a consolidated 11-bug + 25-question review is still pending user's per-item decisions. When this parked item gets a chat, start by pulling `admiring-carson-81435d` audit findings as the source list rather than auditing from scratch. **Effort:** M-L (depends on which entries get touched + persona walks). **Dependencies:** likely benefits from theme #3 Phase A page-label renames landing first (so modal labels align with renamed menu entries); not blocking, but cleaner if sequenced after. Re-surface when user signals "ready for FAB-friction chat" or when theme #3 closes.
- **Raw-card empty-state primitive canonicalization (§3.12 theme #7 ADJACENT, parked 2026-05-18)** — Surfaced by §3.12 theme #5 audit Pass 3 sibling sweep (`9bad88f` ship, `trusting-proskuriakova-8e23c2` chat). 4 surfaces use raw-card shape `bg-white rounded-xl border ... p-8 text-center` for empty states instead of the canonical `EmptyStateCard` primitive ([src/components/EmptyStateCard.tsx](src/components/EmptyStateCard.tsx)) used by 6 sibling surfaces. Theme #5 ship updated COPY on these raw-card surfaces (all 4 got the new 3-part frame) without swapping primitive — primitive unification is structural cohesion work, not copy work. Belongs to §3.12 theme #7 (cross-view consistency) which is already parked + queued for design pass. Revisit during the cross-view cohesion design pass (likely Phase 6 design-system completion, or when #7 gets prioritized). **Source:** plan file `C:\Users\marsh\.claude\plans\trusting-proskuriakova-8e23c2.md` lines 122 (Pass 3 ADJACENT bullet) + 143 (Pass 4 theme-#7 lock-hygiene note). **Effort:** S-M (mechanical primitive swap + visual regression check via Preview MCP).

---

## 5. Recently shipped (last ~10)

Most recent first. For full history, use `git log`.

- **2026-06-11 AI Fill quick wins — mature dims write pipeline + Growing Notes description fallback (Dispatch-spawned)** — `f7100e7`. Two mechanical fixes from the AI Fill diagnostic (Findings #8+#9; Ship 2 — strict-search retry + when-to-plant overhaul — still queued as its own chat). **Fix 1:** `mature_height`/`mature_width` were prompt-, schema-, library-, and display-plumbed end-to-end but NO profile writer mapped them — added to fill-blanks-for-profile (profile SELECT + `setStr` block + `EnrichDataForCache` construction), shared `buildUpdatesFromCacheRow` + `writeEnrichToGlobalCache` in [fillBlanksCache.ts](../src/lib/fillBlanksCache.ts) (so the global-cache loop carries them forward — without the write side the cache-read mapping would be permanently inert), [enrichProfileFromName.ts](../src/lib/enrichProfileFromName.ts), and the settings fill-in-blanks profile SELECT (Pass 3 BLOCKING: a writer mapping a field its SELECT doesn't fetch sees every profile as blank → cache values would overwrite user data). Prompt template / Zod schema / library upsert / display all verified pre-existing, untouched. **Fix 2:** Growing Notes card ([VaultProfileAboutTab.tsx](../src/app/vault/[id]/VaultProfileAboutTab.tsx)) borrows `plant_description` + its Source line when `growing_notes` is empty instead of vanishing — NORTH_STAR "Take mental load OFF the user"; cards stay structurally distinct (merge alternative rejected in brief; revisit after fallback feel-test); fallback gated `!isLegacy` matching the Description card's own guard. **Pass 3 parks:** settings fill-in-blanks route's inline AI mapping block is a legacy narrower field set vs fill-blanks-for-profile (ADJACENT — fill-pipeline consolidation candidate); [backfillPlantDescriptionsBatch.ts](../src/lib/backfillPlantDescriptionsBatch.ts) carries a private duplicate of `buildUpdatesFromCacheRow` (CONCERN — duplicate-implementation drift). 5 files (+28/−5). Tests 623/623; build clean; e2e grep clean (no Growing Notes / mature-dim assertions). Auto-pushed (no schema/migration/RLS/auth/data-loss). **NO Preview MCP** per brief (tests+build+diff; surface AuthGuard-blocked). Plan + 4-pass audit: `.claude/plans/ai_fill_quick_wins.md` (local). **Syd dogfood path:** run AI Fill on a profile missing mature dims (canna/fern) → About tab Plant Characteristics shows Mature Height/Width; open a profile with a Description but no Growing Notes → Growing Notes card now shows the description text with its Source line.
- **2026-06-11 Form Add/Edit parity sweep — Save buttons + FormError + submit overlays converge (Dispatch-spawned)** — `650a1e6`. Systemic sweep across all Add/Edit form surfaces (13 audited; parity matrix in [docs/plans/form_parity_sweep_2026-06-11.md](plans/form_parity_sweep_2026-06-11.md)) instead of per-finding nitpicks. **Save buttons (VISION §8 emerald CTA lock):** editGrow "Edit Plant" Save + AddPlantManualModal submit flip `bg-emerald-900 hover:opacity-90` → `bg-emerald-600 hover:bg-emerald-700` (+ `rounded-lg`→`rounded-xl`) — closes the FAB-saga ADJACENT park named in `8b744cb`; "Add packet"→"Add Packet" (Title Case lock); Edit Plant Profile "Save Changes"/"Saving..." (ASCII dots) → "Save"/"Saving…" (anchor: EditPacketModal + editGrow, 2-of-3 prevailing). **Form-level errors (VISION §8 form-level error treatment lock):** 8 plain `text-red-600`/`text-citrus` `<p>`s converge on `<FormError>` — EditPacketModal, Edit Plant Profile modal, editGrow modal, AddPlantManualModal, PlantingForm, QuickLogModal, BatchAddSeed (capture + review steps; per-item `item.error` annotations + webcam/load-error field-level notes intentionally kept). **Submit loading:** `SubmitLoadingOverlay` (anchor: AddPlantModal:1066 relative-panel pattern, existing saving flags — no new state) added to EditPacketModal, Edit Plant Profile, editGrow, AddPlantManualModal; PlantingForm keeps its seedling celebration, BatchAddSeed keeps its multi-step progress semantics, QuickLogModal keeps upload-phase-only overlay (all named as intentional non-targets). **Queued findings verified ALREADY RESOLVED, no code:** "Add Plant defaults to In storage" — all 6 `grow_instances` insert sites hardcode `status:"growing"` (enum collapsed growing|archived by `67a6344`); `"vault"`/"In storage" exists only as a profile-status option + derived packet-filter bucket; per-context profile statuses (active/in_stock/out_of_stock) are semantically intentional. **Field-set parity verdict (NORTH_STAR "No duplicate paths"):** full Add↔Edit parity REJECTED — editGrow edits the instance record; profile fields / group (3 assignment doors) / status (archive-restore + End batch) / photos-notes (journal) all have canonical edit paths elsewhere; adding them would CREATE duplicate paths. **One genuine gap escalated, not shipped:** "Number of plants" collected for both types at Add (AddPlantModal:364) but editGrow-editable only when `isPermanent` — recommend un-gating (one line, vault/[id]/page.tsx:1564 gate). **ADJACENT (logged in plan doc §5):** `bg-emerald-900` on non-form CTAs (vault/[id] "Add Photo" + VaultProfilePacketsTab "Add Packet"); step-level `importError` plain ps (QuickAddSeed/QuickAddSupply); input corner-radius mixing inside the 3 touched modals; QuickAddSeed "Add to vault (with packet)" casing question. 6 code files + plan doc (+192/−22). Tests 623/623; build clean; e2e grep clean (no spec references the changed labels); layout regression tests' asserted strings untouched. Auto-pushed (no schema/migration/RLS/auth/data-loss). **NO Preview MCP** per brief (tests+build+diff; worktree `.env.local`-absent/AuthGuard-blocked). Shipped from isolated worktree off origin/main — main checkout was mid-flight on another chat's branch. **Syd dogfood path:** Library profile → Edit (pencil) → Save button reads "Save"; save shows a brief spinner overlay; profile Plants tab → pencil "Edit Plant" → Save is now the same emerald as every other form; Vault packet → Edit → save shows the overlay; force an error (e.g. airplane mode mid-save) → every form shows the same italic red warning-icon error line.
- **2026-06-11 Pre-tester hardening bundle — S1 invite-endpoint deletion + feedback device context + Garden Tracker tab title (Dispatch-spawned)** — `9c8e582`. Three small audit-driven fixes in one ship. **Item 1 (auth audit S1):** `src/app/api/invite/route.ts` accepted an unauthenticated POST from anyone and called `supabaseAdmin.auth.admin.inviteUserByEmail()` with the service-role key (spam/abuse + email-enumeration exposure); its only UI consumer [InviteMemberModal] was dead code, never imported. Route + test + modal deleted; TESTING.md bullet removed; household invite code in Family settings stays the one canonical invite path (NORTH_STAR "No duplicate paths"). **Item 2 (feedback audit rec #2):** [FeedbackModal](../src/components/FeedbackModal.tsx) submit now auto-captures `user_agent` + `viewport_w`×`viewport_h` + `app_version` into a new `user_feedback.metadata` jsonb — testers won't volunteer "Pixel 7, Chrome 126, 412×915" (NORTH_STAR "Take mental load OFF the user"; U21/U22/U23 device-bug history). NEW additive idempotent migration `20260611000000_user_feedback_metadata.sql` — file committed at ship time with `db push` held per the brief; **Syd greenlit same day → APPLIED to remote 2026-06-11** (`supabase db push` clean; `migration list --linked` shows Local+Remote; column verified via PostgREST select; end-to-end insert with metadata round-tripped as the E2E test user — one labeled "safe to ignore" test row remains in the inbox since `user_feedback` has no DELETE policy). The PGRST204/42703 fallback that retries the insert without metadata stays in the code as a permanent deploy-order guard. `admin_list_feedback()` is `RETURNS SETOF user_feedback` + `SELECT *` so the column flows through with NO RPC change; [feedback inbox](../src/app/settings/developer/feedback-inbox/page.tsx) renders a Device line (vX · WxH · UA, anchored to the existing "Page:" line register); modal disclosure line extended ("Includes your browser type, screen size, and app version…" — transparency posture preserved, T3 debug-log opt-in untouched). `APP_VERSION` centralized into [src/lib/appVersion.ts](../src/lib/appVersion.ts) (was hardcoded in the profile page; now imported by both). **Item 3 (browser audit Y1):** layout.tsx metadata title `"Seed Vault — Garden Tracker"` → `"Garden Tracker"`, description aligned to the manifest's existing copy, `appleWebApp.title` + manifest `name`/`short_name` → "Garden Tracker" (tab title on every page = most visible browser-tester branding surface; PWA install name updates on next install/update; "Seed Vault" as the packets-tab feature name intentionally untouched). 11 files (+64/−296). Tests 623/623; build clean (`/api/invite` absent from route manifest); `migration:check` clean; e2e grep clean (`toHaveTitle(/Garden|Track|Seed/i)` tolerant). Auto-pushed per brief (code tier; DB push is the gated step). **NO Preview MCP** per brief (tests + build + git diff sufficient). Plan + 4-pass audit: `.claude/plans/pretester_hardening_bundle_3items.md` (local). **Item 4 (follow-up `58ee57f`, same bundle thread):** Vercel Web Analytics — Syd enabled it on the Hobby tier (50K events/mo, 30-day history); `<Analytics />` from `@vercel/analytics@2.0.1` (`/next` App Router entry per current Vercel docs) rendered once at the bottom of the root layout `<body>`. Cookieless/anonymous (privacy posture unchanged), renders nothing visible, no-op outside Vercel. Installed with `--legacy-peer-deps`: the package's *optional* `@sveltejs/kit` peer chain demands vite@8, conflicting with the vite@5 vitest toolchain; the optional peer is unused (next@14 + react@18 satisfy the relevant peers) — future bare `npm i <new-pkg>` adds may need the same flag (no `.npmrc` added; flagged, not decided). Tests 623/623 + full build re-verified with the component mounted. **Syd dogfood path:** browser tab now reads "Garden Tracker" on every page; after the migration is greenlit + pushed, send a test feedback → wrench modal shows the new disclosure line → `/settings/developer/feedback-inbox` shows a Device line under the entry; Vercel dashboard → Analytics should start showing page views within minutes of the deploy.
- **2026-06-11 Branded 404 + error-page hardening — pre-tester polish (Dispatch-spawned)** — `1c615ab`. **Audit changed the brief's premise:** `error.tsx`/`global-error.tsx` already existed (mostly branded) — the real gaps were (a) NO `not-found.tsx` anywhere → default unbranded Next.js 404 on any bad/expired URL, (b) every boundary rendered raw `error.message` to the user, (c) [vault/error.tsx](../src/app/vault/error.tsx) rendered a full stack trace in an open-by-default "Technical details" block, (d) `global-error.tsx` used Tailwind classes that never load (it replaces the root layout → no globals.css) so it rendered unstyled. **Ships:** NEW [not-found.tsx](../src/app/not-found.tsx) — server component, Sprout icon `w-16 h-16 text-neutral-300` (anchor: ShedView.tsx empty-state illustration), h1 "Page Not Found" (Title Case per §8), "We couldn't find that page. Let's get you back to your garden.", emerald-600 **Back to Garden** CTA; [RouteErrorFallback](../src/components/RouteErrorFallback.tsx) hardened — raw `error.message` dropped for calm "We couldn't load {pageName}." / "Something went wrong on our end.", `logEvent("error_boundary","route_error",{prefix,name,message,digest})` capture (anchor: ErrorBoundary.tsx:27 — feeds the debug-log ring buffer), muted `Error reference: <digest>` line on prod server errors (support correlation without the scare), `fullScreen` prop, Back → **Back to Garden**; root `error.tsx` + the 4 legacy inline boundaries (garden/journal/vault/calendar) now delegate to the shared primitive (NORTH_STAR "No duplicate paths" — all 13 route-level error surfaces converge; vault's stack-trace block removed); `global-error.tsx` rebuilt inline-styles-only (token hexes: paper #F9FAFB, emerald-600 #059669, rounded-xl=12px) + console capture + plain `<a href="/">` Back to Garden (router may be dead at that level). **Capability-honesty call:** brief's suggested "We've been notified." copy dropped — no server-side error reporting exists. 8 files (+129/−161). Tests 629/629; build clean; e2e grep clean (no spec asserts the old strings). Auto-pushed (no schema/migration/RLS/auth/data-loss). **Preview MCP partial:** SSR fetch verified HTTP **404 status** + h1/copy/CTA/icon all present at 412×915; screenshot auth-blocked (AuthGuard's Supabase session never resolves headless) — documented skip; error boundaries verified by code + build (can't force a render error from preview). Plan + 4-pass audit: `.claude/plans/branded_error_pages.md` (local). **ADJACENT (logged):** component-level [ErrorBoundary.tsx](../src/components/ErrorBoundary.tsx) fallback keeps its own inline markup (copy already calm, no raw message) — candidate for a future cohesion pass. **Syd dogfood path:** while logged in, type any bogus URL (e.g. `/gardn` or `/vault/packets/junk-id-x`) → calm branded "Page Not Found" with a Back to Garden button instead of the black-and-white Next.js 404; if a page ever crashes, the error screen no longer shows technical text (Vault especially — the stack-trace box is gone), just "Something Went Wrong" + Try Again / Back to Garden, with a small "Error reference" code you can screenshot for support.
- **2026-06-10 Seasonal/Permanent toggle removed from ALL Add Plant creation flows — AI lifecycle owns the type (Syd dogfood 2026-06-10, Dispatch-spawned)** — `c29ed34`. Syd: *"for add plant, purchase order, the 'permanent' / 'seasonal' toggle is still visible on step 1 import review page."* Sweep found **2 live toggle surfaces** (review-import's addPlantMode Permanent/Seasonal picker — the flagged one — + AddPlantModal add-new's inline "Plant type" control, the Ship-4 replacement); both removed. All other creation flows verified toggle-free (PlantingForm derives from profile; AddVarietyModal/QuickAddSeed hardcode seed; PurchaseOrderImport input step cleaned in Ship 4 — stale comments updated). **Pass-1 headline finding:** the brief's premise ("derived back-compat handles profile_type from lifecycle") existed ONLY on the Edit-modal save (B6) — and the create-path client `enrichProfileFromName` was dropping the AI `lifecycle` on the floor entirely. The sweep makes the premise true, **opt-in only**: new `deriveProfileType` flag on `enrichProfileFromName` (now persists `lifecycle`) + `fill-blanks-for-profile` (both cache + AI branches) applies the B6 mapping (Annual→`seed`, Biennial/Perennial→`permanent`); flag is OFF for the 3 other lib callers (QuickAddSeed / AddVarietyModal / PlantingForm) + seed-import / profile-page AI fills, so existing profiles never auto-flip (un-gated derivation would break `.eq("profile_type","seed")` packet queries — parked as a product decision). **AddPlantModal add-new rewiring:** profile inserts with the seed default; enrichment now ALWAYS awaited (the permanent-background split is meaningless without a toggle) with `deriveProfileType:true`; post-enrichment the derived type corrects `is_permanent_planting` (single keyed update) or runs care templates + harvest task; AI-fail degrades to today's seasonal-default behavior. Neutral `vault_add` note "Added to Garden." (anchored to the existing-mode no-parenthetical sibling); placeholders merged from the two removed variants. **review-import:** picker gone; the existing post-save fill-blanks fire-and-forget gains `deriveProfileType:true` in addPlantMode so PO/photo plant imports self-correct `profile_type`. **Latent strict-bug fixed in the same hunk:** `enrichProfileFromName:182` assigned `sowing_depth` to `updates.sowing_method` (immediately overwritten) — `sowing_depth` was never persisted on the create path despite the column existing. Regression guards rewritten (`addPlantTypeDerivation.regression.test.ts`): toggle absent both surfaces, opt-in derivation, link-to-existing derivation unchanged, Ship-4 invariants retained. 6 files, +126/−126. Tests 629/629; build clean; e2e grep clean (zero specs reference the toggle). Auto-pushed (no schema/migration/RLS/auth/data-loss). **NO Preview MCP** per brief (tests+build+diff; AuthGuard-blocked anyway). Plan: `.claude/plans/remove_seasonal_permanent_toggle_sweep.md` (local). **Parked follow-ups:** (a) PO/photo-imported permanents — `profile_type` self-corrects post-save but the already-created grow instance keeps `is_permanent_planting:false` and gets care templates/harvest task before the AI verdict lands (fill-blanks is fire-and-forget; reconciliation = product decision); (b) universal (un-gated) lifecycle→profile_type derivation on ALL enrichment paths — flips perennial-seed profiles, needs the packet-query consumers audited first; (c) pre-existing: AddPlantModal link-to-existing copies care templates keyed on the prop-seeded `plantType` (always "seasonal" from FAB) rather than the selected profile's type — same gate Ship 4 left in place, named not fixed. **Syd dogfood path:** FAB → Add Plant → Manual Entry → no "Plant type" toggle; add a tree (e.g. "Avocado Tree") → after save the profile shows Lifecycle Perennial + lands under permanent framing without you choosing anything; FAB → Add Plant → Purchase Order → Extract → step-1 review page no longer shows the Permanent/Seasonal picker; save → imported items' profiles self-fill lifecycle (Library Edit modal still has the Lifecycle dropdown to override).
- **2026-06-10 Pre-tester onboarding copy ship — de-Sanctuary the taught path (G1/G2/G3 + sweep)** — `0b3b288`. Ships the top-3 critical fixes from the [onboarding persona audit](plans/onboarding_persona_audit_2026-06-10.md) (all Syd-locked 2026-06-10, options A) ahead of her new-account dogfood. **G1:** step-3 task pre-fill `"Observe Hillside"` → **`"Check on my plants"`** ([OnboardingContext](../src/contexts/OnboardingContext.tsx)) — the legacy Sanctuary-plan vocabulary was baked into the canonical taught flow; plain words, works for all 5 personas (NORTH_STAR "Take mental load OFF the user"). **G2:** dock step 2 `"Add your first seed packet"` → **`"Add your first plant or seeds"`**, CTA now opens the **FAB Add menu** (`openMenu()`) instead of hard-routing to QuickAddSeed — teaches the universal + entry point in one tap; zero backend change (`useOnboarding` `vaultPopulated` already accepts `seed_packets` OR `plant_profiles`, and `reportAction("seed_added")` fires from AddPlantModal/AddVarietyModal too, so any add path advances the dock in-session). The reroute rides the FAB's own open path (same `addMenuOpen` flag + mount) — menu state machine untouched. **G3:** [Help page](../src/app/help/page.tsx) refreshed to current IA (NORTH_STAR "No duplicate paths" — a stale canonical reference is a phantom second path): Library = top-level varieties encyclopedia; Vault = Packets + Shed tabs; **Add to Library** chip added; established-plant path documented (Add Plant → Manual Entry → plant type Permanent — the "Established Plant" menu button no longer exists per Finding 30, brief lagged code); "Home → Planting Schedule" → "When to Plant (Action now / Monthly view / Yearly view)"; FAB Tips list now names all 6 surfaces incl. Library; stale "+ Entry on Journal page" claim dropped (button doesn't exist). **Sanctuary sweep (grep `sanctuary|hillside|vista`, src/, case-insensitive):** NewTaskModal placeholder `"e.g. Water Hillside"` → `"e.g. Water the tomatoes"`; VaultPageContent plant-confirm label `"Notes (hillside details)"` → `"Notes"` + placeholder `"Lower terrace"` → `"Raised bed"`; zone10b notes `"Hillside airflow helps"` → `"Good airflow helps"` (Banana) + `"In Vista"` → `"In Zone 10b"` (frost-sensitive perennial note); stale comments updated (OnboardingContext / NewTaskModal / UniversalAddMenu / Home "Sanctuary Quick Start" → "Quick Start"). **Left as-is (named):** SeedVaultView `"active on hillside"` legacy status-VALUE comparisons (data matching only, never rendered — ADJACENT: future data migration if Syd wants the value retired); profile `"e.g. Vista, CA"` location example (a location field needs a real city; any is equally generic); weatherSnapshot Vista fallback coords (functional default); scrape-url zone-10b prompt text (G7 roadmapped). 8 files, copy + one handler swap. Tests 628/628; build clean; e2e grep clean. Auto-pushed (no schema/migration/RLS/auth/data-loss). **NO Preview MCP** — dock needs a fresh-account state + AuthGuard-blocked; Syd is about to dogfood exactly this flow. Plan: `.claude/plans/pretester_onboarding_copy_ship.md` (local). **Audit follow-ups NOT in this ship (still open from the audit's §4 optional fold-ins):** G4 zone-helper link at the Profile zone field, G5 dock step-1 deep-link to `/settings/profile`, G6 Quick Start re-entry row. **Syd dogfood path (new account):** Home dock step 2 reads "Add your first plant or seeds" and opens the full Add menu; after adding any plant or packet + setting zone, step 3 "Schedule your first task" pre-fills "Check on my plants"; Settings → Help (or dock "View Full Guide") → content matches the app you're holding; New Task title placeholder reads "e.g. Water the tomatoes".
- **2026-06-10 Sprint 4 Chunk B — profile display split + pill+detail + responsive Propagation + anchor nav + couldn't-find UX** — `af67c59` (rebased onto `8b744cb`/`e914a92`). **The Sprint 4 tester-ready milestone** per the plan-doc §4 (C-surgical → A → B). **B1:** About tab splits into **Plant Characteristics** (3-tag taxonomy pills, mature size, family/genus/species, pollination, toxicity, deer/rabbit, drought/salt, native origin, invasiveness, wildlife, synonyms) + **How to Grow** (sowing method, legacy + 4 per-season windows, spacing, sowing/planting depth, germination, days-to-maturity edible-gated, sun/water/soil, disease, harvest season, uses, special features); profile query widened to the Sprint 4 columns (kept ONE literal — concatenated selects degrade supabase row-type inference). **Brief-vs-schema gaps named, not faked:** bloom color/season, hardiness-zone range, expected lifespan, germination temp have NO columns (not in Chunk A's migration; migrations locked out of scope) — skipped. **B2:** one shared `PillDetailField` primitive (bold neutral pill + muted `text-neutral-600` detail beneath, both always visible per Q8; "—" per empty-cell convention) — NORTH_STAR §1 at the primitive level. **B3:** "Propagate / Save Seeds" → **"Propagation"** (predictable header); sub-sections adapt to `propagation_method` ("Propagated by Division/Cuttings/…" + "Starting from Seed & Saving Seeds"; no-methods fallback keeps the generic two blocks); **"tap ✨ to fill" default CTAs removed** → "—" (VISION §10 empty-by-default). **B4:** sticky quick-jump anchor pills (Characteristics / How to Grow / Companion Planting / Propagation) with IntersectionObserver scroll-spy + smooth scroll, anchored to the GroupTabs tab-slot register (`p-1 bg-neutral-100` + active `bg-white text-emerald-700 shadow-sm`). **Tactical deviation (NORTH_STAR §1):** brief's pill list included Plants/Packets/Journal — those are TABS one row above; duplicating them in the pill row = duplicate paths; pills cover About scroll sections only. **B5 (Syd FINAL lock):** species-level fallback REMOVED from `researchVariety` (exact-match-only prompt); returns `{found:true,data}` / `{found:false}` / `null`(AI-failure); `found:false` NEVER cached; threads researchVariety → enrich-from-name → fill-blanks → client; toast `Couldn't find data for this plant. Check the spelling of name and variety.` + inline info-note notice `We couldn't find data for '<name variety>'…` + secondary-teal **Try Again** (session-local state; auto-clears on successful fill / edit-save / dismiss). All 6 backfill/extract callers consciously adapted (not-found = skip, never species fill). **B6:** Edit modal Seasonal|Permanent toggle → **Lifecycle** gray-fill dropdown (Annual/Biennial/Perennial; `profile_type` derived per Q1 lock ONLY when lifecycle set — empty never clobbers); "Plant Type" → "Plant Name"; helper text dropped; no per-field AI badge (tactical: calmer; Description keeps its source line). New tests: researchVariety contract (7) + enrich-from-name found-flag/no-cache (3 added). Tests 628/628; build clean; e2e grep clean (no spec asserts the changed strings). Auto-pushed (no schema/migration/RLS/auth/data-loss). **NO Preview MCP** per chunk brief (tests+build+diff). **Process finding:** mid-execute, the 8b744cb session's working-tree edits interleaved with this chunk's in the SAME checkout (its in-flight hunk briefly broke the build); resolved via tree-quiet wait + hunk-level staging separation + rebase — parallel chunks should ALWAYS use isolated worktrees (8b744cb's own entry says it pushed from one; its main-tree edits still landed here). **Syd dogfood path:** open a plant profile → sticky pill row under the header; tap "Propagation" → smooth-scrolls; scroll back up → active pill follows; About shows Plant Characteristics + How to Grow cards (mostly "—" until re-enrichment populates; tap ✨ → Overwrite to fill one profile); misspell a variety (e.g. "Tomatoo Cherokee Purpl") → ✨ Fill blanks → toast "Couldn't find data for this plant…" + inline notice with Try Again; fix the spelling via Edit (note "Plant Name" label + Lifecycle dropdown, no Seasonal/Permanent toggle) → Try Again → fields fill.
- **2026-06-10 P0 modal-footer reachability bundle + Packets-tab action parity (Syd dogfood + lock 2026-06-10)** — `8b744cb`. **Root cause of the "can't save a packet edit" P0:** [EditPacketModal](../src/components/EditPacketModal.tsx) already HAD a sticky footer — but its overlay was `z-50`, tied with [BottomNav](../src/components/BottomNav.tsx) (`fixed bottom-0 z-50`, rendered after page content in AuthGuard), so the nav painted OVER the full-height modal's Save/Cancel band on mobile; the footer is outside the scroll region, so no scrolling reached it. Fix: overlay → `z-[60]` (the established form-modal tier shared by 9 sibling sheets) + safe-area footer pad. **Same class fixed in the same ship:** (a) vault/[id] editGrow "Edit Plant" modal — worse shape (Save/Cancel at the END of one big scroll region, landing under the nav) → restructured to pinned header / scrolling body / `flex-shrink-0` footer + `z-[60]` (button styles untouched — FAB-saga ADJACENT park); (b) vault/import "New Plant Detected" dialog `pb-4` → `pb-20` nav carve-out. **Packets-tab action parity (Syd lock 2026-06-10, NORTH_STAR "No duplicate paths"):** profile Packets-tab rows now carry the same Edit (pencil) + Journal icon-button primitive as the Plants-tab cards (exact classes; `canEdit`-gated; row body still links to the canonical packet detail page, chevron stays). Pencil → EditPacketModal hosted on the profile page (back-close registered); journal → QuickLogModal with new `preSelectedPacketId` prop writing `journal_entries.seed_packet_id` (surfaces in the packet's "Used in Journal"). **Form-footer audit (chunk item 3):** 9 sibling sheets verified OK (`z-[60]+pb-20`); Edit Plant Profile modal OK (guarded by vaultEditModal.layout.test); PO confirm footer gap already fixed in `47af8fa`; CONCERNs logged (centered `z-50` + `max-h-[85vh]` modals can marginally clip on short viewports → belongs to the parked VISION §11 modal-placement audit; full-screen form z-tier split 60/81/100 pre-exists). New regression lock [editModalFooter.layout.test.ts](../src/components/editModalFooter.layout.test.ts). Tests 623/623; build clean; e2e grep clean. Auto-pushed (no schema/RLS/auth/data-loss). **Shipped from an isolated worktree** — the main checkout was mid-flight with Chunk B's uncommitted work; verified against HEAD + only this bundle's changes. **NO Preview MCP** — per the chunk brief (tests+build+diff) + AuthGuard-blocked; Syd phone-verifies. **Syd dogfood path:** packet detail page → Edit pill → scroll the form → Save/Cancel pinned at the bottom, tappable above the nav; plant profile → Packets tab → rows show pencil + journal buttons; pencil opens the packet editor; journal logs an entry that shows under the packet's "Used in Journal"; profile → Plants tab → pencil ("Edit Plant") → Save/Cancel pinned + reachable.
- **2026-06-10 Sprint 4 Chunk A — AI enrichment depth + cache-bypass + honest feedback (+ A5 re-enrichment sweep + A6 source artifacts + End-batch/AGE fixes)** — `9e9956c` + `3de33bc` + `1ff86ab`. **Schema (migration `20260610120000`, applied):** +32 AI-fillable `plant_profiles` columns (3-tag lifecycle / growth_form / plant_category, characteristics, per-season windows, paired summary/detail fields, taxonomy) + `global_plant_library` widened to the full deep field set + `journal_entries.source_url` / `source_po_ref` (Q10). **Enrichment depth:** RESEARCH_PROMPT expanded for all new fields with locked enum vocabularies + 3-tag worked-example anchors. **Cache-bypass (leak audit §7/§8):** `forceRefresh` skips the `global_plant_library` branch → fresh Gemini → writes the full set back — fixes the silent no-op where AI Overwrite / Fill Blanks served thin cache without ever calling Gemini; enrich-from-name read+write carry all new fields; fill-blanks respects fill-only-blanks vs overwrite. **Honest feedback toasts (locked copy, §6 entry this date):** "Updated N fields" / "Nothing new to add" / "AI unavailable, try again later". *(Supersession note: Chunk A's species-level thin-result fallback was reversed same-day by Chunk B B5's Syd FINAL lock — exact-match-only, NO species fallback; see `af67c59` + the §6 couldn't-find entry.)* **`3de33bc` fixes:** End batch → Plant Died wrote `status='dead'`, rejected by the live 2-state check constraint (growing | archived since the 2026-05-28 Phase A collapse) — planting stuck at "Growing" (Syd dogfood: Snow Pea Beauregarde); both terminal handlers (GardenView.handleEndBatch + useVaultPlantingsHandlers.handlePlantingsEndBatch) now write `archived`, death preserved via `end_reason='plant_died'` + the death journal entry. AGE "NaN yr": `formatAge` blindly appended `T12:00:00` so full-ISO `sown_date` parsed Invalid — extracted to [formatAge.ts](../src/lib/formatAge.ts) with date normalization + NaN guard (returns "—" per the empty-cell convention; 6 new tests). **A5 re-enrichment (`1ff86ab`):** cleanup.ts shallow-cohort sweep — profiles missing ≥5 Sprint 4 fields re-enriched via fresh AI (researchVariety, fill-blanks only, idempotent across days, resumable via recomputed worklist; each Gemini call logged to `api_usage_log`). **A6 source artifacts (Q10):** the import-as-plant `vault_add` journal entry now persists acquisition source — Link import → `source_url`; PO import → `source_po_ref` (file name + price/qty line-item context); Photo already persists via `image_file_path`. Insertion point corrected from the plan's assumed AddPlantModal to review-import:865 where the Link/PO context actually lives; rendered later by Chunk D2.
- **2026-06-10 FAB tree — "Add Variety" → "Add to Library" (chip + form header)** — `dddafab`. Noun-led destination naming matching sibling chips ("Add Seed Packet", "Add to Shed", "Add Plant"); clarifies the Library as the canonical encyclopedia destination (NORTH_STAR "No duplicate paths"). Chip subcopy already read "Add to your Library" and the form submit already read "Save to Library" — chip → screen → submit now all read "Library" (cohesion-by-aggregation). Label-only; no schema/RLS/auth. Per the same-day Vault-terminology lock (§6): only this rename was warranted — "Add to Vault" CTAs preserved.
- **2026-06-10 AI pre-paid-tier hardening — kill 429/503 retry storm + cap hero attempts + durable per-user daily ceiling** — `b9746ff` (+ `506b13d` empty redeploy activating `GEMINI_DAILY_LIMIT_ALLOWLIST`). Fixes Leaks 1–3 from [the AI usage leak audit](plans/ai_usage_leak_audit_2026-06-10.md), gating the Q9 paid-tier flip (NORTH_STAR "Take mental load OFF the user" — silent retry storms burned quota for zero user value during the Jun 10–11 429 wall). **Leak 1 (retry amplifier):** 429 + 503 dropped from `fetchWithRetry` RETRYABLE_STATUSES — both are Gemini quota/load signals and every attempt is counted (free tier) or billed (paid); 408/500/502/504 + network throws (genuine transients) still retry; regression tests assert 429/503 return on the FIRST call. **Leak 2 (compounding hero attempts):** find-hero-photo's fallback Gemini attempt gated behind `allow_fallback` (default FALSE) — fan-out + background callers (import, fill-blanks, background-hero, dev batch repair) capped at ONE Gemini attempt per profile; only the explicit user "AI refresh" opts into primary+fallback; fill-blanks' hero sub-call drops its fetch-layer retry. **Leak 3 (durable ceiling):** new [aiDailyCeiling.ts](../src/lib/aiDailyCeiling.ts) — per-user-per-day cap counted against the existing `api_usage_log` (default 100/day, env `GEMINI_DAILY_LIMIT_PER_USER`; `GEMINI_DAILY_LIMIT_ALLOWLIST` bypass; fails OPEN — cost backstop, not an auth gate); wired into all 9 burst-limited Gemini routes AFTER their free cache tiers (cache hits stay free); gallery + scrape degrade gracefully instead of hard-429ing free work; blocked attempts return 429 `DAILY_AI_LIMIT` (distinct from `RATE_LIMITED` so PO import's 5s burst-retry doesn't re-try a capped user) + log a daily-cap-hit audit row. In-memory burst limiter kept as the same-instance fast brake. 600/600 tests (14 new); build clean; auto-push tier.
- **2026-06-10 Sprint 4 Chunk C-surgical — "Vault Status" → "Packet Inventory" + Plants-tab calm-down + PO footer gap + OOS border soften** — `47af8fa` + `5ade563`. Quick-wins batch (no schema/AI): **(1)** Library/Packets refine filter "Vault Status" → **"Packet Inventory"** (VaultGridRefineModal + VaultPacketWing, both surfaces for app-wide uniformity — NORTH_STAR "No duplicate paths"; Title Case per VISION §8). **(2)** Orange "No location label" warning dropped from planting cards — location is optional and optional fields don't generate warnings (VISION §10 empty-by-default); editing stays on the existing Edit pencil, no redundant affordance. **(3)** Over-explanatory Plants-tab helper paragraph trimmed — card is self-evident (VISION §10 calm aesthetic). **(4)** PO Import confirm-form sticky footer: spurious safe-area paddingBottom override removed (double-counted the inset; anchored to sibling PlantingForm.tsx:545 same `bottom-20` no-override pattern). **(5, `5ade563` — escalated as an aesthetic ask, Syd pick: SOFTEN):** out-of-stock Library card border dashed neutral-300 → solid neutral-200, one quiet shade below the in-stock slate-200/80 so the OOS signal survives without shouting (NORTH_STAR "Take mental load OFF the user"); sibling sweep confirmed the dashed OOS treatment existed ONLY in SeedVaultView's getCardBorderClass — Packets/Shed have no status-tied card border. 586/586 tests; build clean; auto-push tier.
- **2026-06-10 Acquisition entries now show on the instance Journal tab (reverses 2026-05-28 lock)** — `30324be`. Removed the `e.entry_type !== "vault_add"` filter at [GrowInstanceModal.tsx](../src/components/GrowInstanceModal.tsx) so `vault_add` acquisition entries surface on BOTH the profile and instance Journal tabs — superseding the 2026-05-28 "Journal = user-logs-only" lock, which predated the instance page existing as a real route (Sprint 3 `02ff1d8`). The `grow_instance_id` linkage was already correct on every instance-creating add path (PO import, AddPlantModal, completeSowTask, PlantingForm) — the entry was only filtered from display. `[...journalEntries]` spread added before sort to avoid mutating state. New guard test [acquisitionJournalLinkage.regression.test.ts](../src/components/acquisitionJournalLinkage.regression.test.ts) (8 source-level invariants) locks the per-flow linkage + the reversal so a future chat can't silently re-add the filter. NORTH_STAR "Information-hub framing" — acquisition is load-bearing on the instance hub; also the **Sprint 4 Sources-section foundation** (Sources finds the artifact via `grow_instance_id`). Tests 586/586; build clean; e2e grep clean. Auto-pushed (no schema/migration/RLS/auth/data-loss). **NO Preview MCP** — AuthGuard-blocked + needs a real instance with an acquisition entry; Syd phone-verifies. See §6 2026-06-10 decision entry. **Syd dogfood path:** FAB → Add Plant → Purchase Order → Save → open that instance (`/garden/grow/<id>`) → Journal tab → the "Added … to vault" entry now appears there (and still on the profile Journal tab).
- **2026-06-10 Sprint 3 — growing instance → standalone `/garden/grow/[id]` page (+ NORTH_STAR.md)** — `236dc1e` (doc) + `02ff1d8` (conversion). Closes the last detail surface still rendered as a modal overlay; brings it to real-page parity with Library / Packet / Shed (NORTH_STAR "No duplicate paths" + "Take mental load OFF the user" — working browser/hardware back, deep-link/shareable URLs, filter-aware swipe). **Doc hygiene first:** new [`docs/NORTH_STAR.md`](NORTH_STAR.md) consolidates the cross-cutting principle-set previously scattered across §6 decision-log cites + FEEDBACK_AND_FEATURE_NOTES "North star"; added to CLAUDE.md required-reading. **Architecture (Q1 lock):** reuse [`GrowInstanceModal`](../src/components/GrowInstanceModal.tsx)'s 1180-line render tree via a new `variant: "modal" | "page"` prop (default `"page"`; single source of truth, no display drift). The fixed-overlay shell + scrim + focus-trap + Escape + hero back-arrow gate behind `variant === "modal"` — now **dormant** (zero callers; kept-not-deleted, full prune deferred to a Sprint 4 tidy). The new [`/garden/grow/[id]/page.tsx`](../src/app/garden/grow/[id]/page.tsx) wrapper owns the unframed Back chip (§8), desktop prev/next arrows + touch swipe (50px threshold reused), and the **HarvestModal host** (the modal previously delegated harvest to the Garden parent; the page bumps a `reloadKey` to refetch in place after a harvest). + `loading.tsx`/`error.tsx` sibling parity. **Filter-aware swipe:** `SwipeScope += "instances"`; `GardenView` snapshots its filtered+sorted order (`useSwipeOrderSnapshot("instances", sortedBatches…)`); the page reads the snapshot, else Q4 fallback `status='growing'` / `sown_date` desc (archived excluded). **Callsite migration — one path (Q2 "no duplicate paths"):** Garden grid+list card-taps → `/garden/grow/<id>`; Calendar care deep-link → `/garden/grow/<id>?instanceTab=care`; Packet "Used in Plantings" → adds `?from=packet&packetId` (Q3 context Back); **Library profile peek → navigates** with `?from=library&profileId` (was a read-only modal peek — now the fully-editable page, edit-ability by ownership; modal render + `growViewId` state removed); Garden page drops the modal render, legacy `?grow=` deep-links **redirect** to the page (back-compat for shared/bookmarked URLs). **Pass 1 bidirectional state-transition matrix** traced (open/close/forward-swipe/back-swipe/archive-redirect/harvest × state-reconciliation / refetch-on-id / sub-sheet-lazy-mount / unmount) — clean; sub-sheet swipe guard via `onSubSheetOpenChange`. Pass 3 sibling-aligned to packet/shed (threshold, arrow chrome, Back chip, `getSwipeOrder` fallback shape, framed action pills). 14 files (+ new `instancePage.regression.test.ts`, `"instances"` swipeOrder case; updated gardenView + InstanceCareTab route assertions). Tests **578/578**; build clean; e2e grep clean (no spec references the route). Auto-pushed (no schema/migration/RLS/auth/data-loss; Phase 4 fully clean). **NO Preview MCP** — AuthGuard-blocked + needs a real instance id; Syd phone-verifies. **Resolves the swipe-on-modal decision surfaced in the `d274b3d` entry below** (the instance is a real page now, so swipe is a clean drop-in, not swipe-on-a-modal). **Syd dogfood path:** Garden → tap a planting card → lands on a real `/garden/grow/...` page (browser/hardware Back works); swipe left/right cycles the plants in your current Garden filter/sort; ← Back returns to Garden; tap a planting from a plant profile's Plants tab → lands on the editable page, Back returns to the profile; tap a "Used in Plantings" row on a packet → Back returns to the packet; a shared old `/garden?grow=…` link still opens the plant (redirects).
- **2026-06-10 Filter-aware Shed swipe (shared snapshot helper, Syd scope expansion)** — `d274b3d`. Extends the [`lib/swipeOrder`](../src/lib/swipeOrder.ts) snapshot pattern to Shed (scope `"supplies"`): `ShedView` writes its displayed filtered+sorted supply order; `vault/shed/[id]` reads it for swipe (fallback updated_at desc for deep-links). Drops the global updated_at-desc-always behavior — Shed swipe now follows the user's filter/sort, consistent with Library + packet. Clean drop-in (Shed already had full-page swipe + chevrons; only the order source changed). 3 files. 560/560 tests; build clean. Auto-pushed. **Growing-instance swipe SURFACED for decision (not built):** `garden/grow/[id]` is a redirect → `GrowInstanceModal` popup over the Garden page, not a real detail page — adding swipe = swipe-nav on a modal (prev/next via the `?grow=` param, no hero for arrows, gesture-vs-modal-tabs conflict). Per Syd's "surface if more than a drop-in" guard, awaiting her ship-now-vs-queue call.
- **2026-06-10 Filter-aware swipe via shared sessionStorage snapshot (Library + packet vault-context, Syd dogfood)** — `6c756b6`. **Bug:** with a filter on the Library list, opening a profile then swiping traversed ALL profiles A–Z, ignoring the filter (the detail page fetched its own all-profiles order, blind to the list's client-side filter/sort). Same class on the packet vault-context swipe (newest-first, ignored the vault filter). **Fix — new shared snapshot pattern, [`lib/swipeOrder.ts`](../src/lib/swipeOrder.ts) (documented in-file for reuse):** the list writes its displayed (filtered+sorted) ordered ids to sessionStorage (`useSwipeOrderSnapshot`); the detail page reads them for swipe (`getSwipeOrder`), falling back to its own default order (A–Z profiles / newest-first packets) when absent. sessionStorage is the only mechanism that survives the list→detail unmount (filter state too complex for the URL; React context dies on unmount). `includes(currentId)` guard prevents stale/cross-scope contamination; SSR-guarded + try/catch for private-mode. **Trade-off (accepted, Syd):** snapshot captured at entry time — if the list changes afterward, swipe uses the entry snapshot. **Scope:** Library (`SeedVaultView` writes `profiles`; `vault/[id]` reads) + packet vault-context (`PacketVaultView` writes `packets`; `vault/packets/[id]` reads); packet profile-context unchanged (already variety-filtered). **Out of scope (Syd, queued in BACKLOG):** Shed (milder, didn't flag) + growing-instance page (no swipe at all = add-swipe, different scope). 6 files (+ `swipeOrder.test.ts`). Pass 1 bidirectional trace (filter→snapshot→detail-reads→swipe-within-set→re-read on id change→fallback on deep-link) + Pass 3 sibling (one shared helper) clean. Tests 560/560 (6 new helper tests); build clean. Auto-pushed (gesture + storage helper, no schema). **NO Preview MCP** — AuthGuard-blocked; Syd phone-verifies. **Syd dogfood path:** Library → filter (e.g. yellow flowers) → open one → swipe left/right now cycles only the filtered set in the displayed order (not all profiles A–Z); same for Vault → Seed Packets with a filter applied.
- **2026-06-09 Context-aware swipe-between-packets (Option A — packet detail page chapter FULLY CLOSED)** — `60c5e31`. Reuses the [shed/[id]](../src/app/vault/shed/[id]/page.tsx) swipe pattern (`handleSwipeStart/End`, 50px threshold, horizontal>vertical guard, `touch-pan-y`, framed `md:` arrow buttons over the hero per VISION §8 photo-overlay-nav framed-chevron exception). Swipe left = next, right = previous. **Context-aware set (Option A), keyed off the entry URL param:** `from=profile&profileId=X` → traverses ONLY that variety's packets (matching the profile Packets-tab order: in-stock first, then newest); otherwise (Vault list entry) → ALL the user's packets, newest-first (the list's user-chosen sort is component state, not accessible from the URL → the locked newest-first fallback). `contextQuery` preserves `from`/`profileId` across swipes so the traversal set stays stable. First/last → arrow hidden + swipe no-ops; swipe disabled while any modal/sheet is open; household-foreign packet not in the own-set → no prev/next (graceful). 1 file. Pass 1 bidirectional nav trace (left/right × first/last/mid × modal-open guard) + Pass 3 sibling (shed/[id] BLOCKING-align) clean; e2e N/A (no spec asserts swipe). Tests 554/554; build clean. Auto-pushed (gesture + URL routing, no schema). **NO Preview MCP** — AuthGuard-blocked; Syd phone-verifies the swipe. **Packet detail page chapter is now FULLY CLOSED** (all dogfood items + both follow-ups shipped). **Syd dogfood path:** open a packet from a profile's Packets tab → swipe left/right cycles only that variety's packets; open one from Vault → Seed Packets → swipe cycles all packets; at the first/last there's no further swipe.
- **2026-06-09 Packet description fallback attribution (Option C — CLOSES the packet detail page chapter)** — `57a139f`. Final description-handling follow-up. Precedence: a packet's own captured `vendor_specs.plant_description` wins; otherwise fall back to the profile's canonical `plant_description` **with a muted "From plant profile" label** (`text-xs text-neutral-500 italic`, matching the About-tab source-label register) so the user never sees empty and always knows the source. Data source unchanged (still reads profile) — UI label only. 1 file. Tests 554/554; build clean. Auto-pushed (UI-only). **Packet detail page chapter now CLOSED** (only swipe-between-packets remains parked in BACKLOG pending Syd's A/B/C call). **Syd dogfood path:** packet with no packet-specific description → "How to Grow" shows the variety description with a muted "From plant profile" line beneath it.
- **2026-06-09 Packet page — profile-sourced growing recs + read-only + pill row (Syd 2026-06-09 items 6/7/8/9)** — `f352265`. **Item 6 (the "empty vendor recs" root cause):** the page read the rarely-populated `seed_packets.vendor_specs` copy; the real growing data is **profile-canonical** (`plant_profiles.sun` / `plant_spacing` / `days_to_germination` / `harvest_days` + `botanical_care_notes.sowing_depth` + `plant_description`), shown in the profile's How-to-Grow and edited via the Edit Plant Profile modal. Fix = **read from the profile** (no migration — data already there; rejected migrate/backfill = duplication + drift). Syd's "vendor rec from profile" instinct was right. **Item 7:** the editable "Vendor Recommendations" form → **"How to Grow" READ-ONLY** section ("—" for empty per §8), sourced from the profile, edited on the profile; dropped the always-editable `vendor_specs` inputs + the now-dead `updatePacketVendorSpec` handler. **Notes/storage stay inline-editable** (open-edit per Syd; also keeps the e2e valid). **Item 9 (nav cohesion):** top-right **framed pill row** matching [GrowInstanceModal:694-744](../src/components/GrowInstanceModal.tsx) (VISION §8 chrome-control-framing): **Profile** (→ plant profile) + **Edit** (→ `EditPacketModal` — full packet editor: vendor name / price / qty / rating / date / notes / storage / tags). **Remove stays buried** at bottom (destructive safety). Replaces the header "View Plant Profile" text link. **Item 8:** cleanup folded via the read-only + pill conversion (the page is now a display surface). **Supersedes Q3** (inline packet vendor_specs edit — edited the wrong/empty copy) **and Q2** (vendor description now from the profile's canonical `plant_description`); `vendor_specs` column preserved in DB (reversible). 2 files (`vault/packets/[id]/page.tsx`, `useVaultPacketHandlers.ts`). Pass 1 factual (profile fields verified) + modal-lifecycle trace (Edit pill open/close/saved/reopen) + Pass 2 (read-only populated from profile; harvest_days→"N days"; sowing_depth jsonb guard) + Pass 3 sibling (pill↔GrowInstanceModal BLOCKING-align, "How to Grow"↔profile section name, EditPacketModal↔canonical editor, "—"↔§8; e2e preserved) + Pass 4 — clean. Plan §12 (Ship 4 revision) in [`.claude/plans/sprint2_packet_detail_pages.md`](../.claude/plans/sprint2_packet_detail_pages.md). Tests 554/554; build clean. Auto-pushed (UI rework, no schema/RLS/auth/data-loss). **NO Preview MCP** — AuthGuard-blocked; Syd phone-verifies. **Syd dogfood path:** Vault → Seed Packets → tap a packet → (a) "How to Grow" now shows the variety's growing recs (from the profile — no longer all empty), read-only; (b) top-right pills "Profile" + "Edit" — Profile → plant profile, Edit → packet editor modal; (c) growing recs are edited on the plant profile (via Profile pill), not inline; (d) notes/storage still quick-edit inline; (e) Remove still at the bottom.
- **2026-06-09 Packet detail page — dogfood bundle (6 items, Syd 2026-06-09)** — `963cdd7`. Visual/affordance rework of `/vault/packets/[id]` from Syd's phone dogfood. **(1) Hierarchy flip:** plant name+variety is now the primary `<h1 text-2xl font-bold>` (matches profile header `displayName`); vendor+year demoted to `text-sm text-neutral-500`. **(2) Hero image:** top-of-page hero — packet photo → plant profile's STORED photo (`hero_image_path` journal-photos → `primary_image_path` seed-packets; external `hero_image_url` skipped to avoid the 2026-06-08 broken-image class) → `PlantPlaceholderIcon`, with `<img onError>` fallback so the page is never sparse. **(3) Nav title:** `AuthGuard` maps `/vault/packets/*` → "Packet" (singular), matching the detail-page convention (shed/[id]="Shed"); disambiguates from the "Packets" tab/list (AuthGuard is pathname-only, can't show the variety). **(4) "Plant from This Packet" CTA** (`bg-emerald-600`, when `canEdit && !isArchived`): opens `AddPlantModal` with profile + this packet pre-selected via a new additive `initialPacketId` prop; planting consumes the packet (existing behavior) → page reloads + CTA hides. **(5) Demoted "View Plant Profile"** to a header text link (navigation, not a CTA). **(6) Vendor specs empty = correct empty state** (read path verified) + info-note "No vendor info captured yet — add it below." in edit mode. 3 files (`vault/packets/[id]/page.tsx`, `AddPlantModal.tsx` additive prop, `AuthGuard.tsx`). Pass 1 factual + modal-lifecycle bidirectional trace + Pass 2 persona + Pass 3 sibling (header↔profile, hero↔shed/profile, modal-mount↔plantAgain, CTA↔§8 emerald, title↔shed/[id]) + Pass 4 — clean. Plan §12 in [`.claude/plans/sprint2_packet_detail_pages.md`](../.claude/plans/sprint2_packet_detail_pages.md). Tests 554/554; build clean; no e2e asserts the changed strings. Auto-pushed (UI + additive prop). **Concurrency:** clean fast-forward on top of the Groups chat's `f0c881d`–`0a53efb` (which added a Group field to AddPlantModal planting flows) — verified linear, nothing lost; complementary (the new CTA inherits the Group field). **NO Preview MCP** — AuthGuard-blocked; Syd phone-verifies. **Syd dogfood path:** Vault → Seed Packets → tap a packet → plant name is the big title, vendor+year smaller; a hero photo sits on top (never blank); nav header reads "Packet"; "Plant from This Packet" opens the planting flow with this packet pre-selected; "View Plant Profile" is a small link; empty vendor recs show an italic add-invite.
- **2026-06-09 Packet detail page — confirm-on-delete fold-in (Syd amend)** — `6f3fad4`. Folded the easy follow-up into the packet page: "Remove Packet" now opens a confirmation dialog (mirrors the Delete Plant Profile alertdialog in `vault/[id]` — `z-[60]` overlay, `max-w-sm` card, Cancel + red Remove, `deleting` guard) instead of deleting immediately — higher-stakes on a dedicated page than the old inline Packets-tab remove. The **second follow-up (swipe-between-packets) stays queued** (BACKLOG): it's not a quick gesture reuse — the packet page's two entry contexts (Vault list = all packets vs. profile Packets tab = one variety) make "what does swipe traverse?" a real UX decision needing Syd input, ~60–90 min. Tests 554/554; build clean. Auto-pushed (pure UI confirm modal). Syd phone-verifies. **Syd dogfood path:** packet page → Remove Packet → confirm a "Remove Packet?" dialog appears → Cancel keeps it / Remove deletes + returns to the list.
- **2026-06-09 Dedicated packet detail pages + vendor info split (Sprint 2 parallel chapter — sister-dogfood vendor-info bounce)** — two commits: **`03eee40`** (feature) + **`72cc01f`** (Q4 correction). New route **`/vault/packets/[id]`** ([page.tsx](../src/app/vault/packets/[id]/page.tsx)) is the canonical per-packet surface (VISION §1 four roles: packet = inventory + vendor-specific facts; closes VISION §6 Theme 4 "lost" / mental-model-mismatch for packet data). Pattern-matched to `vault/shed/[id]`. **Renders:** inventory (rating / qty pills / purchase date / storage / notes), **editable vendor specs** (the 5 `vendor_specs` fields + vendor description, tap-to-edit, merged into the jsonb via new `useVaultPacketHandlers.updatePacketVendorSpec` — mirrors the notes/storage inline-edit sibling; fixes the "photo extraction got germination/spacing wrong, no way to correct" friction), germination + journal linkage, and **first-class cross-layer nav** — prominent "View Plant Profile" CTA + "Used in Plantings" rows. **Q1:** Library cards never rendered vendor name (only a filter chip) — stale premise, dropped. **Q2:** `vendor_specs.plant_description` moved to the packet page (per-packet vendor prose; avoids duplicating the canonical profile description). **Q4 (corrected):** "Used in Plantings" rows link directly to the standalone instance route **`/garden/grow/{instanceId}`** (it exists — earlier audit wrongly said it didn't), not the profile Plantings tab. **Vendor split:** removed "Vendor Recommendations" from profile About tab + dead "Original Details" (`scraped_details`) from Packets tab; **Packets tab → tap-through summary list** (inline-edit removed; editing now lives on the packet page). **Routing fix:** Vault → Seed Packets tap now lands on the packet page (`PacketVaultView` handleClick/goToPacket/handleRowClick) instead of the plant profile. **No schema/migration/RLS/auth changes** (display relocation + new read route + `vendor_specs` jsonb edit). Pass 1 bidirectional nav trace (6 paths × forward/reverse) + Pass 2 concerns/persona + Pass 3 sibling sweep (reused `useVaultPacketHandlers` + qty/star/image helpers; **e2e/vault-profile-edit updated** to test notes/storage on the moved-to packet page) + Pass 4 lock hygiene — all clean. 7 files + 2 e2e tests. Tests **554/554**; build clean (2 non-blocking `<img>` warnings matching sibling pattern). **Auto-push qualified** (no schema/RLS/auth/data-loss) — Syd expanded push authority + explicit "push" greenlight. **NO Preview MCP** — surfaces are AuthGuard-blocked + need a real packet id; Syd phone-verifies. Plan: [`.claude/plans/sprint2_packet_detail_pages.md`](../.claude/plans/sprint2_packet_detail_pages.md). **Parked follow-ups (BACKLOG, not greenlit):** confirm-on-delete + swipe-between-packets on the packet page. **Syd dogfood path:** Vault → Seed Packets → tap a packet → lands on the new packet page (not the profile); edit a vendor spec (e.g. days to germination) → it saves; tap "View Plant Profile"; on a profile → Packets tab → tap a row → packet page → ← Back returns to the Packets tab; confirm "Vendor Recommendations" + "Original Details" are gone from the profile; on a packet with plantings → tap a "Used in Plantings" row → lands on that instance's `/garden/grow/...` page.
- **2026-06-08 AI-hero state cleanup — broken-image fallback + stuck "Researching…" pulse + fill-blanks retry (Syd dogfood 2026-06-08, Honeysuckle VN S + Canna Lily SS LMN)** — two ships: **`a4bef47`** (2A+3A strict bugs) + **`55e7509`** (B enrichment hardening). Investigation answered the spawn question first: **PO Import → new-profile creation IS wired to enrichment** (review-import fires `fill-blanks-for-profile` + `background-hero-for-profile` per new profile; the route calls the same `/api/seed/enrich-from-name` AI endpoint Add Variety uses) — so the blank "Canna Lily SS LMN" was **not** a missing wire but **silent transient failure** (possibility #2): the route's two internal AI fetches used bare `fetch` with swallowed catches, no retry, no trace. **2A (`GardenView.tsx`):** Garden card showed the browser broken-image icon when `hero_image_url` is an external AI-found URL that 404s/hotlink-blocks — the two `<img>` had no `onError` (unlike the profile page). Added a `failedThumbUrls` set + `onError` → clean `PlantPlaceholderIcon` fallback (mirrors profile-page gallery onError `:846`). **3A (`vault/[id]/page.tsx`):** profile page pulsed "Researching…" forever when the DB `hero_image_pending` flag gets stuck `true` (background hero job killed by serverless timeout before its `finally` resets it); the page read the flag raw with no client cap, unlike `SeedVaultView`. Ported the 30s `HERO_PENDING_TIMEOUT_MS` cap, gating only the DB-flag-driven pending (client-driven `findingStockPhoto` search left ungated). **B (`fill-blanks-for-profile/route.ts`, fast-follow to `20d4819`):** that commit extended the shared `fetchWithRetry` util to the client import callsites but **not** this server route — the shared chokepoint for PO-scan AND link-import enrichment. Wrapped both internal AI calls (`enrich-from-name` + `find-hero-photo`) in `fetchWithRetry`, **bounded to a single retry (`[1500]`)** because the route is `maxDuration=60` and enrich is a slow Gemini call (tight bound avoids the timeout-kill that strands `hero_image_pending` — the held 3B mode); replaced the two silent catches with `console.warn` server-log traces. Pass 1/2/3/4 clean; tests 548/548; build clean; e2e grep N/A (no strings). No schema/RLS/auth → both auto-pushed per the 2026-06-07 push-tier relaxation. **NO Preview MCP** — bugs need prod auth + the specific broken URL/stuck flag to reproduce (documented per auth-blocked skip); Syd phone-verifies. **HELD per Syd (deferred follow-ups):** **2B** (stop writing unvalidated external hero URLs — needs a load-validation step that adds latency; surface before shipping) and **3B** (timeout-resilient flag / serverless hard-kill recovery — architectural, needs Syd input; a `finally` can't survive a function kill, so the real fix is client-timeout-as-guard (3A, shipped) + a stale-pending sweep). **ADJACENT (parked):** `SeedVaultView` grid `<img>` could also 404 with no onError fallback — same class as 2A, not reported, future cohesion pass. **Syd dogfood path:** Vault → Honeysuckle VN S → Garden card now shows the clean white sprout placeholder (not the broken-image icon); the profile icon stops pulsing within 30s instead of forever. Future PO/link imports retry transient AI hiccups instead of silently landing blank.
- **2026-06-08 `fetchWithRetry` fast-follow — ADJACENT URL-import + standalone find-hero callsites (hardening, Syd-greenlit)** — `fix(import): extend fetchWithRetry to URL-import + standalone find-hero callsites`. Mechanical extension of the prior ship (`a727d5e`) across the rest of the transient-failure class. Swapped `fetch` → `fetchWithRetry` on **15 callsites in 9 files**: URL-extract — `extract-single-url` (QuickAddSeed), `extract-from-url` (QuickAddSupply + shed/[id]), `extract-metadata` + `extract-rescue` (vault/import link flow), `re-extract-url` (developer); standalone `find-hero-photo` — useVaultHeroHandlers (×2), review-import, photos/hero, vault/import (×3), manual (fire-and-forget), developer. **Import-only / call-swap — zero copy touched** (deliberate: per the 2026-06-08 D1 evolution the standalone "Find Hero Photos" step is being killed entirely / folded into the silent parallel extract in the unified Import rewrite, so any "Find Hero Photos" user-visible copy is not worth polishing now — "don't polish what you'll rewrite"). Same retry contract inherited (3 attempts, 1s/2s backoff, network-throw + {408,429,5xx}, never 4xx). Bad-URL 4xx correctly does NOT retry; transient 5xx does. No new tests (helper already unit-tested; this is callsite wiring). Tests 548/548; build clean; e2e grep N/A (no strings changed). Auto-pushed per the 2026-06-07 push-tier relaxation. **NO Preview MCP** — Syd phone-verifies. **Syd dogfood path:** Link-import a seed/supply URL, or trigger a hero-photo search, on a transient AI hiccup → it now silently retries instead of erroring on the first failure.
- **2026-06-08 AI-extraction auto-retry + honest error copy (Syd dogfood 2026-06-08 — PO Extract intermittent failure)** — `fix(import): auto-retry transient AI-extraction failures + stop blaming the user's image`. Syd hit the PO Extract API failing **twice in a row** then succeeding on the third try with **no change to the image** (recurred the prior day too) — a transient AI/API hiccup mis-surfaced as user error. **Root cause:** the Gemini call in `extract-order/route.ts:71` throws on rate-limit/503/timeout → route returns **500** → client (`PurchaseOrderImport.tsx`) showed the error on the *first* failure with **no retry anywhere** in the codebase, forcing 2-3 manual re-clicks. **Two fixes:** (1) **New shared client helper [`fetchWithRetry`](src/lib/fetchWithRetry.ts)** — 3 attempts, exponential backoff [1000ms, 2000ms] (try→1s→retry→2s→retry→then error), retries on a thrown fetch (network drop) OR retryable status {408,429,500,502,503,504}, **never** on 4xx (permanent). Returns the final Response so callers keep existing body-handling; delays/fetchImpl/sleep injectable for tests. Most transient failures are now hidden from the user. (2) **Honest error copy** — new `EXTRACTION_RETRY_FAILED = "Couldn't read the image right now — try again in a moment."` (sentence case per §8) replaces the image-blaming copy on the transient/API-failure branches; the misleading unconditional subtext `"Try a clearer screenshot or replace the image."` (`PurchaseOrderImport.tsx:349`) is now **conditional** (`showImageHint` state) — shown only for the genuine "No items found in this image" case (AI succeeded, found nothing = legitimately image-related), hidden for transient failures. **Pass 3 sibling sweep — helper applied uniformly to all AI image-extraction client callsites:** PurchaseOrderImport (seed + supply `extract-order`), BatchAddSeed (`extract` + `extract-order`), BatchAddSupply (`extract-from-photo`), Photos Import (`extract` + `find-hero-photo`). **ADJACENT (deferred, logged):** URL-import callsites (`extract-single-url`, `extract-from-url` in QuickAddSeed/QuickAddSupply/shed) + standalone find-hero callsites share the same intermittent class but their copy isn't image-blaming — helper applies cleanly in a fast-follow if desired. **CONCERN (logged, not blocking):** `extract-order` returns **200 + `error:"Could not parse AI response"`** on AI parse-garbage — status-based retry won't catch it (it's 200); rarer than the reported 500; future server-side single-retry on parse-fail. **Pass 1 bidirectional trace clean** (extract flow: forward success→nav, fail-after-retry→`EXTRACTION_RETRY_FAILED`+no-blame-subtext, no-items→clearer-shot+subtext, back→re-extract resets all state, close/reopen→open-effect resets; no Suspense/lazy axis). 7 files (`fetchWithRetry.ts` new, `fetchWithRetry.test.ts` new, `addFlowError.ts`, `PurchaseOrderImport.tsx`, `BatchAddSeed.tsx`, `BatchAddSupply.tsx`, `vault/import/photos/page.tsx`). Tests **548/548** (535 + 8 new retry tests, +5 baseline); build clean; e2e grep clean (error strings are runtime-only, not asserted). Auto-pushed per the 2026-06-07 push-tier relaxation (UI + retry logic, no schema/RLS/auth/data-loss). **NO Preview MCP** — error state is auth + live-API-failure gated; Syd phone-verifies. **Syd dogfood path:** Add Plant → Scan Purchase Order → Extract a cart screenshot → on a transient AI hiccup the extract now **silently retries** (1s, then 2s) and usually succeeds with no error shown; if it genuinely fails after all 3 tries, the message reads "Couldn't read the image right now — try again in a moment." (no "try a clearer screenshot" blame); only a truly unreadable image with zero items shows the clearer-screenshot hint.
- **2026-06-08 FAB-handler audit + "From Vault" → "From Library" rename (tester readiness)** — `21c7769` `fix(fab): rename Add Plant 'From Vault' → 'From Library' + Library icon`. Full read-only audit of every FAB entry point (label↔handler↔destination) against Syd's queued 7-item FAB-rot cluster (`project_gt_queued_chapters` context). **Audit verdict: 5 of 7 already shipped** in Sprint 5 / FAB-tree bundle — #1 Shed→Photo Import (all 5 pages + `VaultShedWing` wire `onSupplyOpenBatchPhotoImport → closeAll(); setBatchAddSupplyOpen(true)`, `BatchAddSupply` mounted; traces clean), #2 From Vault is already inline `<PlantingForm mode="modal">` (no route nav), #3 Seed Link is already inline `step="link"` (create-side correctly distinct from Shed: seed_packet vs supply), #4 profile contextual add already correct (VISION §8 lock 2026-05-30 — Plants tab `onPlantAgain→AddPlantModal{profileId}` prefilled/skips picker; Packets tab `Add a Packet→AddPlantManualModal{profileId}` inserts seed_packet), #7 BatchAddSeed "Save to Vault" shipped `e684331`. **#5 (the one open strict bug, shipped here):** the Add Plant submenu's variety picker pulls from `plant_profiles` = the **Library** (`/plants`), not the seed-packet **Vault** (`/vault`) — but read "From Vault" / "Plant from your existing vault" with `ICON_MAP.Plant`. Renamed to **"From Library"** + subtitle "Plant from your Library" + icon `ICON_MAP.Plant → ICON_MAP.Sprout` (canonical Library icon, anchored to `navItems.tsx:125` `/plants` nav + `styleDictionary.tsx:642`); `help/page.tsx:19` "From Vault" → "From Library". Internal screen key `"add-plant-from-vault"` left as-is (not user-facing). **Scoped sweep — critical distinction:** Library (`/plants`) and Vault (`/vault`) are **distinct tabs** (the 2026-05-30 Ship 2b nav split), so a blanket Vault→Library sweep would be WRONG — the `Add Seed Packet` "Seeds for your vault" subtitle + seed-form "Add to Vault" buttons are CORRECT (seed packets genuinely live in the Vault) and were left intact. Only the plant-variety picker mislabeled. **#6 (Shed Link multi-link "+") HOLDS for greenlight** — seed import HAS multi-link backend (`/api/seed/batch-import` accepts `urls[]`; `/vault/import` multi-URL textarea) but Shed has only single `/api/supply/extract-from-url`; no supply batch endpoint exists → real backend+UI add, out of scope for a handler audit. 2 files, copy+icon only (no state-machine change → Pass-1 bidirectional matrix N/A per the styling/copy skip clause; Pass-3 Vault-terminology sweep done). Tests 540/540; build clean; e2e grep clean (no spec asserts "From Vault"). Auto-pushed per the 2026-06-07 push-tier relaxation (no schema/RLS/auth/data-loss). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** FAB "+" → Add Plant → confirm the second method now reads **"From Library"** with the sprout/Library icon (not "From Vault" with the plant icon) → tapping it still opens the inline Planting picker; Help page "Add a plant" line now says "Manual Entry or From Library". The Seed Packet flow still correctly says "Seeds for your vault" (unchanged — seeds live in Vault).
- **2026-06-07 PO/Import 4 critical dogfood bugs — F19 data loss + F13 hero-lied + F15 normalization + #9 loading (Syd dogfood 2026-06-01)** — `bc0bef3` `fix(import): 4 critical PO/Import bugs from Syd 2026-06-01 dogfood`. Closes the 4 critical (non-cosmetic) findings from the PO/Import dogfood chapter (`project-gt-po-import-polish-chapter`); the ~20 cosmetic findings stay deferred to the unified Import architecture rewrite (D2+D3). **F19 (CRITICAL DATA LOSS):** `/api/seed/extract/route.ts:585` parsed the model's JSON array of items then **deliberately `arr[0]`**, silently dropping items 2+ — a cart screenshot / multi-packet photo with 3 items created 1, no notification. Root cause was the route, exposed via BOTH single-image consumers (`vault/import/photos/page.tsx:90`, `BatchAddSeed.tsx:269` — the "Confirm or edit Plant Type and Variety" = Syd's "Confirm & Save" surface). **Fix = Option B (scoped, non-silent):** route still returns the first item (no regression) PLUS `additional_items_detected` + `additional_item_labels`; both consumers render an amber warning naming the dropped items and routing the user to **Add → Import from a receipt** (which already extracts all lines via `extract-order`). Full multi-item-per-photo extraction is the unified-rewrite's job (one-photo→one-packet architecture is being redone there). **F13 (Find Hero checkmark lied):** trace proved persistence + read are correct in code — hero page writes `hero_image_url` + persists, review-import reads `stock_photo_url || hero_image_url` + validates http. Real disconnect: `hero/page.tsx:103` marked "Ready" the instant find-hero-photo returned ANY URL, unverified; external-fallback URLs (downloadAndStore failed; `journal-photos` IS public so stored URLs load) 403/hotlink-block → review-import's `<img onError>` (`:1325`) wipes the hero → and the PO flow has **no packet-image fallback** (`imageBase64: ""`) so nothing remains → "No image" after the green ✓. Fix: hero page now verifies the URL actually loads (client `Image()` preload, 6s timeout, `referrerPolicy=no-referrer` matching the render) before "Ready"/persist; unloadable → honest "No hero found". **F15 (normalization drift):** audit corrected the brief's hypothesis — both PO modes use `extract-order` (no normalization) while the photo `extract` route DOES normalize, and `getCanonicalKey` does NOT reorder words, so **no code path produces "Lily Canna"→"Canna Lily"** (Syd's observed plant-vs-seed difference was AI nondeterminism, not a code divergence). Fix per intent (uniform canonical names, fewer duplicate profiles): cloned `stripPlantFromVariety` + `cleanVarietyForDisplay` into `extract-order` so PO imports (plant + seed) normalize variety consistently with photo import; user still reviews/edits in review-import before save. **#9 (no loading state):** `PurchaseOrderImport.handleExtract` called `onClose()` BEFORE the hard `window.location.href` nav → modal unmounted → silent gap ("menu disappears"); only a button spinner existed. Fix: full-screen "Reading your order…" overlay during `isExtracting` (style anchored to `SubmitLoadingOverlay`) + dropped the premature `onClose()` on both seed + supply success paths so the overlay persists through navigation; applies to plant + seed PO → flows converge. 6 files (`api/seed/extract/route.ts`, `api/seed/extract-order/route.ts`, `vault/import/photos/page.tsx`, `vault/import/photos/hero/page.tsx`, `BatchAddSeed.tsx`, `PurchaseOrderImport.tsx`). Pass 1 bidirectional state-transition matrix traced (PO modal: open→extract→success-nav / error / cancel / back-hidden-during-extract; hero state machine: pending→processing→verify-loadable→Ready|NoHero with stopRequested guard after the new await) — clean. Pass 3 sibling sweep: both `/api/seed/extract` consumers fixed, both PO success paths fixed, `extract-order` consumers inherit normalization, hero page is the single shared processing surface. Tests 540/540; build clean; e2e grep clean (no spec touches these flows). No schema/RLS/auth; F19 is a data-loss FIX → auto-pushed per the 2026-06-07 push-tier relaxation. **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** (F19) Add → Photo Import a multi-item cart screenshot → confirm an amber warning names the dropped items + points to receipt import (no more silent 3→1); then Add → Import from a receipt with the same screenshot → all 3 lines extract. (F13) seed packet PO → Find Hero Photos → Continue → any "Ready" card now shows its image on Review Import (no "Ready then No image"); items with no real photo honestly say "No hero found". (F15) upload the same PO to plant + seed flows → variety names normalize consistently. (#9) FAB → Add Plant/Seed Packet → Scan Purchase Order → Extract → confirm a full-screen "Reading your order…" overlay stays up until Review Import loads (no silent gap).
- **2026-06-07 Remove residual "Established Plant" FAB entry (Finding 30 MINIMAL — Syd dogfood 2026-06-01)** — `21bc16b` `feat(add-plant): remove residual 'Established Plant' FAB entry (Finding 30 minimal)`. Syd dogfood verbatim: *"I clicked add established plant. didn't we already discuss this. this page is a How not a what."* Establishment was never a separate entity — the FAB "Established Plant" entry only forced permanent type + relabeled copy (date → "Acquired date", title, journal note, submit button). It conflated **acquisition** (did you sow it / buy it grown — Axis B) with **lifecycle** (permanent vs seasonal — Axis A). **Minimal cut only:** removed the chip + `add-plant-established` sub-screen + its `UniversalAddMenuScreen` union member from `UniversalAddMenu.tsx`; removed the `establishedMode` prop from `AddPlantModal.tsx` and collapsed all 8 branches to the `false` case, so the inline Seasonal/Permanent control now renders **unconditionally** on the add-new path (it was hidden in establishedMode). Established plants stay fully addable via **Manual + Permanent type**. Remaining FAB Add Plant methods: Manual / From Vault / Scan PO / Photo Import. Regression test Test 4 flipped from guarding establishedMode behavior to guarding its removal. **Broader Add Plant restructure stays DEFERRED** to the unified Import architecture rewrite (Model 1/2, Step-1 method-first, persisted acquisition field — see `.claude/plans/add_plant_flow_restructure.md`). Pass 1 bidirectional state-trace clean (only `:243` set the removed screen; `screen` can't reach it post-cut; `UniversalAddMenuScreenTarget` never included it so no deep-link orphan), Pass 3 e2e grep clean. Tests 540/540, build clean. Auto-pushed per the 2026-06-07 push-tier relaxation (no schema/RLS/auth/data-loss)
- **2026-06-07 Swipe-complete UNDO safety fix (Syd dogfood 2026-06-01 — follow-up to `9f2d2e0`)** — `5362f7f` `fix(complete-task): add Undo to swipe-complete snackbar via deferred-commit useToast — recover accidental swipes (Syd dogfood 2026-06-01)`. Syd caught a real data-loss footgun: swipe-left marks an item purchased/done + snackbar appears + **no undo** → a stray swipe is unrecoverable. **Core decision = deferred-commit, NOT write-then-revert:** swipe → optimistic row removal (local state) + snackbar "Marked as purchased/done · Undo" → the real DB write fires only when the 5s window (`UNDO_WINDOW_MS`) closes; tapping Undo cancels the pending write and restores the row, so **undo never reverses a write** — critical because `completeTask` is an irreversible cascade (grow_instance + journal + packet decrement/archive + profile status + harvest task + care schedule) that can't be cleanly un-done. Fail-safe: app killed mid-window → write never happened → row reappears on next fetch. **Primitive-level (both surfaces inherit):** the undo window lives in the shared `useToast` snackbar (`useToast.tsx` extended with optional `action` + per-call `durationMs` + `onAutoDismiss` deferred-commit trigger + single-pending-slot replace-flush + unmount-flush; backward-compatible — existing toast callers untouched). The 3 handlers (`page.tsx` `handleMarkPurchased` + `handleMarkTaskDone`, `shopping-list/page.tsx` `handlePurchased`) supply only page-specific glue (remove / restore / write). **At-a-Glance gained a snackbar** (previously had none) — now "Marked as done · Undo". **Walter** (swipe-excluded `md:` checkmark) inherits undo too — his button routes through the same handler. Label "Undo" Title Case per casing convention (locked 2026-05-27), not all-caps; style = right-aligned underlined white text on the emerald bar (iOS Mail / Gmail pattern, lighter than a pill), `min-h-[44px]` tap target. **Pass 1 bidirectional matrix** traced (show / forward-replace / undo / commit / unmount-flush × state/timer-ref/mount; double-commit guarded by clearing timer + nulling ref before any commit). **VISION §8** amended (undo-required note on the complete-task primitive lock). **Pass 3 ADJACENT (parked, BUGS U29):** Calendar swipe-complete (`calendar/page.tsx:529` single + `:744` batch) still commits immediately with no undo — same footgun, deliberately out of scope (larger surface w/ batch-select + snooze); follow-up bundle will route it through the same pattern. 4 files (`useToast.tsx`, `page.tsx`, `shopping-list/page.tsx`, new `useToast.test.tsx`) + docs (`VISION.md`, `ROADMAP.md`, `BUGS.md`). Tests 540/540 (535 + 5 new deferred-commit/undo tests); build clean; e2e grep clean (no spec asserts "Marked as purchased"/"Undo"/snackbar). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** (a) Home → Shopping List → swipe an item LEFT → row vanishes + "Marked as purchased · Undo" → tap **Undo** within 5s → the row returns (and stays unpurchased); (b) repeat but DON'T tap → after ~5s it commits (item stays gone on refresh); (c) Home → At a Glance → swipe a task LEFT → "Marked as done · Undo" → Undo returns the task; (d) `/shopping-list` page → same swipe → Undo behavior; (e) swipe an item then immediately navigate away → it still commits (no loss).
- **2026-06-06 Ship 4 scope-gap close — remove stale "My Plants/Active Garden" toggle from PO import sub-flow (Syd dogfood 2026-06-01)** — `fix(add-plant): remove stale My Plants/Active Garden type toggle from Purchase Order import — closes Ship 4 (55f38ad) scope gap`. Ship 4 removed the entry-point toggle from `UniversalAddMenu`'s Add Plant chip, but Syd's screenshot caught the same dead-tab-name labels surviving in the **Purchase Order import** input step. **Audit corrected the brief's hypothesis** ("toggle also in Photo + Link import"): a vocabulary-breadth sweep (destination-framing AND lifecycle-framing greps) found the stale `[ My Plants | Active Garden ]` toggle renders in **exactly one** live surface — [PurchaseOrderImport.tsx:284-304](src/components/PurchaseOrderImport.tsx) (gated `addPlantMode && mode==="seed"`). **Photo import** (BatchAddSeed → photos → review-import) has no inline toggle, and **Link import** for plants doesn't exist. The decisive finding: the PO input toggle was both **stale-labeled AND redundant** — its `profileType` state only *seeded* `setReviewImportData(...defaultProfileType...)` ([:224](src/components/PurchaseOrderImport.tsx)) → [review-import/page.tsx:1210-1227](src/app/vault/review-import/page.tsx) already renders an **already-correctly-labeled `[ Permanent | Seasonal ]` picker** for BOTH the PO and Photo flows. So a PO Add-Plant user saw two type pickers, the first with dead labels. **Fix = removal, not relabel** (maps Ship 4's "type lives in the form body" strategy — here the form body is the review step): deleted the input-step toggle block + the now-vestigial `profileType` state (the prop flows straight through to review-import's picker), unifying PO with Photo (both pick type once, at review, with correct labels). DB derivation untouched (`is_permanent_planting` / `profile_type` still written from `defaultProfileType` at review-import) — blast radius is label-only. **Pass 1 bidirectional trace trivial** — removed a conditional render-block + passthrough state; no open/close/forward/back lifecycle cell touched. **2 source-text regression guards added** to `addPlantTypeDerivation.regression.test.ts` (PurchaseOrderImport no longer contains the dead labels / `setProfileType`; review-import retains the Permanent/Seasonal picker). 2 files (`PurchaseOrderImport.tsx`, regression test). Tests 535/535; build clean; e2e grep clean (no spec asserts these labels). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **ADJACENT (surfaced, deferred — not stale, minor cohesion):** review-import's picker is `Permanent`-first with no label/helper, vs AddPlantModal's inline control which is `Seasonal`-first + helper "Perennial, tree, or shrub = permanent…" — order/label polish for a future cohesion pass (or Finding 30 restructure). **Syd dogfood path:** FAB "+" → Add Plant → Scan Purchase Order → upload a cart screenshot → confirm the input step no longer shows a "My Plants / Active Garden" toggle; after Extract, the review step still shows a "Permanent / Seasonal" picker → pick Permanent for a tree → it lands as a permanent planting; same picker reachable via Add Plant → Photo Import.
- **2026-06-05 Drop redundant "View full schedule" link from Home When-to-Plant widget (Syd dogfood 2026-06-01)** — `polish(home): drop redundant View full schedule link from When-to-Plant widget — buttons above cover the same destinations (Syd dogfood 2026-06-01)`. After Ship 1 (`ee43fb5`) repointed the link to land on Yearly view (Finding 15 = A lock), the link became pure duplication of the three view-toggle buttons directly above it (Action now / Monthly view / Yearly view) — which already provide access to all three views. The 2026-06-04 ship (`7172be2`) had already removed the sibling "Zone charts & resources" link + its `·` separator, leaving this `<p>` as the sole footer content, so removal strips the whole footer block cleanly (no orphan separator). Removed the `<p>` wrapping `<Link href="/schedule?view=roadmap">View full schedule →</Link>` from `src/app/page.tsx:404–408`; the 3 view-toggle buttons (the canonical affordance) stay. Audit clean (Pass 1 factual: `<p>` self-contained 404–408, `Link` still used elsewhere; Pass 2: no orphan separator since Zone-charts already gone; Pass 3 sibling sweep: only one render site in `src/`, e2e grep clean for "View full schedule"/`schedule?view=roadmap`). 1 file, −5 lines. Tests 533/533; build clean. **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** Home → When to Plant widget → confirm the footer link "View full schedule →" is gone; the three buttons (Action now / Monthly view / Yearly view) remain and still reach all three schedule views.
- **2026-06-05 Library desktop responsive grid (Finding 6 pre-Desktop-Audit ship, Syd dogfood 2026-06-01)** — `6f789fc` `polish(library): desktop responsive grid — Library cards scale to viewport`. Isolated pre-ship of one item from the queued **Desktop Responsive Audit** chapter (Syd-greenlit). The Library grid (photo/gallery mode, `/plants`) was a fixed `grid-cols-3` across all viewports, so on a desktop browser each card stretched to ~600px at 1920px — only 3 huge cards/row, photo eating ~90% of card area, text hard to read (Syd laptop screenshots). **Single className edit** at [`SeedVaultView.tsx:959`](src/components/SeedVaultView.tsx): `grid grid-cols-3 gap-2` → `grid grid-cols-3 lg:grid-cols-[repeat(auto-fill,minmax(200px,1fr))] gap-2`. Mobile/tablet (`<lg`) keep the exact existing 3-col layout; desktop (`lg:`+) auto-fills ~200px tiles. **Tactical swap from the briefed literal** (`lg:grid-cols-4 xl:5 2xl:6` + per-card `max-w-[200px]`): per-card max-w inside a fixed grid leaves ragged tracks, and fixed col-counts don't cap card size on ultrawide (2xl:6 → ~420px cards at 2560px = huge again). `minmax(200px,1fr)` honors the brief's size-cap intent better — cards stay ~200px at *every* desktop width, fill the row with no seam/ragged gaps. **Verified via injected-element CSS measurement** (surface is AuthGuard-blocked so Preview MCP can't load `/plants` logged-in): 1920px content → **9 equal ~200.9px tracks** (was 3 ~600px); 375px → **3 equal ~103.7px tracks** (unchanged). **VISION §8 amended** (line 160 "Vault grid card layout: 3 cards per row" was a mobile-era note; now reflects mobile-3-col + desktop-responsive reality, prevents re-drift). **Pass 3 ADJACENT (deferred per Library-only scope):** the same `grid grid-cols-3 gap-2` lives in 4 peers (`PacketVaultView`, `ShedView`, `GardenView`, `VaultSkeleton`) — desktop Library is now temporarily denser than Packets/Shed/Garden; the broader Desktop Responsive Audit applies the same primitive to peers + skeleton and reconciles the §8 line-169 cross-view-consistency question. 2 files (`SeedVaultView.tsx`, `docs/VISION.md`). Tests 533/533; build clean; e2e grep clean (no spec asserts the grid class). **Syd dogfood path:** open Library (`/plants`) in a desktop/laptop browser → confirm many small (~200px) cards per row instead of 3 huge ones, photo-to-text rebalanced; on phone → confirm the grid still shows exactly 3 cards per row as before.
- **2026-06-04 Complete-task responsive primitive + Home migrations + 2 subtitle drops (Syd dogfood lock 2026-06-01)** — `feat(complete-task): extract responsive swipe-complete primitive + migrate At-a-Glance & Shopping List + drop 2 Home subtitles`. Locks the complete affordance into ONE shared primitive so every task/item-complete surface reads identically (cohesion-by-aggregation), anchored to Calendar's canonical task row. **Audit correction:** Calendar's complete-swipe lives at the ROW level (`CalendarTaskRow`) and is **bidirectional** (swipe-left = complete, swipe-right = snooze) — the page-top swipe handlers are month-nav, not task-complete. **3 shared pieces extracted:** (1) [`useRowSwipe`](src/hooks/useRowSwipe.ts) — the swipe engine, MOVED verbatim out of `calendar/page.tsx:2118` to a shared hook (custom touch listeners, no library; 100px threshold, 8px direction-lock); Calendar's 2 callers (`CalendarTaskRow` + `ConsolidatedOverdueHeader`) repoint their import, behavior identical. (2) [`SwipeCompleteRow`](src/components/SwipeCompleteRow.tsx) — wrapper rendering emerald-check reveal + `translateX` slide + optional amber snooze reveal (only when `onSnooze` passed). (3) [`RowCompleteButton`](src/components/RowCompleteButton.tsx) — `hidden md:flex` emerald checkmark pill, byte-identical across surfaces. **Migrated 3 surfaces (complete-only config, A/A locks):** At-a-Glance (`page.tsx`) — dropped the always-visible "Done" pill (the truncation cause; `truncate`→`break-words`), now swipe-left = done on mobile + desktop checkmark, freed horizontal space; Shopping List Home widget + dedicated page (`shopping-list/page.tsx`) — swipe-left = mark purchased + desktop checkmark, **X "remove" button stays visible on all viewports** (NOT behind swipe — remove has no undo; Q1=A: swipe-to-delete on a no-undo action is a footgun, Calendar's reversible snooze is not a true parallel). **At-a-Glance complete-only** (no Home snooze — separate feature scope; Q2=A). `enabled={!isMarking}` blocks swipe double-fire alongside the existing `disabled` button guards. Dedicated-page placeholder rows keep their long-press-to-edit handlers on the `<li>` (swipe disabled while editing). **2 subtitle drops** (`page.tsx`): "Action now · Monthly view · Yearly view" (When-to-Plant — redundant with the 3 buttons below) + "Tasks (pending)" (At-a-Glance, full unconditional removal). **VISION §8 amendment:** new "Complete-task affordance — responsive primitive lock (2026-06-01)" subsection — mobile = swipe-left, desktop (`md:`+) = checkmark, same underlying action; secondary actions stay per-surface and off-swipe unless reversible. **Regression test updated:** `calendarBatchSelect.regression.test.ts` gesture-machinery assertions repointed to read the hook's new home (`src/hooks/useRowSwipe.ts`) + new assertion verifying calendar imports the shared hook — same coverage, correct file (the test's own Feature F comment already framed the hook as "extracted... to be reused"). 4-pass audit clean (`.claude/plans/complete_task_responsive_primitive.md`), incl. Pass-1 bidirectional state matrix (commit/cancel × success/failure × double-fire-guard × desktop-click) + Pass-3 sibling sweep (ADJACENT logged: Calendar's own rows still inline their reveal/transform rather than using `SwipeCompleteRow` — future cohesion fold-in, not this ship, due to snooze/select-mode complexity). 8 files (`useRowSwipe.ts` new, `SwipeCompleteRow.tsx` new, `RowCompleteButton.tsx` new, `calendar/page.tsx`, `page.tsx`, `shopping-list/page.tsx`, regression test, `docs/VISION.md`). Tests 533/533 (532 + 1 new import-wiring assertion); build clean; e2e grep clean (no spec asserts "Done"/"Tasks (pending)"/"Action now"/purchased strings). **NO Preview MCP** per HARD RULE — Syd phone-verifies BOTH mobile-swipe AND desktop-checkmark. **Syd dogfood path:** **(mobile)** (a) Home → At a Glance → swipe a task row LEFT → emerald check reveals + task completes; row name no longer truncated with "…"; (b) Home → Shopping List → swipe an item LEFT → marks purchased; the X remove button still visible (not a swipe); (c) `/shopping-list` page → same swipe-left-purchased, X stays; long-press a placeholder still opens inline edit. **(desktop / iPad)** (d) same surfaces show a visible emerald checkmark button (no swipe needed) — clicking completes; Shopping rows show checkmark + X. **(subtitles)** (e) Home "When to Plant" widget has no "Action now · Monthly view · Yearly view" line under the header; (f) "At a Glance" has no "Tasks (pending)" label.
- **2026-06-04 Settings page reorg — Profile-to-top + top-level Sign Out + clean section grouping (Syd dogfood 2026-06-01, 5 items)** — `polish(settings): reorg into grouped sections + surface top-level Sign Out + home orphan toggle`. Single-file (`src/app/settings/page.tsx`) close of Syd's Settings-friction batch. **(1) Profile to top** — was mid-list; now first row of the new **Account** section (Profile + Family). **(2) Top-level Sign Out** — was buried Settings → Profile → account block (`settings/profile/page.tsx:314-321`, kept there for power-user discovery + account context); now also a destructive red row at the very bottom of Settings. Reuses the **exact same `signOut` from `useAuth()`** (`AuthContext.tsx:48` → `supabase.auth.signOut()` → `clearCalendarCache()` → `setUser(null)` → AuthGuard effect `:165` redirects `/login`), so the new row's auth+redirect flow is identical to Profile's by construction — Pass 1 bidirectional trace clean (single-direction terminal auth action, no modal lifecycle/back-path). Treatment anchored to the existing Profile sign-out token (`border-red-200 text-red-600 hover:bg-red-50`) + VISION §8 destructive=red + developer Danger-Zone red-border precedent; no chevron (action, not nav); **no confirm modal** (matches existing Profile sign-out + sign-out is reversible). **(3) Section grouping** — flat single-nav replaced with canonical uppercase headers (`text-xs font-semibold uppercase tracking-widest text-neutral-500`, reused verbatim from profile/developer pages per VISION §8 settings-sub-header): Account · Preferences · Resources · Feedback · Admin · Sign Out. **(4) Orphan "Play sound on success" toggle** — was a floating box above the nav (the misplaced-header look); now homed as the first row of the **Preferences** section (above Help). **(5) Admin gating** — Admin section (Import Vendor Catalog + Developer + Feedback Inbox + Import Logs + Extract Cache) gated behind `useDeveloperUnlock().isUnlocked` (the existing canonical dev-gate that already hid DEV_ITEMS); **Import Vendor Catalog moved ungated → gated** (writes to shared plant cache = admin op). Refactor: per-section arrays + `SettingsLinkRow`/`SettingsSection` helpers + shared `SECTION_HEADER`/`CARD`/`ROW` constants; row primitive unchanged. 2 files (`settings/page.tsx`, `settings/page.test.tsx` +1 Sign-Out-button assertion). Tests 532/532; build clean; e2e grep clean (no spec asserts any Settings string). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** (a) Settings → Profile is the FIRST row (Account section); (b) scroll to bottom → red "Sign Out" row; tap it → signs out + lands on /login (same as Profile's Sign Out, which still works too); (c) sections read with clear uppercase headers; (d) "Play sound on success" toggle now sits inside Preferences (above Help), no longer floating; (e) Admin section appears only when dev-unlocked.
- **2026-06-04 Finding 16 follow-through — remove "Zone charts & resources" link from Home When-to-Plant widget (Ship 1 `ee43fb5` miss)** — `polish(home): remove Zone charts & resources link from When-to-Plant widget`. Finding 16 was LOCKED for v1 removal (Syd 2026-05-30, reaffirmed 2026-06-01: REMOVE for v1; resources/learning returns in v1.x as a properly-architected dedicated surface, not bolted onto Home) but Ship 1 `ee43fb5` missed it — the link was still rendering per Syd's screenshot 2026-06-01. Removed the `<Link href="/resources">Zone charts & resources →</Link>` plus its preceding `{" · "}` separator from the When-to-Plant widget footer in `src/app/page.tsx:407–410`; the "View full schedule →" link (now sole footer link, `text-center`) stays. `/resources` route + page preserved (just unlinked from Home; v1.x will re-link from its dedicated surface). 4-pass audit clean iter 1 (Pass 1 factual: link at :408–410, separator at :407, `/resources` is a string href not an import; Pass 2: orphan-separator concern → strip 407–410 together; Pass 3 sibling sweep: only one render site in `src/`, e2e grep clean for "Zone charts"/"resources"; Pass 4: honors locked Finding 16, not state-touching). 1 file, −4 lines. Tests 532/532; build clean; e2e grep clean. **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** Home → When to Plant widget → confirm the footer shows only "View full schedule →" (no "· Zone charts & resources →").
- **2026-06-01 Sprint 5 cont Ship 5 of 5 — Button-system lock + form sweep + "(optional)" cleanup (Findings 28 + 25) — CLOSES Sprint 5 continuation final ship** — `polish(buttons): lock primary/secondary/tertiary/destructive button system in VISION §8 + sweep photo-source buttons to secondary token + drop redundant "(optional)" labels`. The FINAL pre-tester engineering ship. **Finding 28 (button-system lock, Syd 2026-06-01):** new VISION §8 "Button-system hierarchy" subsection locks 4 tiers — Primary (solid `bg-emerald-600`, the most-prominent control, already canonical via FAB-form saga), Secondary (outlined pill, green text `border-teal-gus/40 text-teal-gus hover:bg-teal-gus/10` — matches the locked Cancel token; alt-source photo buttons + Cancel + dismiss all share it), Tertiary (text-only), Destructive (red pill). **The drift carrier = photo-source button pairs:** the flagged Add Supply form rendered `From Gallery` as a large solid-green button heavier than the actual `Add Supply` submit CTA, with its paired `Take Photo` outlined in black and `Cancel` outlined in green. Swept all `Take Photo` / `From Gallery` / `Use Camera` / `Upload from files` pairs across **14 files** to the matched secondary teal token (no mid-form solid-green out-weighs the submit; equal alt-sources read equal; `min-h-[44px]` tap targets preserved): QuickAddSupply, garden inline quick-add, HarvestModal, vault/[id] hero (+webcam Cancel), EditJournalModal, QuickLogModal, AddPlantModal, BatchLogSheet (4 clusters), BatchAddSupply, shed/review-import, vault/review-import, vault/shed/[id], PurchaseOrderImport, BatchAddSeed. `Capture` (webcam-commit) stays primary emerald — it's the commit action of the webcam sub-flow, not an alt-source. Footer Save/Cancel already compliant (FAB-form saga, untouched). **Finding 25 (drop "(optional)" labels, Syd 2026-05-30):** asterisk `Field *` already marks required, so "(optional)" on non-asterisked fields is redundant double-labeling. Swept ~20 form files dropping the parenthetical from labels/placeholders/aria (AddPlant, AddPlantManual, AddVariety, QuickAddSeed, QuickAddSupply, PlantingForm, NewTaskModal, CareScheduleManager, HarvestModal, FeedbackModal, EditJournal, QuickLog, CollapsibleSupplies, SupplyPicker, VaultPageContent, vault/[id], shed/review-import, vault/review-import). **Preserved:** importPdfCatalog.ts (AI-prompt schema descriptions, not UI) + vault/review-import:1232 (body-copy sentence where "(optional)" describes the whole step). **e2e: zero edits** — button text unchanged (className-only); "(optional)" label strings not asserted (grep-confirmed clean). VISION §8 amended first (doc-lock-then-sweep). ~35 files. 4-pass audit clean iter 1 (`.claude/plans/sprint5_ship5_button_system.md`). Tests 532/532; build clean. **NO Preview MCP** per HARD RULE — Syd phone-verifies. **ADJACENT (surfaced, deferred):** vault/[id]:1286 "Add Photo" no-photo-found CTA (emerald-900, distinct context); remove/delete buttons (amber "Remove Photo") not migrated to red destructive token this ship (not flagged drift). **Syd dogfood path:** (a) FAB → Add Supply → confirm both `Take Photo` + `From Gallery` are matched green-outlined buttons, neither heavier than the green-filled `Add Supply` submit at the bottom; (b) same matched-pair treatment in Add Plant / Add Journal / Harvest / batch-log photo pickers; (c) every form's optional fields no longer say "(optional)" — only required fields show the `*`.
- **2026-06-01 Sprint 5 cont Ship 4 — Add Plant entry-toggle removal (Finding 29, v1-blocking MUST — CLOSED)** — `fix(add-plant): remove My Plants/Active Garden entry toggle + inline plant-type field + derive link-to-existing permanence from profile`. The lone v1-blocking MUST of the Sprint 5 cont dogfood wave. Closes Finding 29 (Ship B half-merge residual: the `[ My Plants | Active Garden ]` entry-point toggle that mis-framed an in-memory type default as a destination picker). **Forward-compat per `.claude/plans/add_plant_flow_restructure.md` §"Forward-compat for Ship 4"** — the toggle's only real job was setting Axis A (`profile_type` + `is_permanent_planting`) on the Manual add-new path; deleting it bare would have silently defaulted every perennial to seasonal. **What shipped:** (1) **Toggle removed** — `UniversalAddMenu.tsx:227-243` "Add to" label + 2 buttons deleted; "How do you want to add a plant?" subhead kept; now-unused `setAddPlantDefaultType` destructure dropped (prop type + context plumbing retained — 5 parent pages still pass it, `openPlant()` deep-link unaffected; stale JSDoc labels refreshed). (2) **Inline `Plant type` control** — `AddPlantModal.tsx` add-new body gains a `[ Seasonal | Permanent ]` segmented control (end of basic-info block, default seasonal, helper "Perennial, tree, or shrub = permanent. Annual or veg = seasonal."), matching the same-file `[ Link to existing | Add new ]` toggle primitive (`:651-664`) + `min-h-[44px]` for Walter touch-target. **Gated `!establishedMode`** so the Established chip stays permanent-locked (DO-NOT-TOUCH path preserved). This replaces the toggle's single responsibility where the v1.x restructure's Step 2 will later anchor — clean seam, no state-lifting. (3) **Link-to-existing bug fix (`AddPlantModal.tsx:501`)** — existing-mode `is_permanent_planting` now derives from the **selected profile's `profile_type`** (extended the existing `:486` profile fetch to `select("harvest_days, profile_type")` → `isPermanentExisting`), not the removed toggle. Fixes BOTH existing-mode routes (in-modal Link-to-existing + external `addToExistingProfile`); a permanent profile now records a permanent planting regardless of default. **Scope-disciplined:** the seasonal-gated packet-link/care-template branches stay `plantType`-keyed (the pre-existing "seasonal-but-established" Axis B gap is explicitly v1.x restructure scope, NOT this ship). (4) **5 source-text regression guards** — new `addPlantTypeDerivation.regression.test.ts` (repo pattern per `permanentPlantProfile.regression.test.ts`): add-new permanent/seasonal derivation, care/harvest seasonal-gating, link-to-existing profile-derived permanence, Established-chip permanence + control hidden, toggle-gone + inline-control-present. 3 files (`UniversalAddMenu.tsx`, `AddPlantModal.tsx`, new test). Pass 1 bidirectional state-transition matrix traced (toggle removal × downstream `plantType` reads — forward-seed, inline-toggle, back-unmount, established-lock, existing-submit, app-reload all covered; plan's #1 risk cell N/A — no Step-2 screen added). Tests 532/532 (527 + 5); build clean. **NO Preview MCP** per HARD RULE — Syd phone-verifies. **ADJACENT (surfaced, deferred to v1.x restructure):** (a) seasonal-but-established plants still get auto-Sow/care-templates (Axis B gap); (b) two "plant an existing profile" paths (AddPlantModal link-to-existing vs PlantingForm From Vault) — restructure converges them. **Syd dogfood path:** (a) FAB "+" → Add Plant → confirm the "My Plants / Active Garden" toggle is GONE (subhead + method chips only); (b) Add Plant → Manual Entry → confirm a "Plant type" `[ Seasonal | Permanent ]` control near the bottom; add a perennial (e.g. "Avocado Tree") with Permanent selected → it should land in the garden as a permanent planting (no harvest task); (c) add a tomato with Seasonal → seasonal planting with harvest task; (d) Add Plant → Established Plant chip → confirm NO "Plant type" control (stays permanent); (e) Manual → "Link to existing" → pick an existing permanent profile → it records as a permanent planting.
- **2026-06-01 Sprint 5 cont Ship 3 — pre-tester Add-flow chrome + empty-state direct-wire (4 items, Syd dogfood 2026-05-30)** — `polish(add-flow): tighten FAB chip density + wire vault/shed empty-state CTAs direct to manual form + Add Supply header divider`. Closes 4 Add-flow findings. **Finding 10 (FAB menu chip-density scroll on web):** `UniversalAddMenu.tsx` — chip vertical padding `py-3`→`py-2.5` (all 11 chips across main + add-plant screens, applied uniformly so the shared chip primitive stays consistent mid-flow), chip-list `space-y-2`→`space-y-1.5`, main title block `pt-5 pb-3`→`pt-4 pb-2`. Icon container unchanged (two-line text ~40px already equals icon height, so shrinking icon yields no chip-height gain). **The briefed "What would you like to add?" subhead does NOT exist** (grep clean app-wide) — main screen has only `<h2>Add</h2>`; nothing to drop. ~36-40px reclaimed. **Finding 11 (vault empty-state → seed manual direct):** `QuickAddSeed.tsx` gains `startOnManual?` prop on SeedPacketForm + QuickAddSeed shell (reset effect opens on `manual` step, skipping the method-choose intermediate). `VaultPageContent.tsx` — new `seedStartOnManual` state + `handleAddFirstManual` (`setSeedStartOnManual(true); openSeed()`), flag reset on every exit (onClose + onSuccess incl. newProfileId branch). **Wired BOTH vault sites** — Packets (`PacketVaultView`, :949) AND Library (`SeedVaultView`, :1217) — both previously `openMenu` with the identical "Add a Packet" label, so aligning prevents two identical buttons with divergent behavior. **Validation-note correction:** the Ship-plan claim that "Library already goes direct" was inaccurate (Library used `openMenu`); Garden's "Add a Plant" is a `<Link href="/vault">` (intentional plant-from-vault nav, NOT the FAB-menu intermediate) — left as-is. **Finding 12 (shed empty-state → supply manual direct):** `QuickAddSupply.tsx` gains `startOnForm?` prop on SupplyForm + QuickAddSupply shell (opens on `form` step). `ShedView.tsx` — new `quickAddDirect` state set true only by the empty-state CTA (toolbar "+" keeps the method-choose step); flag reset on close. **Sizing verify:** shed choose step = 3 short chips in an 85svh flex container — not cramped (no extra change). **Finding 26 (Add Supply header divider):** SupplyForm header block gains `border-b border-neutral-200`, mirroring the existing footer `border-t` for iOS static-scroll-static symmetry (anchored to the existing footer divider token). **Extended to SeedPacketForm** (Syd lock — both vault add-forms are now direct destinations via F11, so the divider lands on both to avoid a visible sibling mismatch). Direct-entry back behavior: both forms' manual/form-step back arrow returns to the (unvisited) choose step as an escape hatch to other methods, matching existing prefill-flow behavior. 5 files (`UniversalAddMenu.tsx`, `QuickAddSeed.tsx`, `QuickAddSupply.tsx`, `ShedView.tsx`, `VaultPageContent.tsx`). 4-pass audit clean iter 1, incl. Pass-1 bidirectional state-transition matrix per CLAUDE.md (`.claude/plans/sprint5_ship3_addflow_chrome.md`). Tests 527/527; build clean; e2e grep clean (no spec asserts "Add a Packet" / "Add a Supply" / "What would you like to add"; no labels changed). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **ADJACENT (surfaced, not shipped):** F26 divider now on both vault add-forms (Supply + Seed); the remaining FAB sub-screen forms (TaskForm, JournalEntryForm, AddVarietyForm) + menu main/add-plant headers still lack it — parked for a future cohesion pass. **Syd dogfood path:** (a) open the FAB "+" menu on web → all 6 chips fit with no scroll indicator (mobile too); (b) Vault → Packets tab with empty vault → tap "Add a Packet" → land directly in the Add Seed Packet manual form (no FAB menu, no method picker); (c) Vault → Library empty → "Add a Packet"/"Add a Plant" same direct landing; (d) Vault → Shed empty → tap "Add a Supply" → land directly in the Add Supply form (no method picker); (e) in the Add Supply form, confirm a divider line sits under the title, matching the line above Cancel.
- **2026-06-01 Sprint 5 cont Ship 2b — pre-tester nav chrome cohesion bundle (4 items, Syd dogfood 2026-05-30)** — `polish(nav): balanced bottom-nav slots + Vault Package icon + remove "?" help button + Library single-state tab slot`. Closes 4 global-nav-chrome findings. **Item 1 (bottom-nav spacing imbalance):** `BottomNav.tsx` container dropped `justify-around`; each tab `min-w-[64px]` → `flex-1` → 6 equal-width slots, content centered (industry-standard bottom-tab-bar layout; balanced regardless of label width). **Item 2 (Vault tab icon):** `navItems.tsx` `VaultIcon` 4-squares grid (`<rect>`×4) → Lucide `package` paths (storage/inventory read; flat-monochromatic-line per §8 icon-style chrome convention; auto-propagates to desktop `Sidebar` which consumes `primaryNavItems`). **Item 3 ("?" help button removal):** deleted the persistent `<Link href="/help">?</Link>` from both `AuthGuard.tsx` mobile header blocks (loading + loaded, `xl:hidden`) AND `Sidebar.tsx` desktop nav (cross-surface cohesion — Syd signal "doesn't do much" applies to both viewports); `/help` route + page preserved and now reachable via a new `Help` row in `settings/page.tsx` `USER_ITEMS` (subtitle `"Where do I…?" — quick reference for finding things`). OnboardingDock remains the reliable teaching path per VISION §10. **Item 4 (Library top-of-page tab slot):** `/plants` (renders `VaultPageContent` in `isPlantsSurface` single-mode — NOT `SeedVaultView` as briefed) now renders one inert "Library" tab matching the exact Packets/Shed switcher primitive (`inline-flex rounded-xl p-1 bg-neutral-100` pill, active `bg-white text-emerald-700 shadow-sm`, `aria-selected={true}`, no-op tap) so the 3 inventory surfaces (Garden All/groups · Vault Packets/Shed · Library) share one header rhythm. **VISION §8 amendment:** new "Single-state tab-slot for cross-surface cohesion" convention locked. 7 files (`navItems.tsx`, `BottomNav.tsx`, `AuthGuard.tsx`, `Sidebar.tsx`, `settings/page.tsx`, `VaultPageContent.tsx`, `docs/VISION.md`). 4-pass audit clean iter 1 (`.claude/plans/nav_chrome_cohesion_ship2b.md`). Tests 527/527; build clean; e2e grep clean (no spec asserts the removed "?", `justify-around`, `VaultIcon`, or a Library-tab label). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** (a) bottom nav — all 6 tabs evenly spaced, Home no longer extra-wide; (b) Vault tab icon now reads as a box/package (not 4 squares); (c) top-right of every page — no "?" button; Settings → "Help" row opens the "Where Do I…?" page; (d) Library (`/plants`) shows a "Library" tab pill at top-of-page matching Garden + Vault tab placement.
- **2026-06-01 Sprint 5 cont Ship 2a — pre-tester chrome cohesion bundle (4 items, Syd dogfood 2026-05-30)** — `polish(chrome): filter button/modal cohesion + Packets empty-state filter bar + Journal FAB dedup`. Closes 4 findings from the pre-tester chrome cohesion bundle. **Finding 3a (X-button alignment) — AUTO-CLOSED moot:** verified Home (`src/app/page.tsx`) has only ONE dismissible card (`OnboardingDock` Quick Start) post Ship 1 Insight-banner removal; Frost Alert has no dismiss-X; no stacked dismissible-banner pair exists anywhere (the single review-import banner in VaultPageContent is unstacked). Nothing to align. **Finding 9 (Packets empty state hides Filter/Search):** removed the `vaultHasSeeds &&` empty-gate on the Packets-list toolbar (`VaultPageContent.tsx:1136`) so search+filter render unconditionally in list view, matching the canonical Shed pattern (`:1139`, ungated). PacketVaultView's empty card has no internal search bar, so no duplication. **Finding 14 (Journal "+ Entry" duplicates FAB):** removed the toolbar "+ Entry" green pill (`journal/page.tsx`), dropped the now-orphaned `openJournal` destructure (was its only consumer), and updated empty-state copy "Tap the + button below or **+ Entry** above…" → "Tap the + button below…". FAB is now the sole canonical add affordance per VISION §8 FAB-only. **Finding 31 (filter button/modal cohesion):** (a) added the funnel icon `<ICON_MAP.Filter className="w-5 h-5 shrink-0" />` to the Garden filter button (`garden/page.tsx` — Library/Packets/Shed already had it); (b) Garden modal CTA "Show results (N)" → "Show Results (N)" (§8 Title Case); (c) Shed modal title "Filter & Sort" → "Filter" (Library/Garden/Packets already "Filter"); (d) sort-section label "Sort" → "Sort By" in Library (`VaultGridRefineModal:144`) + Packets (`VaultPacketWing:734`) to match Garden + Shed. All 4 filter buttons now carry the funnel; all 4 modals titled "Filter"; all sort labels "Sort By"; all accordion-CTA modals "Show Results" (Shed applies-on-tap, no CTA — structure untouched per scope). **Pass 3 ADJACENT (not fixed):** Library/grid toolbar on `/plants` (`VaultPageContent:1031`) has the same `vaultHasSeeds` empty-gate as Packets — left for Syd's call since `/plants` empty state is the primary onboarding moment. 6 files (`garden/page.tsx`, `journal/page.tsx`, `VaultPageContent.tsx`, `VaultGridRefineModal.tsx`, `VaultPacketWing.tsx`, `VaultShedWing.tsx`). Tests 527/527; build clean; e2e grep clean (no spec asserts any changed string). **NO Preview MCP** per HARD RULE — Syd phone-verifies. **Syd dogfood path:** (a) Vault → Packets tab with an empty vault → confirm search + Filter bar now show (matching Shed tab); (b) Vault → Shed → tap Filter → modal title reads "Filter" (not "Filter & Sort"); (c) Garden → confirm Filter button has a funnel icon, open it → CTA reads "Show Results (N)", sort section reads "Sort By"; (d) Library + Packets filter modals → sort section reads "Sort By"; (e) Journal → confirm no "+ Entry" button top-right, only the FAB bottom-right; empty journal copy says "Tap the + button below to log your first entry."
- **2026-06-01 Sprint 5 cont Ship 1 — Home pre-tester polish bundle (5 items, Syd dogfood 2026-05-30) — `ee43fb5`** — `polish(home): remove non-data-driven insight banner + drop weather/when-to-plant header noise + hide empty-Tasks label + fix View full schedule destination`. Single-surface (`src/app/page.tsx`) close of 5 SHOULD-bucket Home findings: (1) **Finding 1** — removed the weekday-rotation "Insight of the day" banner (verified NOT data-driven per APP_AUDIT E1; concept preserved as v1.x queue entry cross-linked to CAN #25 weather-aware care); stripped banner JSX + `insightDismissed` state + `home-insight-dismissed` localStorage + `sowingOk` derived + this file's `PlantPlaceholderIcon` import (all banner-only; `heatAlert` kept — also used by extreme-heat chip). (2) **Finding 15** — "View full schedule →" repointed `/schedule` → `/schedule?view=roadmap` (was defaulting to `view=action` = smallest scope; Yearly/roadmap = fullest schedule, makes label truthful + distinct from the "Action now" button above). (3) **Finding 21** — weather header `Weather & Forecast, {locationLabel}` → `Weather & Forecast`; weather always loads (Vista fallback in weatherSnapshot.ts) so there's no empty branch — added a conditional `Set your location in Settings →` affordance (route `/settings/profile`) inside the existing bordered footer when `location_name` unset (anchored to existing emerald-600 link primitive; removed now-unused `locationLabel` const). (4) **Finding 22** — `When to Plant (by Zone)` → `When to Plant` (Title Case retained per §8; single-zone-lens per VISION §3 makes the parenthetical redundant). (5) **Finding 23** — "Tasks (pending)" category label now guarded by `!loadingTasksAndList && pendingTasks.length > 0` (empty At-a-Glance suppresses structural scaffolding per VISION §10). Tests 527/527; build clean; e2e grep clean (no spec asserts any changed string). **NO Preview MCP** per HARD RULE — Syd phone-verifies. 1 file, +13/-50. **Syd dogfood path:** (a) Home loads with no green "Insight of the day" banner; (b) weather header reads just "Weather & Forecast"; if your location is unset, a "Set your location in Settings →" link appears under the forecast; (c) "When to Plant" header has no "(by Zone)"; (d) "View full schedule →" lands on the Yearly view; (e) when no tasks pending, the "Tasks (pending)" label is gone (empty At a Glance shows only the empty-state).
### Historical (archived from §5 trim 2026-05-29; extended 2026-06-10)

Entries moved from §5 main body during cap-overflow trim per §7 "last ~10" cap. Kept here for quick reference; for older entries (pre-2026-05-16) see the `*Older entries` marker at bottom + use `git log` for full history.

- **2026-05-29 Sprint 4 fully shipped — Established plants + FAB cohesion + polish bundle (multiple chats) — `feat/care-tab-c4-instance` branch** — Rolled-up close-out covering all Sprint 4 ships per gt_v1_scope.md §4: 6 MUST chapters + 3 SHOULD chapters across 5 commits 2026-05-29 + 1 pre-Sprint-4 SHA caught by close-out. **`ff4ac2e` `feat(fab-tree): MUST #6 — Established Plant chip`** — adds 3rd planting-method option (Established plant / from nursery) to FAB Add Plant menu; closes lifecycle gap per gt_v1_scope.md §1.1(b); core nursery/division/established path now has a clean FAB entry (was previously forcing seed-start or direct-sow framing). **`f77fe75` `feat(add-plant): MUST #5 — AddPlantModal Manual symmetry + shared CollapsibleSupplies`** — From Vault / Manual / Established paths now share a single `CollapsibleSupplies` primitive; supplies-used always-expanded inconsistency between the 3 paths closed; redundant seasonal-vs-permanent toggle removed; ~4-6h clean extraction landed. **`9dc8c2d` `fix(fab-tree): MUST #4 sweep — #10 verified + #11 refresh diversity + #12 packet card lock`** — handler/label/destination audit across every FAB entry point lands as one cohesive ship (bundling avoids piecemeal cohesion drift per VISION §6 Theme 4); Add-to-shed → Photo-import wrong navigation verified clean (MUST #10); Refresh-photos returns diverse Wikimedia pool (MUST #11; locks initial 2026-05-28 `6dc6a4f`); packet card expand title-truncation closed via star-rating sm interactive footprint lock (MUST #12; locks initial 2026-05-28 `fcf5dd2`). **`dc63fa9` `feat(fab): SHOULD #16 — shared-option position consistency`** — Photo import / Manual entry / Purchase order occupy parity positions across all 3 FAB menus (Add plant / Add seed packet / Add to shed); muscle-memory cohesion landed per 2026-05-28 UX principle lock. **`e684331` `fix(vault): SHOULD #17 — update stale "Load plant profile pictures" copy`** — BatchAddSeed final-step button text refreshed; greenlit 2026-05-26, shipped this sprint. **`7242cb6` (pre-Sprint-4 2026-05-26) `Convert Add seed packet → Link import to modal flow`** — caught by close-out chat as already-shipped; counts toward SHOULD #18 closure; FAB-tree route-to-modal cohesion holds across Add-seed-packet → Link-import path. **v1 % post-Sprint-4 = 11 / 18 = 61%** (9 MUST shipped of 14 + 2 launch-checklist items shipped of 4); polish % 4 / 8 = 50%. Full per-chapter SHA traceability in gt_v1_scope.md §5.2 (recomputed snapshot). **Sprint 5 = current focus** per gt_v1_scope.md §4 lock; recommended first chunk **MUST #7 app-wide casing sweep** (S; mechanical, well-defined, longest-queued; closes §1.5(a) cohesion gate). Remaining v1 work: MUST #7+#8+#9+#13(L1+L2)+#14 + SHOULD #19+#20+#21 + launch item L5 ≈ ~12-22h focused work. Doc-tier doc-only push from `sprint-4-doc-hygiene-close-out` chat 2026-05-30 reconciles gt_v1_scope.md + ROADMAP §1/§5/§6. **Reconciliation note:** prior ROADMAP §1 + §6 entries from `sprint-3-ship-b-b4-polish` chat predicted "Sprint 4 next: Phase 2.5/2.6" — superseded by canonical Sprint 4 in gt_v1_scope.md §4 (Established plants + FAB cohesion); Phase 2.5/2.6 stay in CAN bucket per gt_v1_scope.md §2.3 (post-v1; cost-scaling optimization, doesn't block any §1 axis at trusted-circle dogfood scale).
- **2026-05-29 Sprint 3 Ship B B4 — polish + REDESIGN doc Zones→Groups rename + ICON_MAP gap fix (`sprint-3-ship-b-b4-polish` chat) — `feat/care-tab-c4-instance` branch** — `feat(groups): B4 polish — manage tooltip + ChevronUp/Delete icons + REDESIGN doc Groups rename + ROADMAP catch-up`. Closes Sprint 3 Ship B. 4 items in one commit: (1) first-visit tooltip on GroupTabs Manage button — localStorage-gated `gt-groups-manage-tooltip-seen` flag, copy "Tap Manage to create your own groups" (Walter-plain), dismisses on Close-X OR Manage tap, `bg-neutral-900 text-white text-xs` callout with caret (new chrome-overlay primitive — first peer in codebase, anchor-able for future first-visit coachmarks); (2) [styleDictionary.tsx](src/lib/styleDictionary.tsx) `ICON_MAP.ChevronUp` (polyline mirrors existing ChevronDown convention) + `ICON_MAP.Delete` (semantic alias of TrashIcon, follows Pencil↔Edit / Trash↔Trash2 aliasing precedent) added; ManageGroupsModal reorder-up button now uses ChevronUp cleanly (drops B3-era rotate-180 workaround at [ManageGroupsModal.tsx:296](src/components/ManageGroupsModal.tsx:296)); (3) [REDESIGN_2026-05-18_add-entry-organization.md](docs/REDESIGN_2026-05-18_add-entry-organization.md) Zones→Groups sweep with dated stamp at top — §2 vocabulary, §3.4 title + body, §3.9 lifecycle, §3.10 migration, §6 Phase 2 list; USDA-hardiness-zone references (Zone 10b, hardiness zone range, zone-aware timing) preserved; proper-noun group NAMES ("Garden", "Permanent") preserved; original §3.4 "On the word 'Zones'" rationale replaced with the 2026-05-29 rename rationale; (4) ROADMAP §1/§5/§6 catch-up (B1+B2+B3+B4 entries + Sprint 4 focus shift). **DEFERRED:** Perennial-pill keep/drop on unified Garden cards — awaiting Syd dogfood of mixed annuals+perennials feel (post-dogfood micro-ship). 4-pass plan-audit terminated clean iter 1 (Pass 1 factual incl. localStorage SSR-safety + ICON_MAP additive verification; Pass 2 concerns hunt — tooltip flagged as new primitive + all 5 personas pass; Pass 3 sibling sweep — no tooltip peer exists, aliasing precedent established for ChevronUp/Delete, no collision with existing exports; Pass 4 lock hygiene clean — VISION §10/§11 untouched, REDESIGN doc rename per CLAUDE.md authority precedence). 7 files (`styleDictionary.tsx` + `ManageGroupsModal.tsx` + `GroupTabs.tsx` + `REDESIGN_2026-05-18_add-entry-organization.md` + `ROADMAP.md`). Tests pass; build clean. **NO Preview MCP** per chat HARD RULE + CLAUDE.md lock 2026-05-27. Syd dogfood path: clear localStorage; navigate to /garden; confirm tooltip below Manage with "Tap Manage to create your own groups"; tap Close-X — dismisses, doesn't reappear; clear again, tap Manage instead — tooltip auto-dismisses; inside Manage Groups, confirm reorder ↑ renders a proper up-chevron. Note: ROADMAP §5 cap-overflow (currently ~24 entries above the "Older entries archived" line vs §7 ~10-entry cap) is pre-existing drift, not B4 territory; surfaced for a future doc-hygiene chat.
- **2026-05-29 Sprint 3 Ship B B3 — Manage Groups modal + AddPlantModal autocomplete — `cb6f050`** — `feat(groups): B3 Manage Groups modal + AddPlantModal group autocomplete`. CRUD UI for user-defined Groups + new-plant group assignment at add-time. ManageGroupsModal: AddPlantModal-shape modal shell (`useFocusTrap` + `useEscapeKey` + `useBodyScrollLock`); Create (name input + Add CTA), Rename (inline editable name + Save when dirty), Delete (inline Yes/No confirm — B1 helper soft-deletes group + hard-deletes `plant_groups`), Reorder (↑↓ adjacent swap writing explicit positions for ALL groups so future ordering stays deterministic). Entry point: "+ Manage" chrome button at end of GroupTabs row. AddPlantModal gains group autocomplete: type a new group name inline creates it on submit + assigns the new planting (mirrors §3.4 Add-plant-time entry point). Reorder-up uses rotated ChevronDown as workaround (cleaned in B4 via new ChevronUp export). Tests pass; build clean per chat HARD RULE; Syd phone dogfood verifies visual.
- **2026-05-29 Sprint 3 Ship B B2 — unified Garden tab + GroupTabs primitive — `04ef744`** — `feat(garden): B2 unified Garden tab — delete Active/My Plants split, add GroupTabs`. Active Garden + My Plants pages merged into a single `/garden` tab; the prior dual-tab toggle is removed in favor of a `<GroupTabs>` row of user-defined Group pills (`emerald-500 STATE token` per VISION §8 emerald-token split; sibling primitive to Vault sub-tab). "All" pill default; per-group filter is client-side via `instance.groups[]` membership. GroupTabs owns the selected-group state externally + emits change callbacks (the parent `/garden/page.tsx` owns it). Sticky header treatment same as Vault sub-tab. Tests pass; build clean; Syd phone dogfood verifies visual.
- **2026-05-29 Sprint 3 Ship B B1 — schema + data layer for user-defined Groups — `f5c7c76`** — `feat(groups): B1 schema + data layer for user-defined Groups`. Schema: new `groups` table (user_id, name, position, soft-delete flag) + new `plant_groups` junction (grow_instance_id ↔ group_id). Data layer: `fetchUserGroups` + `fetchAllUserGrowInstances` + `createGroup` + `renameGroup` + `deleteGroup` + `updateGroupPositions` helpers in [lib/groups.ts](src/lib/groups.ts). Delete is soft on `groups` + hard on `plant_groups` (group history preserved; membership rows drop cleanly). Position is `NULLS LAST`, then oldest first (deterministic ordering even before user reorders). Types: new `Group` + `PlantGroup` in [types/garden.ts](src/types/garden.ts). Migration additive idempotent; applied to remote. Tests pass; build clean.
- **2026-05-28 P0a egress stopgap — skip authenticated e2e specs in CI** — `stopgap(ci): skip authenticated e2e specs in CI to stop prod Supabase egress`. 1-line change to [playwright.config.ts:14](playwright.config.ts:14): `hasAuthCreds` now also requires `!process.env.CI`, so the `setup` + `chromium-authenticated` projects are omitted from the projects array entirely in CI. No more `auth.setup.ts` login round-trip, no more `*.authenticated.spec.ts` Supabase API hits per push. Local dev unchanged (authenticated specs still run when `E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD` set in `.env.local`). Stops the ~60+ MB/day egress before Free-plan monthly quota trips. Picked Option A over the workflow-secrets drop because it's truly 1 line (vs 2), self-documents the egress reason via comment, and provides defense-in-depth (if secrets get restored later for unrelated reasons, CI still won't run authenticated specs). 4-pass plan-audit clean iter 1 (Pass 1: `process.env.CI` already used 5× in this file, pattern established; Pass 2: only meaningful concern is local-coverage preservation which is intact; Pass 3: no sibling CI workflows touch Supabase auth flows; Pass 4: no lock conflicts). Tests 444/444; build clean. **P0b follow-up (canonical fix): create a second Supabase project for CI/staging** so authenticated specs can return to CI without burning prod egress. Recommended for next ship.
- **2026-05-28 Plant instance Phase A follow-up — un-archive UI (Phase A Q2 closed) — `f29ae24`** — `phase-a-followup(plant-instance): un-archive UI — per-row Restore on vault/history`. Closes the bidirectional state-transition gap flagged by Phase A's Pass 1 trace (no UI surface previously wrote `status='growing'` to an existing archived `grow_instances` row). Per-row "Restore" text-link on archived rows in `vault/history/page.tsx`; confirm dialog mirrors GrowInstanceModal Archive precedent (canonical `border-teal-gus/40 text-teal-gus hover:bg-teal-gus/10` Cancel + `bg-emerald-600 hover:bg-emerald-700` Confirm tokens per FAB-form submit saga lock); optimistic `setGrows` on success (row's status flips in place, stays visible in history); `useToast` feedback ("Restored" / "Couldn't restore — please try again"). Mutation: `UPDATE grow_instances SET status='growing', ended_at=null, end_reason=null WHERE id=? AND user_id=?` — clears all 3 archive-set fields to null, resilient to both archive shapes (GrowInstanceModal sets `end_reason='archived'`; ActiveGardenView/MyPlantsView bulk-end don't). Conventions applied: Title Case "Restore" button + "Restore to Growing?" heading (VISION §8 casing), emerald-600 CTA (§8 emerald token split — reversible state change, not red/destructive), `inline-flex min-h-[44px] items-center` wrapper on the text-link for Walter persona tap-area guarantee (Pass 2 persona-walk finding applied as plan revision). 4-pass plan-audit terminated clean iter 1 (Pass 2 ran twice — once on initial render plan, once on Walter-tap-area-revised render). Pass 1 bidirectional state-transition matrix complete — both directions (Growing→Archived in Phase A + Archived→Growing this ship) now have UI surface + backend write. 2 Rule A asks shipped with recommended defaults (Q1 affordance text-link vs pill button; Q2 heading "Restore to Growing?" vs "Restore This Plant?") — both one-line edits to flip if user prefers alternatives. Pass 3 ADJACENT logged: GrowInstanceModal symmetric Restore (separate ship, out of scope per kickoff lock to vault/history surface); ActiveGardenView pre-existing missing `end_reason` field on bulk-end (separate cohesion pass). 1 file (`src/app/vault/history/page.tsx`), +75/-3. Tests 444/444; build clean. **NO Preview MCP** per chat HARD RULE locked 2026-05-27. Concurrent-fetch 0/0 pre-push. Plan + 4-pass audit at `.claude/plans/plant_instance_phase_a_followup_unarchive.md`. **Syd dogfood path:** (a) archive a plant via GrowInstanceModal (Archive button → emerald-600 Confirm); (b) navigate to `/vault/history`; (c) confirm Archived row shows neutral pill "Archived" + emerald-600 "Restore" text-link next to it; (d) tap Restore; (e) confirm dialog Cancel teal-gus + Confirm emerald-600; (f) tap Confirm; (g) confirm toast "Restored" + row's pill flips to emerald "Growing" in place (optimistic); (h) navigate to Active Garden; (i) confirm the plant reappears (since Phase A's `.eq("status", "growing")` filter now matches).
- **2026-05-28 Plant instance redesign Phase A — status enum collapse + AGE/dates derivation + auto-Sow drop (`plant_instance_phase_a` chat — respawned from `local_988678e1`) — `67a6344`** — `phase-a(plant-instance): collapse status enum to growing|archived + derived AGE/dates`. First ship in a 3-phase plant-instance-redesign initiative. **Migration:** `grow_instances.status` enum collapsed from 5-state (`pending|growing|harvested|dead|archived`) to 2-state (`growing|archived`) via additive idempotent migration `20260528120000_grow_instances_status_collapse.sql` with branched perennial preservation (`harvested + is_permanent_planting=true → growing` keeps perennial fidelity for Aria/Maya; `harvested + annual/null → archived`; `dead → archived`). **GrowInstanceModal core:** new `firstPlantedDate` + `lastHarvestedDate` helpers derive display from journal evidence (planting/harvest entries) with sown_date fallback; AGE source switched from static sown_date to derived (fixes today's "Not yet planted" + "Growing" header contradiction); "Date Planted" row replaced with "First Planted"; "Last Harvested" row added (em-dash via existing formatShortDate); STATUS pill removes `capitalize` CSS workaround, adds `statusLabel` helper for explicit Title Case "Growing" / "Archived"; statusColors map collapsed to 2-state with default fallback preserved. **Archive confirm dialog (Q1=A):** Cancel aligned to canonical `border-teal-gus/40 text-teal-gus hover:bg-teal-gus/10` FAB-form submit saga token; Confirm flipped from `bg-red-600` (false destructive semantic) to `bg-emerald-600 hover:bg-emerald-700` per VISION §8 emerald CTA token — Archive is reversible state change, not destructive. **Drop misleading auto-Sow task creation across 5 add-plant sites** (AddPlantModal x2, PlantingForm, VaultPageContent, review-import) — Sow tasks were being created + auto-completed even on catch-up tracking, misrepresenting behavior in task log. Harvest tasks preserved as forward-looking reminders; `handleConfirmSchedule` (user-scheduled future Sow) + `completeSowTask` (user marks Sow done) preserved verbatim. **12 sibling sites collapsed from 5-state to 2-state references via mid-execution sweep extension** beyond plan's enumerated 9 — settings/developer/page.tsx, AddPlantManualModal.tsx, BatchLogSheet.tsx (pending→growing transplant transition removed), NewTaskModal.tsx, PacketVaultView.tsx, SeedVaultView.tsx, vault/[id]/page.tsx + VaultProfilePlantingsTab.tsx + useVaultPacketHandlers.ts, generateCareTasks.ts, revertProfileStatus.ts. **VISION §11 HarvestModal parked entry re-framed (Q4):** 2-state enum supersedes auto-end-after-harvest model; recommended direction revised to defer indefinitely (manual-Archive at season end becomes canonical). **Q2 deferred:** Archived → Growing un-archive UI — Pass 1 bidirectional state-transition trace flagged the gap; Phase A scope guard was forward-only; un-archive ships in follow-up chat in `vault/history/page.tsx` per-row. **Phase B/C deferred:** visual redesign (hero size, header layout, scroll behavior, back arrow) + Care tab + year-over-year season timeline + dormancy display + planning-flow Sow tasks. **4-pass plan-audit terminated clean** with revision applied at Pass 2 (perennial-mapping refinement on migration SQL) + re-run on revised SQL clean. Plan + 4-pass audit at `.claude/plans/plant_instance_phase_a.md`. 22 files changed (+540/-89: 1 SQL migration + 1 type narrowing + 19 code files + 1 doc VISION §11 + 1 plan file). Tests 444/444; build clean. **NO Preview MCP** per chat HARD RULE locked 2026-05-27 (tests + build + git diff sufficient; Syd dogfood verifies visual). Migration applied to remote — verified via `supabase migration list --linked` Local|Remote columns match. **Dogfood path:** (a) open Growing plant → confirm AGE no longer reads "Not yet planted" + "Growing" together; (b) confirm First Planted + Last Harvested rows visible with em-dash if no harvest yet; (c) confirm STATUS pill Title Case "Growing" / "Archived"; (d) tap Archive → dialog Cancel teal-gus + Confirm emerald-600 (no longer red); (e) vault/history shows Archived plants with Title Case label + neutral pill; (f) Active Garden no longer shows Archived; (g) adding new plant doesn't create phantom Sow task. **Chat-shape note:** prior chat `local_988678e1` hung at `supabase db push` invocation; respawn chat verified migration was actually applied successfully on remote (CLI either silently succeeded then hung waiting for input or call exceeded chat tolerance) + completed commits/push from pre-stated state.
- **2026-05-28 Inventory polish bundle — 6 items + VISION §8 discovery-vs-lookup sort framework lock (`inventory-polish-bundle-5items` chat) — `a51fbd7`** — `polish(inventory): bundle 6 items + lock discovery-vs-lookup sort framework (VISION §8)`. Closes Syd's 2026-05-27 dogfood batch after `fb5d9a0` gallery card standardization. 6 items + framework doc lock in one cohesive ship (Q3=A): (1) default-view-gallery across 5 surfaces (Library, Packets, Shed, Active Garden default-flip; My Plants already gallery); (2a) Packets default sort = variety asc + OOS-bottom tiebreaker when sortBy === "variety" regardless of direction (Q2=A); (2b) Shed default sort = name asc (no OOS — supply_profiles has no stock column); (3) gallery card top-anchoring (mt-auto removed across 3 surfaces — refines fb5d9a0 lock); (4) owner-pill foreign-only across 4 inventory surfaces with hide-own gate (`user.id !== currentUserId`); (5) Library list view + binary gallery↔list toggle matching 4 peer surfaces (Q1=A replaces orphan `"condensed"` mode; persisted-condensed users auto-migrate to gallery). **VISION §8 framework lock:** "Default sort by use case (discovery vs lookup)" — discovery surfaces (Library, Active Garden, My Plants) = most-recent first; lookup surfaces (Packets, Shed) = alphabetical asc; lookup + OOS-equivalent → OOS-bottom regardless of direction. CLAUDE.md quick-lookup callout added (same pattern as other 2026-05-27 convention locks). **Pass 1+2 re-audit run mid-Phase-1 for Shed extension amendment** (Syd articulated framework mid-build); two Pass 3 sibling-sweep misses caught + applied inline during execution (ActiveGardenView list-view OwnerBadge site + SeedVaultView's 2 OwnerBadge sites). 13 files, +140/-112. Tests 444/444; build clean. **NO Preview MCP** per HARD RULE; Syd dogfood verifies. Concurrent-fetch 0/0 pre-push. Plan at `.claude/plans/inventory_polish_bundle_5items.md`.
- **2026-05-28 Grow-now Phase 2 — per-profile zone-window AI enrichment (`grow-now-phase-2-zone-enrichment` chat) — `dcfdbe4`** — `feat(enrich): Phase 2 — per-profile zone-window via AI (BUGS Post-Launch #1 capability gap)`. Closes the capability gap behind the Phase 1 honest-framing stopgap (feb2e32, 2026-05-27). `/api/seed/enrich-from-name` now reads `user_settings.planting_zone` server-side and threads it to a 5th optional `userZone` param on `researchVariety()` in `src/lib/researchVariety.ts`. When set, an appended `zoneClause` instructs the Gemini prompt to bias `planting_window` to that USDA zone using Jan-Dec 3-letter month abbreviations (preserving compatibility with the existing `plantingWindow.ts:getDefaultSowMonthsForZone10b` parser). Library-cache skipped for non-10b users (`global_plant_library` lacks a zone column); 10b + unset-zone users see UNCHANGED behavior. Response gains `zoneUsed` field; 3 new `logEvent("enrich", ...)` breadcrumbs (`zone_requested` / `zone_window_generated` / `zone_fallback`) flow into `/settings/developer/debug-log`. 4 caller modals (AddVarietyModal, AddPlantModal, QuickAddSeed, PlantingForm) wire `useUserPlantingZone()` + pass `userZone` option (client-side for logging only; server is authoritative). **Architecture decision:** server-side zone-read is the single source of truth; client-side `userZone` is observability-only. Why: prevents client/server mismatch; `fill-blanks-for-profile` transparently benefits (its internal server-to-server fetch of `enrich-from-name` reads zone the same way); backfill scripts keep their non-user context. **Behavior matrix:** Walter (5a) → zone-keyed window ("Indoor sow Feb-Mar, transplant May-Jun"); Sydney (10b) UNCHANGED (library serves); Aria (no zone) UNCHANGED (generic prompt); Maya (whatever zone) → accurate per-zone. **4-pass plan-audit terminated clean iter 1:** Pass 1 factual (every cite verified incl. `extract/route.ts:171` re-export confirmation so surgical edit point covers all 3 production callers + admin scripts pass `undefined` for backward compat) + bidirectional state-transition matrix SKIP-with-reason (stateless background fetch — no modal/menu lifecycle); Pass 2 concerns hunt + persona walk all 5 pass + cohesion-by-aggregation anchors named for every micro-decision (logEvent format ↔ existing scope pattern; response field shape ↔ snake/camel mix; prompt phrasing as named new pattern); Pass 3 sibling sweep across 3 researchVariety callers + vocab-breadth grep `zone|window|usda|hardiness|climate|sow|planting`; Pass 4 lock hygiene clean (VISION §10 don't-touch + §11 Phase 3 multi-zone Schedule rebuild explicitly deferred). 9 files, +218/-5: src/lib/researchVariety.ts (+9/-1), src/app/api/seed/enrich-from-name/route.ts (+32/-1), src/lib/enrichProfileFromName.ts (+24/-0), 4 caller modals (each +3), AddPlantModal.test.tsx (+3 — mock useUserPlantingZone), enrich-from-name/route.test.ts (+143 — new `makeSupabaseWithZone` per-table mock factory + 6 zone-aware tests). Tests 444/444 (438 → +6); build clean. **NO Preview MCP** per chat HARD RULE (data-layer change, not browser-observable). Concurrent-fetch validation 0/0 pre-commit. Plan + audit at `.claude/plans/grow_now_phase_2_zone_enrichment.md` (full 4-pass log inline). BUGS Post-Launch #1 annotated with Phase-2-capability closure; ticket remains open until Phase 3 (multi-zone cross-profile aggregate-schedule view) closes. Out-of-scope (parked): Phase 3 Schedule rebuild stays parked; vendor-URL extraction at `extract/route.ts:441` (zone-agnostic); backfill scripts; schema changes (reuses existing `planting_window text` column — zero migrations).
- **2026-05-27 Vault Propagate / Save Seeds contextual empty-state (Syd dogfood gap) — `39d5faa`** — `feat(vault-profile): contextual seed-propagation note for non-seed-grown varieties`. Closes Syd's report that Sedum donkey tail / grafted fruit trees / sterile hybrids read as ambiguously blank on Vault profile because seed-related rows in How to Grow rendered "—" with no explanation. New `plant_profiles.seed_propagation_context` TEXT column (additive migration); Gemini prompt extended to ALWAYS populate one sentence covering 4 cases: (1) doesn't grow from seed — propagate from cuttings/division/runners/offsets; (2) grows from seed but high variability — propagate by grafting/cuttings for true-to-type; (3) sterile/won't fruit — propagated by grafting; (4) standard seed-grown — empty string (no caveat). UI renders the note in 2 sites: italic `text-sm text-neutral-600` at top of "Propagate / Save Seeds" card body AND as footer in "How to Grow" card when all care/growing rows are em-dash (where ambiguity is first read). Empty-cell em-dash preserved on structured rows — note explains rather than replaces. **New info-note primitive locked in VISION §8** (separate from FormError red+italic error semantic + separate from helper-text field-scoped — calm explanation register, no callout box / icon / background fill). **Lazy backfill** — existing profiles stay NULL until ✨ Fill Blanks per profile; no token surprise. 9 source files (researchVariety prompt + ResearchVarietyResult type + enrich-from-name route type + fill-blanks-for-profile route SELECT/missing-check/parse/write + enrichProfileFromName helper + fillBlanksCache.ts type/write paths + VaultProfileAboutTab.tsx UI/2 sites + PlantProfile type + VISION.md §8 doc) + 1 new migration (`20260527120000_plant_profiles_seed_propagation_context.sql`) + 1 renamed migration (`20250330000000` → `20250330000001_user_settings_onboarding_complete.sql` per documented U24-era orphan-collision cleanup in CLAUDE.md SQL migrations subsection). **3 migrations applied together via `db push --include-all`:** orphan rename + parked `20260518000000_admin_feedback_inbox.sql` (unapplied on remote since `bc253be` 2026-05-18 per CLI tracking; idempotent re-apply via `CREATE TABLE IF NOT EXISTS` + `CREATE OR REPLACE FUNCTION` safe regardless of Dashboard-applied state) + new seed_propagation_context column. **All 5 personas pass** Pass 2 walk (Maya — accurate per-variety, AI-generated > templated; Sydney — encyclopedia feels knowledgeable; Walter — plain language iPad-readable; Aria — most of her houseplants propagate from cuttings, note IS the value; Sam — null-state quietly absent on Day-1 profiles). 4-pass plan-audit terminated clean iter 1 (Pass 1 factual / Pass 2 concerns + persona walk + cohesion-by-aggregation italic anchored to existing FormError italic pattern via different color register / Pass 3 sibling sweep — no existing info-note primitive → new primitive documented / Pass 4 lock hygiene clean — §10 don't-touch + §11 parked untouched). Tests 438/438; build clean. Preview MCP auth-blocked at `/vault/[id]` per documented condition; user phone-verifies on prod (open Sedum donkey tail or any cuttings-propagated profile, tap ✨ Fill Blanks, confirm contextual note replaces blank ambiguity AND a normal seed-grown variety like tomato still renders cleanly with no spurious note). Concurrent-fetch validation 0/0 pre-push.
- **2026-05-27 Plant Profile card inventory-display polish (Syd dogfood re-flag) — `9ab4dab`** — `polish(vault): remove inventory dot + packet count from Plant Profile cards`. Closes Syd's 2026-05-26 dogfood re-flagged 2026-05-27 — Plant Profile cards in Vault → Plant Profiles tab showed both a color-coded inventory dot AND a packet count number alongside the green "actively growing" outline, creating mixed signals ("0 inventory" + green outline read as contradictory). Card title row simplifies from `<name + HealthDot + count>` to `<name>` only across both grid card variants (photo + condensed). Now-orphaned `HealthDot` + `getHealthColor` helpers deleted per CLAUDE.md no-dead-code rule. `getCardBorderClass` untouched — green active-growing outline preserved verbatim. `packet_count` data still flows through filter / sort / out-of-stock dashed-border logic; this is purely a display removal. Packet inventory display stays scoped to the Seed Packets tab. 1 file ([src/components/SeedVaultView.tsx](src/components/SeedVaultView.tsx)), +1/-29. Tests 438/438; build clean. **Pass 3 ADJACENT finding logged:** photo-card variant doesn't currently apply `getCardBorderClass` (hardcoded `border border-black/5 hover:border-emerald-500/40` instead) — only condensed variant renders active green outline via helper. Pre-existing asymmetry; default `gridDisplayStyle="condensed"` masks it. Separate polish item. Pass 1-4 audit clean iter 1 (XS scope, inline plan). Preview MCP auth-blocked at `/vault` per documented condition; user phone-verifies on prod. Concurrent-fetch 0/0 pre-push.
- **2026-05-27 Empty-cell display convention lock + app-wide sweep (`gt-empty-cell-convention` chat) — 2 commits `29f37a1` + `0bc4342`** — `docs(empty-cell): GT empty-cell display convention — em dash "—" lock + Phase A doc` + `feat(empty-cell): app-wide sweep — "--" → "—" per VISION.md §8 lock`. Locks em dash "—" (U+2014, single character) as canonical empty-state symbol for data cells in profile / table / detail views. **Phase A doc lock (`29f37a1`):** VISION.md §8 new "Empty-cell display convention" subsection (after Casing) + CLAUDE.md "Empty-cell convention (locked 2026-05-27)" callout (after Casing convention) + ROADMAP §6 new top entry + §1 lead-line refresh per §7 2-paragraph cap. **Phase C sweep (`0bc4342`):** 20 line-edits across 7 files — settings/developer (1 — archived planting Ended date) + settings/vendors (3 — formatAvg + 2 rating spans) + vault/[id]/VaultProfilePacketsTab (1 — vendor_name) + vault/[id]/page (9 — yieldLabel + 6 About care fields + Days to Maturity ternary) + vault/history (3 — location + plantings summary + harvest_count) + PacketVaultView (2 — vendor_name + purchase_date) + StarRating (1 — readonly null fallback, propagates to 7 consumer files). **Cohesion-by-aggregation drift resolved:** existing canonical "—" anchor at scraper-audit (5 sites) + ActiveGardenView:372 (1 site) brought into alignment with older "--" peers across Vault profile About / settings / vault history / packet table / star rating. **Preserved:** all 28 "Unknown" sentinel in AI extraction + filter chains (semantic state); 2 `<option value="">None</option>` user-choice selectors; en-dash range separators in plantDefaults.ts (U+2013, different convention). **ADJACENT parked:** QuickAddSeed.tsx 4 sites + shopping-list/page.tsx 1 site use `|| "Unknown"` in sentence-fragment rendering — em-dash doesn't fit; logged for future polish with better empty-vendor copy. **All 5 personas pass** Pass 2 walk (Maya — standard table convention; Sydney — cohesion across profile views; Walter — universally-recognized symbol; Aria + Sam — less visual noise on low-data states). 4-pass plan-audit terminated CLEAN on iteration 1 (no amendments). Tests 438/438; build clean (exit 0); Preview MCP dev-server console error-free post-HMR; affected surfaces auth-blocked per documented `.env.local`-absent + AuthGuard-redirect condition; user phone-verifies on prod. Concurrent-fetch validation #15 (Phase C push) 0/0 clean. Plan file at `.claude/plans/gt_empty_cell_convention.md` (committed). Same shape as same-day casing-policy sweep `3ee1063` + `5b5617c` + `cb6a203`.
- **2026-05-26 FAB-form submit pattern standardization saga (`continue-from-prev-chat-fab-form-submit-saga` chat) — 5 commits `fc1463d` + `36d0ffb` + `003e3b4` + `8d6562f` + `ba9319f`** — `refactor(forms): close VISION §11 Save/Cancel consistency + AddPlantModal C1 across 10 FAB-tree forms`. Closes the FAB-tree polish saga that started with Path Y A-D (2026-05-19) and ran through the modal-consistency cluster (2026-05-26 commits `488e7f3` → `aa4c116`). **Commit 1 Foundation (`fc1463d`)** — UniversalAddMenu panel restructure from `p-6 overflow-y-auto` single-scroll to `p-0 flex flex-col overflow-hidden`; per-screen header / body / footer triplet. Enables fixed-bar footer for every in-menu form. **Commit 2a (`36d0ffb`)** — TaskForm + JournalEntryForm migrated to AddPlantModal template (Cancel-left + Save-right + border-t + bg-emerald-600); NewTaskModal + QuickLogModal standalone shells flex-col-converted; JournalEntryForm disable rule = ≥1 of {Quick action≠default, note non-empty, photo, linked plant}. **Commit 2b (`003e3b4`)** — SeedPacketForm + SupplyForm same template; HTML5 `form="<id>"` attribute used on footer Save so split layout (form body in scroll area, Save button in footer) preserves Enter-key submit semantics across multi-mode rendering; Save-for-later (new-mode only) moved inline to body next to its helper text, disable tightened to `submitting || !plantName.trim()`. **Commit 3 (`8d6562f`)** — BatchAddSeed + BatchAddSupply + PurchaseOrderImport standalone modals same template; per-step footer (capture: Cancel + "Review pending (N)" / "Extract & Review (N)"; extracting: Cancel only, primary hidden in-flight; review: Cancel + "Load Plant Profile Picture"; success: single Done button); BatchAddSeed gains header back-arrow on review step (→ setStep("capture")) preserving previous in-body Back-button functionality after migration; Close-X dropped from all 3 modal headers (cohesion with TaskForm/JournalEntryForm/AddPlantModal); PurchaseOrderImport amendment from plan: single-step modal (review happens on separate page after redirect), footer = Cancel + Extract, Extract disabled until `!previewUrl || isExtracting`. **Commit 4 (`ba9319f`)** — AddPlantModal C1 closed via `embedded?: boolean` prop; useFocusTrap + useEscapeKey + useBodyScrollLock all gated on `!embedded` (menu owns these); enrichmentFailed secondary modal stays as-is (already z-[60] fixed inset-0); UniversalAddMenuScreen union grows `"add-plant-manual"`; Manual entry chip handler changed from `onClose() + onAddPlantManual()` to `setScreenDirection("forward") + setScreen("add-plant-manual")` (slides in-place matching the 4 other FAB chip sub-screens); 5 page mounts drop `onAddPlantManual` prop wiring + unused `openPlant` destructure (garden/page.tsx keeps `openPlant` for empty-state). PlantingForm bundled verify-only tweaks: `border-black/10` → `border-neutral-200`, `bg-emerald-500 hover:bg-emerald-600` → `bg-emerald-600 hover:bg-emerald-700` on Confirm Planting button (no shell change). **VISION §11 "Save / Cancel button consistency across modals" parked decision CLOSES** + `.cursor/plans/fab_addplantmodal_extraction.plan.md` parked plan CLOSES + Save-button color `bg-emerald` (= emerald-500) → `bg-emerald-600 hover:bg-emerald-700` canonicalized across 9 Save buttons in 5 forms. **Amendment captured mid-execute:** plan-of-record originally specified "Back + Save" per-step footers; amended to "Cancel + Save" per cohesion-by-aggregation with 2a anchors; re-audit Pass 1-4 clean. **Concurrent-chat fetch gate validation #9-#13** (5 push points, all 0/0 clean). **ADJACENT findings parked for future cohesion pass** (non-FAB-tree edit/utility modals with similar submit-shape): AddPlantManualModal (Vault Packets tab), HarvestModal, EditJournalModal, EditPacketModal, AddItemModal, FeedbackModal, InviteMemberModal. **Tests 438/438 across all 5 commits; builds clean.** Preview MCP at 412×915: dev server boots clean, no new console errors from diff, AuthGuard-blocked from FAB-tree paths (no auth session) — per CLAUDE.md "auth-blocked → ship with note"; real-device verification needed by user. Plan file at `.claude/plans/fab_form_submit_standardization.plan.md`.
- **2026-05-25 Debug-log 8-layer instrumentation (`continue-from-prev-chat-stalled-logger-coverage` chat) — `f79a8aa`** — `feat(debug-log): 8-layer in-app instrumentation for diagnostic breadcrumbs`. Additive event logging across 8 layers (helper, buffer cap 50→500, boot/auth/SW, network/replay, navigation, onboarding, supabase mutations, error boundary + error toasts + 6 form envelopes) into existing sessionStorage debug buffer. Privacy contract: payload SHAPE only — counts/ids/table/op/ms/error.message; never user content. 6 canonical FAB-flow form envelopes (add_plant / add_seed / add_supply / add_journal / add_task / harvest). 19 files (2 NEW, 17 EDITED). Tests 434 → 438 (+4 from `debugLog.test.ts`). Build clean. Pass 1-4 audit terminated clean; CONCERN logged (codebase console.error format split bracket vs colon — new `logEvent` joins bracket family per handoff substance, peers unchanged; future cohesion pass parked in §4). ADJACENT items (BatchAddSeed/BatchAddSupply/edit-flow forms/BatchLogSheet) parked in §4. **Plan rebuilt in-chat** after previous chat's plan file was lost (stalled on AskUserQuestion before `f4c2075` shipped the AskUserQuestion-forbidden rule). Plan file at `.claude/plans/gt-debug-log-instrumentation.md`. Concurrent-fetch 0/0 pre-commit. Dogfood verification path written in commit body + handoff.
- **2026-05-24 FAB polish bundle (`intelligent-pare-f7abd4` chat continuation) — `006dd69`** — `fix(add-menu): icon-style unification on Seed + Supply choosers, header casing parity, Scan PO subtitle fix`. Polish ship from Syd's phone-dogfood of the prior sub-form transition bundle. 3 items closed: (1) Icon-style unification on Seed + Supply choose-step buttons — both submenus had platform emoji icons (📝/📷/📸/🌐/🧾) in white/grey square wrappers, breaking the canonical green-circle outline treatment used by Main FAB + Add Plant. Scope expanded mid-execution per Syd's "audit EVERY FAB menu and submenu" directive — found Seed had same broken shape as Supply (both predate the canonical pattern). New `ICON_MAP.Link` icon added to styleDictionary matching existing stroke-1.2/24×24/monochrome convention (classic chain-link visual); other 3 icons (ManualEntry / PhotoImport / PurchaseOrder) reuse existing entries exact-verbatim from Add Plant submenu. (2) Header casing standardization — all form sub-component headers were Title Case (pre-Path Y); now sentence case to match Path Y's "Add plant" + main menu labels. Per Q1 = H2 lock: lowercase nouns preserving the existing words (not mirroring menu labels verbatim); "Quick log" stays distinct from "Add journal." "Active Garden" stays Title Case (proper noun). 5 forms touched (Seed / Supply / Task / Journal / AddPlant headers). (3) Add Plant > Scan Purchase Order subtitle copy fix — was "Screenshot of cart or order with seeds" (reused from Seed flow verbatim, never updated for plant context); now "with plants." Inline doc comment at UniversalAddMenu.tsx:29 also fixed. Test fix: AddPlantModal.test.tsx:48 assertion updated from "Add Plant" to "Add plant" to match heading casing change. 8 files, +36/-24. Tests 439/439; build clean.
- **2026-05-24 sub-form transition bundle Fix A + Fix B (`intelligent-pare-f7abd4` chat) — `1c2d403` + `ba1da34` + `4e9fb25`** — `refactor(add-menu): SeedPacketForm + SupplyForm internal step swaps slide; U28 cross-flow callbacks threaded`. Extends Path Y's slide pattern one level deeper into form sub-component internal steps (SeedPacketForm choose ↔ manual; SupplyForm choose ↔ link ↔ form). New `stepDirection` state in each form mirrors UniversalAddMenu's `screenDirection` — same `animate-submenu-slide-forward` / `animate-submenu-slide-back` keyframes; no new state-machine shape; no new keyframes. All Back arrows + step-changing buttons set direction before setStep, so both forward + back are symmetric by construction. **U28 closed in same bundle** (commit `4e9fb25`): Path Y Commit E (`d49e1ae`) regressed 3 buttons on the menu's Add Seed Packet choose step (Photo Import / Link Import / Purchase Order) by missing the cross-flow callback threading. Sibling-sweep also caught matching regression on SupplyForm choose step (Photo Import + Purchase Order). 6 new optional props on `UniversalAddMenuProps` (4 Seed + 2 Supply); each of the 5 add-button-mounting pages wraps the same callbacks they already pass to standalone modal mounts, but uses `closeAll()` instead of `closeActiveModal()` since the callbacks fire from menu surface. **F1 chosen mid-execution** over F3 (full AddPlantModal extraction for symmetric in/out on the Manual entry path) after honest scope-discovery — AddPlantModal's sticky-flex-col-overflow + secondary nested modal + 1008 LOC + multi-mode complexity made the original §14 "~2-3h" estimate wrong (~4-6h realistic). AddPlantModal Manual entry path stays asymmetric for this ship; deferred to follow-up plan at `.cursor/plans/fab_addplantmodal_extraction.plan.md` (re-open when Syd phone-verifies whether the asymmetry actually grates in dogfood). All other Path Y A-D extracted forms covered. Tests 439/439 across all 3 commits; build clean. Worktree Preview MCP skipped per documented `.env.local`-absent condition. Plan file at `~/.claude/plans/intelligent-pare-f7abd4.md` (§13 + §14 amendment block).
- **2026-05-20 Path Y stack (`intelligent-pare-f7abd4` chat) — `143a07f` revert + `bee5338` + `6652a25` + `b9cb40b` + `c812a5a` + `d49e1ae`** — `refactor(add-menu): U7 corrective Path Y — all 5 add-button picks slide in-place; forms inside menu` — BUGS U7 closed. The menu container stays mounted through the entire FAB flow; forms live INSIDE the menu via 4 extracted named-export form sub-components (`<TaskForm>` from NewTaskModal, `<JournalEntryForm>` from QuickLogModal, `<SupplyForm>` from QuickAddSupply with `screen→step` rename, `<SeedPacketForm>` from QuickAddSeed with `screen→step` rename + preserved `CheckmarkIcon` helper). UniversalAddMenu screen union expanded `"main" | "add-plant"` → adds `"seed" | "shed" | "task" | "journal"`; 4 new branches mirror the existing add-plant branch shape (forward/back slide via `animate-submenu-slide-forward` / `animate-submenu-slide-back`). 4 `onAdd*` props dropped from menu interface. 5 add-button-mounting pages updated (home / garden / calendar / journal / vault page) — drop the 4 `onAdd*` prop wirings + drop `onBackToMenu={backToMenu}` on the 4 standalone-modal mounts (kept on AddPlantModal mount). UniversalAddContext.backToMenu STAYS (used by AddPlantModal — amendment mid-execution after plan §7's "delete `backToMenu`" was wrong). All non-FAB callers preserved verbatim: OnboardingContext.openTaskForOnboarding (Observe Hillside) → NewTaskModal; calendar `?openTask=1` deep-link → NewTaskModal; OnboardingDock CTA + VaultPageContent autoload → QuickAddSeed; journal "+Entry" → QuickLogModal; QuickLog "+ Add New Supply" cross-modal → openShed → QuickAddSupply; garden My Plants empty-state CTA → openPlant → AddPlantModal; vault Plant Again + vault/shed/[id] + ShedView + VaultShedWing untouched. 2nd NewTaskModal at calendar/page.tsx:2066 (edit-task path) keeps its custom local back-to-menu callback (not from context). Form sub-components do NOT call `useBodyScrollLock` (single-source-of-truth — shell or menu owns); focus traps stay in standalone-modal shells (NewTaskModal + QuickLogModal); QuickAddSeed + QuickAddSupply have no focus trap (pre-existing inconsistency, captured as ADJACENT for future cohesion pass). Followed misfire `e7d59d7` (choreographed close-then-open via `fab-menu-exit` keyframe + `isExiting` state + `EXIT_FALLBACK_MS` timeout) which Syd rejected as "still feels close-then-open" — reverted via `143a07f` (pushed standalone to main) before Path Y refactor landed. **Two accepted regressions per Q1 Option B locked at greenlight (minimize complexity vs full-resume wiring):** (a) cross-modal "+ Add New Supply" closes menu and does NOT auto-resume on journal sub-screen after QuickAddSupply closes; (b) pages don't auto-refresh after in-menu successful add. Both noted as acceptable post-ship; user pulls / navigates to see fresh entries. **4-pass plan-audit terminated clean** with 2 mid-execution amendments re-audited inline. Tests 439/439 across all 5 commits; build clean across all 5 commits. Worktree Preview MCP skipped per documented `.env.local`-absent condition. ROADMAP §6 FAB transition standard entry amended same-commit (all 5 picks now classified as submenus). Plan file at `C:\Users\marsh\.claude\plans\intelligent-pare-f7abd4.md`. ~1843 LOC reorganized across 6 src files + 5 page files.
- **2026-05-18 `9bad88f` (`trusting-proskuriakova-8e23c2` chat)** — `feat(empty-state): §3.12 #5 — guiding-not-scolding copy across 17 surfaces` — Per-surface empty-state copy rewrite resolving §3.12 catalog theme #5 (Sam day-1 cliffs + Walter low-tech-comfort jargon). 17 code files / ~22 distinct copy strings / per-surface 3-part frame anchored (purpose-describing title / plain next-step body / verb-led CTA "Add a {noun}"). Surfaces touched: Home (H1 shopping section, H2 tasks section), Vault main (V1 SeedVaultView main + V2 grid/list × newUser/existing variants, V3 PacketVaultView), Vault profile (V4 PlantingsTab × 3 branches, V5 PacketsTab, V6 JournalTab, V7+V8 AboutTab propagation+seed-saving), Vault sub-pages (V9 tags page, V10 history page, V11 shed product usage history), Garden (G1 ActiveGardenView × 2 branches collapsed, G2 MyPlantsView, G3 Photos in GrowInstanceModal, G4 history primary + secondary preserved), Calendar (C1 no-upcoming, C2 no-tasks-on-day), Journal (J1 main with rotating prompts preserved unchanged), Shed (S1 × 2 branches collapsed), Shopping list (SL1). **Walter-plain swaps:** "long-living plants" replaces "permanent plants"/"perennial plants" jargon; drops "FAB," "active batches," "Vault import," "Map your hillside," "Plant from the Seed Vault," "polyculture," "Tap + to..." instructions where the CTA button is already visible; "library" softens "Vault"/"Shed" in body copy. **Aria-light:** "fruit trees, bushes" → "trees, bushes, or anything else you keep year after year" (full outdoor-sweep is theme #2). **`isNewUser` branch collapse** in V1/V2/S1/G1 where the new copy reads correctly for both first-time + returning users (small code-complexity reduction). **J1 day-rotating prompts** preserved unchanged (already on-voice). **VISION §10 empty-by-default LOCK preserved verbatim** — copy refined ON existing empty states; state-default unchanged, OnboardingDock untouched, no notifications/tasks-by-default added. All 5 personas (Maya/Sydney/Walter/Aria/Sam) pass Pass 2 walk. **4-pass plan-audit terminated clean at iteration 2 on Pass 2** (Pass 1 + 3 + 4 iter 1; Pass 2 iter 1 surfaced 7 concerns A-G — CTA-collision risk on G2, V4 branching preservation, `isNewUser` collapse safety, H1↔SL1 mirroring, G4 secondary empty preservation, per-surface persona walk, cohesion-by-aggregation anchors — all resolved inline; iter 2 re-passed clean on revised plan). Pass 3 vocabulary-breadth sibling sweep clean iter 1 across 5 grep families (`empty`/`Empty`, `No \w+ yet`, `haven't`, `first \w+ to`/`Get started`/`Welcome`/`Start by`, audit-time vocab) — BLOCKING aligned (CTA verb "Add a {noun}" cohesion anchor across all 6 EmptyStateCard sites + all raw-card sites), ADJACENT captured to §4 (4 raw-card empty states use `bg-white rounded-xl border ... p-8 text-center` instead of canonical `EmptyStateCard` primitive — belongs to §3.12 theme #7), CONCERN logged (page-label renames "Vault"/"Shed"/"Plantable" remain theme #3 territory; body-level softening only this ship). Pass 4 lock hygiene clean iter 1 (VISION §10 + §11 + ROADMAP §3.12 themes #2/#3/#7/#8 boundaries respected). Tests 405/405; build clean. Preview MCP skipped per documented `.env.local`-absent worktree condition (functionally auth-blocked equivalent — AuthGuard redirects before empty-state surfaces mount). **Concurrent-chat fetch (Phase 2 gate item 9) validation #7:** 0 ahead, 0 behind at plan-lock time (verified pre-commit on the §3.12 #5 ship). Plan file at `C:\Users\marsh\.claude\plans\trusting-proskuriakova-8e23c2.md`. **Doc-trail recovery context:** the original `trusting-proskuriakova-8e23c2` chat experienced ENOSPC during the Phase 5 ROADMAP write that truncated its local `docs/ROADMAP.md` to 0 bytes; code commit `9bad88f` had already landed on origin/main and is unaffected. This separate doc-only recovery commit (`naughty-kalam-54efba` worktree, 2026-05-18) replays the §1/§3.12/§4/§5/§6 doc edits from the intact 26 KB plan file as source of truth.
- **2026-05-17 `bc253be` (`mystifying-lichterman-55e122` chat)** — `feat(feedback): §3.12-tester T4 — admin feedback inbox dev-gated page` — Admin viewer for `user_feedback` rows at `/settings/developer/feedback-inbox`. Closes T1+T2 loop (`e50a667` data → in-app viewer instead of Dashboard SQL Editor). **Q1 security model = Option B** security-definer Postgres function `admin_list_feedback()` + `developer_users` allowlist table; raises 42501 for non-devs; page renders friendly "Not authorized" guidance with paste-able INSERT. **One-time setup:** user pastes `INSERT INTO public.developer_users (user_id) VALUES (auth.uid())` in Dashboard SQL Editor while signed in. Page: list (newest first, 200-cap) + category filter + has-voice/has-debug-log toggles + per-row render of timestamp + email + message + page_url + screenshot + `<audio controls>` + collapsible debug-log textarea + refresh button. All UI primitives anchored to existing patterns. Architecture autonomously locked: developer_users table over hardcoded UUID list / app_metadata / API route; plpgsql + RAISE EXCEPTION over empty rows; public URLs not signed URLs (bucket already public); no new icon; debug log collapsed per-row. Migration `20260518000000_admin_feedback_inbox.sql` additive (CREATE TABLE IF NOT EXISTS + CREATE OR REPLACE FUNCTION + GRANTs); user applies via Dashboard SQL Editor (worktree's supabase CLI not linked). 5 files, +533/-1. Tests 432 → 439 (+7). Lint clean, build clean. **4-pass plan-audit terminated clean at iteration 1** with Pass 2 revision applied mid-audit (function flipped from sql to plpgsql for IF/RAISE; re-passed clean). Pass 3 vocabulary-breadth sibling sweep clean across 8 families. Pass 4 lock hygiene clean. Persona walk: 4/5 pass by exclusion (dev-only surface). Preview MCP skipped per documented condition + doubly auth-blocked. Concurrent-chat fetch (Phase 2 gate item 9) validation #6: 0 ahead, 0 behind at commit time. Plan file at `C:\Users\marsh\.claude\plans\mystifying-lichterman-55e122.md`.
- **2026-05-17 `e50a667` (`vigorous-nightingale-039e05` chat)** — `feat(feedback): §3.12-tester T1 + T2 — voice memo + debug-log opt-in attach` — Bundled ship of two §3.12-tester items. **T1:** new `useVoiceRecorder` hook + visible Record/Stop UI + native `<audio controls>` playback + Re-record button (always visible per T3 Walter-safe lock) + iOS Safari MIME fallback (webm/opus → mp4/aac → default) + 60s auto-stop + plain-language permission-denial recovery. **T2:** opt-in toggle (SettingsSuccessSoundToggle shell, default OFF, ephemeral per-session NOT localStorage) + scrollable read-only content preview when ON + plain-language disclosure copy ("Attaches recent technical messages from your browser. Helps us figure out what went wrong. You can review the text below before sending."). **Migration:** `20260517120000_user_feedback_voice_and_debug_log.sql` adds nullable `voice_path` + `debug_log_text` columns to `user_feedback`; user applies on prod via Supabase Dashboard SQL Editor (standard pattern). Voice blob uploads to existing `journal-photos` bucket — no new RLS migration. **Cohesion:** new `Mic` entry in `ICON_MAP` (not inline SVG); 6 BLOCKING Pass 3 sibling-sweep alignments matched (hook ↔ useDesktopPhotoCapture cleanup; upload signature ↔ 4 sibling sites; toggle ↔ SettingsSuccessSoundToggle; preview ↔ formatEntriesForCopy; etc.). 2 new patterns explicitly documented for future siblings (MIME fallback ordering + privacy-disclosure copy frame). **T3 locks honored verbatim** — feedback button stays public on all 3 sites (zero gating); voice = visible record/stop + 60s + plain-language permission copy + retake always visible + no auto-start; debug-log = default OFF + opt-in + content preview + plain-language disclosure. **Persona walk:** all 5 pass both features; Walter is the most-served by T1 (voice > typing on iPad). 5 files (NEW migration + NEW useVoiceRecorder hook + NEW regression test 146 lines / 27 assertions + EDIT FeedbackModal +132 net + EDIT styleDictionary +14). Tests 405 → 432 (+27). Lint clean (no new warnings from new files). Build clean. Preview MCP skipped per documented `.env.local`-absent worktree condition; user phone-verifies on prod. **Concurrent-chat fetch (Phase 2 gate item 9):** 0 ahead, 0 behind at commit time. Validation #5 of the rule running clean in 2 days. **Auto-mode override flagged:** user shifted to AUTO MODE mid-chat ("code auto after lint/test + self-audit" then clarified "code push still needs explicit greenlight"); CLAUDE.md amendment deferred to a future doc-hygiene chat. 4-pass plan-audit terminated clean at iteration 1 on as-written plan. Plan file at `C:\Users\marsh\.claude\plans\vigorous-nightingale-039e05.md`.
- **2026-05-17 `0c3b655` (`flamboyant-lamport-f5cc74` chat)** — `docs(roadmap): §3.12-tester T3 ✅ locked Option A — public stays as-is` — Doc-only ship; ZERO code change. §3.12-tester T3 (Tools-bar feedback button visibility scope) decision locked: Option A (public, no gating). Three options surfaced (A public / B existing tap-unlock / C new tester allowlist); user picked Recommended Option A via Rule A. **Decision rationale:** app-store distribution locked 2026-05-17 (VISION §1, §3) inverts the calculus — public users are who we want feedback from on launch; gating cuts that off. Feedback ≠ developer tool (§3.12-pre's `4a8b04c` resolved destructive admin actions; feedback is the opposite). Persona walk: 3/5 personas neutral-to-positive on public; 5/5 lose if gated. Sensitive payload pieces (T1 voice, T2 debug log) get gated at attach-time, not at channel-level. **Pass 1+2+3 audit inline** (XS scope, doc-only): Pass 1 verified 3 feedback button sites (AuthGuard:195 loading-shell + AuthGuard:297 post-auth mobile + Sidebar:162 desktop) + auth page shows zero feedback buttons (all inside `{!isAuthPage}` blocks); Pass 2 persona walk on all 3 options + app-store-surface + privacy + downstream T1/T2 scoping; Pass 3 vocabulary-breadth sibling sweep confirmed `useDeveloperUnlock` sole access-gate pattern in repo (no tester/beta/allowlist concept exists). ADJACENT: BUGS U27 unrelated. **Concurrent-chat collision caught + reconciled via Phase 2 gate item 9:** 2 commits (`efe588d` + `42ed0b8`) landed on origin/main during my audit; fast-forward rebase clean; re-audit on rebased state confirmed Pass 1-3 findings still hold. **Validation #4 of the Phase-2-gate-item-9 rule running clean.** §3.12-tester updates: T3 status flipped 🔵 → ✅ + Option A rationale + persona-walk findings; T1 + T2 status entries amended with public-context constraints (Walter/Sam-safe voice UX + debug-log default OFF + opt-in toggle + content preview); §1 lead-line refreshed (demoted `flamboyant-wilson-34fc0c` paragraph to archive note per 2-paragraph cap; inserted new T3-close paragraph); §1 Next session focus refreshed (T3 removed; T1/T2 promoted as next-ready chats); §6 new 2026-05-17 entry. 1 file (`docs/ROADMAP.md`). Tests N/A (no code). Doc-only push tier per CLAUDE.md.
- **2026-05-17 `efe588d` (`distracted-wilson-616865` chat)** — `feat(ui): §3.12 theme #4 — lower swipe-fallback button breakpoint to md: for Walter persona` — 3 calendar surfaces gain visible buttons at iPad-portrait+ alongside swipe; phone-portrait keeps swipe-only per Principle 9 spirit. Code: 3 className edits in [src/app/calendar/page.tsx](src/app/calendar/page.tsx) (5 distinct line-edits: month-nav arrows :976+:985, singleton row buttons wrapper :2442, consolidated overdue Snooze + Complete :2242+:2253+:2254) + 2 in-code comment refresh (consolidated header docstring :2151, CalendarTaskRow swipe comment :2357). Tests: 4 assertion-string flips `lg:` → `md:` in `calendarBatchSelect.regression.test.ts` + 2 describe/comment blocks reworded. Docs: VISION.md §4 Principle 9 worked-example narrowed with persona-accessibility refinement; §11 row-primitive parked-direction footnoted. **Pass 4 lock-hygiene** caught 3 brushed locked decisions (Principle 9, §11 resolution-direction, `3de1c2c`/`cea21e0`/`f77507a` ship shapes); user re-greenlit via Rule A ask (Recommended Option A: `md:` breakpoint matching Vault/Shed anchor). **Pass 3 vocabulary-breadth grep** (rule locked same-day in `flamboyant-wilson`) covered 7 gesture-vocabulary families; found user-prompt's "Garden drag-to-move" + "Journal swipe-delete" do NOT exist in codebase (logged in plan for honesty). **Concurrent-chat collision** caught + reconciled via fast-forward rebase before code edits (`33955b4` + `3396d03` landed mid-execute on origin/main). 4 files, tests 405/405; build clean. Preview MCP visual check at 412×915 + 768×1024 pending pre-push. Plan file at `C:\Users\marsh\.claude\plans\continue-from-prev-chat-lazy-valiant.md`.
- **2026-05-17 `fdc295b`** — `docs: close sharing-purpose chat — 10 decisions locked + PERSONAS.md` — Non-build design chat (`competent-blackwell-5de2a2`) Phase 5 captures shipped. All 6 open-territory items from VISION §11 "Sharing UX / purpose narrowing" resolved + 4 bonus locks (public boundary 🕐 long-term; sharing data model = communication + transfer-at-time; persona system; app-store distribution context). **New file:** `docs/PERSONAS.md` (5 personas with cross-scan table — Maya/Sydney/Walter/Aria/Sam). **VISION.md:** §1 (public-distribution boundary line), §3 (Layer 2 + Layer 3 expanded with group-mode structure), §9 (new public-stream 🕐 row + Level 3 trade row updated with two-shape note), §10 (new no-activity-feed don't-touch), §11 (sharing-UX entry replaced with resolved summary pointing to ROADMAP §6). **ROADMAP.md:** §6 comprehensive entry with all 10 decisions + reasoning + 3 buckets + dependencies, §3.5 trade sub-item expanded with two-shape model, new §3.11 chunk "Trusted-circle infrastructure" 🟣 + §1 lead-line refresh. **CLAUDE.md:** "What this project is" refined to name app-store distribution + plan-audit standard Pass 2 + Pass 3 gain persona-walk subcategory + Required-reading mentions PERSONAS.md. **Concurrent-chat collision (`5ca4bae` + `d61fff0` ecstatic-rubin-5f30c8):** caught + reconciled mid-Phase-5 via stash + pull --rebase + pop stash + surgical conflict resolution on §1 lead-line + §6 2026-05-17 (both ships preserved). 4 files, +294/-11. Tests 404/404 (sanity — no code). Doc-only push tier. Classifier-bypass via `permissions.allow` confirmed working clean (parallel chat's P4 open-verification validated). Plan file at `C:\Users\marsh\.claude\plans\continue-from-prev-chat-frolicking-token.md`.
- **2026-05-17 (doc + config batch — `5ca4bae` + `d61fff0` ecstatic-rubin-5f30c8 chat)** — `docs: U25 ✅ verified on prod + concurrent-chat fetch rule + push-classifier settings allowlist (P2 superseded mid-chat by f620e37 archive policy)` — Single doc + config commit closing 4 purposes. **(P1)** U25 ✅ Verified on prod (`11eaf37`) — user phone-verified pen icon match on My Plants list; BUGS U25 row marks `· ✅ Verified on prod 2026-05-17`; §5 "Verified clean" gains `11eaf37` row. **(P2 superseded mid-chat)** ROADMAP shortening was the original P2 to park as §3.10 R10 — but `f620e37` shipped a better archive policy (§7 + 3 numeric caps) while this chat was in flight. Plan amended mid-Phase-3: rebase via `git stash + git pull --rebase origin main + drop stash + surgical re-apply` (user picked Recommended option via Rule A ask). P2 dropped; P1/P3/P4 re-applied on rebased state per new §7 caps. **(P3)** CLAUDE.md Phase 2 readiness gate gains 9th checklist item: `git fetch origin main` before amending canonical docs — catches concurrent-chat collisions earlier than rebase. **Ironically, the P3 rule I'm shipping would have caught this very collision.** Sibling pattern to Pass 1 cite-by-path. **(P4)** `.claude/settings.json` gains `permissions.allow: ["Bash(git push origin HEAD:main)"]` block + CLAUDE.md push-classifier caveat appended with settings-allowlist-bypass note. Trade-off heard via Rule A: removes runtime backstop on this push form; rule enforcement moves to plan-mode + AskUserQuestion only. **Open verification (testable on this push):** classifier-bypass behavior. 5 files changed (`CLAUDE.md`, `docs/BUGS.md`, `docs/ROADMAP.md`, `docs/VISION.md`, `.claude/settings.json`). Tests 404/404 (sanity — no code). Audit re-runs: pre-rebase 4-pass clean (caught §3.10 D1 stale side-finding); rebase reconciliation captured as Phase 3 amendment. Plan file at `C:\Users\marsh\.claude\plans\new-chat-phone-verify-agile-conway.md`. Doc + config push tier per CLAUDE.md.
- **2026-05-16 (ROADMAP shortening — same chat, doc-only)** — Aggressive archive policy locked + applied: §1 lead-line capped at 2 paragraphs (dropped 5 older dated paragraphs, full content preserved in §6); §5 capped at ~10 entries (dropped 20 older entries, full history in `git log`); §6 capped at 14 days inline with `docs/archive/ROADMAP_log_YYYY-MM.md` destination named (no archive needed yet). New §7 "Archive policy" subsection codifies the rules. ROADMAP dropped from ~621 → ~600 lines (~3.5% in this pass; rolling savings as future sessions don't re-add older lead-lines). Triggered by alignment-audit Finding D + user direct ask "should we shorten our roadmap doc?" User chose Option A (action now in this chat) over Option B (AGENTS.md split, parked) / C (queue for Chat D) / D (defer to AGENTS.md split entirely).
- **2026-05-16 (audit-only, no code) — POST-AMENDMENT** — Vision + Roadmap alignment audit ran in frosty-wright-d437a7 chat. Read-only walk of VISION §1/§4/§6/§9/§11 + ROADMAP §1/§3.1-3.10/§4/§6 against current intent. 4-pass audit terminated clean. Output: 7 findings (A-G) + 9 SURFACE / 14 LEAVE-ALONE / 0 RETIRE + 2 VISION §11 wording-update flags. **Mid-close-out fetch surfaced parallel chat collision:** `11eaf37` (U25 ship) + `241c29a` (U25 close-out + 2 CLAUDE.md rule locks) landed on origin/main between audit-start and audit-close. Audit's original Chat A=U25 recommendation moot; collapsed to 4-chat sequence (now A=aesthetic-decision sweep). New LEAVE-ALONE item added: Vault profile aria-label cross-surface cohesion (parked by parallel chat per Rule A). Close-out commit: ROADMAP §1 lead-line (rewritten to acknowledge collision + collapse sequence) + §5 (this entry) + §6 (audit decision log entry). No VISION / BUGS / BACKLOG / code edits this chat. Plan file updated with POST-AUDIT AMENDMENT block. Full deliverable in `C:\Users\marsh\.claude\plans\fresh-chat-purpose-vision-sorted-clover.md`.
- **2026-05-16 (doc batch from `pedantic-panini-9d963c`)** — `docs: lock 2 rule reinforcements + U25 close-out + ROADMAP §6` — Two procedural rule additions captured in CLAUDE.md following the U25 ship: **(1) Cohesion-by-aggregation cross-surface scope-check sub-clause** — inserted between the "Two-branch resolution" and "failure-mode signal" blocks of the Pass 2 cohesion-by-aggregation subcategory. Sub-clause: when changing a trigger / icon / treatment / copy on Surface A, explicitly Grep for matching `aria-label` / action / semantic match on Surface B/C/D before declaring scope complete. Sibling surfaces are "in-scope or explicitly named as parked." **(2) Migration-naming guard** appended to "Things to never do without explicit user permission" — never create a migration file with a duplicate timestamp prefix; use unique `YYYYMMDDHHMMSS` 14-digit timestamps. Cites the `20250330000000_journal_entry_type_prune.sql` + `20250330000000_user_settings_onboarding_complete.sql` collision that contributed to U24 + the `user_settings.onboarding_completed_at` gap. Plus BUGS U25 row marked ✅ Fixed with Vault-sites parked note; ROADMAP §1/§4/§5/§6 close-out entries. Doc-only per CLAUDE.md Push tiers. 3 files, +N/-N. Audit looped to termination at Pass 3 — Pass 2 iteration 1 surfaced 3 additional Vault sites missed by handoff, dispositioned via Rule A ask (user chose park), plan revised, Pass 2 iteration 2 + Pass 3 clean on as-written-now plan.
- *Older entries (back to 2026-05-07 — `11eaf37` U25 MyPlants icon swap 2026-05-16 + `7207164` U24 Phase A bulk-end handler error surfacing 2026-05-14 + `2cc313a` doc-hygiene batch 2026-05-14 + `884cf9d` §3.10 R1-R5 strict drift batch + chunks 3.1 + initial framework + placeholder fixes + `fb67e10` U21 fix, `79836f4` FAB UX U22+U23, `6d76c20` calendar expand-all toggle, `3de1c2c` chunk 3.9 #2+#4 batch, and earlier) archived. Full history in `git log`. Archive policy locked 2026-05-16 (see §7); §5 cap = ~10 entries; oldest dropped when adding new.*

---

## 6. Decision log

Chronological log of key decisions made during design and build. New decisions append here. *Provides historical context — different from VISION.md (which is current state).*

### 2026-06-10 (Three-tag plant schema lock — Lifecycle / Growth form / Plant category — replaces the binary Seasonal|Permanent toggle)

**Decision (Syd lock 2026-06-10).** Plant typing moves from the binary Seasonal|Permanent toggle to a **three-tag schema** on `plant_profiles`: **Lifecycle** = Annual / Biennial / Perennial; **Growth form** = Tree / Shrub / Vine / Herbaceous / Grass / Groundcover / Bulb / Tuber; **Plant category** = Flower / Fruit / Vegetable / Herb / Ornamental / Houseplant. AI enrichment owns populating the tags (Chunk A `9e9956c` schema + locked prompt vocabularies); the legacy toggle is removed from ALL Add Plant creation flows (`c29ed34`), and the Edit modal exposes Lifecycle as a dropdown (`af67c59` B6 — `profile_type` derived only when lifecycle is set; empty never clobbers). Serves the profile-as-encyclopedia mission (NORTH_STAR "Information-hub framing" — a profile is a reference page, and three orthogonal botanical tags carry real meaning where the binary toggle conflated lifecycle with permanence) and NORTH_STAR "Take mental load OFF the user" (AI fills the tags; the user is never asked to classify a plant at add-time).

### 2026-06-10 (Instance status — `archived` stays the sole terminal value, Option B; NO separate lifecycle-status column)

**Decisions (Syd locks 2026-06-10).** Two related locks on `grow_instances.status`: **(a)** the enum keeps **`archived` as the single terminal value** — `dead` does NOT return as an enum value. End batch → Plant Died now writes `archived` (`3de33bc`), with the death preserved via `end_reason='plant_died'` + the death journal entry. Syd: *"i like archived > gone."* **(b)** A lifecycle-status field is **NOT added as a separate column** (Option B): the existing 2-state enum (growing | archived) carries the lifecycle. Both locks deliberately avoid the enum re-expansion that the 2026-05-28 Phase A status collapse intentionally removed (see that date's §6 entry) — the state machine stays 2-state; nuance lives in `end_reason` + journal, not in new status values.

### 2026-06-10 (Couldn't-find AI UX — NO species-level fallback; honest toast copy locked)

**Decision (Syd FINAL lock 2026-06-10).** When AI enrichment can't find the exact plant + variety: **NO species-level fallback.** Fields stay empty + clear toast + inline notice + **Try Again** button (`af67c59` B5; supersedes Chunk A's same-day thin-result species fallback in `9e9956c`, which never reached a tester build). Honest > fake intelligence per Syd's app-store framing (VISION: the app ships to strangers — silently filling a variety profile with generic species data is a lie the user can't detect; `found:false` is never cached). **Toast copy locked:** *"Updated N fields"* / *"Nothing new to add"* / *"AI unavailable, try again later"* / *"Couldn't find data for this plant. Check the spelling of name and variety."*

### 2026-06-10 (Vault terminology lock — Vault = inventory, Library = encyclopedia)

**Decision (Syd lock 2026-06-10).** **Vault** = the user's physical inventory (Packets + Shed); **Library** = the varieties encyclopedia. "Add to Vault" CTAs are correct as-is and preserved; the only warranted rename was the FAB **"Add Variety" → "Add to Library"** chip + form header (`dddafab`), aligning chip → screen → submit on the Library destination (NORTH_STAR "No duplicate paths" — two names for one destination read as two paths). No further Vault/Library renames pending.

### 2026-06-10 (Pre-tester scope rule — Syd dogfood flags ship pre-tester by default)

**Decision (Syd lock 2026-06-10).** Everything Syd dogfoods + flags during the pre-tester window **ships pre-tester** unless explicitly marked post-tester. Sprint 5a is absorbed into the pre-tester batch. Rationale: the tester build should be a faithful snapshot of the product Syd has actually validated, not a known-stale build with a queue of fixes she's already confirmed — testers re-discovering flagged-and-fixed issues wastes their signal.

### 2026-06-10 (Pre-tester onboarding copy locks — G1/G2/G3 from the persona audit)

**Decisions (Syd locks 2026-06-10, options A from [the audit's](plans/onboarding_persona_audit_2026-06-10.md) §7).** (1) **G1:** onboarding step-3 task pre-fill = **"Check on my plants"** — replaces the Sanctuary-era "Observe Hillside" (plain words, universal across all 5 personas, action Sam can actually do). (2) **G2:** dock step 2 = **one rewritten link "Add your first plant or seeds" opening the FAB Add menu** — teaches the universal + entry point in one tap, no new primitive; completion logic already accepted any plant. (3) **G3:** Help page content refreshed to current IA (severity promoted by Ship 2b making `/help` the single help destination). Added scope per Syd: full Sanctuary-era vocabulary sweep of user-facing strings ("get rid of hillside references"). NORTH_STAR cites: G1/G2 "Take mental load OFF the user"; G3 "No duplicate paths" (documentation layer). The "welcome instructions" park (§4) stays parked — these are repairs to the existing taught path, not new welcome content. Audit's G4 (zone-helper link), G5 (step-1 deep-link), G6 (Quick Start re-entry) remain open, undecided. Shipped `0b3b288` (see §5).

### 2026-06-10 (Acquisition entries surface on the instance Journal tab — reverses the 2026-05-28 "Journal = user-logs-only" lock)

**Decision (Syd lock 2026-06-10).** Acquisition (`vault_add`) journal entries now show on BOTH the profile Journal tab AND the instance Journal tab. **Supersedes** the 2026-05-28 lock that excluded `vault_add` from the instance Journal tab ("Journal = user-logs-only; Task History owns task completions"). **Reasoning:** the 2026-05-28 lock predated the instance page existing — Sprint 3 (`02ff1d8`) shipped the growing instance as a real route (`/garden/grow/[id]`), making the instance Journal tab the canonical, user-facing surface for that planting's history. Acquisition is the load-bearing first event of a planting; on a real detail page it belongs in the user-facing journal, not hidden. The data linkage was never the problem — `grow_instance_id` was already set on every instance-creating add path (PO import, AddPlantModal, completeSowTask, PlantingForm); the entry was only filtered from display.

**Reference — NORTH_STAR "Information-hub framing":** the instance page is an information hub for one planting (Learn / Overview / History / What-next); the acquisition event earns its place in that hub's history, not as a duplicate path. This also locks the **Sprint 4 Sources-section foundation** — Sources finds the acquisition artifact via `grow_instance_id`, already correct everywhere.

**Implementation.** Filter `e.entry_type !== "vault_add"` removed at [GrowInstanceModal.tsx](src/components/GrowInstanceModal.tsx) (source comment now records the reversal); `[...journalEntries]` spread added before sort so the unfiltered derivation never mutates state. Guard test [acquisitionJournalLinkage.regression.test.ts](src/components/acquisitionJournalLinkage.regression.test.ts) (8 source-level invariants) locks both the per-flow `grow_instance_id` linkage AND the reversal so a future chat can't silently re-apply the old filter. Shipped `30324be` (see §5); tests 586/586; build clean; auto-push tier (no schema/migration/RLS/auth).

### 2026-06-09 (Groups follow-up ship — single-group membership lock + auto-journal language + annual/perennial relocation)

**Decision 1 — ONE group per plant (Syd lock 2026-06-09).** Group membership is single, not multi. The shipped Door 1 (AddPlantModal B3 autocomplete, `cb6f050`) was multi-select (`selectedGroupIds[]`); reconciled to single-select this ship. Rationale: matches the locked auto-journal language ("Moved A → B" only has meaning with one group at a time), Door 2's "a Group tag," Door 3's "Move to group," and the folder mental model. Schema unchanged (`plant_groups` M-N stays; `setInstanceGroup` clears all prior rows before insert, which also collapses any legacy multi-membership lazily on next change — no destructive backfill).

**Decision 2 — Auto-journal on every group change via ONE shared path.** All assignment routes (Door 1 Add Plant / Door 2 plant page / Door 3 batch FAB / delete-group reassign) funnel through `setInstanceGroup()` ([groups.ts](src/lib/groups.ts)), which writes a `group_change` journal entry: "Added to {X}" (first assignment) / "Moved {A} → {B}" (between groups) / "Removed from {X}" (cleared). Locked language: never "Moved" for a first assignment. New `entry_type 'group_change'` via additive CHECK migration `20260609120000` (applied to remote 2026-06-09). Compact rendering: VISION §8 info-note register (`italic text-neutral-600`) in all three journal views. Shared `createJournalEntry()` helper added ([createJournalEntry.ts](src/lib/createJournalEntry.ts)); existing inline journal inserts NOT retrofitted (ADJACENT, future cohesion ticket).

**Decision 3 — Annual/perennial is a VARIETY-level property; edit lives at the Library entry.** Canonical field = `plant_profiles.profile_type` ('seed' | 'permanent'), edited via Seasonal/Permanent toggle in the Edit Plant Profile modal (existing seed/permanent language per Syd — no parallel "Annual/Perennial" taxonomy). Garden cards: Perennial pill REMOVED (closes the Sprint 3 B4 perennial-pill park); batch "Mark as perennial/annual" actions REMOVED. Accepted trade-off (flagged at ship): instance-level `is_permanent_planting` is no longer user-editable from Garden — it stays creation-time-derived and still drives card display branches (Planted-vs-Sown, progress bar). Multi-select FAB sheet final order: Move to group · Journal · End batch · Delete.

**Also locked this ship:** Manage Groups modal hardening (single modal-level `busy` lock kills the reorder-during-create race that caused the "Adding…" hang; 10s save timeout with reconcile-on-finally; optimistic add; reserved Add-button width) + delete-group-with-plants reassign flow ("{Group} has N plants. Where should they go?" → per-plant journaled reassign → delete; 0 plants = clean delete, no prompt). Merge-groups deferred to v2 (manual workaround: batch-move + delete empty source). Tabs row restructured: Manage pinned outside the scroll strip + right-edge fade (fixes 4+ groups break). Shipped `c7fe6f6`; see §5.

### 2026-06-01 (Complete-task affordance — responsive primitive lock; secondary actions stay off-swipe unless reversible)

**Decision (Syd dogfood lock 2026-06-01, A/A).** The complete affordance for every task/item-complete surface is now a SINGLE shared primitive (`useRowSwipe` + `SwipeCompleteRow` + `RowCompleteButton`), anchored to Calendar's canonical task row. Mobile (phone-portrait) = swipe-LEFT to complete; desktop/iPad-portrait+ (`md:`) = checkmark button; same underlying complete action, viewport dictates the interaction mode (per Principle 9, Walter persona). Locked into VISION §8.

**Q1 = A (Shopping List swipe scope).** Shopping List = swipe-left = mark-purchased ONLY; the "remove" button stays a persistent visible control on all viewports, never mapped to a swipe. Rationale: remove has no undo — swipe-to-delete on a no-undo action is a footgun. Calendar maps swipe-right → snooze, which is *reversible*, so it is not a true parallel for remove. **Generalizable rule:** a secondary row action only earns a swipe slot if it is reversible.

**Q2 = A (At-a-Glance snooze parity).** At-a-Glance stays complete-only — no snooze on Home this ship. Adding a Home snooze handler + modal is separate feature scope, not a primitive-extraction concern.

**Architecture.** `useRowSwipe` moved verbatim out of `calendar/page.tsx` to `src/hooks/useRowSwipe.ts` (single canonical source; Calendar repoints its import, behavior identical). Calendar's own rows keep their bespoke reveal/transform JSX (snooze + select-mode complexity) rather than adopting `SwipeCompleteRow` this ship — logged as Pass-3 ADJACENT for a future cohesion fold-in. Shipped 2026-06-04 (see §5). Tests 533/533; build clean.

### 2026-06-01 (Button-system hierarchy lock — primary/secondary/tertiary/destructive — Sprint 5 cont Ship 5, FINAL pre-tester engineering ship)

**Decision (Finding 28, locked by Syd 2026-06-01).** GT buttons split into 4 tiers, locked as a new VISION §8 "Button-system hierarchy" subsection:
- **Primary** (submit / main CTA) = solid `bg-emerald-600 hover:bg-emerald-700` white, larger, sticky footer; must be the most prominent control on the surface (already canonical via the FAB-form submit saga, 41+ files).
- **Secondary** (alt-source / cancel / dismiss) = outlined pill, green text `border-teal-gus/40 text-teal-gus hover:bg-teal-gus/10` (= the already-locked Cancel token; `teal-gus` #2A9D8F is the green Syd referenced). Photo-source `Take Photo`/`From Gallery` buttons are alt-source → secondary.
- **Tertiary** = text-only low-emphasis link.
- **Destructive** = red pill (never green).

**Why.** Syd dogfood Finding 28: the Add Supply form had `From Gallery` as a large solid-green button heavier than the real `Add Supply` submit, its paired `Take Photo` outlined in black, and `Cancel` outlined in green — three drifts (false-primary mid-form, mismatched equal alternatives, inconsistent secondary color). The tier system makes weight communicate hierarchy so the eye lands on the real CTA first.

**Judgment call (anchored, not invented).** Secondary = the existing locked Cancel token, not a new color — pure cohesion anchor per the cohesion-by-aggregation rule. Both photo-source buttons drop to secondary (outline-vs-fill carries hierarchy; tap targets stay ≥44px for Walter).

**Scope.** Doc-lock-then-sweep (same pattern as the casing MUST). Swept `Take Photo`/`From Gallery`/`Use Camera`/`Upload from files` pairs across 14 files to matched secondary teal; `Capture` (webcam-commit) stays primary. Bundled with **Finding 25** ("(optional)" label removal — asterisk already marks required; redundant parenthetical dropped across ~20 form files; AI-prompt schema + one body-copy sentence preserved). e2e zero-edit (className-only on buttons; label strings unasserted). Tests 532/532; build clean. **Closes Sprint 5 continuation — all Claude-implementable pre-tester engineering done** (pending Syd Supabase dashboard L1/L2/L5 + import-processes dogfood).

### 2026-05-30 (Variety presentation reversal — stacked italic across Library + Packets + Garden — code+VISION ship `dc173ef`)

**Chat purpose.** Syd-driven canonical reversal of variety rendering across all inventory list surfaces. Replaces the prior implicit inline `Name (Variety)` parens-wrapped pattern (last canonicalized 2026-05-27 "Variety presentation standardization across list views" chapter, Sprint 5 SHOULD #19 VERIFIED CLOSED 2026-05-30 in polish bundle `332f0d6`) with stacked-italic-no-parens render. Single code+VISION commit `dc173ef` on `feat/care-tab-c4-instance` → `main` 2026-05-30 followed by this doc-trail close-out commit.

**Decision 1 — Reversal direction: stacked italic + no parens, anchored to "independent skimmability of two axes" principle.** Syd's signal verbatim: *"I like the variety under plant type because it's easier to skim and read. These pages get incredibly long so being able to easily skim the variety OR the name was nice."* The prior inline `Name (Variety)` pattern visually merges name+variety into a single hybrid token, forcing the eye to parse the whole compound string left-to-right on every row. Stacked-with-distinct-register lets users sweep down EITHER the bold-name column OR the italic-variety column independently. Italic + muted register signals "structurally distinct but subordinate axis" — botanist convention (`Solanum lycopersicum / 'Cherokee Purple'`), iOS Contacts (first-name + last-name stacked), Apple Music (song / artist stacked), iOS Settings sub-rows. The visual vocabulary is universally recognized; the reversal lands on industry-standard pattern, not a Garden-Tracker-specific invention.

**Decision 2 — Helper `formatBatchDisplayName` preserved as-is; only the visible list-row callsite refactored.** [GardenView.tsx:229](src/components/GardenView.tsx:229) defines `formatBatchDisplayName(name, variety) → "{name} ({variety})"`. Used at 5 callsites in GardenView: 1 visible list-row (L1304 — VISIBLE, flips to two-element render) + 4 non-visual (L1261 aria-label, L928 modal body, L975 delete-confirm prose, L641 toast). Decision: keep helper; only refactor the visible callsite. Per VISION §8 amendment, body-copy-equivalent single-line text (aria-label, toast, modal body) has no skimming axis to preserve, so inline `Name (Variety)` reads naturally there. Avoids unnecessary abstraction (per CLAUDE.md "don't add abstraction beyond what task requires"). No new shared `<PlantNameWithVariety>` component introduced — the 7 visible sites have slightly different surrounding markup (h3 vs span vs td; gallery vs list vs table) and inline the two-element pattern is cleaner than parameterizing a wrapper.

**Decision 3 — Desktop Packets table cell flips too (cohesion-by-aggregation guard).** [PacketVaultView.tsx:846-855](src/components/PacketVaultView.tsx:846) `<td>` in the `hidden sm:block` desktop table renders the same Variety column as the mobile list. Q2 at audit time asked whether desktop table should also stack or stay inline. Syd locked: stack consistently. Rationale: same data, same surface family — keeping mobile stacked but desktop inline would create within-Packets-list cohesion drift (exactly the cohesion-by-aggregation failure mode the new rule rejects). Slight table-row height increase accepted.

**Decision 4 — VISION §8 amendment generalizes beyond the 4 surfaces audited at lock time.** Per Syd's mid-Phase-2 amendment ("they should reach for stacked-with-distinct-register whenever a list of named-with-variation items appears, not just on these 4 surfaces"), the new §8 subsection codifies the rule as a framework, not a per-surface lock. Future list surfaces rendering named-with-variation items inherit the rule by default; single-item profile views (no skimming axis) are explicitly exempt. Pattern matches how §8 default-sort and chrome-vs-content rules were written — name the principle once, decision-criterion once, then surfaces inherit.

**Decision 5 — Single code+VISION commit (per Syd's brief); separate doc-trail commit for ROADMAP catch-up.** Two-commit shape rather than three-commit: code commit (`dc173ef`) bundles src/components/*.tsx + docs/VISION.md (the actual rule lock + tokens used in code); doc-trail follow-up (this commit) captures §1 lead-line + §5 entry + §6 decision-log entry. Code commit goes via code-tier (Syd's explicit yes-push per "this message = explicit yes-push for code + doc-trail"); doc-trail goes via doc-tier per CLAUDE.md push-tier rule. Bundling VISION §8 amendment into the code commit (instead of separating) reflects that the VISION text + the code edits ARE the same logical ship — both express the same rule.

**Decision 6 — Supersedes-acknowledged, not silent drift.** Prior inline `Name (Variety)` pattern was canonical for ~3 days (2026-05-27 "Variety presentation standardization across list views" chapter lock → 2026-05-30 reversal in this chat) and was VERIFIED CLOSED on the very same day this chat reversed it (`332f0d6` polish bundle close-out at 2026-05-30 marked Sprint 5 SHOULD #19 closed against the inline pattern; ~hours later Syd flagged dissatisfaction with the inline result and locked the stack-italic-no-parens reversal). VISION §8 amendment explicitly names the supersedes per CLAUDE.md authority-precedence rule — prior ship work landed inline-uniform pattern as intermediate state; this amendment flips the target. Transparency captured so future-Claude doesn't treat the reversal as drift to re-litigate.

**Open / parked.** Calendar widget pills (separate chapter in flight per spawn-time note from orchestrator — NOT touched by this ship); Shed (supplies have no variety field — confirmed via grep, no change applicable); modal body / aria-label / toast inline `Name (Variety)` rendering (intentionally preserved per VISION §8 amendment — body-copy-equivalent, no skimming axis). All other v1 items remain per gt_v1_scope.md §5.2: 15/18 = 83% (L1 PITR + L2 restore drill + L5 retention dashboard check = 3 Syd-dashboard items remaining).

### 2026-05-30 (Sprint 5 SHOULD bucket ✅ FULLY CLOSED — polish bundle `332f0d6`)

**Chat purpose.** Sprint 5 SHOULD bucket close-out per gt_v1_scope.md §2.2: SHOULD #19 (variety presentation standardization) + #20 (Shed vs Garden list text styling) + #21 (Add plant Manual loading-overlay coverage bug) + PacketVaultView casing leak left over from MUST #14 V5.2 sweep coverage gap + ROADMAP doc-trail catch-up. Single code commit `332f0d6` on `feat/care-tab-c4-instance` → `main` 2026-05-30 followed by this doc-trail close-out commit.

**Decision 1 — SHOULD #21 root cause + uniform 10-wrapper `relative` fix.** Bug traced to `SubmitLoadingOverlay`'s `absolute inset-0` lacking deterministic positioned ancestor in the embedded form path. The standalone modal path works (AddPlantModal wrapper at [AddPlantModal.tsx:1125](src/components/AddPlantModal.tsx:1125) is `relative` so overlay covers full shell). The embedded path (since 2026-05-26 `ba9319f` Path Y FAB-form submit saga) renders form content inside [UniversalAddMenu.tsx](src/components/UniversalAddMenu.tsx) sub-screen wrappers that had `${slideClass} flex-1 min-h-0 flex flex-col` with no `relative`. Transform-bearing slide animations create inconsistent containing-block behavior across browsers — in practice the overlay scoped to the body scroll area only, leaving header + footer interactive during submit (Syd dogfood: "top half only; bottom appears interactive"). Pass 3 sibling sweep BLOCKING-aligned: same bug shape applies to ALL 6 embedded form types — TaskForm (task sub-screen) + JournalEntryForm (journal) + SeedPacketForm (seed) + SupplyForm (shed) + AddVarietyForm (variety) + AddPlantModal-embedded (add-plant-manual + add-plant-established via `embedded` prop). All 6 form sub-components ship SubmitLoadingOverlay inside their JSX (verified across NewTaskModal:447 + AddVarietyModal:266 + QuickAddSupply:730 + QuickAddSeed:1233 inside extracted SeedPacketForm + QuickLogModal:593 + AddPlantModal:1115). **Fix scope:** added `relative` to ALL 10 sub-screen wrapper divs uniformly (main + add-plant + add-plant-manual + add-plant-established + add-plant-from-vault + seed + shed + task + journal + variety). Cohesion-by-aggregation rationale: uniform addition keeps all 10 wrappers identical-shape (no 2-pattern drift introduced); future-proofs against any new overlay-bearing form added to a different sub-screen. Single-edit fix scope = 10 line edits in UniversalAddMenu.tsx; closes 6 embedded form types' overlay positioning in one change.

**Decision 2 — SHOULD #19 + #20 marked VERIFIED CLOSED via prior-ship verification (no code change).** Per gt_v1_scope.md §2.6 explicit queue-lag note ("queue file snapshots Syd's mental queue but lags ship cadence"), both items were closed by Sprint 3 B2 + Sprint 4 `c851b3d` ship work without explicit close-out. Engineer-PM tactical call (per chat brief): mark CLOSED via verification reasoning + doc-trail audit citation, rather than bikeshedding micro-styling for ship-claim cover. **#19 variety presentation** — `Name (Variety)` inline pattern already unified across 3 list-view surfaces: Packets mobile list ([PacketVaultView.tsx:788](src/components/PacketVaultView.tsx:788) `block text-sm font-semibold text-neutral-900 truncate` + plain inline `Name (Variety)`) + Garden list ([GardenView.tsx:1300](src/components/GardenView.tsx:1300) `font-semibold text-sm text-neutral-900 group-hover:text-emerald-700` + plain inline via `formatBatchDisplayName(name, variety)` helper); Active Garden + My Plants merged into single GardenView via Sprint 3 Ship B B2 `04ef744` 2026-05-29 (both surfaces now inherit the helper). Desktop Packets table (`hidden sm:block` at L810) uses different `font-medium text-neutral-800` — different rendering context (table cell vs row card), intentional and out of mobile-list scope. **#20 Shed-vs-Garden list text styling** — row wrapper / title font (`font-semibold text-sm text-neutral-900`) / subtitle font (`text-xs text-neutral-500 mt-0.5`) all identical post-`c851b3d` 2026-05-28; row wrapper `flex items-center gap-3 px-3 py-2 min-h-[44px] hover:bg-gray-50 transition-colors` matches across Shed + Garden. Remaining `text-[11px]` chip-register (Shed) vs `text-xs` sentence-register (Garden sown/harvest paragraph) difference is intentional content-shape preservation explicitly documented in c851b3d commit message ("My Plants meta chip-row text-xs → text-[11px] to match Shed's chip register exactly. Active Garden's sown/harvest paragraph stays text-xs (sentence register, not chip register — content-shape preserved)"). Garden emerald-tinted thumbnail (`bg-emerald-50 border border-emerald-100`) vs Shed neutral (`bg-neutral-100`) is semantic active-growing signal, not drift.

**Decision 3 — PacketVaultView casing leak folded into same commit.** [PacketVaultView.tsx:618](src/components/PacketVaultView.tsx:618) empty-state CTA "Add a packet" → "Add a Packet" per VISION §8 Title Case (AP-style mechanic: verb `Add` capitalized as first word, article `a` lowercase mid-string per articles exception, noun `Packet` capitalized as last word + noun). V5.2 sweep canonical (`3e4c02a` Sprint 5 MUST #14 onboarding pass 2026-05-30) hit SeedVaultView ×2 ("Add a Packet" / "Add a Plant") + ShedView ×1 ("Add a Supply") + Home ×2 + shopping-list ×1 ("Add an Item") but missed this PacketVaultView site — single coverage gap caught in close-out audit. 9bad88f wordform "Add a {noun}" preserved per Q2=A precedence (locked 2026-05-30 ROADMAP §6); only casing register flips. Per CLAUDE.md authority precedence: VISION §8 wins on locked design decisions; 9bad88f wordform survives, casing changes. Bundled into same commit as SHOULD #21 (one cohesive Sprint 5 polish ship rather than two separate commits — both items XS, both ship the cohesion bar, sensible bundle).

**Decision 4 — v1 % recomputed 15/18 = 83% (+1 catch-up for MUST #9).** gt_v1_scope.md §5.2 snapshot at MUST #14 close-out was 14/18 = 78%. MUST #9 contextual add actions shipped `8897fe8` 2026-05-30 prior to this polish bundle close-out but the §5.2 snapshot was not yet updated at that time. This close-out incorporates the catch-up: 13/14 MUST shipped + 2/4 launch-checklist = 15/18 = 83%. **Polish % = 7/8 = 87.5%** (SHOULD bucket goes 4/7 → 7/7 = 100% complete; only L5 retention dashboard check remains on the polish denominator). All Claude-implementable Sprint 5 work is now CLOSED; remaining v1 = L1 PITR decision + L2 restore drill + L5 retention dashboard check (all Syd dashboard actions, ~40 min her time total).

**Decision 5 — Single commit for code (SHOULD #21 + casing leak); separate commit for doc-trail.** Two-commit shape rather than one combined ship: code commit (`332f0d6`) keeps the diff focused on user-visible polish for clean cohort retrospective; doc-trail follow-up (this commit) captures gt_v1_scope.md §2.2 row flips + §5.2 snapshot recompute + ROADMAP §1 lead-line + §5 entry + §6 decision-log entry. Doc-trail goes via doc-tier per CLAUDE.md SHA push policy (no per-push code greenlight needed for doc-only diffs; verified via `git diff --stat`). Code commit goes via code-tier (explicit yes-push required per push from Syd per CLAUDE.md push tier rules).

**Open / parked.** Sprint 5 #7+#8 §5 doc-trail entries (pre-existing debt from those chats; flagged but not in this close-out's scope); theme #7 raw-card empty-state primitive canonicalization (pre-existing park, not v1-blocking); perennial-pill keep/drop on unified Garden cards (still parked from Sprint 3 B4 — needs Syd dogfood signal on mixed annuals+perennials feel).

### 2026-05-30 (Sprint 5 MUST #14 ✅ SHIPPED — Onboarding pass — `3e4c02a`)

**Chat purpose.** Sprint 5 MUST #14 closure per gt_v1_scope.md §2.1 row 14: verification-only onboarding pass + targeted gap-close micro-polish. Single code commit `3e4c02a` on `feat/care-tab-c4-instance` → `main` 2026-05-30. Followed by this doc-trail close-out commit.

**Decision 1 — Brief-vs-plan-doc scope reconciliation: locked (A) verification-only.** Chat opened with orchestrator brief expanding MUST #14 to net-new tooltips on Add Plant FAB / Library Add Variety FAB / Calendar + first-time Add-Plant defaults + empty-state polish across 5 surfaces. Conflicted with gt_v1_scope.md §2.1 row 14 explicit "Not net-new feature work; verification + gap-close pass against §1.7 criteria" + VISION §3 disclosure-by-data ("features unfold as data unfolds, not skill-mode toggles") + §10 empty-by-default LOCK ("empty IS the onboarding"). Plus the §3.12 theme #5 empty-state cliff audit already shipped 2026-05-18 (`9bad88f` — 17 files / 22 strings). Per memory note `feedback_verify_brief_vs_locked_plan.md`, Claude surfaced the conflict via plain-English ask at chat open instead of executing the over-expanded brief. Syd ratified (A) plan-doc-scope: V1-V5 verification (V1 5 surface empty-states / V2 3 Add Plant paths / V3 zone setting discovery / V4 Manage Groups tooltip / V5 targeted gap-close ≤50 lines per surface). Governance worked as designed; existing rule + memory note caught the over-expansion before code shipped.

**Decision 2 — Q2=A §8 CTA Title-Case precedence over 9bad88f sentence-case canonical.** During V5 audit, Pass 3 sibling sweep surfaced doc-vs-doc conflict: VISION §8 casing convention (locked 2026-05-27) says button labels are Title Case (AP-style), while ROADMAP §6 9bad88f Decision #2 (locked 2026-05-18) shipped empty-state CTAs as sentence-case "Add a packet" / "Add a plant" / "Add a supply". The MUST #7 app-wide casing sweep `7db44c5` (2026-05-29) hit Calendar "Mark Done" / error-fallback "Try Again" / Settings "Sign Out" but did NOT sweep these empty-state CTAs — coverage gap. GardenView already drifted to Title Case ("Add a Plant"); SeedVaultView/ShedView/Home/shopping-list still sentence-case. Pattern split across codebase = cohesion-by-aggregation drift. Syd locked Q2=A: **VISION §8 wins per CLAUDE.md authority precedence (VISION wins on locked design decisions)**. 9bad88f "Add a {noun}" wordform stays canonical (the verb-led "Add a" pattern + "an" article for vowel-initial nouns like Item); only casing register flips to Title Case. V5.2 sweep applied to 4 files (6 edits): SeedVaultView ×2 → "Add a Packet" / "Add a Plant"; ShedView ×1 → "Add a Supply"; Home ×2 + shopping-list ×1 → "Add an Item". GardenView CTAs already Title Case, stay as-is. Future-Claude: do not re-flip these to sentence-case citing 9bad88f canonical — §8 supersedes 9bad88f on casing register; 9bad88f's wordform survives, only casing changes.

**Decision 3 — V5.1 Garden em-dash decoration is misapplication of §8 (drift fix).** GardenView.tsx empty-state titles at L1057 + L1082 had drifted to em-dash bracketing decoration "— Your garden is empty —" / "— No plants in this group yet —" with a misleading code comment claiming "VISION §8 em-dash convention." This conflates two §8 sub-rules: §8 em-dash convention is for missing-data CELL display in profile/table/detail views (`—` as the literal display value for empty fields), NOT for empty-state title decoration. Peer surfaces (SeedVaultView/ShedView/Home/Calendar) all use sentence-case title + period without decoration per `9bad88f` canonical. Pass 3 sibling sweep confirmed Garden was the sole outlier on em-dash decoration (0 peers). V5.1 strict drift fix: drop em-dash bracketing, add period, match peer shape ("Your garden is empty." / "No plants in this group yet."). Code comments updated to remove the misleading §8 reference and cite 9bad88f canonical instead.

**Decision 4 — v1 % recomputed 14/18 = 78% (includes Sprint 5 #7+#8+#14 catch-up).** gt_v1_scope.md §5.2 snapshot was stale at 11/18 = 61% (post-Sprint-4 baseline); Sprint 5 MUST #7 (`7db44c5` 2026-05-29 app-wide casing sweep) + #8 (`a4f2235` 2026-05-30 default sort flip) + #14 (`3e4c02a` 2026-05-30 onboarding pass) shipped without §5.2 catch-up. This close-out updates §5.2 to 12/14 MUST shipped + 2/4 launch = 14/18 = 78%. Honest framing: §5 "Just shipped this session" entries for #7 and #8 NOT added here (those are separate chats' doc-trail debt); only #14 gets the full §5 entry. The §5.2 number-update is per CLAUDE.md authority precedence (ROADMAP wins on current-state truths) — accurate math takes priority over close-out scope hygiene; missing §5 entries for #7+#8 flagged as deferred follow-up.

**Decision 5 — Sprint 5 next chunk = MUST #9 contextual add fixes (Recommended).** Remaining Sprint 5 v1 work after #14: MUST #9 (S; strict bug fixes on Plant profile contextual add actions — "Add plant" on Plants tab opens Add Seed menu wrong; "Add packet" missing from Packets tab; profile-tab `Add X` should create `X` pre-filled for current profile) + MUST #13 launch checklist L1 PITR decision + L2 restore drill (Syd-paced) + SHOULD #19 variety presentation standardization + SHOULD #20 Shed-vs-Garden list text styling + SHOULD #21 Manual modal loading-overlay coverage bug. MUST #9 recommended next: well-defined, S-sized, unblocks profile contextual-add cohesion. L1/L2 need Syd's Supabase dashboard time (Syd-paced not Claude-paced); SHOULDs bundleable as a single polish ship after #9.

**Open / parked.** Sprint 5 #7+#8 §5 doc-trail entries (deferred — separate doc-hygiene chat); theme #7 raw-card empty-state primitive canonicalization (pre-existing park, not v1-blocking); perennial-pill keep/drop on unified Garden cards (still parked from Sprint 3 B4 — needs Syd dogfood signal).

### 2026-05-29 (Sprint 4 fully shipped — Established plants + FAB cohesion + polish bundle; doc reconciliation 2026-05-30)

**Chat purpose.** Doc-hygiene close-out chat (no code changes) reconciling gt_v1_scope.md + ROADMAP §1/§5/§6 to reflect Sprint 4 fully shipped per gt_v1_scope.md §4. Sprint 4 commits landed 2026-05-29 (`ff4ac2e` + `f77fe75` + `9dc8c2d` + `dc63fa9` + `e684331`); SHOULD #18 caught by close-out as already-shipped pre-Sprint-4 (`7242cb6` 2026-05-26). Doc-tier single-commit push to `main` on 2026-05-30.

**Decision 1 — Sprint 4 = Established plants + FAB cohesion is canonical (overrides prior "Phase 2.5/2.6" prediction).** ROADMAP §1 (Sprint 3 B4 entry) + §6 2026-05-29 (Sprint 3 Ship B fully shipped) both predicted "Sprint 4 next: Phase 2.5 zone-aware library schema + Phase 2.6 retrofit prompt." This was superseded by gt_v1_scope.md §4 which has Sprint 4 = MUST #6 Established plant + MUST #4 FAB-tree sweep + MUST #5 AddPlantModal symmetry + MUST #10-12 FAB-tree bugs + SHOULD #16-18 polish. Per CLAUDE.md authority precedence, gt_v1_scope.md §4 is the canonical sprint structure; ROADMAP §1 lead-line predictions are best-effort planning hints, not lock. Phase 2.5/2.6 stay in CAN bucket per gt_v1_scope.md §2.3 (post-v1; cost-scaling optimization, doesn't block any §1 axis at trusted-circle dogfood scale).

**Decision 2 — v1 % recomputed post-Sprint-4 = 11 / 18 = 61% (with honest counts-as-open caveat).** gt_v1_scope.md §5.2 snapshot updated from stale 0/18 (planning-lock-day baseline) to 11/18: 9 MUST shipped (#1+#2+#3 pre-Sprint-4; #4+#5+#6+#10+#11+#12 Sprint 4) + 2 launch-checklist items shipped (L3 migration guard `f793a55` + L4 composite indexes `c81c3ab`). Honest framing: if any pre-Sprint-4 chapter status is ambiguous (e.g. MUST #9 contextual add partially shipped via FAB-tree sweep but not explicitly called out in commit messages), it stays counted-as-open until explicitly closed in a future ship. Polish % = 4/8 = 50% (SHOULD #15 Sprint 3 + #16+#17+#18 Sprint 4).

**Decision 3 — SHOULD #18 (`7242cb6` 2026-05-26) counts toward Sprint 4 SHOULD closures despite being pre-sprint.** Close-out chat caught it as an already-shipped item the queue file hadn't reconciled. Per gt_v1_scope.md §2.6 note ("queue file snapshots Syd's mental queue but lags ship cadence"), this is the kind of drift the framework expects; counting it toward Sprint 4 closures maintains accurate v1 % tracking. Recorded against Sprint 4 in §2.2 with explicit "(caught pre-Sprint-4)" tag for SHA traceability.

**Decision 4 — Sprint 5 first chunk = MUST #7 app-wide casing sweep (Recommended).** Mechanical, well-defined, longest-queued; closes §1.5(a) cohesion gate (Title Case / sentence case rule applied app-wide). Other Sprint 5 items per gt_v1_scope.md §4: MUST #8 sort gap (XS) + MUST #13 launch checklist L1+L2 (Syd actions) + MUST #14 onboarding pass (S-M) + SHOULD #19+#20+#21 polish + optional L6 public-storage trade-off + optional VISION §11 icon-stroke-weight resolution. Sprint 5 = final pre-launch sprint targeting v1 ship-ready bar (per gt_v1_scope.md §5.3 projection: ~95-100% v1 % at Sprint 5 close).

**Open / parked.** Perennial-pill keep/drop decision on unified Garden cards still parked from Sprint 3 B4 — needs Syd dogfood signal on the mixed annuals+perennials feel before acting. Should resurface naturally as Syd dogfoods Established-plant additions through the FAB during Sprint 5.

### 2026-05-29 (Sprint 3 Ship B fully shipped — B1+B2+B3+B4 — IA discussion §5.2 chunk B closes)

**Chat purpose.** Ship Sprint 3 Ship B per `.claude/plans/ia_restructure_discussion.md` §5.2 — user-defined Groups feature on a unified `/garden` tab merging Active Garden + My Plants. 4 chunks shipped same day in sequence: B1 schema + data layer (`f5c7c76`) → B2 unified Garden tab + GroupTabs primitive (`04ef744`) → B3 Manage Groups modal + AddPlantModal autocomplete (`cb6f050`) → B4 polish + REDESIGN doc rename + ROADMAP catch-up + ICON_MAP gap fix (this commit).

**Decision 1 — Group terminology over Zone (B4 doc rename).** REDESIGN_2026-05-18 §3.4 originally locked "Zone" with USDA-hardiness-zone collision noted as mitigation work. Syd's 2026-05-28 lock at implementation time renamed Zone → Group. Cuts the collision at the noun. CLAUDE.md authority precedence: current user-vision lock supersedes older locked design-doc terminology. REDESIGN doc swept noun-by-noun; schema names + proper-noun group NAMES preserved.

**Decision 2 — Soft cap stays at 10 groups + 15-char names (B1 schema).** Inherited verbatim from REDESIGN §3.4. Per CLAUDE.md persona walk: Sam-persona stays under cognitive cap (15 chars forces concision); Maya power-user gets 10 groups before hitting the cap (covers typical multi-bed setup). No mid-Sprint-3 amendment surfaced.

**Decision 3 — Soft-delete on `groups`, hard-delete on `plant_groups` (B1).** Group history preserved across renames + reorders; membership-only rows drop cleanly on group delete. Aligns with the "logging IS state management" principle from REDESIGN §3.5 — a deleted group is a state transition, not a data erasure.

**Decision 4 — First-visit tooltip on the Manage button (B4).** Per IA discussion Q8 lock: Sam-persona discovers group creation without an in-app walkthrough. localStorage-gated `gt-groups-manage-tooltip-seen` flag; copy "Tap Manage to create your own groups" (Walter-plain, verb-led, ≤7 words). Tooltip is a new chrome-overlay primitive in the codebase — first peer; anchor-able for future first-visit coachmarks (cohesion-by-aggregation: explicitly named as new pattern, not silently introduced).

**Deferred — Perennial pill on unified Garden cards.** B4 brief explicitly deferred this decision pending Syd dogfood of the mixed annuals+perennials feel in the unified Garden list. Reasoning: blind decision without lived-use signal. Queued as a post-dogfood micro-ship.

**Sprint 4 next.** Phase 2.5 (zone-aware library schema + lifespan field on `plant_profiles` — closes the per-profile zone story started by `dcfdbe4`'s prompt-side enrichment with a structured schema column) + Phase 2.6 (retrofit prompt — surface existing-profile re-enrichment paths for users who change their zone or who imported in zone-blind state).

### 2026-05-29 (`/plants` viewMode lock hotfix — `c45d6c6`)

**Chat purpose.** Syd dogfood-flagged: `/plants` Library route rendering Packets surface (vendor + qty stock overlay + `"Search packets…"` placeholder + Sarcococca/Apple/Apricot encyclopedia entries NOT visible). Urgent — Library broken since IA Ship A landed for any user whose sessionStorage held `"list"` from a prior `/vault?tab=list` visit. Prior corrective ship `4e5f3eb` had claimed "Data source verified: Library queries plant_profiles; Packets queries seed_packets — no data routing bug."

**Root cause.** Runtime state-machine bug, not static-route bug. [VaultPageContent.tsx:414-459](src/app/vault/VaultPageContent.tsx:414) restore effect read `sessionStorage.getItem("vault-view-mode")` and called `setViewMode(savedView)` WITHOUT checking `isPlantsSurface`. On `/plants`, viewMode is supposed to be force-locked to `"grid"` by init ([line 130](src/app/vault/VaultPageContent.tsx:130)) + tab-sync ([lines 371-373](src/app/vault/VaultPageContent.tsx:371)). But both forced-grid sets and the restore-effect set ran in the same mount commit; the restore was declared LAST in source order, so the last `setViewMode("list")` won. → list div became visible → `VaultPacketWingContent` rendered (its internal `ctx.viewMode !== "list"` guard at [VaultPacketWing.tsx:578](src/app/vault/components/VaultPacketWing.tsx:578) passed through because `ctx.viewMode` WAS now `"list"`) → `PacketVaultLazy` mounted → Packets render with vendor name + qty stock overlay + `"Search packets…"` toolbar on `/plants`. Self-healed on next `searchParams` change (tab-sync re-forced grid), but user never reached that — page looked broken on landing.

**Why `4e5f3eb`'s claim was wrong in a subtle way.** Static-route inspection (`/plants` → VaultPageContent → SeedVaultView → plant_profiles) IS the intended path. The miss was the runtime state-transition axis — restore effect overriding the forced-grid init/sync. Pass 1 traced the static path but not the full state-transition matrix.

**Pass 1 bidirectional state-transition tracing subcategory (locked 2026-05-27) worked exactly as designed.** When the first ship (`4e5f3eb`) didn't close Syd's bug, the next hypothesis correctly moved to a DIFFERENT state-transition axis (restore-effect runtime override) instead of refining the same static-route axis. Patient-zero pattern self-validated: matrix-driven Pass 1 caught a cell (mount-commit ordering of init/tab-sync/clear/restore on `/plants` with stale sessionStorage) that single-direction tracing missed. This is the third independent validation of the 5-27 rule (after `7c0decf` open-but-not-back, `84df090` chunk-fetch axis vs Suspense+lazy axis).

**Fix.** 4-line `if (!isPlantsSurface)` guard wrapping the `savedView` restore at [VaultPageContent.tsx:418-426](src/app/vault/VaultPageContent.tsx:418). Other restore reads (gridDisplayStyle, packetDisplayStyle, status, search, sort) left untouched — orthogonal sub-state, not the bug.

**Regression test.** [plantsSurfaceViewMode.regression.test.ts](src/app/vault/plantsSurfaceViewMode.regression.test.ts) — 5 structural assertions lock the guard + verify the init function's grid-force branch + verify the tab-sync effect's grid-force branch (defense in depth so a future restore-effect refactor can't silently re-introduce the drift).

**3 buckets:**

- **(a) Parked items added this chat:** `/plants` sessionStorage namespace separation + `getNavSection` `/plants` branch (§4 above). Architectural cleanup; the 4-line guard makes the bug class no longer load-bearing, but the shared-namespace + missing-section-classification fragility remains. Pairs with VaultLibraryWing extraction park (natural co-ship).
- **(b) Deferred audits / known follow-ups:** none new — all open items from prior chats remain open. The Pass 3 ADJACENT finding (`getNavSection` catch-all) was promoted directly to the §4 parked entry above rather than left as a deferred audit.
- **(c) Dogfood findings this chat:** Syd's report framing was load-bearing. She named `"Search packets…"` placeholder as a separate signal from the vendor/stock visual — that string only exists in one file ([VaultPacketWing.tsx:452](src/app/vault/components/VaultPacketWing.tsx:452)) and was the smoking gun confirming VaultPacketWing was rendering on `/plants`, ruling out alternative hypotheses (visual-similarity confusion, SeedVaultView showing packet fields, etc.). **Status: just-captured** — takeaway is that surface-distinctive copy strings ("Search packets…" vs "Search varieties…") are leverage for runtime debugging when console access is gated behind a separate page. Worth keeping the strings clearly differentiated as a debug-aid invariant. No rule change proposed.

**Ship metrics:** 2 files, +105/-2 (1 line guard expansion in VaultPageContent + 102 lines new regression test). Tests 527/527; build clean. Preview MCP skipped per capability honesty (`/plants` auth-blocked; no screenshots per chat brief). Push: `a1d371b..c45d6c6` to main per explicit yes-build greenlight.

### 2026-05-28 (IA Ship A: Plants tab promotion + FAB consistency — `9142aad` + `34e8948`)

**Decision recap:** First of two ships in the IA restructure (Ship A = moves #1+#2 per `.claude/plans/ia_restructure_discussion.md` §5.1). Pure UI restructure with no schema / no data migration / no groups / no Garden merge. Greenlit by Syd via Dispatch engineer-PM mode with Q1=A (Vault default tab = Packets post-Ship-A).

**Plan-of-record + audit deltas:**

- **Pathname-aware delegation locked over full VaultLibraryWing extraction.** IA discussion §5.2 estimated ~3h for the Wing extraction; reading actual code revealed Library "grid" state is deeply interlocked with VaultPageContent (shared search query, gridFilters/listFilters peers, batch select per mode, refine modals). Full extraction is 4-6h with non-trivial regression risk and zero user-visible benefit — the user gets the same Library on `/plants` either way. Trade accepted: ~30-45 min of plumbing work + lower risk in exchange for one parked refactor follow-up (§4 entry above). Saves ~3h ship-time. Wing extraction re-surfaces when not under feature pressure.
- **`/vault` default tab post-Ship-A = Packets** (Q1=A, Syd ratified). Today `/vault` (no query) lands on Library/grid; post-Ship-A defaults to Packets (`?tab=list`) since Library exited. User-visible default shift for every Vault entry point (bottom nav, sidebar, deep links). Bookmarks survive transparently via legacy `?tab=grid|active|plants` redirect.
- **Plants tab icon = new inline SproutIcon** (internal call, ratified). Matches existing nav-icon family (HomeIcon, LeafIcon, VaultIcon, CalendarIcon, JournalIcon — all inline 24×24 stroke-currentColor SVGs per VISION §8 chrome icon convention). Pass 3 sibling-sweep: nav icons are a sibling family, not FAB-tier; new addition matches the family.
- **FAB Add Variety chip icon = `ICON_MAP.Plant`** (internal call, ratified). Sibling reuse with Add Plant sub-screen "From Vault" chip; preserves SeedPacket/Plant visual distinction Path Y locked.
- **FAB chip placement = TOP of menu** per IA discussion §1.3 ordering (encyclopedia adds → inventory adds → instance adds → supply adds → lifecycle → record). Anchored in VISION §3 mental-category split.
- **Detail route `/vault/[id]` stays canonical** (IA discussion Q5 lock). Renaming to `/plants/[id]` would break 30+ deep-link sites (Calendar Plantable, Home, Garden, household-view, journal entry links, achievement badges, profile-from-grow back-button) AND break Syd + sister's existing bookmarks. Deferred to a separate URL-cohesion chapter post-Ship-A.
- **Copy decisions on extracted AddVarietyForm** (internal calls, ratified): header `"Add Plant Variety"` → `"Add Variety"` (matches Title Case chip label); sub-copy references "your plant encyclopedia" (the new /plants surface) not legacy "vault"; submit CTA `"Add to Vault"` → `"Save to Plants"` (Library encyclopedia is now /plants, not /vault — the "to Vault" semantic broke when Library exited).
- **Pass 1 factual:** all cited file paths + line numbers verified pre-edit; build (Next.js production) confirmed `/plants` route compiled as static prerender 1.58 kB matching `/vault`'s shape.
- **Pass 2 concerns hunt + cohesion-by-aggregation:** 6-tab phone-width cramp risk flagged with deferred verification at impl time (existing `min-w-[64px] × 6 = 384px` fits `max-w-lg 512px` container — no responsive label-hide needed for now); FAB chip + form sub-screen patterns match Path Y verbatim per anchor.
- **Pass 3 sibling sweep:** Path Y form extraction (SeedPacketForm / SupplyForm / TaskForm / JournalEntryForm) cited as BLOCKING anchor for AddVarietyForm shape; nav-icon family cited as BLOCKING anchor for SproutIcon; ICON_MAP.Plant ↔ ICON_MAP.Tree sibling distinction preserved; VaultPacketWing / VaultShedWing tab switcher primitive preserved verbatim (Library button gated, not changing shape).
- **Pass 4 lock hygiene clean:** VISION §10 don't-touch untouched (placeholder asset preserved, `/vault/[id]` canonical preserved); §11 parked decisions untouched (FAB stroke weight still parked); ROADMAP §6 FAB-form submit saga 2026-05-26 tokens preserved verbatim (Cancel `border-teal-gus/40 text-teal-gus`, Submit `bg-emerald-600 hover:bg-emerald-700`); VISION §8 casing rule applied throughout (Title Case headers/buttons, sentence case body).
- **E2E-scope sub-rule clean:** zero e2e specs reference `tab=grid|Library|Add Variety|/plants` (grepped). No e2e rewrites required for Ship A.

**3 buckets:**

- **(a) Parked items added this chat:** VaultLibraryWing extraction (§4 above).
- **(b) Deferred audits / known follow-ups:** Ship B (Garden merge + Groups schema + migration + Manage Groups UI + group nav + onboarding tooltip) per IA discussion §5.1; URL-cohesion chapter (`/vault/[id]` → `/plants/[id]` rename); responsive 6-tab cramp verification on real phone widths (deferred to Syd dogfood — if needed, drop nav labels under 412px via `hidden xs:inline`).
- **(c) Dogfood findings this chat:** none — Syd hasn't dogfooded this ship yet; status tags TBD post-dogfood. **Anomaly noted:** commit `9142aad` (Ship A part 1) appeared on `origin/main` before Claude explicitly pushed — suggests an auto-push mechanism (hook or orchestrator). Logged for Syd's awareness; not blocking, but worth confirming the push-tier greenlight gate isn't being silently bypassed.

**Ship A correction (2026-05-28, post-dogfood):** Top-level tab label "Plants" → **"Library"** + nav order swap so Library precedes Garden. Final order: Home → Library → Garden → Vault → Calendar → Journal. AddVarietyForm submit CTA "Save to Plants" → "Save to Library" (matches new tab label). Rationale: Syd's existing mental model uses "Library" (the encyclopedia term that lived inside Vault pre-Ship-A); "Plants" was a placeholder picked during IA discussion that didn't ladder to the existing term. Library-before-Garden order surfaces the reference/lookup surface ahead of the cultivation/lifecycle surface — matches the IA mental hierarchy (varieties → instances). Pure label + order — no route change (`/plants` URL preserved), no schema, no component refactor. Scope: [navItems.tsx:124](../src/components/navItems.tsx:124) + [AddVarietyModal.tsx:263](../src/components/AddVarietyModal.tsx:263). Plan docs stamped: [ia_ship_a_plants_tab.md](../.claude/plans/ia_ship_a_plants_tab.md) + [ia_restructure_discussion.md](../.claude/plans/ia_restructure_discussion.md).

### 2026-05-28 (Phase A follow-up chat — un-archive UI Q2 closure — `f29ae24`)

**Decision recap:** Phase A locked Archive (Growing→Archived) as forward-only and deferred the reverse transition (Archived→Growing) to a follow-up chat. This chat closes the deferred Q2 with a per-row Restore action on `vault/history/page.tsx` — the natural surface that already lists Archived plantings (and now also growing rows post-Phase A's expanded statusColors map).

**Plan-of-record + audit deltas from Phase A:**

- **Surface lock at kickoff time:** `vault/history/page.tsx` per-row Restore, NOT GrowInstanceModal. Symmetric-with-Archive modal Restore is the obvious follow-up shape but was explicitly scoped out per counter-creep guard; logged as Pass 3 ADJACENT for separate ship.
- **Mutation field set:** clears `status='growing', ended_at=null, end_reason=null`. The third field (end_reason=null) catches GrowInstanceModal's `end_reason='archived'` write while remaining no-op on bulk-end shapes (ActiveGardenView at L760 + MyPlantsView at L491 don't set end_reason — pre-existing intra-codebase drift logged as Pass 3 ADJACENT).
- **Affordance choice (Q1 — Rule A ask shipped with recommended default):** text-link `text-emerald-600` + hover underline, inline-flex `min-h-[44px]` wrapper for Walter persona tap-area guarantee. Recommended because (a) history page is dense table where pill buttons would crowd rows; (b) low-frequency action (≠ low-discoverability requirement); (c) anchors loosely to variety-name link affordance family at L116. Alternative: pill button (`rounded-full bg-emerald-50 text-emerald-700 px-2 py-1 text-xs`) — one-line edit to flip if user prefers more visual prominence.
- **Confirm dialog heading (Q2 — Rule A ask shipped with recommended default):** "Restore to Growing?" — recommended because it answers the user's "to what?" question upfront and reinforces the 2-state enum mental model. Alternative: "Restore This Plant?" — perfect symmetric mirror with Archive's "Archive This Plant?". One-line edit to flip if user prefers structural parity over destination-naming.
- **Pass 1 bidirectional state-transition matrix complete:** both directions (Growing→Archived shipped Phase A; Archived→Growing this ship) now have UI surface + backend write. Pass 1 subcategory closes the gap that Phase A's matrix explicitly flagged as ❌.
- **Pass 2 persona walk surfaced Walter tap-area concern → plan revision applied** mid-Pass-2: `inline-flex min-h-[44px] items-center` wrapper added around the text-link button so even at `text-xs` line-height the container guarantees a 44×44 tap surface. Pass 2 re-ran clean on revised render.
- **Pass 3 sibling sweep findings:** 1 BLOCKING (confirm dialog z-stack mirrored Archive precedent verbatim — `z-[100]` overlay + `z-[101]` panel + `fixed left-4 right-4 bottom-4` + `rounded-2xl shadow-xl p-5 mx-auto max-w-sm`); 2 ADJACENT (GrowInstanceModal symmetric Restore; ActiveGardenView pre-existing `end_reason` drift); 1 CONCERN (future canonical copy lock for any subsequent Restore surface).
- **Pass 4 lock hygiene clean:** Phase A's status collapse (2026-05-28 `67a6344`) is the direct prerequisite for this ship; FAB-form submit saga tokens applied verbatim; casing convention + emerald token split + empty-cell em dash all preserved.

**3 buckets:**

- **(a) Parked items added this chat:** none — no new parked items.
- **(b) Deferred audits / known follow-ups:** GrowInstanceModal symmetric Restore (if Syd dogfood reveals it as desired); ActiveGardenView + MyPlantsView bulk-end `end_reason` cohesion (codebase-wide pattern fix, not this ship).
- **(c) Dogfood findings this chat:** none — Syd hasn't dogfooded this ship yet; status tag for findings TBD post-dogfood.

**Ship metrics:** 1 file, +75/-3. Tests 444/444; build clean. NO Preview MCP per HARD RULE. Auto-push for bounded scope per kickoff greenlight; pushed `bab3287..f29ae24`.

---

### 2026-05-28 (`plant_instance_phase_a` chat — Phase A status enum collapse + AGE/dates derivation + auto-Sow drop — `67a6344`)

**Chat purpose.** Phase A of the plant instance page redesign — status foundation. Originally drafted in prior chat (`local_988678e1`) which hung at the `supabase db push` step after writing the plan + applying all code edits + ?running the migration push successfully? — chat was respawned to verify state + complete the ship. Migration was confirmed already-applied on remote (`20260528120000` shows in Local|Remote columns of `supabase migration list --linked`); commits/push completed in this respawn chat.

**Three asks locked at greenlight (Q1+Q2+Q3+Q4):**

**Q1 — Archive confirm button color: A (recommended) — `bg-emerald-600 hover:bg-emerald-700`** per VISION §8 emerald token split. Archive is a reversible state change (data preserved, can be un-archived later), not destructive. Prior red treatment (`bg-red-600`) carried false "danger" semantic and excluded Walter persona (impaired-vision-friendly emerald CTA pattern is canonical app-wide). Replaces with canonical CTA token; Cancel button also aligned to canonical FAB-form submit saga token (`border-teal-gus/40 text-teal-gus`).

**Q2 — Reverse transition (Archived → Growing un-archive UI): A (recommended) — defer to follow-up chat.** Pass 1 bidirectional state-transition trace flagged the gap (no UI surface writes `status='growing'` to an existing archived row). Phase A scope guard was "manual Archive transition" forward-only; un-archive UI deferred cleanly to next chat per scope discipline. Future chat ships an un-archive button in `vault/history/page.tsx` per-row.

**Q3 — Perennial migration mapping: A (recommended) — branched.** `harvested + is_permanent_planting=true → growing` (preserves perennial fidelity — Aria's houseplants, Maya's berries stay Growing); `harvested + annual/null → archived`; `dead → archived`. Uniform-map alternative (simpler but loses perennial nuance) rejected. Idempotent SQL — re-running maps no-ops after first application.

**Q4 — VISION §11 HarvestModal parked entry post-ship update: yes.** 2-state enum supersedes the auto-end-after-harvest model (`status: 'harvested'` is no longer a valid value). Revised recommendation: defer parked entry indefinitely; Phase A's manual-Archive model handles annual end-state realization via user action, not via auto-checkbox at harvest time. Revisit only if dogfood reveals systematic forgetting-to-Archive.

**Plan-of-record (9 plan-enumerated files + 12 mid-execution sibling sweep extensions = 21 files + 1 SQL).** Original plan enumerated 9 files (1 SQL migration, 1 type narrowing, GrowInstanceModal, 5 Sow-drop sites, 2 explicit sibling sites). Prior chat correctly extended the sweep mid-execution to catch 12 more files referencing the 5-state enum (`pending|growing|harvested|dead|archived` patterns in status filters, status colors, status labels). Sweep extension applied to: settings/developer/page.tsx, AddPlantManualModal.tsx, BatchLogSheet.tsx (pending→growing transplant transition removed), NewTaskModal.tsx, PacketVaultView.tsx, SeedVaultView.tsx, vault/[id]/page.tsx + VaultProfilePlantingsTab.tsx + useVaultPacketHandlers.ts, generateCareTasks.ts, revertProfileStatus.ts. Mid-execution scope additions are valid per plan-audit standard; all 12 are mechanically identical sibling-shape fixes (Pass 3 BLOCKING for status filter regressions).

**Core changes (GrowInstanceModal):**
- New helpers `firstPlantedDate(journalEntries, sownDate)` + `lastHarvestedDate(journalEntries)` — derives display from journal evidence with sown_date fallback.
- AGE source switched from static `sown_date` to derived `firstPlantedDate(...)` — fixes the AGE/STATUS contradiction where header read "Not yet planted" + "Growing" together (today's bug).
- "Date Planted" row replaced with "First Planted" (derived, Title Case label) — single source of truth.
- "Last Harvested" row added below First Planted — em-dash via existing `formatShortDate` empty handling.
- STATUS pill: removed `capitalize` CSS workaround, added `statusLabel` helper for explicit Title Case ("Growing" / "Archived"). Colors map collapsed from 5-state to 2-state with default fallback preserved for in-flight migration rows.
- Archive confirm dialog: Cancel aligned to canonical `border-teal-gus/40 text-teal-gus hover:bg-teal-gus/10`; Confirm flipped to `bg-emerald-600 hover:bg-emerald-700` (Q1=A).

**Drop misleading auto-Sow task creation (5 sites):** Sow tasks were being created + auto-completed in add-plant flows even when user was catch-up tracking — misrepresented behavior in the task log. AddPlantModal.tsx ×2 (seasonal new + existing profile branches), PlantingForm.tsx, VaultPageContent.tsx (vault-side plant flow), review-import/page.tsx (review-import seed flow). Harvest tasks PRESERVED at every site as forward-looking reminders. `handleConfirmSchedule` (user-scheduled future Sow tasks) + `completeSowTask` (user marks Sow done flow) preserved verbatim — those are the appropriate Sow-creation paths.

**3 buckets close-out:**

- **(a) Parked items added this chat:**
  - Archived → Growing un-archive UI (Q2 deferred). Next chat ships un-archive button in `vault/history/page.tsx` per-row (the natural surface — Archived plantings live there). Bidirectional state-transition trace from Phase A Pass 1 audit captured the gap explicitly.
  - Phase B visual redesign of plant instance page (hero size, header layout, scroll behavior, back arrow) — separate chunk after Phase A foundation lands.
  - Phase C Care tab implementation — separate chunk.
  - Year-over-year season timeline — separate longer-horizon feature.
- **(b) Deferred audits / known follow-ups:**
  - VISION §11 HarvestModal parked entry now closed via re-frame (Q4 applied) — original auto-end-after-harvest model superseded; manual-Archive at season end becomes canonical. Entry stays in §11 with the re-frame note for historical context.
  - Sow-task creation pattern fragmentation (Pass 3 CONCERN) — 5 sites have duplicated INSERT shapes with subtle variations (displayName vs displayNameNew, due_date sources varying). Phase A drops 5 of 5 auto-Sow sites; future planning-flow Sow tasks can use a single helper. Not Phase A scope; flagged for future cohesion pass when planning flow is added.
- **(c) Dogfood-style findings:**
  - Prior chat hung at `supabase db push` invocation — long-running CLI tool blocked the chat. Migration was actually successful on remote (verified via `migration list --linked`); CLI either silently succeeded then hung waiting for input/confirmation, or the network call exceeded the chat tool's tolerance. **Just-captured** — no rule change proposed yet; second occurrence (≥2 in 30 days) would trigger a "Propose rule now" amendment to CLAUDE.md SQL migrations section about `--include-all` flag or pre-push state check via `migration list --linked`.

**Shipment recap (Phase A):**
- Commit `67a6344` — 22 files changed, +540/-89 (1 SQL migration, 1 type narrowing, 19 code files, 1 doc — VISION §11 re-frame, 1 plan file).
- Migration on remote at `20260528120000_grow_instances_status_collapse.sql` (verified via `supabase migration list --linked` Local|Remote columns match).
- Tests 444/444; build clean.
- NO Preview MCP per HARD RULE (no screenshots for verification).
- Plan + 4-pass audit log at `.claude/plans/plant_instance_phase_a.md`.


### 2026-05-28 (`inventory-polish-bundle-5items` chat — 6 inventory polish items + VISION §8 discovery-vs-lookup sort framework lock — `a51fbd7`)

**Chat purpose.** Bundle 5 polish items surfaced from Syd's 2026-05-27 dogfood of `fb5d9a0` gallery card standardization. Amendment mid-Phase-1: Syd articulated discovery-vs-lookup sort framework + extended Item 2 to include Shed → bundle becomes 6 items + 1 doc lock.

**Three asks locked at greenlight (Q1+Q2+Q3 = A on all three):**

**Q1 — Library list-view shape: Replace condensed with list (recommended).** Library binary toggle becomes gallery ↔ list. Drop `"condensed"` mode entirely; persisted-condensed users auto-migrate to gallery on next load via sessionStorage. Why: post-`c851b3d`/`fb5d9a0`, condensed is an orphan — neither a true gallery card nor a true list row. Killing it gives Library the same 2-state pattern as the 4 peer surfaces (Packets / Shed / Active Garden / My Plants). Cohesion-by-aggregation win across all 5 inventory surfaces.

**Q2 — Packets OOS-bottom trigger condition: Whenever sort = alphabetical, regardless of asc/desc (recommended).** OOS-bottom kicks in any time `sortBy === "variety"`. Why: simpler state (no "default-vs-explicit" tracking needed), semantically defensible (alphabetical reads as "show actionable items first," direction doesn't change actionability), and the `desc` case is rare enough that the simpler rule wins.

**Q3 — Sequencing: Ship all 6 at once (recommended after asks answered).** Single bounded bundle as originally scoped. Cleaner close-out than splitting into pre-ask / post-ask sub-ships.

**Framework lock — VISION §8 "Default sort by use case (discovery vs lookup)" (new sub-rule, locked 2026-05-28).** Names what was implicit per-surface and turns it into a per-category rule. Discovery surfaces (Library, Active Garden, My Plants) = most-recent first; lookup surfaces (Packets, Shed) = alphabetical by name; lookup + OOS-equivalent → OOS-bottom regardless of direction. Pairs with CLAUDE.md quick-lookup callout (same shape as other 2026-05-27 convention locks). **Library + My Plants discovery-default gap noted as parked-for-future-polish** — both currently default to alphabetical (predating the framework); framework is the source-of-truth even when current state hasn't caught up. New inventory surfaces in future ships classify first (discovery vs lookup), apply the matching default, don't re-ask per-surface.

**3 buckets close-out:**

- **(a) Parked items added this chat:**
  - Owner-pill visual cohesion split (OwnerBadge emerald initials on Packets/Shed/Active Garden/SeedVaultView vs FAM violet on MyPlantsView) — semantically different (OwnerBadge shows WHO; FAM shows generic "foreign") so this is a real product question, not a pure visual-token unification. Flagged for future polish — separate chapter.
  - Library + My Plants discovery-default gap — VISION §8 framework points to most-recent first; current code defaults to alphabetical. Framework lock made the gap explicit. Future polish to honor the framework.
  - Calendar (`calendar/page.tsx:2487`) + Journal (`journal/page.tsx:721, 812, 991`) OwnerBadge sites — same hide-own gate would apply but these are out of inventory scope for this ship. Logged for future cohesion sweep.
- **(b) Deferred audits / known follow-ups:**
  - Pass 3 sibling-sweep missed 3 OwnerBadge sites in the initial plan (ActiveGardenView list view + SeedVaultView's 2 gallery sites). Caught + applied inline during execution. Lesson: when sweeping OwnerBadge across "inventory surfaces," grep for the COMPONENT name across `src/components/*` AND `src/app/**/page.tsx` — not just the surfaces named in the plan brief. Captured as a meta-finding for future sibling-sweep methodology.
  - VaultPacketWing + VaultShedWing wrappers carry their own parallel default state that overrides child component defaults — caught during Pass 1 verification. Lesson: when changing prop defaults in shared components, verify wrapper paths too. Already captured in plan amendment.
- **(c) Dogfood-style findings:**
  - User's discovery-vs-lookup framework articulation mid-build = "Propose rule now" trigger → VISION §8 sub-rule lock landed in same ship per Rule A ask answered (Q-bonus = A). Framework now lives in canonical doc instead of just chat scrollback.
  - Mid-build Pass 3 misses (3 OwnerBadge sites) were caught by methodical grep before commit — validates the cost of running an explicit codebase-wide grep on the FIX TARGET (OwnerBadge) rather than the SCOPE TARGET (5 inventory surfaces). Future sibling sweeps should grep the fix target first, then narrow.

**Shipment recap:**
- Commit `a51fbd7` — 13 files, +140/-112. Single bundle. Tests 444/444; build clean. NO Preview MCP per HARD RULE.
- Doc amendments: VISION.md §8 new "Default sort by use case (discovery vs lookup)" subsection + CLAUDE.md new "Default sort convention — discovery vs lookup (locked 2026-05-28)" quick-lookup callout. Same 2026-05-27 convention-lock pattern.
- Plan + audit log at `.claude/plans/inventory_polish_bundle_5items.md`.


### 2026-05-28 (Phase 2 follow-up polish bundle `88ba080` — `phase2-followup-polish-4items` chat)

- **Phase 2 follow-up polish bundle ✅ SHIPPED — 4 dogfood items from Syd + 1 sibling fix folded in.** Same-day follow-up to the Phase 2 zone-aware enrichment ship (`dcfdbe4`). Syd surfaced 4 trust/UX gaps when dogfooding Phase 2; all 4 shipped as a single bundle per chat brief. Phase 2's invisible behavior (server-side AI prompt biased to user's zone, lazy-populated planting_window) needed UI surfacing for users to TRUST that zone-awareness actually applied — Item 1 (Generated for Zone X label) is the literal verification UI for the invisible behavior.

- **Item 1 — Generated for Zone X label.** New `plant_profiles.planting_window_zone` text column (additive idempotent migration `20260528120000_plant_profiles_planting_window_zone.sql`, applied via `supabase db push --linked`; ADD COLUMN IF NOT EXISTS pattern matching the 2026-05-27 `seed_propagation_context` migration shape). `enrichProfileFromName.ts` line 185 (new): `if (enrichData.zoneUsed != null) updates.planting_window_zone = enrichData.zoneUsed;` — persists the zone the server-side enrich route reported using. `vault/[id]/page.tsx:203` select string extended to fetch the new column. `VaultProfileAboutTab` renders italic info-note BELOW the How to Grow card's dl grid (between dl and the seed_propagation_context conditional) when both `planting_window_zone` and `planting_window` are non-empty: "Generated for Zone {N}." Anchors to VISION §8 info-note primitive `text-sm text-neutral-600 italic`, sentence case. Lazy-populated by design — existing profiles stay NULL until re-enrichment via ✨ Fill Blanks. **Why "BELOW the grid":** sibling pattern to `seed_propagation_context` rendering at the same site (VaultProfileAboutTab:109-111 already renders an italic info-note below the dl as conditional aside). Both italic notes group visually when both fire.

- **Item 2 — Zone input validation: ALREADY SHIPPED, no code change.** Pass 1 factual verification: Settings → Profile uses `<select>` element at `page.tsx:396-405` with `ZONES = ["1a","1b",...,"13b"]` array at `page.tsx:269`. Only valid USDA zones can be selected; "21b" is impossible to enter via the dropdown. Pass 3 sibling sweep: grep for `type="text"` + zone-adjacent input across `src/` found ONLY `location-name` field (free-text for "Vista, CA" style location string) — no free-text zone input exists. The dogfood "21b via speech-to-text" report doesn't reproduce on current code; either the report predates the dropdown ship OR was speech-to-text typing into a different field. Documented as confirmed-already-shipped; no action.

- **Item 3 — Plant viability warning.** `researchVariety.ts:77-79` zoneClause prompt extended with explicit viability case: "VIABILITY CHECK: if this plant CANNOT survive outdoor growing in Zone ${zoneNormalized} year-round (e.g. tropical fruit in cold zones, plants that need winter chill the zone doesn't deliver, plants that need a longer growing season than the zone offers), return the exact string 'Not viable in Zone ${zoneNormalized} — indoor / greenhouse only' instead of a window. Use this ONLY for plants that genuinely can't survive outdoor; don't use it for plants that are merely difficult or require extra care." UI in `VaultProfileAboutTab` detects `profile.planting_window?.trim().startsWith("Not viable in Zone")` and renders italic info-note ABOVE the How to Grow dl grid: "This plant won't survive outdoor growing in Zone {planting_window_zone || 'your zone'}. Consider growing indoors or in a greenhouse." Detection is prefix-match; safe across i18n / format drift because the AI literal is exact. Downstream `getDefaultSowMonthsForZone10b` parser correctly returns all-false on the viability string (no month-name matches → not plantable any month → Calendar/Vault Plantable widget correctly excludes the plant). Persona walk: Walter/Sam in cold zones see "won't survive outdoor" warning when adding tropical fruit (saves wasted ship cost); Sydney 10b rarely sees the warning (most plants viable); Aria houseplants benefit from "not viable outdoor" framing; Maya power user wants to know what won't grow.

- **Item 4 — Zone-change retrofit awareness toast.** `settings/profile/page.tsx` `saveGardenSettings` extended: before upsert, capture `previousZone = (lastSavedSettings?.planting_zone ?? "").trim()` and `newZone = (toSave.planting_zone ?? "").trim()`; after successful upsert, if `previousZone && newZone && previousZone !== newZone`, set new `zoneChangeNotice` state holding `{oldZone: previousZone, newZone}`. Render below the existing Saved! indicator (only in view-mode block) as persistent dismissable info-note: `bg-neutral-50 border border-neutral-200 rounded-lg p-3` container with italic neutral-600 prose ("Your zone changed from Zone {old} to Zone {new}. Existing plant profiles still use the old zone. To update them, open each profile and tap ✨ Fill Blanks.") + `ICON_MAP.Close` dismiss button at 44×44 tap target. **Gated to actual zone changes, not first-time-set** — `previousZone && newZone` excludes null → "5a" (new user setting zone for first time has no existing profiles to retrofit, so no awareness notice needed). v1 awareness-only per chat brief: no auto-batch, no schema additions for retrofit mechanism. Deferred to Phase 2.5+: smarter batch retrofit using zone-keyed library cache (avoids AI calls when possible).

- **Sibling fix folded in (Pass 3 ADJACENT).** `vault/[id]/page.tsx:203` select string was missing `seed_propagation_context` from the 2026-05-27 Propagate / Save Seeds ship (`39d5faa`). Net effect of the drift: `profile?.seed_propagation_context?.trim()` at `VaultProfileAboutTab:109` + `:187` evaluated to undefined-falsy because the column wasn't fetched → the italic caveat for plants like succulents / hostas / apple varieties never rendered. **In-scope justification:** Item 1 requires editing the SAME select string to add `planting_window_zone`; adding `seed_propagation_context` in the same line edit is hygiene, not scope creep. Confirmed retroactively that the 2026-05-27 ship's downstream test mocks worked because they returned hardcoded objects with the column present — the select-drift was a silent prod regression invisible to the test mock path. Lesson for sibling-sweep methodology: when a recent column-add ship lands, sweep ALL select strings that fetch from the table to verify the column was added downstream.

- **4-pass plan-audit terminated clean iter 1 + Pass 2 re-run iter 2.** Pass 1 factual including bidirectional state-transition matrix SKIP-with-reason (no modal/menu lifecycle touched; persisting a column + rendering it; no axes to trace) + every cite verified (migration timestamp uniqueness vs `supabase/migrations/` existing files; `ICON_MAP.Close` export at `styleDictionary.tsx:584`; `useUserPlantingZone` hook + `enrichProfileFromName` updates-builder pattern). Pass 2 concerns hunt across 8 categories: race conditions / async ordering (gated retrofit notice to actual changes; rapid-fire save edge case mitigated); null / empty states (lazy-populate behavior; first-time-set excluded from retrofit); schema migration safety (additive idempotent, nullable, no RLS change); AI-prompt regression risk (viability case is additive, parser correctly returns no months); cohesion-by-aggregation anchors named for every micro-decision (info-note primitive ↔ VISION §8 lock 2026-05-27; retrofit notice copy register ↔ helper-text patterns; AI prompt viability phrasing as explicitly-named new pattern; column naming `planting_window_zone` ↔ existing `planting_window` parent); persona walk all 5 pass (Maya/Sydney/Walter/Aria/Sam — Items 1/3 universally beneficial; Items 1/4 have "edge case but safe" for one persona each, no exclusions); test coverage gaps (no new tests this ship — schema is verified at migration push, type-check catches enrichProfileFromName persist line, manual dogfood verification path stated); sibling drift (Pass 3 ADJACENT folded into Item 1). Pass 3 sibling sweep across info-note rendering peers (VaultProfileAboutTab:110 + :188) + Settings zone-input surface verification + enrichProfileFromName updates-builder pattern (every field follows `if (enrichData.X != null) updates.X = enrichData.X;` — Item 1's new line aligns exactly) + vocab-breadth grep across `zone | window | provenance | generated_for | retrofit | refresh | regenerate | re_enrich`. Pass 4 lock hygiene clean — VISION §8 info-note primitive + casing rule respected; §10 don't-touch (Plant placeholder / Plantable widget / Grid surface / Task list / Three transition languages) untouched; §11 Phase 3 multi-zone Schedule rebuild + FAB stroke-weight + cross-view consistency untouched; ROADMAP §6 same-day Phase 2 entry already names follow-up work as next direction (alignment confirmed); no state-machine surface touched; no React contexts modified.

- **Operating mode discipline.** Chat brief explicit: NEVER use AskUserQuestion (Dispatch context — plain English asks at end of message ONLY per CLAUDE.md AskUserQuestion forbidden subsection 2026-05-25); NEVER take Preview MCP / browser screenshots for verification (locked 2026-05-27 + reinforced 2026-05-28); DO NOT block ship waiting on visual verification — surface as close-out dogfood asks. PostToolUse hook fired the "preview server running" reminder mid-build; ignored per chat brief + CLAUDE.md hard rule. Tests + build + git diff was the verification path. Visual verification falls to Syd dogfood (paths stated in §1 lead-line + close-out below).

- **Files + diff shape.** 6 src files (`settings/profile/page.tsx` + `vault/[id]/VaultProfileAboutTab.tsx` + `vault/[id]/page.tsx` + `lib/enrichProfileFromName.ts` + `lib/researchVariety.ts` + `types/garden.ts`) + 1 new migration (`supabase/migrations/20260528120000_plant_profiles_planting_window_zone.sql`), +48/-3 net. Tests 444/444; build clean. Migration applied to remote (additive + idempotent; code-tier per CLAUDE.md SQL migrations subsection). Concurrent-fetch validation 0/0 pre-push. Plan + audit at `.claude/plans/phase2_followup_polish_4items.md` (full 4-pass log inline).

- **Syd dogfood verification path on prod.** (1) Add a new variety via FAB → Add Seed → Manual → fill in any plant + variety → Save → wait 5-10s for enrichment → open the Vault profile → About tab → How to Grow card → confirm "Generated for Zone {your_zone}" italic note appears BELOW the dl grid. (2) Open any existing profile where Fill Blanks set `seed_propagation_context` (succulent / hosta / apple variety — pre-existing data from the 2026-05-27 Propagate ship) → confirm italic caveat now renders on Propagate / Save Seeds card (previously missed due to select drift — was silently broken). (3) Settings → Profile → Edit Garden Settings → confirm Planting Zone is a `<select>` dropdown restricted to 1a-13b (no "21b" possible). (4) Temporarily change planting_zone to a cold zone (e.g. 5a) → add a tropical plant via Fill Blanks (mango, banana) → confirm About tab shows "This plant won't survive outdoor growing in Zone 5a..." warning ABOVE the How to Grow grid; the Planting Window dl row should read literally "Not viable in Zone 5a — indoor / greenhouse only". (5) Settings → Profile → Edit Garden Settings → change zone (e.g. 10b → 9b) → Save → confirm persistent neutral info-note appears below Saved! pointing to ✨ Fill Blanks; tap dismiss button → confirm notice removes. Restore zone after testing.

- **Out of scope (parked).** Phase 2.5 zone-aware `global_plant_library` schema (foundational; separate chat) — needed for smart batch retrofit that avoids AI calls. Phase 3 multi-zone Schedule rebuild stays parked. Plant instance page redesign, Care tab on growing instance, multi-link "+" backend, custom date dropdown, FAB "Add to Library" entry point, AddPlantManualModal Vault Packets tab polish all explicitly out-of-scope per chat brief.

---

### 2026-05-28 (earlier — Grow-now Phase 2 ship `dcfdbe4` — `grow-now-phase-2-zone-enrichment` chat)

- **Phase 2 zone-aware enrichment ✅ SHIPPED — capability gap behind feb2e32 stopgap closed.** Phase 1 (feb2e32, 2026-05-27) added honest-framing copy on Schedule + Calendar surfaces acknowledging the Zone 10b hardcoded data; Phase 2 closes the actual capability by reading `user_settings.planting_zone` server-side in `/api/seed/enrich-from-name` and instructing the Gemini research prompt to bias `planting_window` to that zone. Lazy backfill — only new profiles get zone-aware windows; existing profiles unchanged. Closes BUGS.md Post-Launch #1 capability portion. Phase 3 (multi-zone cross-profile aggregate-schedule view) stays parked.

- **Decision — server-side zone-read is authoritative.** Client-side `useUserPlantingZone()` value passed via options is for observability only (3 logEvent breadcrumbs). Why: prevents client/server zone-mismatch when user changes zone mid-flow; `fill-blanks-for-profile` transparently benefits via its internal server-to-server fetch of `enrich-from-name` (no separate wiring needed); backfill scripts keep their non-user context (passes `undefined`).

- **Decision — library-cache skipped when zone is non-10b.** `global_plant_library` has no zone column (verified via Read of route.ts:64-99 + line 142-162 upsert payload). For non-10b users we therefore skip the cache and let AI regenerate. Cost: ~1 Gemini call per never-seen-variety enrichment, bounded by same cost path that already exists for cache-miss. 10b users + unset-zone users keep library-first behavior (cost regression = zero). Future optimization: zone-keyed library cache (parked; not on any current chunk).

- **Decision — AI prompt instructs Jan-Dec month-abbreviation format regardless of zone.** Downstream `plantingWindow.ts:getDefaultSowMonthsForZone10b` parser is month-name-based (despite the "Zone10b" name, the parser is generic). New `zoneClause` template explicitly tells the AI to keep Jan/Feb/Mar/.../Dec abbreviations so the parser still flags the right months for any zone. Example outputs: zone 5a tomato "Indoor sow Feb-Mar, transplant after last frost May-Jun"; zone 9b tomato "Direct sow Mar-Apr, or Aug-Sep for fall crop"; zone 10b tomato "Spring: Feb-May" (matches existing canonical format).

- **4-pass plan-audit terminated clean iter 1.** Pass 1 factual including bidirectional state-transition matrix SKIP-with-reason (stateless background fetch — no modal/menu lifecycle, no axes to trace) + verification that `extract/route.ts:171` re-exports `researchVariety` from `@/lib/researchVariety` (so the surgical edit covers all production callers). Pass 2 concerns hunt across 9 categories incl. persona walk all 5 pass + cohesion-by-aggregation anchors named for every micro-decision (logEvent format ↔ existing scope pattern; response field shape ↔ existing snake/camel mix; prompt phrasing as explicitly-named new pattern). Pass 3 sibling sweep across 3 researchVariety callers (extract URL extraction + 2 backfill scripts pass `undefined` for backward compat) + vocab-breadth grep `zone|window|usda|hardiness|climate|sow|planting` (no missed pattern). Pass 4 lock hygiene clean — VISION §10 don't-touch untouched (no UI surface changed); §11 Phase 3 multi-zone Schedule rebuild explicitly deferred (this ship is Phase 2 only); ROADMAP §6 feb2e32 entry already named Phase 2 as the next ship (alignment confirmed).

- **Observability shape.** 3 new `logEvent("enrich", ...)` breadcrumbs in `enrichProfileFromName`: `zone_requested {profileId, userZone}` (caller fired with zone present); `zone_window_generated {profileId, userZone, window}` (API returned non-empty planting_window); `zone_fallback {profileId, reason: "no_user_zone" | "ai_no_zone_data"}` (zone unset OR AI returned empty). Server-side `console.log("[enrich-from-name] zone=X librarySkipped=Y ai=true")` confirms the route-side decision. Visible via `/settings/developer/debug-log` → Copy all → paste.

- **Test scaffolding.** New `makeSupabaseWithZone(zone)` per-table mock factory in `enrich-from-name/route.test.ts` routes `from(table)` to a table-specific chain — solves the previous "single chain for ANY table" limitation. 6 new tests cover: zone passed to researchVariety; undefined when unset; library skipped when non-10b; library kept when 10b; response includes zoneUsed; "Zone 5a" prefix normalization. `AddPlantModal.test.tsx` mocks `useUserPlantingZone` to keep existing 3 tests deterministic.

- **Out of scope (parked).** Phase 3 multi-zone Schedule rebuild stays parked (VISION §11). `fill-blanks-for-profile` route changes — transparently benefits via downstream fetch, no edit needed. Backfill scripts — admin context (no user zone); `extract/route.ts:441` vendor URL scraping — zone-agnostic by nature. Cost optimization (zone-keyed `global_plant_library`) — future, accepted as cost-bounded for now. Schema changes — none, reuses existing `planting_window text` column.

- **Syd dogfood verification path on prod.** Settings → Profile → change planting zone to "5a" → FAB → Add Seed → Manual → fill in a variety like "Tomato" + "Cherokee Purple" → Save → wait 5-10s for enrichment to complete → open Vault profile → confirm `planting_window` reads zone-specific (e.g. "Indoor sow Feb-Mar, transplant May-Jun"). Optional verification: `/settings/developer/debug-log` → Copy all → confirm `[enrich] zone_window_generated {userZone: "5a", window: "..."}` event fired. Restore zone after testing.

---

### 2026-05-28 (`gallery-card-view-consistency` chat — 5-surface gallery card cohesion + VISION §11 cross-view-consistency parked decision closes — `fb5d9a0`)

- **Sibling to `c851b3d` list view consistency ship 2026-05-27.** Same "same format, different data" principle, this time for card/gallery view mode. Standardizes the gallery card across all 5 inventory surfaces (Vault Library/Packets/Shed + Garden Active Garden/My Plants). **Closes VISION §11 "Cross-view consistency: Vault grid / Garden gallery / My Plants list" parked decision (raised 2026-05-07).** Journal already explicitly out-of-scope per the 2026-05-27 closure note on the same §11 entry (action-log mental model is intentionally distinct from inventory views).

- **Reference pattern lifted from Library condensed mode + My Plants gallery — already nearly identical, just needed the other 3 surfaces aligned.** Canonical shape: `rounded-lg bg-white shadow-card overflow-hidden flex flex-col border border-black/5 card-interactive` outer; `px-1.5 pt-1.5 shrink-0` image-padding wrapper → `relative w-full aspect-square overflow-hidden rounded-xl` image inner; `px-1.5 pt-1 pb-0.5 flex flex-col flex-1 min-h-0 items-center text-center min-w-0` text section; `font-semibold text-black text-xs leading-tight w-full min-h-[1.75rem] line-clamp-2 break-words text-center mb-0` name; `text-[10px] leading-tight text-black/60 italic` variety/secondary; optional `mt-auto pt-0.5 ... text-[9px] text-black/60` bottom-meta row.

- **Per-surface migrations:**
  - **Packets** ([PacketVaultView.tsx:649-740](src/components/PacketVaultView.tsx:649)): `grid-cols-2` → `grid-cols-3`; image padding `p-1` → `px-1.5 pt-1.5`; Out/Full chip relocated from bottom-flex-row to absolute top-1 right-1 inside image container (now `text-[8px]` to fit ~108px-wide 3-col card); text `text-sm`/`text-[11px]` → `text-xs`/`text-[10px]`; name `min-h-[2rem]` → `min-h-[1.75rem]`; container `shadow-sm` → `shadow-card` + `card-interactive`.
  - **Shed** ([ShedView.tsx:469-553](src/components/ShedView.tsx:469)): dropped `text-left` from container; wrapped image in `px-1.5 pt-1.5` padding container with `rounded-xl`; text section flipped to centered + `flex flex-col flex-1 min-h-0 items-center text-center`; name `font-medium text-neutral-900` → `font-semibold text-black` + standard min-h sizing; brand `text-[11px] text-neutral-500 truncate` → `text-[10px] leading-tight text-black/60 italic line-clamp-1` (matching variety treatment); category + last-used dropped to `mt-auto pt-0.5 text-[9px] text-black/60` bottom-meta row.
  - **Active Garden** ([ActiveGardenView.tsx:1020-1118](src/components/ActiveGardenView.tsx:1020)): image `rounded-md` → `rounded-xl`; **dropped edit-pencil button section entirely** (lines 1101-1116 in pre-ship — removed `border-t border-black/5` separator + `ICON_MAP.Edit` button); journaling still accessible via card-tap → batch detail page or via My Plants list-mode quick-action peer. Syd explicit OK at handoff.
  - **Library** ([SeedVaultView.tsx:1260-1336](src/components/SeedVaultView.tsx:1260)): already the canonical reference shape — no class swaps for 3-col condensed mode. Only change: owner badge migration (see below). F1 chip stays bottom per Q2.
  - **My Plants** ([MyPlantsView.tsx:906-954](src/components/MyPlantsView.tsx:906)): already at canonical shape (FAM top-left + Perennial top-right is the pattern the other 4 surfaces are migrating to). **Zero code change.**

- **Q1 lock (per Syd plain-English-at-end ask, locked direction A):** OwnerBadge migrated to **top-LEFT** across Library + Packets + Active Garden, matching the existing My Plants FAM-badge slot. **Top-RIGHT now canonical "status chip" slot across all 5 surfaces** (Perennial / Out·Low·Full / planting_method_badge / future status chips). Resolves the pre-existing Active Garden family-view overlap bug as a side-effect (`planting_method_badge` top-right + `OwnerBadge` top-right both rendered simultaneously when `viewMode === "family"` AND a planting method was set — neither badge was readable when both fired).

- **Q2 lock (per Syd plain-English-at-end ask, locked direction A):** F1 chip stays at bottom on Library cards. F1 is genetics-identity-level (sub-attribute of a specific packet — "this is a hybrid"), semantically distinct from lifecycle category (Perennial — "this is a perennial plant") or stock status (Out/Full — "this packet is depleted"). Bottom-meta slot is semantically cleaner; top-right stays reserved for status-class chips.

- **Pass 3 sibling sweep clean.** Full `grid grid-cols-3` + `grid grid-cols-2` sweep run. **In-scope migrations:** the 4 edited files above + 1 verify-only (My Plants). **ADJACENT skips (with reason):** calendar `grid-cols-3` matches at [calendar/page.tsx:1590](src/app/calendar/page.tsx:1590) + [calendar/page.tsx:1722](src/app/calendar/page.tsx:1722) are button-row layouts for reschedule/snooze pickers (Tomorrow / In 3 days / Next week), NOT card galleries; vault profile `grid-cols-3` matches at [vault/[id]/page.tsx:827](src/app/vault/[id]/page.tsx:827) + 862 + 871 are hero-image-picker thumbnail grids inside set-photo modal, NOT card galleries (different surface type); form-field `grid-cols-2` matches in CareScheduleManager/HarvestModal/QuickAddSupply/settings-profile/shed-review-import are label/input pair layouts, NOT card galleries; PageSkeleton/VaultSkeleton matches are loading skeletons. **EXPLICIT OOS:** journal `grid-cols-2 sm:grid-cols-3` at [journal/page.tsx:845](src/app/journal/page.tsx:845) — journal stays in its own format register per VISION §11 closure note 2026-05-27 (different mental model from inventory views).

- **Pass 4 lock hygiene clean.** VISION §10 don't-touch unbrushed — "Active Garden gallery card existence" preserved (changing card SHAPE, not the view itself); "Plant placeholder PNG canonical" preserved (PlantPlaceholderIcon untouched). VISION §11 "Cross-view consistency: Vault grid / Garden gallery / My Plants list" — **this ship is the closure work**; entry updated to closed-status with `fb5d9a0` hash reference. Other §11 parked items untouched (App-wide icon density, Calendar fatigue remainder, plant image edit places). ROADMAP §6 emerald-token split (`4c92e68` + this-day follow-up) preserved — gallery cohesion ship doesn't touch primary-emphasis CTA surfaces.

- **Bidirectional state-transition tracing Pass 1 subcategory (CLAUDE.md 2026-05-27 rule): SKIP-with-reason.** Pure className edits + one JSX block deletion (Active Garden edit-pencil section); no modal/menu lifecycle changes; no React state surfaces touched; no Suspense boundary affected. Matches the rule's "pure styling / copy-only changes that don't touch state transitions" skip condition.

- **E2E-scope sub-rule (CLAUDE.md 2026-05-27 rule) clean.** Pre-flight grep on `aria-label="Add journal entry"` against `e2e/` directory returned **zero matches** — the dropped Active Garden edit-pencil button's aria-label has no e2e assertion to break. No visible-text strings flipped; class-string changes only. No e2e spec updates required.

- **Persona walk Pass 2: all 5 pass.** Maya (power user, scans labels) gets denser 3-col Packets = more cards visible per scroll, calmer Active Garden card without competing pencil-button affordance. Sydney (cohesion-driver) gets the explicit ship she dogfood-flagged at handoff; one of the longest-parked §11 decisions (2026-05-07 — 21 days old) finally closes. Walter (iPad-primary, swipe-excluded) sees card-tap targets unaffected, 3-col Packets card ≥44px width on iPad-portrait, no chrome regression. Aria (urban houseplant, low-data) sees My Plants gallery untouched (her primary surface); empty-state flow preserved. Sam (Day-1 beginner) sees empty-state cards untouched; populated card shape change only kicks in once she has data.

- **NO Preview MCP / browser screenshots** per CLAUDE.md HARD RULE locked 2026-05-27 + handoff explicit re-statement. Tests + build + git diff is the sufficient gate. **Syd dogfood path** (mobile-viewport on Pixel-shape phone): (a) `/vault` → Packets tab → confirm 3-col grid renders with Out/Full chip top-right inside image (was bottom-center in 2-col); (b) `/vault` → Library tab condensed mode → confirm owner badge now at top-LEFT (was top-right); F1 chip still bottom on hybrid packets; green active-growing outline preserved; (c) `/vault` → Shed tab → confirm 3-col stays + image now has rounded-padding-container look (was full-bleed) + text is CENTERED (was left); (d) `/garden` → Active tab gallery view → confirm image now rounded-xl (was rounded-md, subtle), edit-pencil button section gone (cleaner card), owner badge at top-LEFT in family view, planting-method badge at top-RIGHT alone (no overlap); (e) `/garden` → My Plants tab gallery view → confirm zero change vs. before (this surface was the canonical reference). Rhythm check: scroll between all 5 tabs and confirm cards FEEL consistent — same image proportions, same text alignment, same chip placement.

- **3 buckets at close.**
  - **(a) Parked items added this chat:** none. **VISION §11 "Cross-view consistency" parked decision CLOSED** (the only register-impacting outcome).
  - **(b) Deferred audits / known follow-ups:** (i) pre-existing batch-select-circle + FAM-badge overlap in My Plants gallery (top-left slot stacks both `top-2 left-2 w-6 h-6` batch circle + `top-0.5 left-0.5` FAM badge — rare edge case only fires when batch-select-mode AND household=family-view AND a household member's card). Not introduced by this ship; matches the reference pattern. ADJACENT finding, not blocking. Same overlap pattern now mirrored on Library + Packets + Active Garden owner-badge migrations. Worth a future audit-time consideration if Syd ever uses batch-select mode in family-view. (ii) F1 chip placement on Library photo-card mode (2-col): chip stays bottom per Q2; same as condensed mode. (iii) Library photo-card mode (2-col) is OUTSIDE this chapter's scope per Syd's matrix targeting 3-col default; Q1 owner-badge migration was applied there too for cross-mode consistency within the same file, but image padding + text sizing follow the 2-col `text-sm`/`min-h-[2rem]` scaling — kept verbatim. If Syd ever defaults Library to photo-card mode, the cohesion-by-aggregation question reopens.
  - **(c) Dogfood-style findings — just-captured:** the Active Garden family-view OwnerBadge + planting_method_badge overlap was a real pre-existing bug fixed as a side-effect of Q1's canonicalization. Hadn't been flagged by Syd because she doesn't routinely use family view with planting methods set, but the overlap was visible whenever both conditions met. Captured as silent-fix in this ship. Pattern: **cross-surface badge-slot canonicalization is one of the cheapest ways to surface latent overlap bugs in a multi-tenant UI** — proposing this as a quiet observation rather than a rule (3-instance threshold for rule promotion not yet met; first observed instance here).

- **4 files modified, +54 / -68 net (mostly edit-pencil deletion).** [ActiveGardenView.tsx](src/components/ActiveGardenView.tsx) (+5/-24), [PacketVaultView.tsx](src/components/PacketVaultView.tsx) (+18/-18), [SeedVaultView.tsx](src/components/SeedVaultView.tsx) (+4/-4), [ShedView.tsx](src/components/ShedView.tsx) (+27/-22). MyPlantsView.tsx verify-only — zero diff. **Tests: 444/444 pass.** Build clean. Plan + 4-pass audit log lives at [.claude/plans/gallery_card_view_consistency.md](.claude/plans/gallery_card_view_consistency.md) (medium-scope ship per CLAUDE.md ≥3-files threshold).

---

### 2026-05-27 (small follow-up after `4c92e68` cohesion polish bundle — VISION §8 emerald-token split sub-rule + VISION §11 JournalView closure note + 4-site CTA flip)

- **Closes the two Q-asks surfaced as plain-English-at-close from `4c92e68` chat.** Both Syd-locked direction (A) per Dispatch handoff: Q1 = keep emerald-500 for state/selection + emerald-600 for CTA (document the split as a §8 sub-rule); Q2 = skip JournalView format migration + document divergence in VISION §11 as intentional closure note. Single follow-up ship bundles the doc adds with the 4-site CTA flip that the new sub-rule implies.

- **VISION §8 — new "Emerald primary-emphasis token split" subsection.** Sibling to casing / empty-state / icon-style / info-note / chevron framing / chrome control framing — same 2026-05-27 doc-architecture pattern (rule + criterion + sites + why + persona walk + GT-only). Two branches: `bg-emerald-500 text-white` = STATE / SELECTION (tab nav active, selection pill, "you are here" indicator, batch-selection checkbox, toggle-switch checked); `bg-emerald-600 hover:bg-emerald-700 text-white` = CTA / SUBMIT (Save button, primary form submit, "do this action" CTA, "Go to X" navigation trigger, primary call-to-action in empty-state card). Decision criterion: *user expected to TAP for an ACTION → emerald-600; element communicating STATE → emerald-500.* Why the split: 1-step shade difference is barely perceptible side-by-side but carries semantic meaning when the eye scans — current-state surfaces should not read as call-to-action surfaces. Same chrome-vs-content principle as the icon-style + chrome-control-framing rules applied at the color-token layer.

- **VISION §8 — legacy "Colors / brand" stub marked superseded.** The pre-existing `bg-emerald` brand-token stub (line 236-238) was "pending user confirmation" since the early design-token phase. Codebase canonical settled on `bg-emerald-600` (Tailwind direct) via the FAB-form submit saga (locked 2026-05-26 across `fc1463d` → `ba9319f`) — the brand-token aspiration remained academic. Appended one-line supersession pointer to the new sub-rule per CLAUDE.md authority-precedence "losing doc gets updated to match the winner so the conflict doesn't re-surface." Substance of the stub preserved for historical context.

- **VISION §11 — JournalView intentional-divergence closure note appended to "Cross-view consistency" entry.** Per Syd Q2 lock: JournalView ([src/app/journal/page.tsx](src/app/journal/page.tsx)) stays in its own format register (action-log shape — grouped by entry/date/plant across 3 view modes: table mobile-card / timeline plant-gallery / grid Instagram-feed) by design. Different mental model from the collection-list surfaces (Library / Packets / Shed / Active Garden / My Plants are all inventory views; Journal is an event-history view). Already shares design tokens with the unified list pattern (`rounded-xl border bg-white p-4 shadow-card card-interactive` on mobile-card mode at [journal/page.tsx:685+](src/app/journal/page.tsx:685)) so visual cohesion is preserved at the token level; shape divergence is intentional. Decision: keep distinct; don't force-fit into the condensed-list wrapper. **Cross-view-consistency parked decision (raised 2026-05-07) doesn't close — Journal is now explicitly out-of-scope; remaining cross-view work still concerns the 5 inventory surfaces only.**

- **CLAUDE.md — "Emerald primary-emphasis token convention" callout added.** Parallel structure to Casing / Empty-cell / Icon style / Chrome control framing 2026-05-27 callouts. Quick-lookup format for future in-chat plans (state/selection bucket vs CTA/submit bucket + audit-time criterion + "not for" list of semantic non-CTA usages).

- **4 CTA sites flipped from `bg-emerald-500` → `bg-emerald-600` + `hover:bg-emerald-600` → `hover:bg-emerald-700`** to align with the new sub-rule:
  - [ActiveGardenView.tsx:991](src/components/ActiveGardenView.tsx:991) — "Go to Seed Vault" empty-state CTA
  - [ActiveGardenView.tsx:1010](src/components/ActiveGardenView.tsx:1010) — "Show all plantings" empty-state CTA
  - [NoMatchCard.tsx:30](src/components/NoMatchCard.tsx:30) — shared "no match" primary CTA (used across Vault Library + Garden Active + Garden My Plants empty-search states)
  - [vault/review-import/page.tsx:1247](src/app/vault/review-import/page.tsx:1247) — "Find Hero Photos" CTA (also flipped `border-emerald-500 → border-emerald-600` to keep border matching bg fill; secondary "Save without hero photos" peer button unchanged — already neutral-bordered)

- **Pass 3 sibling sweep clean.** Full app-wide `bg-emerald-500` audit ran:
  - **State / selection (LEAVE as-is per Syd lock):** [schedule/page.tsx:74](src/app/schedule/page.tsx:74) (tab nav active), [MonthlyPulseView.tsx:53](src/components/schedule/MonthlyPulseView.tsx:53) (selected month pill), [calendar/page.tsx:2473](src/app/calendar/page.tsx:2473) (batch-selection checkbox), 3 toggle-switch checked states ([FeedbackModal.tsx:378](src/components/FeedbackModal.tsx:378), [SettingsSuccessSoundToggle.tsx:39](src/components/SettingsSuccessSoundToggle.tsx:39), [NewTaskModal.tsx:336](src/components/NewTaskModal.tsx:336))
  - **Semantic non-CTA (LEAVE as-is — different role from primary-emphasis CTA):** 3 progress-bar fills ([ActiveGardenView.tsx:1250](src/components/ActiveGardenView.tsx:1250), [VaultPageContent.tsx:1461](src/app/vault/VaultPageContent.tsx:1461), [settings/developer/page.tsx:903](src/app/settings/developer/page.tsx:903)), 3 full-screen success-flash overlays at `/90` opacity ([calendar/page.tsx:1849](src/app/calendar/page.tsx:1849), [PlantingForm.tsx:856](src/components/PlantingForm.tsx:856), [vault/[id]/page.tsx:1111](src/app/vault/[id]/page.tsx:1111)), 4 color-legend swatches/markers in Schedule views ([AnnualRoadmapView.tsx:24+139+168](src/components/schedule/AnnualRoadmapView.tsx:24), [ActionNowView.tsx:60](src/components/schedule/ActionNowView.tsx:60)), 1 status indicator dot ([ShedView.tsx:417](src/components/ShedView.tsx:417)), 1 column-resize-handle active state ([SeedVaultView.tsx:1413](src/components/SeedVaultView.tsx:1413)), 1 calendar entry-type color helper ([calendar/page.tsx:90](src/app/calendar/page.tsx:90)), 2 regression-test assertions ([calendarBatchSelect.regression.test.ts:136](src/app/calendar/calendarBatchSelect.regression.test.ts:136), [feedbackModalAttachments.regression.test.ts:139](src/components/feedbackModalAttachments.regression.test.ts:139))
  - **Zero additional CTAs missed.** The 4 named sites are the complete CTA set; the rule's "Not for" list documents the semantic non-CTA cohort that intentionally stays at `bg-emerald-500`.

- **Bidirectional state-transition tracing Pass 1 subcategory (CLAUDE.md 2026-05-27 rule): SKIP-with-reason.** Pure className edits + doc adds; no modal/menu lifecycle changes; no state-machine surface touched. Matches the rule's "pure styling / copy-only changes that don't touch state transitions" skip condition.

- **Pass 4 lock hygiene clean.** §10 don't-touch unbrushed (all 7 items preserved). §11 cross-view-consistency entry receives the intentional closure note (the prompted Syd-locked action); other §11 parked items untouched. §8 receives one new subsection + one supersession marker (authority-precedence compliant — legacy stub points to the winner). FAB-form submit saga lock (2026-05-26) preserved + extended by this sub-rule.

- **E2E-scope sub-rule (CLAUDE.md 2026-05-27 rule) clean.** No visible-label string changes — class-string changes only. `e2e/` grep on the flipped CTA button labels ("Go to Seed Vault", "Show all plantings", "Find Hero Photos") returned no class-string-coupled assertions. No spec updates required.

- **Persona walk Pass 2: all 5 pass.** Maya (power user, scans labels) sees the action/state split as the expected industry-standard tonal distinction (Apple HIG filled-prominent vs selected-segment). Sydney (cohesion-driver) sees primary-emphasis surfaces now obey one rule across the app instead of drifting per-component; one of her own Q-asks from `4c92e68` close-out resolved. Walter (iPad-primary, standard-iOS-app expectations) reads the slightly-deeper CTA shade as the universal "filled prominent button" convention; state surfaces read as the lighter "selected segment" convention. Aria + Sam (low-data new-user states) see action buttons in empty-state cards (the primary affordance pushing them toward first plant / first seed — `ActiveGardenView` empty-states, `NoMatchCard` clear-filters CTA) carry the slightly-stronger visual weight signaling "tap me" without the surface being shouty.

- **NO Preview MCP / browser screenshots** per chat HARD RULE locked 2026-05-27 (tests + build + git diff is sufficient; visual verification falls to Syd dogfood). **Syd dogfood path:** (a) `/garden` Active tab when no batches present → confirm "Go to Seed Vault" CTA renders slightly deeper green (emerald-600); (b) `/garden` Active tab with grow-id deep-link to a non-existent planting → confirm "Show all plantings" CTA renders slightly deeper green; (c) `/garden` Active or My Plants tab with a search yielding zero matches → confirm `NoMatchCard` "Clear filters" CTA renders slightly deeper green; (d) `/vault` → Import → Purchase Order flow → review-import page → confirm "Find Hero Photos" CTA renders slightly deeper green AND its border matches the bg fill (no two-tone border/fill drift); (e) confirm Schedule tab nav active state + Monthly view selected month pill + Calendar batch-selection checkboxes + toggle switches all stay at the lighter `emerald-500` shade (no regression on STATE surfaces).

- **3 buckets at close.** (a) **Parked items added this chat:** none — both Syd Q-asks from `4c92e68` close-out are now CLOSED (Q1 = sub-rule shipped; Q2 = §11 closure note shipped). (b) **Deferred audits / known follow-ups:** the broader cross-view-consistency parked decision (raised 2026-05-07) remains open for the 5 inventory surfaces (Library / Packets / Shed / Active Garden / My Plants); Journal explicitly out-of-scope per this ship. (c) **Dogfood-style findings — just-captured:** the pattern "Pass 3 sibling sweep on a recent ship surfaces a token-split decision that grows into its own sub-rule" repeated twice in two consecutive ships (4c92e68 → this ship). Worth monitoring whether token-split sub-rules (this one + chevron framing + chrome control framing + casing) accumulate to a meta-pattern around "audit-time micro-aesthetic decisions become §8 sub-rules whenever the resolution lands at the codebase-wide level rather than the single-site level." Not promoted to a CLAUDE.md rule yet (3-instance threshold for that promotion; currently at 4 instances if you count the original casing convention but the doc-architecture pattern is implicit rather than rule-named).

- **5 files, +12/-4 net.** `src/components/NoMatchCard.tsx` (+1/-1), `src/components/ActiveGardenView.tsx` (+2/-2), `src/app/vault/review-import/page.tsx` (+1/-1) + 4 doc files (VISION.md, ROADMAP.md, CLAUDE.md, this entry). Tests 438/438; build clean. Concurrent-fetch validation pre-push: 0 ahead / 0 behind. Plan + audit log lives in chat scrollback (XS scope, inline-chat plan per CLAUDE.md threshold).

### 2026-05-27 (earlier — `cohesion-polish-bundle-5items` chat — list-title font-size + Schedule child-view header + photo-card cohesion — `4c92e68`)

- **4-item cohesion polish bundle ships in one commit; 2 items surfaced as plain-English asks.** All 4 shipped items were ADJACENT findings from recent ships (`75e73d1` Packets title size, `fc338c2` photo-card A1/A2, `feb2e32` grow-now Phase 1 child-view header drift). Bundling rationale: each fix XS-size, all cohesion-only, none user-blocking; per WORKFLOW §"Batching small fixes" (3-5 items per deploy).

- **Item 1 — list-title font-size cohesion.** PacketVaultView:786 + SeedVaultView:1504 mobile-compact list titles missed explicit `text-sm` and were inheriting `text-base` (16px); ShedView:423, MyPlantsView:873, ActiveGardenView:1228 all already use explicit `text-sm` (14px). Prompt named PacketVaultView as the only outlier; Pass 3 sibling sweep caught SeedVaultView Library list as same-shape ADJACENT — included in fix. All 5 inventory list-title spans now share `text-sm` cohesion.

- **Item 2 — Schedule child-view headers drop "Zone 10b reference:" prefix.** Parent Schedule page header (`feb2e32` grow-now Phase 1) now renders zone-aware advisory conditionally; the static "Zone 10b reference:" prefix in [ActionNowView:30](src/components/schedule/ActionNowView.tsx:30) + [MonthlyPulseView:37](src/components/schedule/MonthlyPulseView.tsx:37) + [AnnualRoadmapView:54](src/components/schedule/AnnualRoadmapView.tsx:54) was redundant + risked contradicting parent's per-user advisory for non-10b zones. Dropped both the prefix AND the redundant "Reference only — not your vault." / "Reference only." trailings — parent already states "Not your vault." Children now carry just the substantive view-specific tip ("What to start or plant in {monthName}." / "Tap a month to see what the guide recommends." / "When to start indoors (blue) vs direct/outdoor (green). Vertical line = today."). Sentence case preserved per VISION §8 body-copy rule. Directly closes the `feb2e32` close-out entry's ADJACENT finding ("3 child view headers could either also become zone-aware OR drop the 'Zone 10b reference:' prefix entirely").

- **Item 4 — SeedVaultView photo-card shadow + interactive parity.** Photo-card paths ([SeedVaultView.tsx:1231](src/components/SeedVaultView.tsx:1231) + [1251](src/components/SeedVaultView.tsx:1251)) used `shadow-sm` and lacked `card-interactive`; condensed paths (1313 + 1326) use `shadow-card card-interactive`. Pass 3 sibling sweep confirmed condensed pattern is codebase consensus (ActiveGardenView:1025, Journal page:687+862+915 — all use `shadow-card card-interactive`). Photo-card was the lone outlier. Brought to baseline. `card-interactive` utility ([globals.css:62](src/app/globals.css:62)) = transition-[box-shadow,transform] 150ms + hover box-shadow + active scale(0.99) with `@media (prefers-reduced-motion: reduce)` guard — additive interaction, non-breaking, no state surface touched. Directly closes the `fc338c2` close-out's ADJACENT A1/A2 findings.

- **Items 3 + 5 surfaced as plain-English asks at chat close** (per CLAUDE.md AskUserQuestion-forbidden — Dispatch context). Item 3 = emerald-500 (tab-state/selection) vs emerald-600 (Save-canonical CTA) token decision; Item 5 = JournalView format migration call. Both have multi-surface implications + aesthetic judgment beyond strict-bug threshold, surfaced rather than shipped silently per RULES CARD #4.

- **Pass 3 sibling-sweep findings — Item 3 (emerald token):** `bg-emerald-500 text-white` appears in 5+ primary-emphasis sites — Schedule tab nav active ([schedule/page.tsx:74](src/app/schedule/page.tsx:74)), vault/review-import "Approve all" CTA, NoMatchCard CTA, ActiveGardenView CTAs (lines 991+1010), MonthlyPulseView selected-month pill (line 53). FAB-form-submit saga (2026-05-26) canonicalized Save to `bg-emerald-600 hover:bg-emerald-700`. App-wide drift between state/selection emerald-500 and CTA emerald-600. **Recommended:** keep emerald-500 for state/selection (semantically distinct from CTA — "you are here" vs "submit this") + add §8 sub-rule documenting the distinction; the alternative (unify to emerald-600) refactors 5+ sites for a 1-step shade most users won't see. Pending Syd decision.

- **Pass 3 sibling-sweep findings — Item 5 (JournalView):** [src/app/journal/page.tsx](src/app/journal/page.tsx) is structurally different from the 4 inventory list surfaces by design — Journal is an action-log (grouped by entry/date/plant), not a collection list. 3 view modes (table mobile-card / timeline plant-gallery / grid Instagram-feed) each serve different mental models than the unified condensed-list pattern. Mobile-card mode (line 685+) already uses `rounded-xl border bg-white p-4 shadow-card card-interactive` — the condensed-list pattern's design vocabulary but in card-stack shape rather than list-shape. **Recommended:** skip migration; document intentional divergence in VISION §11 closure note. Pending Syd decision.

- **Bidirectional state-transition tracing Pass 1 subcategory** (CLAUDE.md 2026-05-27 rule): **SKIP-with-reason.** Pure className edits + static-string edits; no modal/menu lifecycle changes; no state-machine surface touched. Matches the rule's "pure styling / copy-only changes that don't touch state transitions" skip condition.

- **Pass 4 lock hygiene clean.** §10 don't-touch unbrushed (Plantable banner / Calendar grid / placeholder PNG / Active Garden gallery existence / empty-by-default onboarding / no-activity-feed all preserved). §11 parked items — chrome-control framing (2026-05-27) untouched; FAB-form Save canonical (2026-05-26) untouched; Packets list↔grid toggle (`fc338c2`) untouched (fix is on photo-card primitive, not chrome toggle). VISION §8 Color brand-token rule untouched (Item 3 surfaced as ask, not shipped silently).

- **Persona walk Pass 2: all 5 pass.** Maya (power user) — slight density gain on list titles; photo-card hover parity adds polish she expects on cards she scans constantly. Sydney (cohesion) — closes 2 ADJACENT-tagged gaps from recent ships + closes Schedule child-view framing drift; one cohesive ship that ties off open threads. Walter (iPad-portrait) — 14px list titles match iOS-standard list-row register; hover affordance is desktop-only via `@media (hover: hover)`, no behavior change on touch. Aria (urban houseplant, primary My Plants user) — Library + Packets titles match her primary surface (My Plants) typographically; doesn't use Schedule but parent-header zone-advisory still works for her if she lands there. Sam (Day-1 beginner) — empty-state unchanged across all surfaces; only affects populated rows.

- **E2E-scope sub-rule clean** (CLAUDE.md 2026-05-27 rule). Visible-label changes: "Zone 10b reference:" prefix dropped from 3 child view headers — grep of `e2e/` for "Zone 10b reference" returned zero hits (only [src/app/resources/page.tsx:27](src/app/resources/page.tsx:27) descriptive context outside e2e). Class-string changes don't affect e2e assertions. No spec updates required.

- **NO Preview MCP / browser screenshots** per chat HARD RULE locked 2026-05-27 (tests + build + git diff is sufficient; visual verification falls to Syd dogfood). **Syd dogfood path:** (a) `/vault` → Library + Packets tabs → confirm list-view title sizes match Shed/My Plants/Active Garden lists (14px register); (b) `/schedule` → all 3 tabs (Action now, Monthly view, Yearly view) → confirm child view bodies no longer carry "Zone 10b reference:" prefix; parent header still owns the zone-advisory; (c) `/vault` → Library → photo-card view (grid) → tap-and-hold or hover on desktop → confirm scale(0.99) active feedback + hover shadow appear (now matches condensed-grid behavior).

- **3 buckets.** (a) **Parked items added:** none captured to docs yet — Items 3 + 5 surfaced as plain-English asks at close. If Syd picks "keep emerald-500 for state/selection" on Item 3, a one-line VISION §8 sub-rule add will close it; if "unify to emerald-600", a follow-up chat refactors 5+ sites. Item 5 most likely resolves as a VISION §11 closure note documenting Journal-divergence-is-intentional. (b) **Deferred audits / known follow-ups:** Item 3 + Item 5 pending Syd decision. The prior ADJACENT findings (`75e73d1` Packets title size + `fc338c2` photo-card A1/A2 + `feb2e32` child-view header drift) are all NOW CLOSED by this ship. (c) **Dogfood-style findings — just-captured:** Item 4's photo-card cohesion gap was the second recent instance of "a new view variant (condensed grid) silently introduced a class divergence from the photo-card sibling" — first was `fc338c2`'s active-outline parity work. Pattern worth monitoring: when a new view variant ships on an existing surface, do a Pass 3 sweep against existing variants of the same surface to catch silent cohesion drift early. Not promoted to rule yet (single drift class, second instance).

- **5 files, +7/-7 net.** PacketVaultView (+1/-1), SeedVaultView (+3/-3 — title + 2 photo-card wrappers), ActionNowView (+1/-1), MonthlyPulseView (+1/-1), AnnualRoadmapView (+1/-1). Tests 438/438; build clean. Concurrent-fetch validation pre-push: 0 ahead / 0 behind. Plan + audit log lives in chat scrollback (XS scope, inline-chat plan per CLAUDE.md threshold).

### 2026-05-27 (earlier — `grow-now-zone-investigation` chat — Phase 1 zone-aware framing stopgap on grow-now surfaces — `feb2e32`)

- **Phase 1 of a 3-phase scoped plan ships.** Syd dogfood signal 2026-05-26: *"different zones have very different growing windows + plant viability. Hard-coding to 10b is a regional blind spot."* Investigation this chat confirmed BUGS.md Post-Launch #1 (`plantingWindow.ts` + `zone10b_schedule.ts` hardcoded; `user_settings.planting_zone` stored but never read). Real fix is structural (Phase 2 = per-profile AI-enriched windows; Phase 3 = multi-zone dataset OR frost-date computation). Both phases stay queued for dedicated chats. **Phase 1 = honest framing only — zero data-model change.** Syd's lock: ship Phase 1 now; defer Phase 3 strategy (license vs AI-generate vs frost-date) until Phase 2 ships as live test of AI-generate trust at scale.

- **3 surfaces ship:** (a) new shared hook [src/hooks/useUserPlantingZone.ts](src/hooks/useUserPlantingZone.ts) reads `user_settings.planting_zone` once per surface + exports `isReferenceZone(zone)` helper (returns true for "10b" or null — null = no-advisory so we don't surface claims we can't substantiate); fetch pattern mirrors [useOnboarding.ts:91](src/hooks/useOnboarding.ts:91). (b) Schedule page header at [schedule/page.tsx:53](src/app/schedule/page.tsx:53) conditionally renders advisory copy when zone is set and ≠ "10b": `"Showing Zone 10b reference — your zone is {N}. Recommendations may not match your microclimate yet. (Coming soon: zone-aware schedule.)"`. 10b + null users see existing copy verbatim. (c) Calendar "Plant this month" widget at [calendar/page.tsx:1037](src/app/calendar/page.tsx:1037) appends info-note to footer of expanded panel when zone ≠ "10b": `"Showing plants with empty windows defaulted from Zone 10b. Edit a plant's profile to set a custom window."` Info-note primitive per VISION §8 (`text-sm text-neutral-600 italic`, sentence case, no fill/border/icon) — anchored to the 2026-05-27 lock from `39d5faa`.

- **Pass 3 sibling-sweep findings.** 4 user-facing "Zone 10b" static labels exist in `src/`: schedule/page.tsx header (covered), ActionNowView L31 + MonthlyPulseView L38 + AnnualRoadmapView L55 (3 child view headers; remain as-is). **ADJACENT logged:** the child view "Zone 10b reference:" prefix is now redundant with the new parent advisory for non-10b users but not contradictory (both honestly state "Zone 10b data"). Out of scope per Syd's locked scope (in-scope = parent header per her prompt). Future cohesion polish call. The 3 child view headers could either also become zone-aware OR drop the "Zone 10b reference:" prefix entirely since the parent now owns the framing — separate decision.

- **Persona walk Pass 2.** Walter (zone variable) + Sam (zone variable, first-time beginner) + Aria (urban houseplant, possibly different zone) + Maya (power gardener, broad zone exposure) all non-10b → now see honest framing instead of misleading advice ("Plant tomatoes in Feb-May" while their actual zone is 5a-7b). Sydney (10b, vision-driver) → unchanged. Closes the 3-of-5-persona failure documented in this chat's Phase B audit. Per Pass 2 persona-walk rule, was failing 3+ personas = redesign required; the stopgap doesn't fix the data gap but does close the deception gap (honest copy + honest framing) which is the §6 Theme 3 "Micro-climate timing untrusted" failure mode acknowledgment.

- **Pass 4 lock hygiene clean.** VISION §10 don't-touch — "Plantable widget separation from tasks/calendar grid" preserved verbatim (info-note nests inside the existing expanded panel, doesn't merge plantable into tasks or grid). VISION §11 parked items — none touched. No state-machine surface; no contexts touched. ROADMAP §6 BUGS.md Post-Launch #1 status preserved (still tracked as the underlying gap; Phase 1 is a partial closure of the framing aspect only).

- **Bidirectional state-transition matrix Pass 1 subcategory** (CLAUDE.md 2026-05-27 rule): **SKIP-with-reason.** Pure inline-rendered text swap + helper-call addition; no modal/menu lifecycle changes; no state-machine surface touched. Matches the rule's skip condition for "pure styling / copy-only changes that don't touch state transitions." Loading-state behavior: defaults to current copy (no flash for 10b/null users; brief one-time advisory swap-in for non-10b users when `useUserPlantingZone` resolves — acceptable since the advisory is informational, not load-bearing UI).

- **E2E-scope sub-rule clean.** Visible-label additions only conditionally rendered; existing labels preserved verbatim for 10b/null users. Grep of `e2e/` for "Zone 10b" / "Plant this month" / "planting schedule" returned zero behaviorally-coupled spec assertions on the swap-able strings. No spec updates required.

- **NO Preview MCP / browser screenshots** per chat HARD RULE (2026-05-27 lock + explicit Syd prompt re-statement). Tests + build + git diff verification path used. Syd dogfood path on prod: (a) `/schedule` → confirm header reads zone-aware advisory (if Syd's prod zone is "10b" she sees no change — verify by temporarily editing Settings → Profile zone to e.g. "5a" + refresh schedule, then revert); (b) `/calendar` → expand "Plant this month" widget → confirm info-note appears at footer (same conditional + same revert dance if needed).

- **3 buckets.** (a) Parked items added: Phase 2 (per-profile zone-window enrichment via AI) + Phase 3 (multi-zone dataset OR frost-date computation) — both stay queued for dedicated future chats; Phase 3 strategy decision (license vs AI vs frost-date) explicitly deferred until Phase 2 dogfood data exists. (b) Deferred audits / known follow-ups: BUGS.md Post-Launch #1 stays open as the underlying capability gap; recommend promoting it from "Medium / post-launch" to a named ROADMAP §3.X chunk once Syd is ready to scope Phase 2. (c) Dogfood-style findings — *Just-captured:* Schedule page tab nav (line 67-78 of schedule/page.tsx) uses `border-emerald-500 bg-emerald-500 text-white` for the active tab; the FAB-form submit saga (2026-05-26) canonicalized Save buttons to `bg-emerald-600 hover:bg-emerald-700`. Schedule tab nav is a different surface (tab nav, not form submit) so not a direct collision, but worth a future cohesion call on whether emerald-500 vs emerald-600 should align across all primary-emphasis surfaces.

- **Tests 438/438; build clean.** Concurrent-fetch validation pre-push: 0 ahead / 0 behind (validation #16 of the Phase-2-gate-item-9 rule running clean). Concurrent-chat collision earlier in the chat (`75e73d1` + `69390cf` landed mid-investigation, touching ActiveGardenView + MyPlantsView + ROADMAP §6 — verified non-overlapping with this ship's 3 files). Plan + audit log lives in chat scrollback (XS scope, inline-chat plan per CLAUDE.md threshold).

### 2026-05-27 (earlier — Syd dogfood polish bundle on c851b3d migration — My Plants variety inline + subtitle/meta-row cohesion — `75e73d1`)

- **2-item polish bundle on the just-shipped c851b3d list-view migration.** Both items real Syd-dogfood cohesion findings.

- **Item 1 — My Plants variety presentation standardized.** My Plants list was the lone outlier across the four list surfaces showing variety on a second `<p>` line below the plant name (text-xs neutral-500 truncate pattern). Packets + Active Garden both use the inline `Name (Variety)` pattern via the existing `formatBatchDisplayName` helper. My Plants already had the identical helper at [MyPlantsView.tsx:332](src/components/MyPlantsView.tsx:332) (used in delete/end modals + quick-tap toast at L348/L794/L811) but the list row wasn't calling it. Now consolidated: single `<h3>` rendering `formatBatchDisplayName(plant.profile_name, plant.profile_variety_name)`. Empty-parens guard preserved by helper (returns name-only when variety null/blank). Cohesion outcome: Library photo-card / Packets list / Shed list / Active Garden list / My Plants list now all render variety consistently (inline-paren where applicable).

- **Item 2 — text styling cohesion sweep across the 4 list surfaces.** Pass 3 sibling-sweep finding: **titles use identical classNames** across Shed / My Plants / Active Garden (`font-semibold text-sm text-neutral-900`). Syd's "feels slightly different" signal isn't a title-typography drift — it traces to two smaller className-level outliers:
  - **(a) Active Garden subtitle color drift:** [ActiveGardenView.tsx:1237](src/components/ActiveGardenView.tsx:1237) paragraph used `text-black/50` (~#808080 over white) while Shed + My Plants + Packets subtitle uses `text-neutral-500` (#737373). Active Garden was the lone outlier on this color token. Flipped to `text-neutral-500` (3-of-4 anchor).
  - **(b) My Plants meta-row gap drift:** [MyPlantsView.tsx:875](src/components/MyPlantsView.tsx:875) meta-row used `gap-x-2` without `leading-tight`; Shed meta-row at [ShedView.tsx:425](src/components/ShedView.tsx:425) uses `gap-x-1.5` + `leading-tight`. Flipped My Plants meta to match Shed exactly (gap-x-1.5 + leading-tight).
  - **Skipped candidate:** Shed title `leading-tight` not added to Garden views — Garden uses single-line `truncate` so leading-tight has zero visual effect there. Honest skip per cohesion-by-aggregation "anchor or skip with reason."

- **Pass 3 sibling-sweep adjacent finding logged:** Packets mobile-compact title at [PacketVaultView.tsx:786](src/components/PacketVaultView.tsx:786) uses `font-semibold text-neutral-900` (no explicit `text-sm` — inherits text-base default 16px) while Shed + My Plants + Active Garden all use explicit `text-sm` (14px). One-axis cohesion break between Packets and the rest. Out of scope this chat (Syd's framing was Shed-vs-Garden, not Packets-vs-Garden) — surfaced for future cohesion polish.

- **Bidirectional state-transition tracing Pass 1 subcategory** (CLAUDE.md 2026-05-27 rule): SKIP-with-reason. Pure className edits + helper-call substitution; no modal/menu lifecycle changes; no state-machine surface touched. Matches the rule's "refactors that preserve state-machine shape verbatim" skip condition.

- **Pass 4 lock hygiene clean:** §10 don't-touch (Active Garden gallery card) unbrushed — only list branch text touched, gallery untouched. §11 parked cross-view-consistency partially closes further (My Plants now joins variety-inline cohesion). §11 grid/gallery parked stays parked. c851b3d's "DATA preserved verbatim" framing extended (not contradicted): the c851b3d migration explicitly framed itself as the wrapper-shape ship; cohesion polish on the data-presentation side was queued for follow-up — Item 1 is exactly that follow-up.

- **Persona walk Pass 2:** all 5 pass. Maya (power user) — variety still visible, just consolidated to title; meta-row tighter aids scan-density. Sydney (cohesion) — the four list surfaces now share variety-presentation cohesion + subtitle-color cohesion + meta-rhythm cohesion. Walter (iPad-primary) — no behavioral change; long-press + Add-journal still work. Aria (urban houseplant, My Plants primary surface) — title with `(Variety)` reads cleaner than the old two-line stack. Sam (Day-1) — empty-state unchanged; only affects populated rows.

- **E2E-scope sub-rule clean:** zero visible-label changes (all edits are className flips + helper-call substitution; no user-visible string changed beyond variety-format-on-page which has no e2e assertion). Grep of `e2e/` for "My Plants" / "Active Garden" returned zero hits pre-edit. No test updates required.

- **NO Preview MCP / browser screenshots** per chat HARD RULE (2026-05-27 lock + explicit prompt header re-statement). Tests + build + git diff verification path used; user dogfood path on prod: `/garden` → switch My Plants list view → confirm `Plant (Variety)` inline format renders on populated rows + confirm subtitle/meta-row cohesion with Shed.

- 2 files (`src/components/MyPlantsView.tsx` + `src/components/ActiveGardenView.tsx`), +3/-4 net. Tests 438/438; build clean. Concurrent-fetch validation pre-push: 0 ahead / 0 behind. No plan file (XS scope — inline-chat plan per CLAUDE.md threshold; Pass 1+2+3+4 audit log lives in chat scrollback).

### 2026-05-27 (earlier — `garden-list-condensed-format` chat — Active Garden + My Plants list views adopt Packets+Shed condensed format — `c851b3d`)

- **Active Garden list view + My Plants list view migrated to the condensed wrapper pattern.** Format change only — DATA preserved verbatim. The "format change only" framing is load-bearing: Active Garden progress bar (h-2), seed/sprout/plant/harvest counts, planting_method_badge, location chip, owner badge, Link href + handlers, scroll-into-view `highlightBatchRef`, long-press → bulk-select, scroll-target ring on `highlightGrowId` match — all preserved. My Plants Perennial chip, FAM chip, planted-ago + care/journal counts, `getLongPressHandlers`, `batchSelectMode`, household family-view gating, "Add journal entry" pen icon (preserves U25 cross-surface cohesion) — all preserved. Only the wrapper changes: outer `rounded-xl border border-black/10 bg-white overflow-hidden` + inner `divide-y divide-black/5` + row `flex items-center gap-3 px-3 py-2 min-h-[44px] hover:bg-gray-50` + 40×40 thumb + tighter type register.

- **Cohesion outcome:** four list surfaces (Vault Packets list / Vault Shed list / Garden Active Garden list / Garden My Plants list) now share one wrapper rhythm. Each list shows the data that's useful for its content — but the wrapper shape is identical across them. Closes the wrapper-shape portion of VISION §11 "Cross-view consistency: Vault grid / Garden gallery / My Plants list" parked decision (parked 2026-05-07). Grid/gallery view consistency remains parked (different cohesion question; out of scope this chapter).

- **Anchors cited:** [PacketVaultView.tsx:744-803](src/components/PacketVaultView.tsx:744) mobile compact list + [ShedView.tsx:387-467](src/components/ShedView.tsx:387) list mode. Decision tree at audit time: chose Shed's `px-3 py-2` over Packets' `px-3 py-3` because both targets have multi-line content (title + subtitle + meta-row) matching Shed's content shape, not Packets' tighter 2-line shape. Selection ring anchored to Shed's `bg-emerald-50/80 ring-inset ring-2 ring-emerald-500` (preferred over Packets' `border-2` which would double up against the row divider). Meta-row register `text-xs → text-[11px]` for My Plants matches Shed's chip-strip register (My Plants meta IS chip-strip shape); Active Garden's sown/harvest paragraph keeps `text-xs` (sentence register, not chip register — content-shape preserved). Every micro-decision named in the plan's Pass 2 cohesion-by-aggregation log with a cited anchor.

- **Pre-build plan revision caught at Pass 2 cohesion-by-aggregation:** initial draft slimmed Active Garden progress bar `h-2 → h-1.5`. Reverted before build because (a) no anchor for `h-1.5` in reference patterns and (b) the progress bar is DATA-display primitive (visual encoding of grow-cycle progress), not wrapper-shape — per the "format change only" framing it stays untouched. Pass 2 re-ran clean on the revised plan per strict-clean-pass clause.

- **Bidirectional state-transition tracing Pass 1 subcategory** (CLAUDE.md rule locked 2026-05-27): SKIP-with-reason documented. List-view migration preserves state-machine shape verbatim (no modal/menu lifecycle changes; click handlers + long-press behaviors preserved literally; `highlightBatchRef` ref attachment preserved on `<li>`). Matches the rule's explicit skip condition: "refactors that preserve state-machine shape verbatim (extract-component, prop-rename, file-move)" — wrapper CSS substitution falls in the same skip category.

- **Pass 3 sibling sweep clean:** Packets + Shed verified as the two existing peer surfaces with this list pattern; both used as anchors. JournalView gallery/list noted ADJACENT (likely uses a different format) — out of scope this chapter, logged for future cohesion polish. The Active Garden gallery view + My Plants grid view (`displayStyle === "grid"`) keep their card-pattern as-is — that's intentional (grid card pattern is correct for grid; list pattern is correct for list).

- **Pass 4 lock hygiene clean:** VISION §10 don't-touch (Active Garden gallery — the bug was clipping, not the view itself) preserved verbatim — gallery view untouched; only list branch migrated. §11 parked cross-view-consistency decision partially closes (list-view portion). §11 parked grid-view consistency stays parked. No CLAUDE.md locked rules brushed. No ROADMAP §6 decisions touched.

- **Persona walk Pass 2:** all 5 pass. Maya (power user) scans Active Garden list faster — consolidated wrapper reads as one cohesive list rather than 20 separated cards. Sydney (cohesion) sees the rhythm she expects across all four list surfaces. Walter (iPad-primary, swipe-excluded) — 44×44 row min-height preserved, long-press still works, "Add journal entry" button still visible in row. Aria (urban houseplant) — My Plants reads less heavy (her primary surface). Sam (Day-1) — empty-state behavior unchanged (this migration only touches rendered-list when items exist).

- **E2E-scope sub-rule clean** (CLAUDE.md rule locked 2026-05-27 after `5b5617c`): zero visible labels changed — pure CSS class swaps. Grep of `e2e/*.spec.ts` for "My Plants" / "Active Garden" returned zero hits. No e2e updates required.

- **NO Preview MCP / browser screenshots** per chat HARD RULE locked 2026-05-27 (tests + build + git diff is sufficient; visual verification falls to Syd dogfood). User dogfood path: `/garden` → switch both Active + My Plants tabs to list view → confirm rhythm matches Library / Packets / Shed list views.

- 2 files (src/components/ActiveGardenView.tsx + src/components/MyPlantsView.tsx), +24/-16 net. Tests 438/438; build clean. Concurrent-fetch validation pre-push: 0 ahead / 0 behind. Plan + audit log at [.claude/plans/garden_list_condensed_format.md](.claude/plans/garden_list_condensed_format.md).

### 2026-05-27 (newest — chrome control framing sub-conventions lock — doc-only)

- **VISION §8 new subsection "Chrome control framing — sub-conventions" locked.** Captures the framed-vs-unframed split for chrome controls that surfaced organically from two 2026-05-27 ships: `b339f56` (Calendar month-nav arrows dropped square frame → bare chevron-icon-only — established the UNFRAMED chevron principle) and `fc338c2` (Packets list↔grid toggle added with framed pill `rounded-xl border border-black/10 bg-white` — established the FRAMED toggle principle alongside the existing Library tab toggle). Two distinct sub-conventions named with explicit decision criterion: chrome controls representing STATE wear a frame; chrome controls representing ACTION read unframed.

- **Relationship to existing §8 subsections.** Sub-rule WITHIN the chrome lane of the chrome-vs-content icon-style split (locked earlier 2026-05-27 in `gt-icon-style-convention` chat — `cea6c71`). Icon-style sets the visual treatment of the icon (flat monotone for chrome); this convention sets the framing of the control around that icon. Orthogonal axes — both apply to chrome icons in chevron and toggle controls. Independent of §11 "App-wide icon density" parked stroke-weight decision (framing ≠ stroke weight; both can lock independently).

- **Exception captured:** photo-overlay nav (Vault profile prev/next, Shed product prev/next) keeps framed chevrons (`rounded-full bg-white/90 border w-6 h-6`) where chrome aids legibility over a photo. Honored as documented carve-out, not a contradiction of the unframed-chevron rule.

- **Quick-lookup pointer added to CLAUDE.md** as sibling to the existing Casing / Empty-cell / Icon style convention quick-lookup sections — same shape so future-Claude scans them as a coherent §8 design-token quick-reference set.

- **Persona walk:** all 5 personas pass (Maya — stateful-vs-action distinction reads as expected; Sydney — cohesion expectation met; Walter — universal-iOS convention for both shapes; Aria + Sam — less visual noise on unframed prev/next; only stateful toggles announce with a frame).

- **3-pass plan-audit terminated clean iter 1** (Pass 1 factual: both cited commits verified via `git log --oneline`, Lucide chevron usage verified in `src/app/calendar/page.tsx`, toggle pill pattern verified in PacketVaultView.tsx L745 + SeedVaultView.tsx L1466; Pass 2 doc-batch concerns hunt clean; Pass 4 lock hygiene clean — §10/§11 unbrushed). Pass 3 sibling sweep on doc structure: new subsection matches the established 4-subsection shape (Info-note / Casing / Empty-cell / Icon style) verbatim — title with lock date / rule with two branches / decision criterion / why / relationships / persona walk / GT-only.

- **Doc-only ship**, zero code touched. 3 files (VISION.md + CLAUDE.md + ROADMAP.md). Tests N/A; build N/A. Push tier per CLAUDE.md push-tiers (doc-only = push immediately, no per-push greenlight needed). Concurrent-fetch validation at preflight: 0 commits behind origin/main.

### 2026-05-27 (newest — `gt-plant-profiles-to-library` chat — Vault tab rename "Plant Profiles" → "Library")

- **Vault sub-tab "Plant Profiles" renamed to "Library" surgically.** Locked in Dispatch after boyfriend-dogfood surfaced Profile-vs-Packet confusion (Library / Packets / Shed reads cleaner as three distinct concepts than Plant Profiles / Packets / Shed). **Conceptual model preserved:** "A Library contains many plant profiles" — the SECTION renamed, the INDIVIDUAL ENTRY noun stays "plant profile" (one per variety; encyclopedia entry; matches locked GT vocabulary). DB table `plant_profiles`, TypeScript `PlantProfile` type, `plantProfiles` state variable, body-copy "plant profile" noun usage — all preserved verbatim.

- **Scope:** 6 files / 10 line-edits in src/ + 2 doc files (VISION + ROADMAP). **Major scope reduction caught at Pass 1:** URL slug is already `?tab=grid` not `?tab=plant-profiles` (confirmed at [VaultPageContent.tsx:974](src/app/vault/VaultPageContent.tsx:974) + sync logic L360-369). **No URL change, no redirect, no sessionStorage migration needed** — the rename is text-content-only on the tab pill button. Internal state machine (`viewMode: "grid" | "list" | "shed"`) unchanged; legacy fallback paths unchanged.

- **src/ edits:** [VaultPageContent.tsx:981](src/app/vault/VaultPageContent.tsx:981) tab pill text (load-bearing change) + 3 comments referring to the tab (L366, L406, L1126). [AddVarietyModal.tsx:34](src/components/AddVarietyModal.tsx:34) doc-comment ("Vault Plant Profiles tab toolbar" → "Vault Library tab toolbar"). [NoMatchCard.tsx:15](src/components/NoMatchCard.tsx:15) + [shed/page.tsx:6](src/app/shed/page.tsx:6) jsdoc references. [types/vault.ts:6,9](src/types/vault.ts:6) status-filter + sort comments. [types/garden.ts:174](src/types/garden.ts:174) section divider gains parenthetical anchor `// Plant Profiles (Vault Library)` — section divider labels the data shape (PlantProfile type + plant_profiles table), the parenthetical anchors the reader to the user-facing surface.

- **Doc edits:** VISION.md §2 section heading `### Vault (Plant Profiles, Packets, Shed)` → `### Vault (Library, Packets, Shed)` + §8 casing example table-row updated to `\`Library\` · \`Packets\` · \`Shed\`` (also corrects stale "Seed Vault" peer reference to current "Packets") + §8 proper-noun preservation list updated with lineage cite (`Library` renamed from `Plant Profiles` 2026-05-27) + §11 cross-view-consistency parked-decision text refreshed. ROADMAP §3.12 Phase A.2 task (a) + (d) annotated to capture partial close (the Plant Profiles → Library piece ships here; Seed Vault → Packets cohesion + Vault URL/sidebar/copy rename remain Phase A.2 territory) + §3 build-chunk text at L258 ("Vault Plant Profiles grid" → "Vault Library grid"). CLAUDE.md untouched (no "Plant Profiles" references in CLAUDE.md per grep).

- **4-pass plan-audit terminated CLEAN iteration 1.** Pass 1 factual: every cited path verified (caught the URL-slug-already-internal observation that reduced scope from "rename + redirect" to "text-only flip"). Pass 2 concerns hunt + bidirectional state-transition matrix (per CLAUDE.md 2026-05-27 rule) — built 5-row × 3-column matrix (tab activation / deactivation / deep-link / legacy URL fallback / sessionStorage restore × state-change / Suspense / Mount-Unmount), every cell clean (text-content-only swap, no state-machine surface changed) + persona walk all 5 pass (Maya — destination noun reads natural; Sydney — closes the Profile-vs-Packet collision she dogfooded; Walter — plain-English noun; Aria — doesn't imply outdoor-only; Sam — aligns tab label with body-copy "library" softener already in use since `9bad88f`). Pass 3 vocabulary-breadth sibling sweep across 5 forms ("Plant Profiles" / "plant profiles" / "plant-profiles" / "plantProfiles" / "plant_profiles") — TAB/SECTION uses flipped; ENTRY/TYPE/STATE/TABLE uses preserved; **2 ADJACENT findings logged**: (1) stale "Seed Vault" peer references in NoMatchCard L15 + shed/page.tsx L6 + VISION §8 L337 — current tab is "Packets" (logged for future cohesion sweep); (2) existing body-copy use of generic-softener "library" (since `9bad88f` 2026-05-18) may muddle with the new specific-tab-label "Library" — needs future copy audit pass to disambiguate. Pass 4 lock hygiene: VISION §10 don't-touch + §11 parked decisions untouched (cross-view consistency parked-decision text refreshed for naming, substance preserved); no CLAUDE.md locked rules touched.

- **e2e/ coverage clean:** grep of `e2e/*.spec.ts` for "Plant Profiles" returned zero hits before edit — no Playwright assertions reference the old label. Per CLAUDE.md Pass 3 E2E-scope sub-rule (locked 2026-05-27 after `5b5617c`), verified pre-ship; no test updates required.

- **Truncation risk DECREASES** with rename: "Library" is 7 chars vs "Plant Profiles" 14 chars — fits more comfortably on small phones; tab pill width relaxes.

- Tests 438/438; build clean. **Preview MCP skipped per chat-locked rule** — user explicitly locked NO Preview MCP / browser screenshots for this chat (prior chat `local_66471e50` hung indefinitely on a screenshot tool call); tests + build + git diff verification path used instead. jsdoc-comment edits are non-browser-observable regardless; tab-pill change is text-content-only with zero state-machine surface touched. User phone-verifies on prod (Vault → confirm first pill reads Library, tab switches still work, no truncation). No plan file (inline-chat plan per XS scope — this chat only needed 1 src edit + audit + ship; bulk of rename work was already in working tree from prior hung chat). Concurrent-fetch validation 0/0 pre-push.

- **Partial close on §3.12 theme #3 Phase A.2 task (d).** The Plant Profiles → Library tab-label sub-piece is shipped via this surgical rename outside the bundled Phase A.2 scope (which still owns: Vault URL rename + sidebar nav + Seed Vault → Packets cohesion + "Add to vault" copy + 50-file impact + redirect strategy). Documented at L136 (Phase A.2 description).

### 2026-05-27 (earlier — `gt-seed-propagation-context` chat — Vault contextual seed-propagation empty-state ship + new info-note primitive lock + U24-era orphan-migration cleanup)

- **Vault Propagate / Save Seeds contextual empty-state ✅ SHIPPED (`39d5faa`).** Syd's dogfood gap closed end-to-end: Sedum donkey tail / grafted fruit trees / sterile hybrids no longer render as ambiguously blank em-dashes on the Vault profile. New `plant_profiles.seed_propagation_context` TEXT column (additive migration `20260527120000`); Gemini prompt extended to populate one sentence covering 4 cases (doesn't grow from seed / high variability / sterile-won't-fruit / standard seed-grown empty). UI renders italic note at top of "Propagate / Save Seeds" card + as footer in "How to Grow" when all care/growing rows are em-dash (the ambiguity site read first). Empty-cell em-dash convention preserved on structured rows — note explains rather than replaces.

- **Approach B locked over A / C / D after Phase A audit.** Pass 1 factual showed: schema has only free-text `propagation_notes` + `seed_saving_notes` + no `propagation_method` enum + no `seeds_applicable` bool — meaning no structural signal exists to distinguish "doesn't apply" from "not enriched yet." Approach (A) "derive from existing data" therefore doesn't work; (C) "canned templates per propagation method" doesn't work (no enum to template against); (D) "heuristic on existing `propagation_notes` keywords" only covers Case 1 (Sedum); (B) "new field + AI extension" was the only approach covering all 4 cases Syd enumerated. Bounded ~75-90 min code estimate accepted; user picked all 4 of Claude's recommendations via Rule A (Approach B + UI placement at 2 sites + visual treatment italic-text-neutral-600 + lazy backfill).

- **New "Info-note treatment" primitive locked in VISION §8.** Rendered as `text-sm text-neutral-600 italic` (sentence case per casing rule). Calm explanation register — distinct from FormError (italic + `text-red-600` + warning-icon prefix + `role="alert"`, blocker semantic), distinct from helper-text under inputs (`text-xs text-neutral-500`, field-scoped), distinct from empty-state body (full-sentence 3-part frame, replaces missing content). New primitive needed because no existing pattern matched the "calm aside about why this section's empty" semantic. Persona walk: 5/5 pass — italic body register lands the "aside / commentary" semantic without inventing a callout box that would compete for attention on dense profile cards. Initial roll-out sites: VaultProfileAboutTab `Propagate / Save Seeds` card body + `How to Grow` card footer.

- **Lazy backfill chosen over active backfill.** Existing profiles stay NULL on the new column until user taps ✨ Fill Blanks per profile. Trade-off: existing profiles render unchanged until enrichment runs (acceptable — user opts in per profile she cares about). Alternative (developer-page action to run Fill Blanks across all profiles) deferred — would incur Gemini-token cost surprise and the new field's value is plant-specific (user gets to choose which varieties to enrich).

- **AI prompt prompt-engineering risk mitigated by explicit "use empty string for normal seed-grown plants" rule + worked examples in prompt.** Pass 2 concerns hunt flagged over-triggering risk (Gemini generating the contextual note even for standard tomato/bean/annual varieties). Prompt's case (d) explicitly returns empty string; sentence-case + ≤140-char output explicitly named per VISION §8 casing convention compliance. Acceptable downside: occasional mis-classification (sterile hybrid mis-tagged as variability, etc.) — user can re-run Fill Blanks or edit free-text downstream.

- **3 migrations applied together via `supabase db push --include-all`** after first push attempt hit the U24-era `20250330000000` orphan-collision (`schema_migrations_pkey` duplicate key). Resolution: rename orphan file `20250330000000_user_settings_onboarding_complete.sql` → `20250330000001_...` (preserving git history via `git mv`) + `supabase migration repair --status applied 20250330000001` (metadata only — schema column `onboarding_completed_at` was already on remote per the NOTICE skip the first push surfaced) + re-run `db push` which then applied 2 remaining migrations clean: `20260518000000_admin_feedback_inbox.sql` (sitting unapplied on remote since `bc253be` 2026-05-18 — admin_feedback page had been silently functional via Dashboard SQL Editor application path, never CLI-reconciled; idempotent re-apply via `CREATE TABLE IF NOT EXISTS` + `CREATE OR REPLACE FUNCTION` safe regardless of Dashboard-applied state) + new `20260527120000_plant_profiles_seed_propagation_context.sql`. **Closes the "residual collision (open)" doc note in CLAUDE.md SQL migrations subsection** that's been sitting since 2026-05-17. Side benefit: future `db push` invocations from this repo no longer wrestle with the orphan-row tracking conflict.

- **Out-of-scope discovery surfaced to user via Rule A before action:** `20260518000000_admin_feedback_inbox.sql` was unapplied on remote per CLI tracking. Three options surfaced (A push both together / B Dashboard SQL Editor for mine only / C repair-then-push targeted). User picked Recommended Option A: idempotent SQL safe to re-apply + side benefit of closing CLI/remote drift + same shape as CLAUDE.md "investigate before deleting/overwriting" guidance (investigated, safe, closed cleanly).

- **Parallel-chat work present in working tree but excluded from this ship.** `src/components/AddVarietyModal.tsx` (251 LOC, untracked) + matching `src/app/vault/VaultPageContent.tsx` modifications (Add Variety button + dynamic import + state) belong to a different in-flight chat. Build + tests both ran with that work present and stayed green (438/438 tests; clean build); no interaction risk with my changes. Staged only my 9 files + 2 migrations via specific `git add` calls per CLAUDE.md "never `git add -A`" rule.

- **4-pass plan-audit terminated clean iteration 1.** Pass 1 factual: Schema + UI + enrichment audit, verified `propagation_notes` / `seed_saving_notes` are free-text only, no enum exists. Pass 2 concerns + persona walk + cohesion-by-aggregation: 5/5 personas pass, italic anchored to existing FormError italic pattern (different color, same italic semantic) + helper-text `text-neutral-500` register, casing per §8 (AI prompt instructs sentence case). Pass 3 sibling sweep: no existing info-note primitive in codebase — new primitive documented in VISION §8. Pass 4 lock hygiene: §10 don't-touch + §11 parked untouched, §8 casing/empty-cell/icon-style/form-error conventions preserved. Tests 438/438; build clean (after `rm -rf .next` to clear stale type cache from prior Windows dev-server state — unrelated `/admin/scraper-audit` type-cache miss, not from diff).

- **Preview MCP auth-blocked at `/vault/[id]` per documented condition** (no `.env.local` in worktree; AuthGuard redirects to `/login` before VaultProfileAboutTab renders). Console clean on login page post-build; user phone-verifies on prod after Vercel auto-deploy. Dogfood test path: open Sedum donkey tail (or any cuttings/grafted variety) → tap ✨ Fill Blanks → confirm italic contextual note appears at top of Propagate / Save Seeds card AND as footer in How to Grow card; open a standard seed-grown variety (tomato/bean/etc.) → confirm NO spurious note appears (Case (d) empty-string honored).

### 2026-05-27 (Plant Profile card inventory-display polish — Syd dogfood re-flag — `9ab4dab`)

- **Plant Profile cards in Vault → Plant Profiles tab now show variety + green-outline-when-growing only.** Color-coded inventory dot (HealthDot) + packet count digit removed from both grid card variants (photo + condensed). Syd flagged 2026-05-26 + re-flagged 2026-05-27: card mixed signals — "0 inventory" implied nothing-happening while the green outline said active. Cleaner card resolves the conflict; inventory remains visible in the Seed Packets tab + on the profile detail page.

- **`getCardBorderClass` preserved verbatim** — green active-growing outline still wired via `seed.status === "active"`; out-of-stock dashed border still wired via `packet_count === 0`. `packet_count` data continues to flow through filter / sort / border logic; this was purely a display removal.

- **Pass 3 ADJACENT finding logged for future polish:** photo-card variant of Plant Profile cards (lines ~1252-1280 of [src/components/SeedVaultView.tsx](src/components/SeedVaultView.tsx)) doesn't apply `getCardBorderClass` (hardcoded `border border-black/5 hover:border-emerald-500/40` instead) — only condensed variant (lines 1341 + 1354 post-ship) renders the active green outline via the helper. Pre-existing asymmetry that predates this ship; default `gridDisplayStyle="condensed"` means users see the outline normally on default cards, but explicit photo-mode toggle currently lacks it. Separate cohesion polish item — surfaced here so it doesn't get lost.

- **Trade-off heard + accepted (Maya persona walk):** at-a-glance card-level inventory signal is gone; Maya (power gardener) loses card-level low-stock signal but retains it in Seed Packets tab + profile detail. Syd consciously made this trade-off; logged for transparency. Walter / Aria / Sam: cleaner cards are net-positive.

### 2026-05-27 (newest — `gt-icon-style-convention` chat — chrome vs content icon-style split lock)

- **Icon style convention locked — chrome vs content split.** Industry-standard split adopted (Apple HIG SF Symbols + Material Design icon families parallel). Locked to [VISION.md §8](docs/VISION.md) as new "Icon style — chrome vs content split" subsection — same authority-precedence shape as today's earlier casing-policy + empty-cell convention locks (visual / typographic locked design decisions belong in VISION.md per CLAUDE.md authority precedence 2026-05-16).

- **The rule (two branches):**
  - **Chrome icons** — UI structure that recurs constantly (FAB chips, nav bars, settings gears, tab icons, form-field icons, list-row affordances, modal close-X, header utility) use a **flat, monochromatic line style**. Single stroke weight, single color via `currentColor`, no fill, no illustration detail. Calm + clear + non-competing.
  - **Content icons** — emphasis on specific things the user is actively engaging with (achievement badges, plant-category markers, milestone markers, journal milestones, seasonal markers, celebratory moments, illustrated empty-state hero art) can be **illustrated, colored, or emoji-style**. These are moments, not chrome.

- **Decision criterion at audit time (one question):** *Is this icon UI structure (recurring + persistent + supports finding/tapping) or content (specific + attention-worthy)?* Chrome → flat monotone. Content → illustrated OK.

- **Worked example — FAB Add plant icon (queued semantic-fix swap).** Current FAB "Add plant" icon visually reads as a seed rather than a plant. Per this convention: icon is **chrome** (FAB primary chip, persistent + recurring + UI structure), so fix stays **flat monotone** — just needs a better-shaped plant icon from the existing Lucide library, NOT a switch to illustrated / colored / emoji-style. Same shape as 2026-05-12 chunk 3.9 #4 (Garden card "Add journal entry" trigger icon swap, `3de1c2c`) — flat icon swap within chrome lane to fix semantic mismatch, style unchanged.

- **Relationship to §11 "App-wide icon density" parked entry (preserved, NOT closed).** §11 (raised 2026-05-08 / widened 2026-05-11) asks which canonical stroke weight + size the **chrome** lane should standardize on (stroke 1.2 from `styleDictionary.tsx` vs stroke 2.0 from `navItems.tsx`). This convention sits **above** that question: §11 settles unification within the chrome lane; this convention sets the chrome-vs-content category split itself. Both are needed, neither closes the other. When §11 unlocks, its outcome applies to chrome icons defined here; content icons remain unaffected (illustrated artwork doesn't have a stroke weight).

- **Rationale.** Industry-standard split (Apple HIG SF Symbols system, Material Design icon families). Mixing styles intentionally **by role** is correct; mixing styles randomly within a single role is drift. Chrome that competes for attention exhausts the user across a long session; content that stays as flat chrome misses the emotional beat the moment is supposed to land.

- **Persona walk.** All 5 personas pass. Maya (power user) reads chrome staying calm — doesn't compete with content she scans. Sydney (cohesion) sees industry-standard split as the polished-app feel she expects. Walter (iPad-primary) reads flat monotone chrome as the SF Symbols convention; illustrated content icons read as moments worth lingering on. Aria (urban houseplant, low-data) sees chrome stay calm — never overwhelmed by FAB / nav. Sam (beginner Day 1) experiences chrome that doesn't shout, so content moments (first sprout, first harvest) actually land emotionally when they appear.

- **NORTH_STAR cite:** "Take mental load OFF the user" (chrome that stays quiet = lower cognitive load across sessions) + "No duplicate paths" (consistent chrome style IS the cohesion expression across sibling UI surfaces). Same shape as today's earlier casing-policy + empty-cell convention locks: industry-standard alignment + persona-walk validation + GT-only scope + authority-precedence remediation.

- **GT-only.** Voyager has its own visual register (parchment + in-world illustrated artwork); this convention does NOT apply there.

- **Authority-precedence remediation.** Lock destination is VISION.md §8 (canonical) + CLAUDE.md callout (quick-lookup) + ROADMAP §6 (dated log). Three-tier parallel to casing-policy + empty-cell convention locks earlier today. Per CLAUDE.md authority precedence 2026-05-16, voice / tone and locked design decisions belong in VISION.md.

- **Doc-only push tier per CLAUDE.md** — 3 files edited (VISION.md / CLAUDE.md / ROADMAP.md) + this §6 entry + §1 lead-line refresh per 2-paragraph cap; no code touched; pushed immediately after self-audit. Concurrent-fetch validation: pre-amendment `git fetch origin main` showed 0 ahead / 0 behind cleanly (Phase 2 readiness-gate item 9 ran clean).

### 2026-05-27 (later — `gt-empty-cell-convention` chat — em-dash empty-cell lock + app-wide sweep)

- **Empty-cell display convention locked — em dash "—" for empty data cells.** Industry-standard typography adopted (Apple Numbers / Google Sheets / iOS Settings / financial dashboards parallel). Locked to [VISION.md §8](docs/VISION.md) as new "Empty-cell display convention" subsection — same authority-precedence shape as the casing-policy lock 2026-05-27 (visual / typographic locked design decisions belong in VISION.md per CLAUDE.md authority precedence 2026-05-16).

- **The rule (two branches):**
  - **Use "—" for:** unambiguous "no data" cells in profile / table / detail views — Vault profile About fields (Germination / Days to Maturity / Spacing / Light / Water / Soil type when no value), plant cards' optional metadata fields, any data row where the field is structurally present but has no current value AND where there's no semantic distinction between missing / didn't-enter / doesn't-apply.
  - **Preserve semantic strings for:** fields where the displayed value carries meaning. `"None"` = user deliberately chose none. `"N/A"` = column doesn't apply to this row by design (e.g., germination time on an established-plant lifecycle path). `"Unknown"` = we tried to infer but couldn't determine (known-unknown distinct from never-entered).

- **Decision criterion at audit time (one question):** *Does this field's absence carry meaning the user needs to read?* No → "—". Yes → preserve.

- **NOT for:** loading states (use spinners / skeletons) · disabled states (use opacity / muted color) · form placeholder text (input prompts) · empty-state body copy (full-sentence 3-part frame per §3.12 #5 ship `9bad88f`).

- **Character lock:** em dash "—" = U+2014. NOT en dash "–" (U+2013), NOT hyphen-minus "-" (U+002D), NOT double-hyphen "--". Use the Unicode literal in source.

- **Phase A doc commit:** `29f37a1` — VISION.md §8 new "Empty-cell display convention" subsection + CLAUDE.md "Empty-cell convention" callout (after Casing convention) + ROADMAP §6 entry + §1 lead-line refresh per §7 2-paragraph cap.

- **Phase C sweep commit:** `0bc4342` — 20 line-edits across 7 files. **settings/developer/page.tsx** (1 — archived-planting Ended date fallback); **settings/vendors/page.tsx** (3 — formatAvg null return + group rating span + per-packet rating span); **vault/[id]/VaultProfilePacketsTab.tsx** (1 — vendor_name); **vault/[id]/page.tsx** (9 — yieldLabel + 6 About care fields: Sowing Method / Planting Window / Spacing / Sowing Depth / Sun / Water / Germination + Days to Maturity ternary); **vault/history/page.tsx** (3 — location / plantings summary / harvest_count); **components/PacketVaultView.tsx** (2 — vendor_name + purchase_date); **components/StarRating.tsx** (1 — readonly null fallback, propagates to 7 consumer files). 4-pass plan-audit terminated clean on iteration 1.

- **Preserved (no change):** all 28 "Unknown" sentinel occurrences in AI extraction routes / filter chains / matching helpers (semantic state, e.g., `plant_name !== "Unknown"` filter); 2 `<option value="">None</option>` user-choice selectors in CareScheduleManager + NewTaskModal; en-dash range separators in plantDefaults.ts (U+2013, different convention); sentence em-dashes in toasts + paragraphs; existing "—" usage at scraper-audit (5 sites) + ActiveGardenView:372 (the canonical anchor pattern). ADJACENT bucket parked for future polish: QuickAddSeed.tsx 4 sites + shopping-list/page.tsx 1 site use `|| "Unknown"` in sentence-fragment rendering — em-dash doesn't fit; different convention shape; logged for future polish pass with better empty-vendor / empty-item-name copy.

- **Rationale.** Industry-standard typography for missing data in tables and structured data displays. Single character avoids the visual weight of "--" (reads as strikethrough / dashes-as-separator) and the verbal weight of "N/A" / "None" / "Unknown" (which all imply more meaning than the absence actually carries). Apple Numbers / Google Sheets / iOS Settings convention.

- **Persona walk:** all 5 personas pass. Maya (power user) reads "—" as standard table convention; Sydney sees cohesion across profile views; Walter (iPad-primary) reads "—" as universally-recognized "no value" without learning per-app vocabulary; Aria + Sam (low-data new-user states) see less visual noise than "--" / "N/A" / "None" — empty fields visually recede instead of competing for attention.

- **NORTH_STAR cite:** "No duplicate paths" (consistent symbol IS the cohesion expression across sibling empty fields) + "Take mental load OFF the user" (single universally-recognized symbol vs per-field word). Same shape as the casing-policy lock earlier today: industry-standard alignment + persona-walk validation + GT-only scope.

- **GT-only.** Voyager has its own voice; this convention does NOT apply there unless surfaced separately.

- **Authority-precedence remediation.** Lock destination is VISION.md §8 (canonical) + CLAUDE.md callout (quick-lookup) + ROADMAP §6 (dated log). Three-tier parallel to casing-policy lock 2026-05-27. Per CLAUDE.md authority precedence 2026-05-16, voice/tone and locked design decisions belong in VISION.md.

### 2026-05-27 (latest — `gt-title-case-split` chat — casing-policy reversal + app-wide sweep)

- **Casing policy split locked — Title Case for headers + buttons, sentence case for body.** Industry-standard split adopted (Apple HIG / Material Design / iOS Settings parallel). Locked to [VISION.md §8](docs/VISION.md) as new "Casing (headers + buttons vs body)" subsection — promotes the rule from previously-only-ROADMAP-dated entries into a durable design token, closing the doc-architecture gap flagged via authority-precedence audit (locked design decisions belong in VISION.md per CLAUDE.md authority precedence 2026-05-16; the prior casing rule was logged only in dated ROADMAP §6 entries).

- **AP-style Title Case mechanic locked.** Articles (`a`, `an`, `the`) + short conjunctions (`and`, `but`, `or`, `nor`, `for`, `yet`, `so`) + prepositions ≤4 letters (`in`, `on`, `to`, `of`, `by`, `at`, `for`, `with`, `from`, `into`, `onto`, `over`, `up`) lowercase mid-string. First + last word ALWAYS capitalized regardless of part of speech. Prepositions 5+ letters capitalize (`Through`, `Without`, `After`, `Across`). Compound-verb particles capitalize (`Sign Up`, `Log Out`). Examples: `Save to Vault` · `Add to Shed` · `When to Plant (by Zone)` · `Continue to Import Review` · `Overwrite with AI?` · `Save for Later` · `At a Glance`. **Counter-recommendation considered + rejected:** every-word Title Case — simpler mental model but less iOS-conventional + reads marketing-shouty on longer labels.

- **Reverses** the implicit "all sentence case" cohesion bar that operated 2026-05-24 → 2026-05-27 — specifically reverts 2026-05-24 `006dd69` Item 2 sentence-cased form headers + Q4 widen of option labels in choose-steps + 2026-05-25 `a78dbd6` 22-edit FAB-tree sweep (HEADERS + BUTTONS lanes only; field labels / aria-labels / enum tokens / longer body copy / QUICK_ACTIONS / supply categories / volume pills stay sentence case per the body lane preservation in VISION.md §8 taxonomy).

- **Phase A doc commit:** `3ee1063` — VISION.md §8 new "Casing" subsection + CLAUDE.md "Casing convention" callout + lineage updates on 2026-05-24 + 2026-05-25 ROADMAP entries.

- **Phase C sweep commit:** `5b5617c` — 59 files / +133/-132. Full inventory in `.claude/plans/gt_title_case_split.md` §4.1-§4.2. Headers: page h1 + modal h2 + section h2 + tracking-uppercase sub-headers sites. Buttons: form submit + secondary CTA + chip / option labels in choosers. Tab labels (`Plant Profiles` / `Seed Vault`). Settings menu row labels. Test assertions updated where touched (AddPlantModal.test.tsx + calendarBatchSelect.regression.test.ts — prevents cc5e65b-shape e2e drift). 438/438 tests passing; clean build.

- **Preserved (no change):** field labels above inputs (Plant name / Variety / cultivar / Vendor / nursery) · aria-labels matching field labels · volume pills (Full / Partial / Low / Empty as enum tokens) · QUICK_ACTIONS labels (Pot up / Plant out / Cold stratify — enum tokens) · supply category tokens (Fertilizer / Pesticide / Other) · helper text / placeholders · toast messages · Journal day-rotating prompts · empty-state bodies · page-label proper nouns (`Active Garden`, `My Plants`, `Plant Profiles`, `Seed Vault`, `Plant This Month`).

- **NORTH_STAR cite:** "No duplicate paths" (consistent casing IS the cohesion expression across sibling labels) + industry-standard alignment (Apple HIG / Material Design / iOS Settings split). Persona walk: all 5 personas pass (Maya scans labels → Title Case meets expectations; Walter iPad-primary → standard iOS app feel; Sydney → cohesive; Aria + Sam empty-state Title-Case-title-vs-sentence-case-body = clear info hierarchy).

- **Authority-precedence remediation logged.** Previous casing locks lived only in dated ROADMAP §6 entries, never in VISION.md. Per CLAUDE.md authority precedence locked 2026-05-16, voice/tone and locked design decisions belong in VISION.md. Today's amendment closes the gap.

### 2026-05-27 (later — `fab-tree-route-to-modal-conversion-cluster` chat — From Vault in-menu overlay)

- **From Vault → in-menu overlay ✅ SHIPPED (`bde3745`).** PlantingFlowModal bottom-sheet (z-60, `animate-modal-slide-up`, `sm:max-w-2xl` iPad+) converted to in-menu overlay mounted inside UniversalAddMenu's flex-col panel as new `"add-plant-from-vault"` sub-screen. The FAB "From Vault" chip now slides horizontally in-place matching the other 4 FAB chips. Closes Syd's "From Vault menu slides up and is diff than our other menus" dogfood feedback structurally. 7 files (5 page mounts + UniversalAddMenu + PlantingFlowModal deletion), +30/-206 net.

- **Q2=B OVERRIDE locked in.** The bottom-sheet decision from `.cursor/plans/fab_tree_modal_consistency.plan.md` Item 1 (2026-05-26 morning, shipped via `aa4c116`) explicitly chose bottom-sheet (~90vh phone, `sm:max-w-2xl` iPad+) over in-menu mount. AddPlantModal C1 embedded-mount ship (2026-05-26 evening, `ba9319f`) shipped 24h later proved the in-menu pattern works for complex forms with secondary modals + sticky bottom-bars. User explicitly reaffirmed override after hearing trade-off (iPad-portrait tightens from ~672px to 448px). Per CLAUDE.md "user direction contradicts a locked rule": contradiction flagged, trade-off surfaced, override respected after reaffirm. CLAUDE.md doesn't need amending — this is a Q2=B-specific override; the broader rule (in-menu canonical FAB shape) is reinforced not weakened. Locked: in-menu overlay is now the canonical FAB sub-modal shape for all 5 FAB chips. No remaining FAB-tree paths use full-page routes or bottom-sheet exit patterns.

- **Chapter B closed as no-op.** Orchestrator handoff framed "Add seed packet → Link import currently routes to a page" as Chapter B. Per Step 0 search-before-respond, current code verified: `7242cb6` (2026-05-26) already moved Seed Link import to inline `step="link"` inside SeedPacketForm, mirroring SupplyForm's template. The `/vault/import` route remains for Maya's batch processor (preserved deliberately per Q1=A in modal-consistency cluster plan). Closed as no-op — no remaining residual work on Link import path.

- **Step 0 search-before-respond — first real-world test of orchestrator-spawn-prompt stale-memory detection.** Both Chapter A + B framings were stale per current code state (`aa4c116` + `7242cb6` already shipped 2026-05-26). Surfaced via grep + commit inspection + reading current PlantingFlowModal + SeedPacketForm code BEFORE plan-of-record locked. User explicitly reaffirmed Chapter A as override + closed Chapter B as no-op via plain-English-at-end ask (per AskUserQuestion-forbidden rule from `f4c2075`). Pattern validated: orchestrator memory ≠ code canon; Step 0 catches the drift before invasive work begins.

- **First real-world test of CLAUDE.md provisional bidirectional state-transition tracing rule (`789688a`, shipped earlier today).** Per the new rule, traced state transitions across 4 axes (Open/Close, Forward/Back, Mount/Unmount on 5 page mounts, Suspense/Lazy resolution). Matrix surfaced C3 (celebration/onSaved ordering) which pure Pass 2 concerns hunt may have missed — verified clean at [PlantingForm.tsx:456-465](src/components/PlantingForm.tsx:456): 800ms `setTimeout` fires `setShowSeedlingCelebration(false)` + `onSaved?.()` together so menu close cascades cleanly after celebration completes. Rule running as designed. Status: **just-captured** — promote to memory if it surfaces unique findings 2+ more times. **Notable**: the rule itself shipped earlier in the same session via `789688a`; this is the first ship to consume it.

- **Accepted regressions (Path Y Q1=Option B precedent extended).** Pages don't auto-refresh after in-menu successful add — Garden + Vault need manual refresh to see fresh data. Same shape as 5 sibling in-menu paths (TaskForm / JournalEntryForm / SupplyForm / SeedPacketForm / AddPlantModal). Functionally important enough on Garden + Vault to warrant a `notifyPlantingSuccess` channel on UniversalAddContext if Syd flags it. Captured as deferred follow-up.

- **ADJACENT findings parked.** **C2:** PlantingForm modal-mode footer has Confirm planting only (no Cancel button). Adjacent to FAB-tree cohesion since SeedPacketForm / SupplyForm / TaskForm / JournalEntryForm all have own Cancel. Deferred since reshaping requires both page-mode + modal-mode work + affects single-button bar shape. Two-tap exit (Back → Cancel at add-plant level) accepted for this ship; logged for future cohesion pass when PlantingForm itself gets the form-submit-saga template treatment. **C8:** iPad-portrait viewport tightens from `sm:max-w-2xl` (~672px) to menu's `max-w-md` (448px). Accepted per peer consistency (all 5 sibling FAB chip sub-screens use same width); gather Walter dogfood signal first before widening.

- **3 buckets close-out:**
  - **(a) Parked items added this chat:** C2 PlantingForm Cancel button cohesion gap (ADJACENT for future polish pass) + C8 iPad max-w-md cramp (monitor Walter dogfood signal) + page refetch loss on Garden + Vault (follow-up `notifyPlantingSuccess` channel if user flags).
  - **(b) Deferred audits / known follow-ups:** Real-device dogfood verification of the conversion (Syd phone-tests on prod — verify From Vault slides horizontally in-place, Back returns to add-plant sub-screen, planting completes + celebration visible, refetch loss is OK on Garden/Vault) + Chapter B verified-as-shipped on prod (`7242cb6`).
  - **(c) Dogfood-style findings:** **(1)** First real-world test of CLAUDE.md provisional bidirectional state-transition tracing rule (`789688a`) — matrix surfaced unique concern (C3 celebration timing) verified clean. Status: just-captured. **(2)** First real-world test of orchestrator-spawn-prompt stale-memory detection (Step 0 search) — both chapters' framings were stale; pivot resolved cleanly via plain-English ask at end of message per AskUserQuestion-forbidden rule. Status: just-captured. **(3)** Q2=B override pattern — locked decisions from 24h prior chats can be legitimately overridden by NEW proof shipped between (C1 embedded mount in `ba9319f`); the override decision needs user reaffirm after trade-off + documentation in plan file + ROADMAP §6. Status: just-captured.

- **Concurrent-fetch gate validation #14.** Pre-push fetch + diff vs origin/main showed 0/0 ahead/behind cleanly at code commit. Phase 2 gate item 9 (run `git fetch origin main` + diff vs HEAD pre-push) continues running clean across single-commit ships.

### 2026-05-27 — CLAUDE.md Pass 1 audit rule add (post-FAB-tree polish bundle dogfood)

- **Bidirectional state-transition tracing locked as Pass 1 subcategory in CLAUDE.md.** Three consecutive drift instances in the 2026-05-26 FAB-tree polish bundle (`7c0decf` items 1+2 + `84df090` item 8 + `c124213` item 9) all had the same root shape: asymmetric tracing across an axis of state transition. **Item 1+2:** code analysis traced the OPEN path correctly but missed the BACK path (same axis, different direction); bug persisted for weeks, caught only by Syd's dogfooding. **Item 8:** chunk-fetch latency hypothesis preloaded modal chunks on FAB-menu open, missed the Suspense first-render gap (different React axis — lazy resolution, not chunk fetch); fix shipped, didn't close the reported bug. **Item 9:** finally caught the Suspense + lazy-resolution gap via pre-resolving React.lazy through unconditional mount; took 3 hypothesis revisions to land. New rule: for any modal/menu lifecycle change, Pass 1's factual trace must cover state transitions in BOTH directions (open/close/forward/back) AND across React commit-phase axes (reconciliation, Suspense + lazy resolution, mount lifecycle). Matrix approach forces every relevant axis onto the plan pre-commit. **When a fix doesn't close the reported bug, the next hypothesis MUST move to a different cell of the matrix — refining the same axis is the drift shape this catches.** ~60-90s cost per affected change; far cheaper than one wasted ship cycle (item 8 → item 9 alone cost 2 ship cycles + Vercel deploys). Pairs complementary with existing Pass 2 cohesion-by-aggregation (catches new-pattern introduction) and persona-walk (catches user-exclusion concerns); Pass 1 subcategory catches **factual tracing completeness** on the state-transition surface.

- **Doc-only push tier per CLAUDE.md** — single-file CLAUDE.md amendment + this §6 changelog entry; no code touched; pushed immediately after self-audit. Concurrent-fetch validation #14: pre-amendment `git fetch origin main` showed 0 ahead / 0 behind cleanly. Phase 2 readiness-gate item 9 (canonical-doc divergence check before amendment) ran clean.

### 2026-05-26 (latest — `continue-from-prev-chat-fab-form-submit-saga` chat — FAB-tree polish saga closes)

- **FAB-form submit pattern standardization saga ✅ SHIPPED (5 commits + verify-only).** Closes the FAB-tree polish thread that started with Path Y A-D (2026-05-19) and ran through the modal-consistency cluster (2026-05-26 morning, commits `488e7f3` → `aa4c116`). All 10 FAB-tree forms now share the AddPlantModal fixed-bar template (3-section flex-col shell + Cancel-left + Save-right + border-t border-neutral-200 + bg-emerald-600 hover:bg-emerald-700 Save + disable-until-required-fields-filled). **VISION §11 "Save / Cancel button consistency across modals" parked decision (raised 2026-05-08) CLOSES** with this saga; the canonical tokens are now anchored in 10 forms + UniversalAddMenu's main/add-plant screens, providing a stable cohesion-by-aggregation anchor for every future form ship. **`.cursor/plans/fab_addplantmodal_extraction.plan.md` parked plan (C1 deferral) CLOSES** via the clean embedded-mount approach made trivial by the Foundation commit's panel restructure.

- **Locked design tokens (saga end-state):**
  - **Outer panel** (standalone shells): `max-h-[85vh] flex flex-col overflow-hidden rounded-3xl bg-white border border-neutral-200/80 max-w-md w-full animate-modal-slide-up` (or `rounded-2xl` for the Batch* + PurchaseOrderImport standalone shells which already had that primitive).
  - **Header** (flex-shrink-0): `px-6 pt-6 pb-4` wrapper with internal `flex items-center gap-2` row: back-arrow (or `<div className="w-11 shrink-0" aria-hidden />` spacer) + h2 (`flex-1 text-center`) + right `w-11` spacer.
  - **Body** (flex-1): `flex-1 min-h-0 overflow-y-auto px-6 pb-6 relative` — `relative` so SubmitLoadingOverlay positioning works.
  - **Footer** (flex-shrink-0): `px-6 py-4 border-t border-neutral-200 flex gap-2.5 justify-end` for 2-button; `px-6 py-4 border-t border-neutral-200` (no flex/justify) for 1-button full-width Cancel.
  - **Save button**: `min-h-[44px] px-4 py-2 rounded-3xl bg-emerald-600 text-white font-medium hover:bg-emerald-700 disabled:opacity-50` (2-button footer) or `w-full py-2.5 ...` (single-action variant).
  - **Cancel button**: `min-h-[44px] px-4 py-2 rounded-3xl border border-teal-gus/40 text-teal-gus font-medium hover:bg-teal-gus/10 disabled:opacity-50` (2-button) or `w-full py-2.5 ...` (1-button main-screen + choose-step anchor).
  - **HTML5 `form="<id>"` attribute pattern**: for forms with submit body in body wrapper + Save button in footer (split DOM tree), give the `<form>` an id and the footer Save `form="<id>" type="submit"`. Preserves Enter-key submit semantics. Used in SeedPacketForm (id="seed-link-form" + id="seed-manual-form"), SupplyForm (id="supply-form"), 2a-shipped TaskForm + JournalEntryForm.

- **Amendment captured mid-execute:** Plan-of-record (`.claude/plans/fab_form_submit_standardization.plan.md`) originally specified "Back + Save" per-step footers (line 165). Amended to "Cancel + Save" mid-Commit-2b after cohesion-by-aggregation re-anchored to 2a-shipped TaskForm + JournalEntryForm + AddPlantModal template (all use Cancel + Save). Header back-arrow already handles step-back navigation in SeedPacketForm/SupplyForm; footer Cancel exits the menu. Re-audit Pass 1-4 clean on the amended plan. **Pattern reinforced:** when 2a establishes a concrete anchor that contradicts the original plan, the anchor wins; capture the amendment in the plan log + re-audit before continuing. This is the cohesion-by-aggregation rule running in real-time.

- **AddPlantModal C1 closed via clean embedded mount.** New `embedded?: boolean` prop on AddPlantModal; when true, the 3-section triplet renders directly into the menu panel without the standalone backdrop + panel wrappers. `useFocusTrap`, `useEscapeKey`, `useBodyScrollLock` all gated on `!embedded` so the menu (parent) owns those single-source-of-truth concerns when embedded; standalone mounts unchanged. UniversalAddMenuScreen union grows `"add-plant-manual"`. Manual entry chip handler in `screen="add-plant"`: changed from `onClose() + onAddPlantManual()` (closes menu + opens standalone modal — the asymmetry the C1 plan was meant to fix) to `setScreenDirection("forward") + setScreen("add-plant-manual")` (slides in-place matching the 4 other FAB chip sub-screens). 5 page mounts drop `onAddPlantManual={openPlant}` wiring; 4 of 5 also drop now-unused `openPlant` destructure (garden/page.tsx retains it for empty-state non-FAB caller). enrichmentFailed secondary modal preserved as-is (already z-[60] fixed inset-0; positioning DOM-parent-independent).

- **Save-button color canonicalization.** Across 9 Save buttons in 5 forms, `bg-emerald` (= emerald-500 = #10b981) migrated to `bg-emerald-600 hover:bg-emerald-700` (= #059669). Visible Save-button darkening on TaskForm, SeedPacketForm (3 Saves), SupplyForm (2 Saves), BatchAddSeed (Review pending + Load Plant Profile + Done), BatchAddSupply (Extract & Review), PurchaseOrderImport (Extract items). JournalEntryForm + AddPlantModal were already at emerald-600 (no change). Per VISION §11 closure + AddPlantModal template anchor.

- **PlantingForm verify-only cohesion tweaks (no shell change).** Sticky bottom-bar's border `border-black/10` → `border-neutral-200` (matches AddPlantModal.tsx:966 + NewTaskModal.tsx:431); Confirm Planting button `bg-emerald-500 hover:bg-emerald-600` → `bg-emerald-600 hover:bg-emerald-700` (matches canonical Save). Single-button bar shape preserved (page-mode = fixed bottom-20; modal-mode = sticky bottom with -mx-6). Confirm Planting remains the only action — Cancel is owned by the parent modal/page Close button.

- **3 buckets close-out:**
  - **(a) Parked items added this chat:** ADJACENT findings logged for future cohesion pass (non-FAB-tree edit/utility modals with similar submit-shape): **AddPlantManualModal** ([AddPlantManualModal.tsx:113](src/components/AddPlantManualModal.tsx:113), Vault profile → Packets tab "+ Add packet"), **HarvestModal** (grow-instance harvest flow), **EditJournalModal**, **EditPacketModal**, **AddItemModal**, **FeedbackModal**, **InviteMemberModal**. All preserve their pre-saga shell shape; each will need the same template treatment when a future polish pass touches them. Also parked: real-device dogfood verification on every migrated form's golden path (auth-blocked from Preview MCP — see bucket (b)).
  - **(b) Deferred audits / known follow-ups:** **Preview MCP visual check was attempted at 412×915 mobile-viewport; dev server booted clean with no new console errors from the diff, but AuthGuard redirected to /login (no auth session in Preview MCP context).** Per CLAUDE.md "auth-blocked → ship with note." Real-device verification needed by user across all migrated paths: FAB → Add task (TaskForm) + FAB → Add journal (JournalEntryForm with the "≥1 of {action, note, photo, linked plant}" disable rule) + FAB → Add seed packet (SeedPacketForm 3 steps × 2 manual-mode sub-shapes) + FAB → Add to shed (SupplyForm 3 steps) + FAB → Add plant → Manual entry (new embedded slide-in path; verify it slides like the other 4 chips) + standalone PurchaseOrderImport from any entry + standalone BatchAddSeed + standalone BatchAddSupply. Save-button darkening (`bg-emerald` → `bg-emerald-600`) is the most visible visual change to confirm read OK on Walter's iPad-portrait + Sydney's phone-portrait. Confirm Enter-key submit still works on every `<form onSubmit>` shape (TaskForm, JournalEntryForm, SeedPacketForm link-form, SeedPacketForm manual-form, SupplyForm). Confirm the AddPlantModal embedded mount's enrichmentFailed secondary modal renders correctly when triggered (z-[60] stacking inside menu's z-[100] panel).
  - **(c) Dogfood-style findings:** **(1) HTML5 `form="<id>"` attribute pattern validated** as the canonical solution for split DOM-tree forms (body in scroll area + Save in fixed footer). Preserves Enter-key submit cleanly; 2a-shipped TaskForm + JournalEntryForm already used a wrapping `<form>` shape (form wraps everything); 2b's SeedPacketForm + SupplyForm needed the split pattern because of multi-mode rendering (two separate `<form>`s in manual step). Status: **just-captured** — promotion to a CLAUDE.md rule may be premature (single ship of the pattern); promote if it recurs in the future. **(2) Cohesion-by-aggregation amendment-during-execute pattern validated** — the original plan called "Back + Save" for footer; mid-Commit-2b the 2a anchor became visible and the amendment kicked in. Re-audit completed in ~3 minutes; total cost vs cost-of-shipping-with-drift = high ROI. Status: **just-captured** — this is the rule running as designed; no promotion needed. **(3) Save-for-later button placement** in SeedPacketForm new-mode: moved from button-row to inline body action next to its helper text. This is the right shape for tertiary actions that don't fit the 2-button footer template (the "do something OTHER than the primary action" shape). Status: **just-captured** — pattern may recur with other tertiary actions; promote to a CLAUDE.md rule if it appears 2+ more times.

- **Concurrent-chat fetch gate validation #9-#13.** 5 push points across this chat (Foundation push was prior chat; this chat's 4 pushes for 2b/3/4 plus pre-execute fetch). All 5 fetches showed 0/0 ahead/behind cleanly. Phase 2 gate item 9 (run `git fetch origin main` + diff vs HEAD before canonical-doc amendments) extends naturally to multi-commit code chains in same chat — preflight at chat-open + before each push. The rule continues to run clean since locked 2026-05-17.

### 2026-05-26 (earlier — `continue-from-stalled-fab-modal-consistency` chat — FAB-tree modal-consistency cluster closes)

- **FAB-tree modal-consistency cluster ✅ SHIPPED (5 commits).** All 5 sibling FAB add-menu picks now stay inside the modal tree — no full-page route exits, no chip-shape asymmetry. **`d269f32`** (prior chat) — Item 5 strict bug fix: VaultPageContent's `onSupplyOpenBatchPhotoImport` no longer routes to `/shed/review-import`; now mounts `<BatchAddSupply>` matching the 4 other pages' pattern. **`488e7f3`** — Items 3+4: optional `onBack` prop on `BatchAddSeed` + `BatchAddSupply` (Back arrow on capture step only); sentence-case h2 on both ("Photo Import" + "Import Supplies from Photos" → "Photo import" per Q3=A cohesion-align). **`7242cb6`** — Item 2: inline `step="link"` in `SeedPacketForm` mirroring `SupplyForm.step="link"` template; new thin `/api/seed/extract-single-url` endpoint wraps `/api/seed/extract-metadata` internally per Q1=A (no batch-path touch, no logic duplication); drop now-obsolete `onOpenLinkImport` prop everywhere; `/vault/import` batch processor stays reachable via direct URL (Maya power-user path preserved as ADJACENT — Vault-toolbar entry flagged as dogfood follow-up). **`aa4c116`** — Item 1: extract 870-line VaultPlantPageInner body into shared `PlantingForm` component with `mode: "page" | "modal"` prop; new `PlantingFlowModal` bottom-sheet per Q2=B (~90vh phone, sm:max-w-2xl iPad+); page route preserved for non-FAB callers (Calendar Plantable Plant button + Vault toolbar batch-select still use `?ids=`); 5 FAB callsites wired with `onAddPlantFromVault` switching from `router.push` to `setPlantingFromVaultOpen(true)`. **Concurrent-chat collision survived clean:** `79c34a7` (form-error unification by `sydknee09-web`) landed on origin/main between Items 3+4 push and Item 2 commit; git auto-merge preserved FormError import + Item 2 changes intact in linear history (`488e7f3 → 79c34a7 → 7242cb6 → aa4c116`); no manual conflict resolution. **Tests 438/438 across all 3 this-chat commits; builds clean; concurrent-fetch validation #8** (caught one collision, reconciled via auto-merge). Preview MCP auth-blocked (snapshot clean, no console errors); Syd phone-verifies on prod with per-item dogfood plans surfaced in chat. **3 questions answered upfront (Q1=A thin route, Q2=B bottom-sheet, Q3=A cohesion-align both Photo import h2)** locked at Phase 1 + greenlight, no mid-chat re-asks. **Cluster closes the FAB-tree shape asymmetry that originated in §3.9 / parked items** — siblings now match the working Add-to-shed Link import template.

- **Dogfood findings captured for follow-up** (per CLAUDE.md Phase 5 Rule + bucket-c reporting): (a) **Vault-toolbar batch-import entry point** — Maya's `/vault/import` power-user path is now reachable only via direct URL after Item 2 dropped the FAB `onOpenLinkImport` props; needs a Vault-toolbar "Batch import" button as a separate small feature to restore in-app entry (file as parked under ROADMAP §4 or new chunk). Status: **just-captured this chat**. (b) **Drilldown modal h2 "{plantType} – Seed inventory" vs widget title "Plant this month"** widget-title-vs-drilldown asymmetry preserved from 2026-05-25 ship — preexisting data-shape work, captured as ADJACENT for separate chunk. (c) **`/vault/plant` page route shape** — page now thin shell with no description paragraph (was page-mode-only "Set how much of each packet to use..."); test path for both modes during dogfood verification.

- **Concurrent-chat fetch gate validation #8 (`79c34a7` interlock):** pre-commit fetch + diff against `origin/main..HEAD` showed clean; commit landed parented to local HEAD (488e7f3); during push, git auto-merged because origin/main had moved to `79c34a7`; result was clean linear history with both changes preserved. No manual conflict resolution required. The Phase 2 readiness gate item 9 (run `git fetch origin main` + diff vs HEAD pre-push for canonical-doc amendments) extends naturally to code commits when a collision on a shared file (`QuickAddSeed.tsx`) is in play; this validation reinforces the existing rule's discipline.

### 2026-05-25 (latest — `gt-casing-theme3-a1` chat)

- **Casing-cohesion sweep ✅ SHIPPED (`a78dbd6`).** 22 Title Case → sentence case edits across 7 files in the FAB tree + 3 same-pattern siblings. **Scope-1** (6 named modals): QuickAddSeed (Plant name / Variety / cultivar / Vendor / nursery ×2 visible + aria-labels ×2), QuickAddSupply (Product name / Soil amendment option / Application rate / Usage instructions), QuickLogModal (QUICK_ACTIONS labels Pot up / Plant out / Cold stratify; Supply used SearchableMultiSelect label; "+ Add new supply" emptyStateAction; Take photo button), PurchaseOrderImport (Purchase order import h2 + Upload from files button). **Scope-2** (Pass 3 ADJACENT same-pattern siblings, scope-expansion validated by `006dd69` close-out): EditJournalModal mirror of QuickLogModal QUICK_ACTIONS + Supply Used + Take Photo (5 fixes), CareScheduleManager `soil_amendment` enum display map (1 fix), BatchAddSeed "Upload from Files" button (1 fix). **NewTaskModal + AddPlantModal CLEAN** (already sentence case; "Active Garden" preserved as proper-noun page label per today's `006dd69` Item 2 lock). **Volume pills** (Full / Partial / Low / Empty) preserved as-is per single-word categorical enum-token rule (consistent with QUICK_CATEGORIES Maintenance/Fertilize/Prune and supply category Fertilizer/Pesticide/Other). **Mid-execution amendment + re-audit:** aria-label="Vendor / Nursery" ×2 occurrences (L700+L865 of QuickAddSeed) flipped to lowercase to maintain visible↔aria cohesion after visible-label rename — caught by Pass 2 cohesion-by-aggregation check before push. **NORTH_STAR cite:** "No duplicate paths" (consistent casing IS the cohesion expression across siblings) + "Take mental load OFF the user" (mixed casing reads as careless to Walter + Sam). Tests 434/434; build clean; concurrent-fetch 0/0 pre-commit. **Superseded 2026-05-27** — the HEADERS + BUTTON labels swept here revert to Title Case (AP-style) per the new [VISION.md §8 "Casing"](docs/VISION.md) subsection. Field labels (Plant name / Variety / cultivar / Vendor / nursery) + matching aria-labels + volume-pill enum tokens + supply-category tokens + QUICK_ACTIONS labels (Pot up / Plant out / Cold stratify) remain sentence case — body / field / enum lanes unchanged. See 2026-05-27 (latest) entry above for reversal context.

- **§3.12 theme #3 Phase A.1 ✅ COMPLETE (`b669a2a` + `8310070`).** 4 plain-language page-label renames shipped across 5 files. **(1)** Vendor Scorecard → "Vendor ratings" — Settings menu [page.tsx:12](src/app/settings/page.tsx:12) + Vendors page h1 [vendors/page.tsx:198](src/app/settings/vendors/page.tsx:198); reuses "ratings" from existing subtitle. **(2)** Monthly pulse → "Monthly view" + **(3)** Annual roadmap → "Yearly view" — Home subtitle [page.tsx:418](src/app/page.tsx:418) + Home buttons L432+L439 + Schedule view-toggle [schedule/page.tsx:26-27](src/app/schedule/page.tsx:26); Option 3 calendar-app-convention pair locked by Syd over Option 1 (plain) + Option 2 (descriptive). **(4)** Plantable → "Plant this month" — calendar/page.tsx widget title L999 + button title attr L1028 + aria-label L1029 ("View plantable in Vault" → "See plants for this month in Vault" on both). **Plantable verb-choice driven by data-source audit:** Syd flagged sharp semantic concern *"ready to sow? like plant by seed? or transplant?"* mid-plan. Audit found `plant_profiles` data source includes ALL profile types (perennials, established plants, anything with `planting_window`), not seed-only; field name + function name (`isPlantableInMonth*`) + existing aria-labels all already lean "plant" not "sow." "Ready to sow" would have semantically narrowed wrong — concrete misfire scenario: apple-tree profile with `planting_window="feb-apr"` + `status="in_stock"` for bare-root nursery stock would render "Ready to sow in March" (wrong; apple trees are planted not sown). Broad-verb "Plant" matches data shape + covers all 3 paths (sow seeds / transplant seedlings / plant out perennial starts). **Drilldown modal h2 "{plantType} – Seed inventory" (L1051) preserved as-is** — the widget-title-vs-drilldown asymmetry (broad source data / seed-narrowed drilldown destination via `/vault?sow=`) is preexisting data-shape work captured as ADJACENT for separate chunk. **Internal variable names** (`plantableProfiles` / `plantableExpanded` / `plantableInventoryPlantType`) + function names (`isPlantableInMonth*`, `getSowMonthsForProfile`, `getSowingWindowLabel`) + URL params (`?view=heatmap`, `?view=roadmap`, `?sow=`) + internal component name `MonthlyPulseView.tsx` all preserved as programmatic identifiers (not user-facing). **Phase A.2** (Vault + Shed page-label renames; URL `/vault/...` + sidebar nav + 50+ files; needs redirect strategy) deferred to dedicated chats. **Dropped from Phase A scope** after audit found zero user-facing strings in src/: Polyculture (likely cleaned by theme #5 ship `9bad88f`), True leaves, Hardening off, Grow instance (purely internal type/component name `GrowInstance` + table `grow_instances`). **Deferred to §8 settings IA chat** (dev-gated since `4a8b04c`, Sam/Walter never see): Repair hero, Extract cache. **NORTH_STAR cite:** "Take mental load OFF the user" (jargon decode → plain action language) + data-honest framing (verb matches the actual data shape, not a narrower interpretation). Tests 434/434; build clean; concurrent-fetch 0/0 pre-commit on both code commits.

- **3 buckets close-out:**
  - **(a) Parked items added this chat:** **(i)** Widget-title-vs-drilldown-destination asymmetry on the Plantable widget — broad source data (all plant_profiles with matching planting_window) but seed-narrowed drilldown destination (modal shows "Seed inventory"; ∞ button navigates to `/vault?sow=` filtering Seed Vault tab). Captured as ADJACENT for future data-shape chunk; not blocking; the rename ship is data-honest at the widget-title level, the asymmetry exists below it. **(ii)** Back-link family casing decision deferred to Phase A.2 (Vault + Shed page-label renames). Pass 3 sweep found 21 `Back to [PageName]` peer sites across the codebase, ALL Title Case (Back to Vault ×9 / Back to Home ×6 / Back to Settings ×5 / Back to Shed ×4 / Back to Calendar/Journal/Garden ×1 each). Destinations are page-label proper nouns protected by today's `006dd69` Item 2 lock (same pattern as "Active Garden" preservation). Flipping just the vendors page would create a 1-of-21 outlier; flipping all 21 now would preempt Phase A.2's holistic page-label rename pass (where Vault + Shed get new destination labels anyway, and the case decision should be made with those new labels in view). Phase A.2 chat must bundle: (a) Vault + Shed page-label renames + URL redirect strategy, (b) sidebar nav label changes, (c) the back-link family casing decision (all 21 sites or none) + new destination labels, (d) "Add to vault" / "Add to shed" body copy in FAB menu, (e) tab labels inside Vault page ("Plant Profiles" / "Seed Vault" — Phase 6 cohesion question, parallel concern).
  - **(b) Deferred audits / known follow-ups:** Phase A.2 page-label renames (Vault + Shed — separate dedicated chats with redirect strategy); Phase A drops confirmed clean (Polyculture / True leaves / Hardening off / Grow instance — zero user-facing strings); Phase A defers (Repair hero / Extract cache) bundled with §8 settings IA chat; Pass 3 ADJACENT casing items from prior chat (8+ Title Case h2s across Vault profile / Garden modal headers / Settings — Delete Batch / Edit Plant Profile / Add Photo / Confirm Planting / Add Manual / Delete Account / Edit Garden Settings) still parked for app-wide casing pass beyond the FAB tree; phone + desktop verification on prod pending for all 3 commits this chat (dogfood plan surfaced in chat).
  - **(c) Dogfood-style findings:** **(1)** Syd's challenge on the "Ready to sow" rename caught what would have been a wrong-narrowed label before ship — pattern reinforced: **verify proposed labels against actual data source before locking**, especially when the proposed verb implies a narrower category than the data shape supports. **Status: just-captured** — broader pattern "do data-source audit on every page-label rename, not just touch-radius grep" may be worth promoting to plan-audit rule but Phase A.2 has 0 candidates left and Phase B body-copy renames don't have the same shape; deferring promotion until next page-label-rename batch. **(2)** Scope-expansion-by-sibling-grep validated again this chat (EditJournalModal + CareScheduleManager + BatchAddSeed caught by Pass 3 sibling sweep on the casing-sweep ship) — same precedent as today's earlier `006dd69` ship. **Status: just-captured** — pattern is now mature across 3 ships within a 24-hour window; promote to memory if it recurs once more.

### 2026-05-25 (later — `continue-from-prev-chat-stalled-logger-coverage` chat — debug-log 8-layer instrumentation)

- **Debug-log 8-layer instrumentation ✅ SHIPPED (`f79a8aa`).** 19 files, +612/-13 (2 NEW: `src/lib/debugLog.ts` helper + `debugLog.test.ts` with 4 tests; 17 EDITED across `debugLogBuffer.ts` + `supabaseWithOffline.ts` + 5 contexts/hooks + 6 forms + ErrorBoundary + ServiceWorkerRegistration + OfflineIndicator + DebugLogInit). **8 layers:** helper `logEvent(scope, action, payload?)` with bracket-prefix `[scope] action {payload}` (parallels [apiErrorLog.ts:7](src/lib/apiErrorLog.ts:7)); buffer cap 50→500 in [debugLogBuffer.ts:10](src/lib/debugLogBuffer.ts:10) (test parametrized via `__test__.MAX_ENTRIES + 25` iterations so cap-agnostic); boot + auth (getSession + onAuthStateChange + signOut in AuthContext) + SW (register/registered/update_found/controller_changed/register_error; previously-silent catch now logged); network online/offline + offline-queue replay (replay_start/replay_ok/replay_retry/replay_done with counts); navigation `[nav] enter {path}` via `usePathname` in DebugLogInit; onboarding action/complete/dismiss; supabase mutations — 5 helpers wrapped with start + ok/error/queued including `table, op, ms, online, error.message`; ErrorBoundary catch + `useToast.showErrorToast` + 6 canonical FAB-flow form envelopes (`add_plant` / `add_seed` / `add_supply` / `add_journal` / `add_task` / `harvest`). **Privacy contract (load-bearing):** payload SHAPE only — counts, ids, table names, op kinds, durations, error.message. Never plant names, notes, journal text, supply names, or any user content. `logEvent` wraps `console.log` in internal try/catch so logger never crashes the app. **Exclusions** (keeps signal/noise high): no read queries, no render cycles, no mouse events, no success toasts, no calendar cache hits, no per-asset SW cache events. **Pass 1-4 audit terminated clean.** Pass 3 sibling sweep CONCERN logged (bracket vs colon split in existing console.error families; new `logEvent` joins bracket per handoff; colon-notation peers unchanged this ship — future cohesion pass parked in §4). Pass 4 lock hygiene clean — no VISION §10 don't-touch brushed; HarvestModal parked end-state realization decision (§11) preserved (envelope addition only, no behavior change to insert path); 6-form picks deliberately exclude multi-stage (BatchAddSeed/BatchAddSupply) and edit-flow forms (parked in §4 as ADJACENT). Dev tooling under `useDeveloperUnlock` — all 5 personas pass by exclusion. Tests 434 → 438. Build clean. Concurrent-fetch 0/0 pre-commit.

- **Plan rebuilt in-chat after previous-chat plan loss.** Previous chat ("GT logger coverage") had completed Phase 1 audit + Phase 2 plan but stalled on an `AskUserQuestion` widget that Dispatch couldn't render — and the plan file was never committed (scratch state only, lost on stall). This chat opened with Phase 2 readiness gate failing (no plan file existed despite handoff claim it did). Per CLAUDE.md "when user direction contradicts a locked rule, flag explicitly + surface trade-off + respect override only after user reaffirms," surfaced the gap via plain-English ask at end of message (per the new [AskUserQuestion forbidden](CLAUDE.md) rule shipped in `f4c2075` 2026-05-25 morning). User picked Option A (rebuild plan in-chat using handoff substance + Pass 1-4 audit). Plan file at `.claude/plans/gt-debug-log-instrumentation.md` committed for audit trail. **Validation #1 of the new AskUserQuestion-forbidden rule** — plain-English-at-end ask delivered cleanly through Dispatch on first try; no widget-stall. Same pattern that previous chat hit on `AskUserQuestion` now resolves without the silent-stall failure mode.

- **3 buckets close-out:**
  - **(a) Parked items added this chat:** §4 new parked entry "Debug-log instrumentation — ADJACENT expansion (parked 2026-05-25 post-`f79a8aa`)" with 5 sub-items: BatchAddSeed.handleSaveAll form envelope, BatchAddSupply.handleExtractAll form envelope, edit-flow envelopes (EditJournalModal/EditPacketModal/GrowInstanceModal), BatchLogSheet.handleSubmit envelope, console.error format standardization (codebase-wide cohesion pass for the 16+ existing sites split bracket vs colon).
  - **(b) Deferred audits / known follow-ups:** Phone dogfood verification on `f79a8aa` pending — open `/settings/developer/debug-log` after deploy, trigger boot/auth/nav/form events, confirm structured shapes + privacy-clean Copy all. Pre-existing 7 M-status files (FAB-tree Back-arrow consistency on BatchAddSeed/BatchAddSupply + 5 page callers) discovered in working tree — left uncommitted as another chat's in-progress work; the originating chat owns the commit. Honest framing surfaced: this is Validation #1 of staging-by-specific-file (per CLAUDE.md "Never use `git add -A`") catching unrelated parallel-chat state cleanly.
  - **(c) Dogfood-style findings:** **(1)** Previous chat's plan file was lost when it stalled mid-Phase-2 (never committed). The new AskUserQuestion-forbidden rule (`f4c2075`) prevents the same stall going forward, but it doesn't help recover lost plan-state. **Status: just-captured** — the new rule should make this rare enough that no preventive change is needed yet; promote if it recurs. **(2)** First validation of the AskUserQuestion-forbidden rule in production use — plain-English-at-end ask delivered cleanly through Dispatch with no widget-stall. **Status: just-captured** — strong real-time validation of `f4c2075`. **(3)** ADJACENT-items capture pattern reinforced: 5 follow-up candidates surfaced during Pass 3 sibling sweep + Pass 2 cohesion-by-aggregation hunt; parked to §4 with file:line citations for next-chat discoverability rather than dropped into commit body alone. **Status: just-captured** — pattern is now mature across recent ships.

### 2026-05-24 (`gt-fab-polish-2` chat C1 doc-only ship)

- **Item D locked — gray-fill = dropdown affordance + white-outline = free-text input design convention.** Captured to [VISION.md §8 Design tokens](docs/VISION.md) as new "Field treatments — dropdown vs free-text" subsection. User-locked 2026-05-24 during Phase 1 of FAB polish bundle 2 planning chat. Two field treatments signal two interaction patterns; don't accidentally unify them — the visual distinction is doing semantic work. Doc-only push tier per CLAUDE.md.
- **Item 3 locked X2 — delete `/journal/new`.** Syd quote: *"I don't think we have another journal entry page."* Entrypoint sweep confirmed: footer link at [QuickLogModal.tsx:582](src/components/QuickLogModal.tsx:582) was the ONLY live consumer of `/journal/new` in src/ — no `router.push`, no sidebar nav, no calendar deep-link, no Vault cross-link. Tests asserting the contract ([permanentPlantProfile.regression.test.ts](src/app/vault/permanentPlantProfile.regression.test.ts) §1.4 — 2 describe blocks at L41-75) being DELETED, not updated. Companion lock: **QuickLog form header rename "Quick log" → "Add journal"** (N2 option, matches menu label, resolves Item B's journal half automatically). [NAVIGATION_MAP.md:429](docs/NAVIGATION_MAP.md:429) gets a 2026-05-24 changelog entry. Code ships in C3 (X2 delete bundle).
- **Auto-mode Q-picks (FAB polish bundle 2 auto-mode greenlit by Syd via Dispatch).** All 8 open aesthetic decisions ran on Claude's recommended defaults:
  - Q1 — Lifecycle icons: `Seed` for Sow (reuse), new `Sprout` SVG (sprouting shoot with cotyledons), new `PotUp` SVG (pot with leaf above), `Shovel` for Plant Out (reuse), keep `Plant` for Growth.
  - Q2 — Chevron alignment: (b) minimal — add `pr-12` to native `<input type="date">` so all 3 Quick log dropdown fields have matching right-edge padding; browser-native date chevron stays as-is (replacing native date picker = M-size scope, parked).
  - Q4 — Casing scope: widened to option labels in Seed + Supply choose-steps ("Manual Entry" / "Photo Import" / "Link Import" / "Purchase Order" → sentence case) in addition to submit buttons. *(Superseded 2026-05-27 — option labels in choose-steps revert to Title Case (AP-style: `Manual Entry`, `Photo Import`, `Link Import`, `Purchase Order`) per [VISION.md §8 "Casing"](docs/VISION.md) subsection. See 2026-05-27 (latest) entry above.)*
  - Q5 — "Add to Vault" treatment: lowercase "vault" → "Add to vault" / "Add to vault (with packet)" for cohesion with "Add to shed."
  - Q6 — Form header → menu label precedent: (c) fix Seed only ("Add seed" → "Add seed packet"); other 3 form headers handled separately via Q9 (Journal gets rename via X2) + Task/Supply preserved (each has product reasoning).
  - Q7 — Cancel/Back unification: (i) all TEXT Cancel/Back buttons use the canonical (rounded-3xl teal-gus from UniversalAddMenu). Icon-only Back arrows in headers stay as-is.
  - Q8 — Header centering: (a) add `<div className="w-11 shrink-0" aria-hidden />` right spacer to every off-center form header. Matches AddPlantModal:552's existing pattern.
  - Q9 — QuickLog header rename: N2 "Add journal" (matches menu label exactly).
  - Q10 — APP_AUDIT_RECOMMENDATIONS.md: skip (legacy doc not load-bearing).
- **Pass 3 ADJACENT findings parked** for future app-wide casing cohesion pass: 8+ Title Case `<h2>` headings + button labels across Vault profile / Garden modal headers / Settings (Delete Batch / Edit Plant Profile / Add Photo / Confirm Planting / Add Manual / Delete Account / Edit Garden Settings / etc). Out of scope for this FAB-tree-only bundle; flag for future sweep.

### 2026-05-24 (later)

- **FAB polish bundle ✅ SHIPPED (`intelligent-pare-f7abd4` chat continuation, 1 commit `006dd69`).** 3 small polish items from Syd's phone-dogfood of the earlier same-day sub-form transition bundle. Closes the FAB-surface visual + copy cohesion gaps.

  **Item 1 — Icon-style unification on Seed + Supply choosers.** Both submenus rendered platform emoji icons (📝/📷/📸/🌐/🧾) in white/grey square wrappers, breaking the canonical green-circle outline treatment used by Main FAB menu + Add Plant submenu. Scope expanded mid-execution per Syd's "audit EVERY FAB menu and submenu, not just Add Supply" directive — Seed submenu found to have identical broken shape. Same fix shape applied to both. Per-button mapping:
  - Manual Entry → `ICON_MAP.ManualEntry` (existing — exact reuse from Add Plant submenu's Manual entry button)
  - Photo Import → `ICON_MAP.PhotoImport` (existing — exact reuse)
  - Link Import → `ICON_MAP.Link` **(NEW)** — added to styleDictionary.tsx matching the canonical convention: same `iconProps()` helper, stroke 1.2, viewBox 24×24, monochrome outline (`PRIMARY_GREEN`). Classic chain-link visual (2 interlocking link halves — Lucide-pattern). Reuse-existing-components rule honored: not inventing a new icon STYLE, just adding a new icon IN the existing style.
  - Purchase Order → `ICON_MAP.PurchaseOrder` (existing — exact reuse)
  - Wrapper className swap: `rounded-xl bg-neutral-100 text-xl` → `rounded-3xl bg-emerald-luxury/10 text-emerald-luxury p-2.5`. Matches canonical pattern verbatim at UniversalAddMenu.tsx:235 (Add Plant submenu).

  **Item 2 — Header casing standardization (Option H2 — lowercase nouns preserving existing words).** *(Superseded 2026-05-27 by [VISION.md §8 "Casing"](docs/VISION.md) — header rule reversed to Title Case (AP-style); sentence case retained for body / field labels / enum tokens / aria-labels matching field labels. See 2026-05-27 (latest) entry above for full reversal context.)* All form sub-component headers were Title Case (pre-Path Y holdovers): "Add Seed" / "Add Supply" / "New Task" / "Edit Task" / "Quick Log" / "Edit Supply" / "Import from Link" / "Add Plant." Standardized to sentence case to match Path Y's "Add plant" + main menu labels ("Add seed packet" / "Add to shed" / etc.). Per Syd's lock: preserve existing words ("Quick log" stays distinct from "Add journal"), only lowercase the verbs/nouns. "Active Garden" preserved as Title Case (proper noun for the page label). 5 form headers touched (Seed / Supply / Task / Journal / AddPlant).

  **Item 3 — Add Plant > Scan Purchase Order subtitle copy fix.** Subtitle at UniversalAddMenu.tsx:250 read "Screenshot of cart or order with seeds" — copy reused from Seed flow verbatim, never updated for plant context. Strict bug per VISION §4 Principle 3 (objective inconsistency: button is in plant-add chooser; subtitle should mention plants). Fixed to "Screenshot of cart or order with plants." Inline doc comment at L29 also fixed.

  **Test update:** AddPlantModal.test.tsx:48 had a literal assertion on heading text "Add Plant" with capital P; updated to "Add plant" to match Item 2's sentence-case change. Initial test run caught the mismatch — fixed before push. Validates the test-first verification rule.

  **3 buckets at close-out:**
  - **(a) Parked items added this chat:** none (all 3 items resolved this ship).
  - **(b) Deferred audits / known follow-ups:** phone + desktop verification of all 3 items (Seed + Supply chooser icons now green-circle outline; all 5 form headers sentence case; Scan PO subtitle plant-appropriate). AddPlantModal Manual entry asymmetry from prior bundle still parked at `.cursor/plans/fab_addplantmodal_extraction.plan.md` (re-open if dogfood reveals it grates).
  - **(c) Dogfood-style findings:** scope expansion (Seed → Supply audit) caught a sibling regression that would have shipped half-fixed. Validates the user's "audit EVERY menu" directive as the right call. Pattern reinforced: when fixing a class of UI bug on Surface A, grep for the same shape on Surface B/C/D before declaring scope complete (cohesion-by-aggregation cross-surface scope-check sub-clause locked 2026-05-16 in CLAUDE.md).

  Tests 439/439 after the test-assertion fix; build clean. 8 files, +36/-24. Worktree Preview MCP skipped per documented `.env.local`-absent condition; phone + desktop verify on prod.

### 2026-05-24

- **Sub-form transition bundle Fix A + Fix B ✅ SHIPPED + BUGS U28 ✅ FIXED (`intelligent-pare-f7abd4` chat continuation, 3 commits `1c2d403` + `ba1da34` + `4e9fb25`).** Extends Path Y's in-place slide pattern one level deeper into form sub-component internal steps; closes the Add Seed Packet picker regression.

  **Forward AND back direction unified for the 2 in-menu forms with internal step machines** (SeedPacketForm choose ↔ manual; SupplyForm choose ↔ link ↔ form). New `stepDirection` state inside each form mirrors UniversalAddMenu's `screenDirection`/`slideClass` pattern verbatim — no new keyframes (reuses `animate-submenu-slide-forward` + `animate-submenu-slide-back` from globals.css), no new state-machine shape, no new context methods. Every step-changing setStep callsite (button onClick handlers + the URL-import success path + the Back arrow + bottom Back buttons) sets `setStepDirection("forward" | "back")` first. Initial-mount setStep calls in reset effects left as-is — the default direction "forward" is what we want on initial render anyway (parallels menu's main-screen-opens-with-forward-slide).

  **U28 closed** in same bundle (commit `4e9fb25`). Path Y Commit E (`d49e1ae`) regressed 3 buttons on the menu's Add Seed Packet choose step (Photo Import / Link Import / Purchase Order) by missing the cross-flow callback prop threading from menu through to `<SeedPacketForm>`. Pass 3 sibling-sweep flagged matching shape on `<SupplyForm>` — also missing `onOpenPurchaseOrder` + `onOpenBatchPhotoImport`. **Fix:** 6 new optional props on `UniversalAddMenuProps` (`onSeedOpenBatch` / `onSeedOpenLinkImport` / `onSeedOpenPurchaseOrder` / `onSeedStartManualImport` / `onSupplyOpenPurchaseOrder` / `onSupplyOpenBatchPhotoImport`); each of the 5 add-button-mounting pages wraps the same callbacks they already pass to standalone modal mounts, but uses `closeAll()` instead of `closeActiveModal()` since the callbacks fire from menu surface (not from a standalone modal). VaultPageContent variant: uses `setBatchAddOpen` instead of `setBatchAddSeedOpen` + routes `onSupplyOpenBatchPhotoImport` to `/shed/review-import` page navigation instead of opening BatchAddSupply modal — preserved verbatim from its existing standalone-modal callback.

  **F1 chosen mid-execution** over the original Q1 = C1 greenlight (Path-Y-treat AddPlantModal for symmetric in/out on the Manual entry path). On detailed reading of AddPlantModal mid-execution: structure is materially different from the 4 Path Y form extractions (sticky-flex-col-overflow + 3-section header/body/footer shell vs the simpler overflow-y-auto + inline Save pattern of TaskForm / JournalEntryForm / SupplyForm / SeedPacketForm; secondary `enrichmentFailed` nested modal; 1008 LOC; multi-mode `existing/new/addToExistingProfile` × `permanent/seasonal` × packet selection × photo flow). Original §14 estimate of "~2-3h, parallel cost to one of Commits A-D" was wrong; realistic ~4-6h. Partial `embedded`-prop edits were attempted then reverted (incomplete dead code). Honest scope-discovery surfaced to Syd; F1 picked (ship Fix A + Fix B now, defer C1 to follow-up after phone-verify reveals whether AddPlantModal Manual entry asymmetry actually grates in practice). C1 deferral plan at `.cursor/plans/fab_addplantmodal_extraction.plan.md` covers the 3 approach options (flatten / dual-render / embedded shim) + the UX trade (sticky-footer loss in standalone vs only-in-embedded) + audit pass requirements + estimated effort by approach.

  **Sole remaining asymmetric path in the FAB tree after this ship:** AddPlantModal Manual entry path (forward = menu closes + modal slides up; back = modal closes + menu fade-scales in). All other paths bidirectional symmetric. ROADMAP §6 FAB transition standard's "all 5 add-button picks are submenus" lock now realized for 4 of 5 picks at the form-content level (Add Plant chooser slides; Add Plant Manual entry deferred).

  **3 buckets at close-out:**
  - **(a) Parked items added this chat:** AddPlantModal Manual entry in-menu treatment — see `.cursor/plans/fab_addplantmodal_extraction.plan.md`. Re-open when Syd dogfood confirms the asymmetry grates.
  - **(b) Deferred audits / known follow-ups:** Phone + desktop verification of all 3 commits (Fix A1 SeedPacketForm slide both directions; Fix A2 SupplyForm slide all 4 step transitions both directions; Fix B / U28 — 4 buttons present on menu's Add Seed Packet choose step + 4 buttons present on menu's Add Supply choose step + each button triggers correct cross-flow). AddPlantModal Manual entry path stays as today (forward instant + slide-up modal; back instant + menu fade-scale) — confirm in dogfood whether the asymmetry matters.
  - **(c) Dogfood-style findings:** Honest scope-discovery during F1 execution proved the value of mid-execution amendment + re-audit. Pattern reinforced: don't silently ship an incomplete extraction when the audit estimate was wrong.

  Tests 439/439 across all 3 commits; build clean across all 3. Worktree Preview MCP skipped per documented `.env.local`-absent condition. Plan file at `~/.claude/plans/intelligent-pare-f7abd4.md` (§13 + §14 amendment block) retained. Concurrent-chat fetch (Phase 2 gate item 9) ran clean — fast-forwarded `2fbddf9` + `094b2a1` + `eb5d2a6` from parallel `fix(quick-journal)` chat before planning; no conflicts.

### 2026-05-20

- **BUGS U7 ✅ FIXED — Path Y refactor (`intelligent-pare-f7abd4` chat, 5 commits + revert).** All 5 add-button picks now use the in-place horizontal slide that Add Plant uses today. Menu container stays mounted through the entire FAB flow. Forms live INSIDE the menu, not as separate modals.

  **Commit sequence (chronological):**
  1. `143a07f` — Revert misfire `e7d59d7` (the choreographed close-then-open Option-B attempt that Syd rejected as "still feels close-then-open"). Pushed standalone before refactor landed.
  2. `bee5338` — Commit A: extract `<TaskForm>` from NewTaskModal (smallest target).
  3. `6652a25` — Commit B: extract `<JournalEntryForm>` from QuickLogModal.
  4. `b9cb40b` — Commit C: extract `<SupplyForm>` from QuickAddSupply (internal `screen → step` rename).
  5. `c812a5a` — Commit D: extract `<SeedPacketForm>` from QuickAddSeed (internal `screen → step` rename; `CheckmarkIcon` helper preserved at file bottom via function hoisting).
  6. `d49e1ae` — Commit E: wire 4 form sub-components into UniversalAddMenu (4 new screen branches); drop 4 `onAdd*` props from menu interface; update 5 pages (home / garden / calendar / journal / vault page) — drop 4 `onAdd*` prop wirings + drop `onBackToMenu={backToMenu}` on 4 standalone-modal mounts. UniversalAddMenu consumes `useUniversalAddModals()` for `openShed` (cross-modal "+ Add New Supply" handoff from menu-internal JournalEntryForm).

  **Two mid-execution amendments (both re-audited clean):**
  1. **Q1 = Option B locked at greenlight** (Syd chose simplicity over "resume menu" wiring). Cross-modal "+ Add New Supply" from menu-internal JournalEntryForm: menu closes + QuickAddSupply opens; menu does NOT auto-resume on journal sub-screen after QuickAddSupply closes. User re-opens menu manually if they want to finish journal entry. Saved ~20 LOC of resume-state wiring.
  2. **AddPlantModal `backToMenu` discovery** — plan §3 caller map missed that all 5 pages mount `<AddPlantModal>` with `onBackToMenu={backToMenu}` (it's reached via menu's "Add plant" → "Manual entry" → openPlant → standalone modal). Deleting `backToMenu` from context (as plan §7 proposed) would have broken AddPlantModal back-to-menu nav. Resolution: KEEP `backToMenu` in context + in each page's destructure. The 4 in-scope modal mounts drop their `onBackToMenu` wiring per plan; AddPlantModal mount keeps it.

  **Two accepted regressions vs prior standalone-modal behavior:**
  - Cross-modal "+ Add New Supply" no auto-resume (Q1 above).
  - Pages don't auto-refresh after in-menu successful add (page-side `onJournalAdded` / `onSuccess` refetch hooks only fire on standalone-modal mount path). User pulls / navigates to see fresh entries. Same "minimize complexity" theme as Q1.

  **Non-FAB callers preserved verbatim** (all keep working through this refactor):
  - OnboardingContext.openTaskForOnboarding (Observe Hillside step 3) → standalone NewTaskModal (uses extracted TaskForm via the shell)
  - calendar `?openTask=1` deep-link → standalone NewTaskModal
  - OnboardingDock onboarding CTA → standalone QuickAddSeed
  - VaultPageContent autoload `openSeed` flows (L344 + L897) → standalone QuickAddSeed
  - journal "+Entry" button (L622) → standalone QuickLogModal
  - QuickLog "+ Add New Supply" cross-modal (all 4 mounting pages) → openShed → standalone QuickAddSupply
  - garden My Plants empty-state CTA (L1048) → openPlant → standalone AddPlantModal
  - vault/[id]/page.tsx Plant Again + similar non-FAB Vault flows → direct mount of QuickAddSupply / QuickLogModal / QuickAddSeed (not via context)
  - vault/shed/[id]/page.tsx, ShedView, VaultShedWing → standalone QuickAddSupply (direct mount)
  - 2nd NewTaskModal at calendar/page.tsx:2066 (edit-task path) — uses custom local `onBackToMenu` callback (not from context); preserved verbatim

  **Why this rule exists in ROADMAP §6:** U7 was a long-standing FAB cohesion friction (~2-3h estimated at capture pre-2026-05-13; ~10h actual including the misfire/revert + Path Y refactor cycle). Records the corrective scope, the misfire+revert, and the rationale for keeping `backToMenu` in context (delete-it-from-context was wrong per plan §7; lock the correction here so future-Claude doesn't re-propose the deletion). Cross-ref BUGS U7 (now ✅), VISION §10 (don't-touch list — preserved verbatim through refactor), ROADMAP §6 2026-05-07 entry (FAB transition standard amended same-commit to reclassify all 5 picks as submenus).

  **3 buckets at close-out:**
  - **(a) Parked items added this chat:** none new — known follow-ups already in §4 or VISION §11.
  - **(b) Deferred audits / known follow-ups:** QuickAddSeed panel missing `animate-modal-slide-up` (Pass 3 ADJACENT, pre-existing cohesion gap captured for future polish pass); `useFocusTrap` inconsistent across modals (NewTaskModal + QuickLogModal use it; QuickAddSeed + QuickAddSupply don't — Pass 3 ADJACENT, pre-existing); auto-refresh callbacks could be added later if accepted regression (b) becomes friction post-ship.
  - **(c) Dogfood-style findings:** none new this chat.

  Plan file at `C:\Users\marsh\.claude\plans\intelligent-pare-f7abd4.md` retained.

- **ROADMAP §6 FAB transition standard amended (same-commit as U7 close).** Per Path Y refactor: all 5 add-button picks now classified as **submenus** (in-place horizontal slide), not just Add Plant. The "target modal" classification (slide-up) now applies only to standalone modal mounts reached via non-FAB callers. See full amendment at the 2026-05-07 entry below (amended in place per CLAUDE.md "lock-correction" pattern — the standard's original locked-2026-05-07 framing is preserved with the 2026-05-20 amendment footnoted inline).

### 2026-05-18

- **§3.12 catalog theme #5 ✅ SHIPPED (`9bad88f`, `trusting-proskuriakova-8e23c2` chat, 2026-05-18).** Empty-state cliff audit + per-surface copy rewrite across the app's 6 EmptyStateCard sites + 14 raw-card / inline `<p>` empty states. **17 code files / ~22 distinct copy strings touched** (~30 edit operations counting collapsed `isNewUser` variants). Resolves §3.12 catalog theme #5 — Sam day-1 risk (cliff-style "No X yet" framing replaced with invitation framing across every surface user lands on day 1) + Walter low-tech-comfort (jargon dropped across body copy: "permanent," "perennial" body, "polyculture," "FAB," "Vault import," "active batches," "Map your hillside," "Plant from the Seed Vault").

  **Per-surface 3-part frame** anchored as the cohesion primitive: purpose-describing title / plain next-step body / verb-led CTA. Every empty state in the app now reads as an invitation, not a cliff.

  **Key autonomous design decisions (per dispatch contract — routine copy phrasing is autonomous; surfaced via Phase 1 plan-relay before greenlight):**
  1. **3-part frame anchor (title / body / verb-led CTA)** — chosen as the cohesion anchor across all 22 distinct copy strings. Picked over alternatives like 2-part (title + body only) or sentence-frame because verb-led CTA standardization is what bottoms out paradox-of-choice for Sam day-1.
  2. **CTA standardization "Add a {noun}"** — chosen over "Add your first {noun}" (excludes mature-state returning users) and "Add {noun}" (less personal/inviting). Walter-tested: works for both first-time + returning. Sam-tested: singular, not paradoxical.
  3. **"Long-living plants"** chosen over "permanent plants" / "perennial plants" as the Walter-plain replacement. "Permanent" reads botanically-wrong (plants die); "perennial" is correct jargon but Walter friction. "Long-living" describes the user-observable property in plain words.
  4. **"Library" softens "Vault"/"Shed" in body copy only** — page-level labels preserved (theme #3 Phase A territory). "Your seed library starts here" reads warmer than "Your Vault is empty." Body-vs-label split is intentional cohesion guard: theme #3 will rename the page labels separately + holistically.
  5. **`isNewUser` branch collapse in V1/V2/S1/G1** — new copy reads correctly for both first-time + returning users, so the per-branch ternary becomes dead complexity. Small code-complexity reduction; behavior preserved; Pass 1 verified `isNewUser` had no other consumers.
  6. **J1 day-rotating prompts preserved unchanged** — the prompt array at [journal/page.tsx:639-650](src/app/journal/page.tsx:639) is already on-voice; replacing it would be over-reach. Only the empty-state title and CTA hint copy changed; the rotating prompts kept intact.

  **Boundaries respected (Pass 4 lock-hygiene):**
  - **VISION §10 empty-by-default LOCK preserved verbatim.** This ship refines COPY ON existing empty states; it does NOT add notifications, tasks, or state-by-default. OnboardingDock untouched. The Sam day-1 fix is copy-only — the principle that empty IS the onboarding stays locked.
  - **§3.12 theme #2 (outdoor-garden assumption sweep)** — light touch only. "Fruit trees, bushes" → "trees, bushes, or anything else you keep year after year" generalizes outdoor framing; full outdoor sweep (page-level renames "Active Garden," "Plantable," beds-as-default) remains theme #2 work.
  - **§3.12 theme #3 (plain-language audit)** — body-level only. Walter jargon dropped in body copy across all surfaces; page-label renames ("Vault," "Shed," "Plantable") remain theme #3 Phase A. Body-vs-label split prevents pre-empting theme #3's holistic rename map.
  - **§3.12 theme #7 (cross-view consistency)** — primitive-canonicalization on raw-card empty states captured as ADJACENT and parked to §4. 4 surfaces use `bg-white rounded-xl border ... p-8 text-center` raw-card shape instead of canonical `EmptyStateCard` primitive. Copy was updated on these surfaces; primitive swap is structural cohesion work belonging to theme #7's design pass.

  **3 buckets at close-out:**
  - **(a) Parked items added this chat:** TWO new §4 entries — (1) `ux-fab-menu-add-entry-friction` (user signal: friction with add-entry flows accessed from the FAB menu once an option is tapped; distinct from theme #6 which addresses the menu shell; cross-ref parallel chat `admiring-carson-81435d` FAB audit's F1-F21 friction items + 11-bug + 25-question consolidated review pending decisions); (2) raw-card empty-state primitive canonicalization (§3.12 theme #7 ADJACENT; 4 surfaces use raw-card shape instead of canonical `EmptyStateCard`; primitive swap deferred to theme #7 design pass).
  - **(b) Deferred audits / known follow-ups:** §3.12 themes #2 / #3 / #6 / #7 / #8 / #10 remain queued per ship-order T4 → #5 → #3 Phase A → #3 Phase B + #8 → #2 + #6 (theme #3 Phase A = next chat ready); theme #4 + #5 awaiting phone verification on prod; FAB-menu friction parked item awaiting user "ready" signal; primitive-canonicalization parked item awaiting theme #7 design pass.
  - **(c) Dogfood-style findings:** `ux-fab-menu-add-entry-friction` user-signal-captured this chat (verbatim quote logged). Status: **file as parked** — landed in §4 as `ux-fab-menu-add-entry-friction` with cross-ref to `admiring-carson-81435d` audit's existing 21 items. No other dogfood findings this chat.

  **Concurrent-chat fetch (Phase 2 gate item 9) validation #7:** clean — 0 ahead, 0 behind at plan-lock time on the §3.12 #5 ship. Validates the rule continues working across both build chats (this) + doc-recovery chats (the replay below).

  Plan file at `C:\Users\marsh\.claude\plans\trusting-proskuriakova-8e23c2.md` (26 KB intact through the disk crisis). 4-pass plan-audit terminated clean at iteration 2 on Pass 2 (Pass 1 + 3 + 4 iter 1; Pass 2 iter 1 surfaced 7 concerns A-G all resolved inline; iter 2 re-passed clean on revised plan).

- **Doc-trail recovery — §3.12 theme #5 ROADMAP replay (`naughty-kalam-54efba` worktree, 2026-05-18).** On 2026-05-18 the original `trusting-proskuriakova-8e23c2` chat experienced an ENOSPC condition during the Phase 5 ROADMAP write that truncated its local `docs/ROADMAP.md` to 0 bytes. Code commit `9bad88f` (the §3.12 theme #5 ship itself) had already landed on origin/main and is unaffected; only the doc-trail edits queued for the same chat's close-out were lost in-worktree. The intact 26 KB plan file at `C:\Users\marsh\.claude\plans\trusting-proskuriakova-8e23c2.md` was used as source of truth for the replay.

  Recovery shape: spawn a fresh worktree (`naughty-kalam-54efba`) with a clean `docs/ROADMAP.md` checkout from origin/main (245 KB intact), execute the mechanical replay checklist (§1 lead-line refresh + Just-shipped block + Next session focus + §3.12 catalog markers + §4 two parked bullets + §5 newest entry + §6 dated block + §7 cap housekeeping), commit doc-only, push, then remove the corrupted §3.12 #5 worktree (its zero-byte ROADMAP + any uncommitted state is unrecoverable).

  **Why captured in §6:** records the disk-crisis context + recovery approach for future reference (if a similar truncation recurs, the playbook is logged here). Reinforces the existing CLAUDE.md "Concurrent-chat fetch" rule (Phase 2 gate item 9) which catches the cross-chat collision shape; this captures a different failure shape (disk-exhaustion mid-write) and the recovery pattern (separate worktree replay from plan file).

### 2026-05-17

- **§3.12-tester T4 ✅ SHIPPED + §3.12-tester chunk CLOSED (`bc253be`, `mystifying-lichterman-55e122` chat, 2026-05-17).** Admin feedback inbox at `/settings/developer/feedback-inbox` un-parked at user greenlight + shipped same-chat. Closes the T1+T2 loop (`e50a667` data → in-app admin viewer instead of Supabase Dashboard SQL Editor). Operating mode: AUTO MODE (dispatch from phone; user explicitly delegated routine implementation calls; only escalates on vision-alignment / persona-fit / scope shape).

  **Q1 security model = Option B (security-definer Postgres function).** User locked at Phase 1 greenlight before audit started. Rationale: keeps gate server-side, doesn't depend on BUGS U27 server-side `/api/developer/*` gating (which is unrelated and still parked), consistent with Supabase-RPC patterns already used in repo. Alternatives considered + rejected: (A) admin RLS policy keyed on hardcoded UUID list — simpler but less extensible; (C) service-role API route — blocked by U27. Function shape adopted from canonical pattern at [20250305000000_households_invite_code_lookup_rpc.sql](supabase/migrations/20250305000000_households_invite_code_lookup_rpc.sql) (STABLE + SET search_path = '' + GRANT EXECUTE TO authenticated).

  **`developer_users` allowlist table — new primitive in repo.** Chosen autonomously per AUTO MODE over: hardcoded UUID list in function body, `app_metadata.is_developer` Supabase-native flag, env-var allowlist. Reasoning: extensible (add devs later via INSERT), portable (no auth.users metadata coupling), RLS-locked (no policies → only the security-definer function reads it as table owner), self-documenting, **future hook for BUGS U27** (the new server-side gate for `/api/developer/*` routes can reuse `developer_users` instead of building its own allowlist). One-time setup post-deploy: user runs `INSERT INTO public.developer_users (user_id) VALUES (auth.uid())` in Dashboard SQL Editor while signed in.

  **Pass 2 revision applied mid-audit.** Initial plan drafted function as `LANGUAGE sql STABLE` returning empty rowset when caller not in `developer_users`. Pass 2 surfaced concern: empty result set could mean either (a) legitimately empty user_feedback table OR (b) caller not authorized — page can't distinguish. Revised to `LANGUAGE plpgsql STABLE` with `RAISE EXCEPTION 'Not authorized' USING ERRCODE = '42501'`. Page now distinguishes cleanly: 42501 → "Not authorized" guidance card with paste-able INSERT; empty rows → "No feedback submitted yet." Re-passed Pass 2/3/4 clean on revised plan.

  **Page architecture (autonomous decisions per dispatch brief):**
  - Visible record/stop / refresh / filter / row primitives all anchored on existing patterns ([scraper-audit/page.tsx:68](src/app/admin/scraper-audit/page.tsx:68) gate copy + Go to Settings link verbatim; [/settings/feedback/page.tsx:66](src/app/settings/feedback/page.tsx:66) row primitive verbatim; [FeedbackModal.tsx](src/components/FeedbackModal.tsx) `<audio controls>` shape from T1 ship; SettingsSuccessSoundToggle shell from T2 ship; filter `<select>` shape from FeedbackModal category dropdown).
  - **200-row server-side cap** (no client pagination; revisit if she hits the cap). Footer note: "Showing N of M row(s) (latest 200 fetched, newest first)."
  - **Public URLs not signed URLs** — `journal-photos` bucket is public per [20250204100000_big_shift_schema.sql:40+48](supabase/migrations/20250204100000_big_shift_schema.sql:40); matches 10 existing call sites of `supabase.storage.from("journal-photos").getPublicUrl(path).data.publicUrl`.
  - **No new icon in ICON_MAP** — Settings menu entries are text-only across all DEV_ITEMS and USER_ITEMS.
  - **`<audio controls preload="none">`** — avoids hammering bandwidth when admin scrolls a list with many voice memos.
  - **Debug log collapsed per-row by default** — toggle-to-expand. Otherwise a list with many debug logs would overwhelm the page.
  - **Filter UI = category dropdown + has-voice toggle + has-debug-log toggle** — bare minimum. No date-range filter, no per-user filter (can add later if needed).
  - **NO mark-as-read / status workflow** — explicitly scoped out; logged as ADJACENT for future enhancement.

  **Pass 3 vocabulary-breadth sibling sweep (rule locked 2026-05-17 in `flamboyant-wilson`).** 8 vocab families grepped:
  - **admin/dev gating:** `useDeveloperUnlock` (13 files, all dev-tools; sole pattern) — BLOCKING aligned.
  - **security-definer functions:** `SECURITY DEFINER` (13 migrations) — shape adopted verbatim, BLOCKING aligned.
  - **dev-user allowlist:** searched `developer_users|dev_users|admin_users|developer_user_ids|admin_user_ids` — none exist. Genuinely new primitive; no siblings to align to or violate.
  - **RPC call patterns:** `supabase.rpc(` (9 sites) — `.rpc(name, params) → { data, error }` destructure shape adopted, BLOCKING aligned.
  - **storage public URL:** `getPublicUrl` (10 sites) — `supabase.storage.from("journal-photos").getPublicUrl(path).data.publicUrl` adopted verbatim, BLOCKING aligned.
  - **`<audio>` usage:** 1 sibling (FeedbackModal T1 ship) — native HTML `<audio controls>` with public URL `src`, BLOCKING aligned.
  - **debug log display:** `debugLogBuffer` consumers (FeedbackModal T2 + `/settings/developer/debug-log`) read LIVE buffer; T4 reads PERSISTED `debug_log_text` column — vocabulary-distinct. ADJACENT logged (one is live-buffer, other is persisted-text; no cohesion violation, just naming the distinction).
  - **list-with-filter dev pages:** scraper-audit (table) vs `/settings/feedback` (cards). T4 picked cards (better match to feedback-row content shape than table). CONCERN logged (two co-existing list shapes in dev pages); not BLOCKING this ship; future dev-page-cohesion pass should unify.

  **3 ADJACENT findings (logged, not in scope this ship):**
  1. Two competing dev-page list shapes (table at scraper-audit vs cards at /settings/feedback + this ship). Future cohesion pass.
  2. `developer_users` table is a primitive that BUGS U27 server-side `/api/developer/*` gating can reuse (saves rebuilding the gate).
  3. No "mark feedback as addressed / read" workflow yet. Future enhancement.

  **Pass 4 lock hygiene clean:** VISION §10 no-touch (dev surface, no default-state change); §11 no parked-decision brush (modal-placement audit doesn't apply — this is a full-page Settings sub-page, not modal/sheet; icon-density question doesn't apply — no icon); §4 Principle 3 disclosure-by-data aligned (admin tools hidden until unlocked); ROADMAP §6 §3.12-pre `useDeveloperUnlock` pattern aligned; §3.12-tester T1+T2 columns consumed as designed; BUGS U27 untouched (uses Supabase RPC not API route); migration timestamp `20260518000000` unique (verified via `ls`). CLAUDE.md Push tiers: additive SQL (`CREATE TABLE IF NOT EXISTS` + `CREATE OR REPLACE FUNCTION`) qualifies for auto-push tier — but worktree's supabase CLI not linked so user applies via Dashboard SQL Editor instead.

  **Persona walk (Pass 2 subcategory, all 5):** dev-only surface gated by `useDeveloperUnlock`. Maya / Sydney / Walter / Aria / Sam all pass by exclusion (never tap version 7 times; never see the surface; never confused). User (developer-persona) sees full surface with refresh + filter + audio playback + screenshot rendering + debug log toggle.

  **Test coverage:** 7 new tests in `page.test.tsx`: (1) locked → unlock prompt + Go to Settings link, (2) no user → sign-in gate, (3) RPC 42501 → "Not authorized" guidance + paste-able INSERT, (4) empty rows → "No feedback submitted yet." copy, (5) full row rendering with category badge + email + message + page_url + screenshot src public URL + voice memo audio + debug log toggle, (6) filter behavior (category + voice-only + debug-log-only), (7) generic error rendering for non-42501 RPC failures. Plus existing settings/page.test.tsx amended with a 2nd "Feedback inbox" link assertion in the unlocked test. Total 432 → 439 (+7).

  **5 files, +533/-1.** Lint clean (no new warnings on new files). Build clean (`/settings/developer/feedback-inbox` = 3.54 kB static). Preview MCP skipped per documented `.env.local`-absent worktree condition + doubly auth-blocked beyond standard skip rationale (`useDeveloperUnlock` gate + needs applied migration + needs `developer_users` INSERT). User phone-verifies on prod after Vercel auto-deploy + Dashboard SQL Editor migration apply + one-time INSERT.

  **Concurrent-chat fetch (Phase 2 gate item 9) validation #6:** clean (0 ahead, 0 behind at commit time). 4-pass plan-audit terminated clean at iteration 1 on as-written-now plan (Pass 2 revision applied during audit; revised plan re-passed clean). Plan file at `C:\Users\marsh\.claude\plans\mystifying-lichterman-55e122.md`.

  **3 buckets close-out:**
  - **(a) Parked items added this chat:** none — T4 was un-parked + shipped same chat.
  - **(b) Deferred audits / known follow-ups:**
    - **Prod migration apply** — user pastes `20260518000000_admin_feedback_inbox.sql` content in Supabase Dashboard SQL Editor (standard pattern). Same time as the T1+T2 `20260517120000` migration if not already applied.
    - **One-time `developer_users` INSERT** — user runs `INSERT INTO public.developer_users (user_id) VALUES (auth.uid())` once in Dashboard SQL Editor while signed in.
    - **Phone verification on prod** — open `/settings/developer/feedback-inbox` post-deploy + post-INSERT; should render rows with filter UI + audio playback + screenshot + debug log toggle working.
    - **§3.12 catalog themes #5 → #3 → #2/#6 → #8** remaining per ship-order recommendation locked this chat. Next chat = #5 (empty-state cliff audit).
    - **BUGS U27** server-side `/api/developer/*` gating — separate security-hardening ship; `developer_users` table is the recommended reuse point.
  - **(c) Dogfood-style findings:**
    - **AUTO MODE dispatch from phone worked clean end-to-end.** Phase 1 → Phase 5 in one chat with zero AskUserQuestion prompts. User greenlit recommendation + assumptions in single plain-text relay; all routine implementation calls absorbed by Claude. Status: **just-captured** — pattern validated for Phase 1 audit + Phase 2 plan-audit + Phase 3 execute + Phase 4 verify + Phase 5 close. If durable mode, future doc-hygiene chat could codify in CLAUDE.md push-tiers + role-lock.
    - **§3.12 ship-order locked at chat-open** — T4 → #5 → #3 Phase A → #3 Phase B + #8 → #2 + #6. Five chats, no mega-bundle. Status: **just-captured** — captured to §1 Next session focus + this entry; subsequent chats follow this sequence unless user redirects.
    - **`developer_users` as a reusable allowlist primitive** — first installation. Likely to be reused by BUGS U27 + any future admin-tier feature. Status: **just-captured** — name surfaced in BUGS U27 entry for future reuse.
    - **Two-list-shape concern in dev pages** (table at scraper-audit, cards at /settings/feedback + T4). Status: **just-captured** — not a rule yet; if a 3rd dev-page list ships with yet another shape, promote to a dev-page-cohesion design pass.
    - **Preview MCP "doubly auth-blocked" condition documented in commit body** — extends standard `.env.local`-absent skip rationale with the page-level gate condition. Status: **just-captured** — first dev-only page where gate is multiple layers; if this pattern recurs, codify in CLAUDE.md Preview MCP section.

- **§3.12-tester T1 + T2 ✅ SHIPPED bundled (`e50a667`, `vigorous-nightingale-039e05` chat, 2026-05-17).** Two tester-tooling items shipped in one ~5-file diff. **T1 = voice memo recorder on FeedbackModal.** New `useVoiceRecorder` hook ([src/hooks/useVoiceRecorder.ts](src/hooks/useVoiceRecorder.ts), +189 lines): MediaRecorder MIME fallback chain (`audio/webm;codecs=opus` → `audio/mp4;codecs=mp4a.40.2` → `audio/webm` → `audio/mp4` → `""` default) for Chrome/Android + iOS Safari coverage; 60s auto-stop via setInterval; state machine `idle / permission-denied / recording / recorded`; stream cleanup on unmount anchored on [useDesktopPhotoCapture.ts:23-35](src/hooks/useDesktopPhotoCapture.ts:23) pattern. UI: visible Record button (dashed-border shell anchored on existing "Attach screenshot"), Stop button during recording (emerald solid + red pulse dot — Tailwind `bg-red-500` is a native utility, not a new design token), native `<audio controls>` playback + Re-record button when `recorded` (always visible per T3 Walter-safe lock). Plain-language permission-denied recovery copy: "Microphone access denied. You can still type your feedback or attach a screenshot." New `Mic` entry in `ICON_MAP` ([styleDictionary.tsx](src/lib/styleDictionary.tsx)) using `iconProps` defaults (stroke 1.2, PRIMARY_GREEN, 24×24). Voice blob uploads to existing `journal-photos` bucket at `{user_id}/feedback-voice-{uuid}.{webm|m4a}` — RLS already path-prefix-only via [20250225000000_user_feedback_storage_rls.sql](supabase/migrations/20250225000000_user_feedback_storage_rls.sql), no new policy needed. New `voice_path text` column on `user_feedback`.

  **T2 = debug-log opt-in attach on feedback submit.** Opt-in checkbox in FeedbackModal — verbatim [SettingsSuccessSoundToggle.tsx](src/components/SettingsSuccessSoundToggle.tsx) peer-sr-only + emerald-500 track shell — defaults OFF (state `useState(false)`, NOT localStorage: ephemeral per-session privacy default; user actively consents each submission). When ON: scrollable read-only textarea renders `formatEntriesForCopy(getEntries())` from existing [debugLogBuffer.ts](src/lib/debugLogBuffer.ts) infrastructure (no buffer changes; `DebugLogInit` already mounted unconditionally in [layout.tsx:51](src/app/layout.tsx:51) so the buffer captures for every user). Empty-log case: "(No console messages captured yet.)" placeholder. Plain-language disclosure copy: "Attaches recent technical messages from your browser. Helps us figure out what went wrong. You can review the text below before sending." On submit: text stored inline in new `debug_log_text text` column on `user_feedback` (~12.5 KB max per MAX_ENTRIES × char-budget; pgsql text column handles fine). Console-content sensitivity sweep clean: no `password|token|api_key|secret` in `debugLogBuffer.ts` source; captured log content = partial user IDs + error stacks + Supabase error codes, no credentials.

  **Migration (`20260517120000_user_feedback_voice_and_debug_log.sql`):**
  ```sql
  ALTER TABLE public.user_feedback
    ADD COLUMN IF NOT EXISTS voice_path text,
    ADD COLUMN IF NOT EXISTS debug_log_text text;
  ```
  User applies on prod via Supabase Dashboard SQL Editor (standard pattern from U24 close-out + `user_settings.onboarding_completed_at` fixes).

  **T3 locks honored verbatim:**
  - Feedback button stays public on all 3 sites — zero gating code in this ship.
  - T1 voice: visible Record/Stop, 60s cap (within 30-60 range), plain-language mic-denial copy, Re-record always visible, no auto-start.
  - T2 debug-log: default OFF, opt-in toggle, content preview visible before submit, plain-language disclosure.

  **Cohesion-by-aggregation table** (10 decisions, 8 anchored to cited paths, 2 new patterns documented):
  - Hook ↔ `useDesktopPhotoCapture` stream-cleanup pattern (BLOCKING — matched).
  - Storage upload signature ↔ 4 sibling sites ([HarvestModal:90](src/components/HarvestModal.tsx:90), [garden/page.tsx:398+441](src/app/garden/page.tsx:398), [BatchLogSheet:243](src/components/BatchLogSheet.tsx:243)) (BLOCKING — matched).
  - Toggle shell ↔ SettingsSuccessSoundToggle (BLOCKING — matched verbatim).
  - Mic icon ↔ ICON_MAP via styleDictionary `iconProps` defaults (BLOCKING — matched; not inline SVG).
  - Preview reuses `formatEntriesForCopy` from existing buffer (BLOCKING — matched).
  - Toggle state local to modal NOT localStorage — privacy-conscious per-session default (BLOCKING — matched).
  - Record-button idle shell anchored on existing "Attach screenshot" dashed-border button (anchored cite-by-path).
  - Recording state container = emerald-500 border + emerald-50 bg (anchored on emerald-as-active semantic per styleDictionary.tsx:3 lock).
  - **NEW PATTERN 1:** MediaRecorder MIME fallback ordering (greenfield — first audio feature in app). Documented in hook JSDoc + this entry.
  - **NEW PATTERN 2:** Privacy-disclosure copy frame ("Include X info" + "Attaches Y. Helps us Z. You can review N before sending."). Reuse template for any future opt-in (e.g. location-sharing, photo-EXIF inclusion).

  **Pass 3 vocabulary-breadth sibling sweep (rule locked 2026-05-17 in `flamboyant-wilson`):** 8 vocab families grepped (audio/media-recording, blob-upload, toggle/preference, console-capture, sensitivity, access-gate, camera-stream-cleanup, ICON_MAP). 6 BLOCKING alignments captured + matched in code. 3 ADJACENT logged (voice memo copy frame as future-sibling anchor; privacy-disclosure copy frame as future-sibling anchor; MIME fallback may evolve as iOS Safari adds Opus). 0 CONCERN. Persona-mismatch sub-check clean. **Validation #3 of the vocabulary-breadth rule in this chat alone** (sweeps fired correctly across all 4 audit passes); rule paying its keep on a 2-day cadence.

  **Pass 4 lock hygiene clean:** VISION §10 no-touch, §11 modal-placement audit not unblocked / not blocked (modal shell layout unchanged; only body content added), §4 Principle 9 aligned (visible buttons at all viewports; no swipe gestures introduced), §3.12 #4 visible-button-fallback aligned, §3.12-pre dev-gating non-conflicting (debug-log buffer mounted unconditionally; T2 reads it for ALL users — correct per T3), migration-naming guard followed (unique 14-digit timestamp `20260517120000`).

  **Persona walk (all 5 pass both features):**
  - **Maya:** ✅ neutral-positive on T1 (option for detailed bug reports); ✅ positive on T2 (will toggle ON when reporting weird bugs).
  - **Sydney:** ✅ positive on T1 (voice describes multi-step bugs faster than typing); ✅ positive on T2 (clear copy + plain-language disclosure).
  - **Walter:** ✅ **MAJOR positive on T1** — voice > typing for retiree fingers + iPad keyboard; visible Record/Stop is critical and locked. ⚠️ neutral on T2 — won't toggle on his own; default OFF protects him; plain-language copy ("technical messages") means he understands if he tries it.
  - **Aria:** ✅ positive on T1 (iOS Safari MediaRecorder ≥14.3 covered via audio/mp4 fallback); ✅ positive on T2 (privacy-aware; default OFF + visible preview).
  - **Sam:** ✅ neutral on T1 (won't use day-1; graceful "Record voice memo" alongside "Attach screenshot"); ⚠️ neutral on T2 (won't toggle; default OFF protects).

  **Auto-mode override flagged once + accepted for this session.** User shifted mid-chat to "AUTO MODE — make autonomous calls on routine technical decisions" (autonomy for routine; escalate only on vision-alignment / persona-fit); then clarified "code push still needs explicit greenlight." Override does not change CLAUDE.md durably — captured here in §6 with the trade-off noted; if durable change desired, doc-hygiene chat amends CLAUDE.md push-tiers + role-lock sections. Trade-off accepted by user: faster execution on routine picks vs. less in-chat dialogue per decision; she'll dogfood UI/UX on prod and surface alignment feedback as she uses the app.

  **5 aesthetic / variant calls locked autonomously within T3 guardrails** (presented in one plain-text relay, user greenlit all 5 in one message):
  1. Duration cap: **60s** (max of locked 30-60 range; ~180KB Opus file; gives Walter time).
  2. Voice section position: **between screenshot and page-url footnote** (parallels screenshot as second optional attach).
  3. Toggle position: **directly above page-url footnote** (body grouped: content → diagnostics → metadata).
  4. Toggle label: **"Include debug info"** (clear, short; matches Apple Mail / Gmail patterns).
  5. Storage bucket: **reuse `journal-photos`** (RLS already path-prefix-only, no MIME restriction; bucket name is internal).

  **5 files, +548/-3.** Tests 405 → 432 (+27 regression). Lint clean (no new warnings from the 5 new/edited files). Build clean. Preview MCP skipped per documented `.env.local`-absent worktree condition; user phone-verifies on prod. **Concurrent-chat fetch (Phase 2 gate item 9) validation #5:** clean (0 ahead, 0 behind at commit time). 4-pass plan-audit terminated clean at iteration 1 on as-written plan. Plan file at `C:\Users\marsh\.claude\plans\vigorous-nightingale-039e05.md`.

  **3 buckets close-out:**
  - **(a) Parked items added this chat:** none new. T4 (admin inbox) already 🟣 parked from `gifted-wright-4184cb` chat — unchanged.
  - **(b) Deferred audits / known follow-ups:**
    - **Prod migration apply** — user pastes ALTER TABLE on Supabase Dashboard SQL Editor (standard pattern).
    - **Phone verification on prod** — 8-step path captured in commit body covers idle / record / stop / playback / re-record / toggle / preview / send / Supabase row check / permission-denial / iOS MIME fallback. Pending.
    - **Auto-mode push-tier amendment in CLAUDE.md** — user shifted mid-chat; if durable, future doc-hygiene chat amends CLAUDE.md push-tiers + role-lock.
    - **§3.12 catalog themes #2 / #3 / #5 / #6 / #8** still queued per `binary-valley` close-out.
    - **BUGS U27** server-side `/api/developer/*` gating — separate security-hardening ship.
  - **(c) Dogfood-style findings:**
    - **Privacy-disclosure copy frame established as new app-wide pattern.** "Include X info / Attaches Y. Helps us Z. You can review N before sending." Status: **just-captured** — first instance in app; watch for second instance to consider promoting to a CLAUDE.md or VISION.md design-system rule.
    - **MediaRecorder MIME fallback ordering established as new app pattern.** First audio feature in app; documented in hook JSDoc. Status: **just-captured** — may evolve as iOS Safari adds Opus support in future iOS versions; for now codebase has its anchor.
    - **Auto-mode + plan-mode + Rule A interplay surfaced.** User can shift autonomy mid-chat; Claude's flagged-once-then-respect pattern works without CLAUDE.md amendment for one-session overrides. Status: **just-captured** — pattern confirms 2026-05-17 `flamboyant-wilson` dogfood finding from same observation class.
    - **Pass 3 vocabulary-breadth rule validation #3 in 3 chats over 2 days.** Promotion threshold (second occurrence-of-a-class) already passed; rule was promoted to CLAUDE.md by `flamboyant-wilson` close. This chat validates the rule firing correctly on a greenfield-audio feature (catches absence of peers as a positive signal). Status: **just-captured** — no rule change; further validation accumulating.

- **§3.12-tester T3 ✅ LOCKED (Option A — public stays as-is). Doc-only ship (`flamboyant-lamport-f5cc74` chat, 2026-05-17).** Three options surfaced (A public / B existing tap-unlock / C new tester allowlist); user picked Recommended Option A via Rule A. ZERO code change — feedback button stays public on all 3 sites ([AuthGuard.tsx:195](src/components/AuthGuard.tsx:195) loading-shell mobile top bar + [AuthGuard.tsx:297](src/components/AuthGuard.tsx:297) post-auth mobile top bar + [Sidebar.tsx:162](src/components/Sidebar.tsx:162) desktop sidebar footer). Pre-sign-in auth page already shows zero feedback buttons (all 3 sites wrapped in `{!isAuthPage}`).

  **Decision rationale:** app-store distribution locked 2026-05-17 (VISION §1, §3) inverts the calculus pre-app-store. Pre-app-store, "tester-only feedback" made sense — user + sister were the only testers. Post-app-store-locked, public users are exactly who we want feedback from on launch; gating cuts off that channel. Feedback button ≠ developer tool — §3.12-pre's `4a8b04c` resolved destructive admin actions visible to fresh users (fill-in-blanks, extract cache, import logs); feedback is the opposite: user-to-developer communication, common public-app pattern. Apps that hide feedback behind hidden gestures look like they're avoiding accountability — bad signal at launch.

  **Persona walk (Pass 2 subcategory, all 5 personas):**
  - **Maya** (power, bug-reporter type): ✅ Option A — sends bug reports normally. ❌ B/C — bug-reporter type silenced (won't find 7-tap gesture; not on allowlist).
  - **Sydney** (spring-planner, moderate investment): ✅ Option A — visible feedback button is normal app pattern. ❌ B/C — same as Maya.
  - **Walter** (retiree, low-medium tech): ⚠️ Option A — one more icon to ignore (not blocking; sits in already-cluttered top bar). ❌ B/C — loses channel entirely, won't unlock; though he wouldn't have written bug reports anyway.
  - **Aria** (houseplant urban, high tech): ✅ Option A — standard iOS app pattern. ❌ B/C — same as Sydney.
  - **Sam** (day-1 beginner): ⚠️ Option A — wrench icon vague on day-1 but not breaking; common app pattern. ⚠️ B/C — clean day-1 but never finds it later. **Net: 3/5 neutral-to-positive on A; 5/5 lose on B/C.**

  **Pass 3 vocabulary-breadth sibling-sweep (rule locked 2026-05-17 in `flamboyant-wilson`):** searched Gate / Guard / Unlock / Allow / Allowlist / Whitelist / Permission / Role / isAdmin / isDev / isUnlocked / tapVersion / hasAccess / canAccess / tester / beta / invite-code vocabularies across `src/`. Tightened from 113-file broad sweep to 9 actual access-control consumers — all `useDeveloperUnlock`-based. Tester / beta / allowlist concept does NOT exist in repo (matches for those terms = unrelated false positives: `Beta vulgaris` Latin name, `invite_code` household-sharing flow, image-proxy URL allowlist). **`useDeveloperUnlock` is the SOLE access-gate pattern.** Option B would reuse it cleanly (1 sibling, like §3.12-pre). Option C would invent a new gating mechanism with zero peers.

  **ADJACENT finding (CONCERN-not-BLOCKING):** BUGS U27 (`/api/developer/*` server-side gating) is unrelated to feedback flow — feedback writes via Supabase RLS-protected `user_feedback` table, NOT via an API route. U27 stays parked as a separate security-hardening ship. **CONCERN finding:** none.

  **Cohesion-by-aggregation check:** Option A introduces ZERO new pattern (0 code change to gating). Option B would have wrapped 3 sites with `isUnlocked` checks reusing existing pattern (clean — 1 sibling). Option C would have introduced new vocabulary the codebase has no precedent for.

  **Downstream T1/T2 public-context constraints LOCKED in §3.12-tester:**
  - **T1 voice recorder:** must stay Walter/Sam-friendly — visible record/stop button (no swipe, no long-press), 30-60s max duration cap, plain-language mic permission, retake button always visible, no auto-start on modal open. Remaining aesthetic picks (button location, exact duration, state vocabulary) defer to T1 planning chat.
  - **T2 debug log auto-attach:** must default OFF, opt-in toggle, content preview visible before submit, plain-language disclosure of what's captured. Public-app privacy-disclosure pattern (no longer a tester-internal feature). Remaining picks (toggle location, preview shape, label copy) defer to T2 planning chat.

  **Aesthetic flags addressed:** Option A means no rename ("Send feedback" stays); icon stays `FeedbackIcon`; no menu re-anchor; no copy change.

  **Concurrent-chat collision caught + reconciled via CLAUDE.md Phase 2 readiness gate item 9** (locked 2026-05-17): pre-edit `git fetch origin main` found 2 commits (`efe588d` §3.12 theme #4 ship + `42ed0b8` ROADMAP hash backfill) had landed on origin/main during my audit. Fast-forward rebase clean since no local edits made yet. Re-audited on rebased state — `distracted-wilson` ship was on different surface (calendar swipe buttons), no substantive overlap with T3. Pass 1+2+3 findings still hold. **Validation #4 of the Phase-2-gate-item-9 rule running clean** (precedent: `flamboyant-wilson` 2026-05-17 founding case → `distracted-wilson` 2026-05-17 validation #3 → this chat validation #4). Rule paying its keep on a 2-day cadence.

  **3 buckets close-out:**
  - **(a) Parked items added this chat:** none. T4 (admin inbox) was already 🟣 parked from prior `gifted-wright-4184cb` chat — unchanged.
  - **(b) Deferred audits / known follow-ups:**
    - **T1 (voice recorder)** ready to plan with public-context constraints locked. Effort S-M.
    - **T2 (debug-log auto-attach)** ready to plan with public-context constraints locked. Effort XS-S. Could bundle with T1.
    - **§3.12 catalog themes #2/#6/#3/#8/#5** still queued per `binary-valley` close-out.
    - **BUGS U27** (`/api/developer/*` server-side gating) still parked — separate security-hardening ship.
  - **(c) Dogfood findings:**
    - **Pass 3 vocabulary-breadth rule validation #2 of an actual audit.** Rule locked 2026-05-17 in `flamboyant-wilson`; validated once in `distracted-wilson` (gesture vocabulary). This chat validated the rule on access-control vocabulary (Gate / Guard / Unlock / Role / Allowlist / etc.). Rule fired correctly: broad sweep returned 113 files (false-positive heavy); tightened to actual consumers showed `useDeveloperUnlock` is the sole pattern. Status: **just-captured** — rule working as designed; no further action; promote-to-permanent threshold (2 occurrences-of-class) approaching but not yet hit, given the rule itself was just promoted to CLAUDE.md from memory yesterday in `flamboyant-wilson`.
    - **Phase-2-gate-item-9 rule validation #4 in 2 days.** Concurrent-chat collision caught + cleanly reconciled. Pattern: every multi-chat day (2026-05-16 + 2026-05-17) is producing collisions; the rule catches them all. Status: **just-captured** — no rule change needed; doc evidence accumulating.
    - **App-store-distribution context reframes earlier captures.** T3 was originally framed as a "tester-only design decision." App-store lock (2026-05-17) reframes it as "public user channel" — exact same code shape, different scoping. This is the second time (after §3.12 catalog 10 themes) that app-store-distribution context flipped a previously-framed-as-tester-feature into a public-user feature. Status: **just-captured** — pattern worth watching; if a third instance surfaces, may warrant a rule (e.g. "any feature framed as tester-only since 2026-05-08 needs re-triage under app-store-distribution context").

- **§3.12 theme #4 visible-button fallback for swipe-only gestures ✅ SHIPPED (`distracted-wilson-616865` chat, 2026-05-17).** Code commit `efe588d` → main; Vercel auto-deploys. Resolves persona-audit P0 theme #4 of 10. 4 files (calendar/page.tsx + regression test + VISION.md + ROADMAP.md), ~10 line-edits in code + ~10 test-assertion edits + 2 doc edits + ROADMAP entry. Tests 405/405. Build clean. Preview MCP visual check at 412×915 + 768×1024 pending pre-push (this commit is a visual ship; mobile-viewport sanity check is the verification gate per CLAUDE.md "Pre-push visual verification").

  **Implementation = lower button-fallback breakpoint on 3 calendar surfaces from `lg:`/`xl:` to `md:` (iPad-portrait+, ≥768px) to align with existing Vault/Shed `md:` anchor.** Code edits:
  - Month-nav arrows ([calendar/page.tsx:976+985](src/app/calendar/page.tsx:976)): `hidden xl:flex` → `hidden md:flex` on both `←` and `→` buttons. Supersedes `3de1c2c`'s xl:1280 breakpoint (which served only iPad Pro 12.9 landscape and larger). Now serves iPad portrait+ for Walter persona.
  - Singleton task row buttons wrapper ([calendar/page.tsx:2442](src/app/calendar/page.tsx:2442)): `hidden lg:flex` → `hidden md:flex` on the `<span>` containing Snooze + Complete. Implements `cea21e0` shape at iPad-portrait+ instead of desktop-only.
  - Consolidated overdue Snooze-all + Complete-all ([calendar/page.tsx:2242+2253+2254](src/app/calendar/page.tsx:2242)): 3 className occurrences of `hidden lg:flex` → `hidden md:flex` (including the `firstSow ? ... : ...` conditional both branches). Implements `f77507a` shape at iPad-portrait+.
  - 2 in-code comments updated: ConsolidatedOverdueHeader docstring at line 2151-2155 + CalendarTaskRow swipe comment at line 2357-2361 — both now reference `md:` + the persona-accessibility narrowing rationale (`Walter persona served on iPad-portrait+`).
  - `useRowSwipe` hook + handler logic NOT modified — only button visibility changes. Swipe still fires identically on phone-portrait.

  **Test updates ([calendarBatchSelect.regression.test.ts](src/app/calendar/calendarBatchSelect.regression.test.ts)):** 4 assertion strings flipped `lg:flex` → `md:flex` (singleton row assertion line 354; consolidated header regex + variable name at line 422 `hiddenLgCount` → `hiddenMdCount`; chevron negative-check at line 434) + 2 describe/comment blocks reworded for accuracy. Net assertion count unchanged.

  **Pass 4 lock-hygiene caught 3 brushed locked decisions, all re-greenlit via Rule A ask:**
  1. **VISION §4 Operating Principle 9** (locked 2026-05-08): *"row quick actions = swipe on mobile, inline buttons on desktop."* Narrowed 2026-05-17 to: *"swipe on phone-portrait, inline buttons on iPad-portrait+ AND desktop alongside swipe."* The Principle's spirit (mobile-first with desktop responsiveness built same-commit) is preserved; the worked example's "mobile" is narrowed from "all non-desktop" to "phone-portrait." Persona-accessibility refinement explicitly named in the doc.
  2. **VISION §11 "Calendar task row primitive — bubbles vs. list" parked decision** (raised 2026-05-08): Resolution direction's "mobile gets swipe gestures and inline buttons hide" footnoted with 2026-05-17 refinement: "phone-portrait hides buttons; iPad-portrait+ shows buttons alongside swipe." Bubbles-vs-list density question remains parked separately — not resolved by this ship.
  3. **`3de1c2c` / `cea21e0` / `f77507a` shipped breakpoint decisions** — explicitly re-greenlit. The new pattern (button-fallback at `md:`) aligns with verified Vault/Shed nav anchor ([vault/[id]/page.tsx:1051+:1060](src/app/vault/[id]/page.tsx:1051) `hidden md:flex` for prev/next profile arrows; [vault/shed/[id]/page.tsx:472+:481](src/app/vault/shed/[id]/page.tsx:472) same).

  **Pass 3 vocabulary-breadth grep (rule locked 2026-05-17 in `flamboyant-wilson`):** covered 7 gesture-vocabulary families (swipe / drag / pan / pointer / long-press / gesture-lib / pinch-zoom). Result: 6 surfaces total in codebase touch gesture handling — 3 calendar (modified by this ship) + 2 vault nav (already at `md:` anchor) + 1 image crop (intrinsic UX). **No gesture libraries** (framer-motion / use-gesture / hammerjs) found anywhere — all touch handling is native onTouch* / onPointer*.

  **Audit honesty correction:** User-prompt enumerated "Garden drag-to-move" + "Journal swipe-delete" as known swipe surfaces. Grep confirmed neither exists in the codebase. Both pages use long-press for bulk-select mode (theme #10 territory, not #4). Flagged in plan + this entry rather than silently ignored. If gaining those gestures is desired, that's a feature addition needing its own chunk.

  **Concurrent-chat collision caught + reconciled** via CLAUDE.md Phase 2 readiness gate item 9 (locked 2026-05-17): pre-edit `git fetch origin main` found `33955b4` (mark `4a8b04c` verified clean) + `3396d03` (§3.12-tester triage capture) had landed on origin/main between my plan-lock and code-edit start. Fast-forward rebase clean since no code edits made yet (plan file is outside the git tree). Validation #3 of the rule running clean in 2 days.

  **Persona walk through all 5 personas (Pass 2 subcategory locked 2026-05-17):**
  - **Maya** (power gardener, multi-device): Neutral-to-positive — visible buttons don't slow her down on iPad; phone surface unchanged.
  - **Sydney** (medium tech comfort, mostly phone): Neutral — phone unchanged is her primary device.
  - **Walter** (retiree, iPad-primary, swipe-excluded): **Major improvement** — primary exclusion resolved on iPad-portrait+.
  - **Aria** (houseplant urban, balcony only): Neutral — calendar surface mostly irrelevant to her usage.
  - **Sam** (first-time beginner): Positive — visible buttons reduce ambiguity ("how do I complete this?") for 55yo flavor.

  **3 buckets close-out:**
  - **(a) Parked items added this chat:** none — this chat ships the unparked. Vault profile / Shed product nav prev-next surfaces (already at `md:` anchor) NOT touched; they're the cohesion anchor for this ship.
  - **(b) Deferred audits / known follow-ups:**
    - **Phone + iPad verification on prod** pending (see §1 "Still TBD verification"). Visual check at both viewports.
    - **`cea21e0` + `f77507a` swipe gesture verification** still TBD — will be implicitly re-verified by this ship's verification (if buttons render correctly AND swipe still fires, swipe surfaces are confirmed working).
    - **§3.12 theme #10 (touch-target sweep)** remains queued — long-press patterns on Garden/Journal/Shopping/SeedVault flagged as ADJACENT during this chat's Pass 3. Walter completes primary actions via tap; long-press is optional bulk-select disclosure. Not in this ship's scope.
    - **Bubbles-vs-list row-primitive density question** (VISION §11) still parked — footnoted but not resolved by this ship.
  - **(c) Dogfood findings:**
    - **User-prompt enumeration mismatch named.** User's "Garden drag-to-move" + "Journal swipe-delete" don't exist. Status: just-captured — flagged for honesty; no rule change implied. The Pass 3 vocabulary-breadth grep caught it which is exactly what the rule is supposed to do.
    - **Pass 3 vocabulary-breadth rule validation #1 of a new audit.** Rule locked yesterday (2026-05-17 in `flamboyant-wilson` close-out); first audit using it that wasn't the founding case. Rule fired correctly: gesture-vocabulary families (swipe/drag/pan/pointer/long-press/gesture-lib/pinch) covered the breadth; found exactly the surfaces that exist + flagged that the user-prompt names 2 that don't. Status: just-captured — rule working as designed; no further action.

- **§3.12-pre `4a8b04c` user-verified clean on prod + tester-tooling triage captured (`gifted-wright-4184cb` chat, 2026-05-17).** Two purposes in one short chat: (P1) user phone-verified `4a8b04c` §3.12-pre dev-tools gating across all 4 verification steps — default 6-item Settings menu, tap-7× → 9-item menu, all 5 dev pages render unlocked, sign-out + fresh sign-in re-engages gate. APP-STORE BLOCKER (persona-audit P0 theme #1) confirmed resolved end-to-end; row moved §1 "Still TBD verification" → "Verified clean." (P2) User raised 2-item tester-tooling feedback batch — voice recorder on Tools-bar feedback + debug-log auto-attach on feedback submit. **Step 0 search:** no prior captures in BUGS / ROADMAP §3+§4 / VISION §11 / BACKLOG. **Triage outcome (Rule A ask, user chose Recommended):** capture all 3 in new §3.12-tester chunk (T1 voice + T2 debug-log + T3 gate-or-public scope-decision) + T4 admin-inbox parked 🟣; T3 ships first (scopes T1+T2); fresh chat opens for T3 next. **Adjacent finding surfaced by Pass 3 sibling-sweep equivalent at triage time:** Tools-bar wrench button [AuthGuard.tsx:297](src/components/AuthGuard.tsx:297) currently visible to all authed users — given app-store distribution context, design decision (T3) pending. **Aesthetic / privacy flags named per item** (T1: voice UI states + duration cap + Walter touch-target check; T2: opt-in vs default-on, privacy preview, console-content disclosure; T3: 3 options A=public / B=existing tap-unlock / C=separate tester-allowlist). 1 file, +~50 lines. Doc-only push tier. Tests N/A (no code). **3 buckets close-out:** (a) Parked: T4 feedback inbox manager 🟣 future. (b) Deferred: T3 → T1+T2 sequencing; user picks (a) §3.12-tester T3 or (b) §3.12 catalog theme #2+#6 as next chat. (c) Dogfood findings: none — verification clean, triage clean.

- **§3.12-pre dev-tools gating ✅ SHIPPED end-to-end (`flamboyant-wilson-34fc0c` chat, 2026-05-17).** Code commit `4a8b04c` → main; Vercel auto-deploys. Resolves persona-audit P0 theme #1 of 10 (APP-STORE BLOCKER named by `binary-valley` 2026-05-17). 7 files, +100/-8. Tests 405/405. Build clean. Preview MCP skipped per documented `.env.long`-absent worktree condition; user phone-verifies on prod (verification path in ROADMAP §1 "Still TBD verification").

  **Implementation = existing `useDeveloperUnlock` tap-count gate.** Settings page menu items split into `USER_ITEMS` (always shown) + `DEV_ITEMS` (gated on `isUnlocked`); each of 5 dev sub-pages (developer hub + debug-log + usage + import-logs + extract-cache) adds `if (!isUnlocked) return <"Developer tools require unlock"> + Go to Settings link` — verbatim pattern from [admin/scraper-audit/page.tsx:68](src/app/admin/scraper-audit/page.tsx:68). Entry gesture pre-existed = tap App Version 7× on Profile bottom (already wired via [settings/profile/page.tsx:34,503](src/app/settings/profile/page.tsx:34) + [layout.tsx:58](src/app/layout.tsx:58) provider). No new mechanism introduced.

  **Two Rule A locks shipped this chat:**
  - **Q1 → initial pick = Option B env-var allowlist (`NEXT_PUBLIC_DEV_USER_IDS`) (Recommended).** Chosen at plan-time based on incomplete Pass 3 sibling-sweep (regex covered `NEXT_PUBLIC_DEV|process\.env\.NEXT_PUBLIC|DEV_TOOLS|is_dev|isDev|isDeveloper|role===dev` but missed `DeveloperUnlock|tapVersion|isUnlocked`).
  - **Q2 → UI gating only this ship (Recommended).** `/api/developer/*` server-side gating captured to BUGS U27 for separate security-hardening ship (4 routes accept any authed user despite UI gate).

  **Amendment 2 (post-build pre-commit, second Rule A ask):** Discovered during stale-mock-cleanup grep for `DeveloperUnlockContext` in `settings/page.test.tsx`: the context existed and is actively used. 5 files reference it (provider in layout, tap entry on Profile, gate on scraper-audit, the orphaned test mock). My Pass 3 regex vocabulary was env-var-flavored, so the existing tap-unlock pattern slipped past — AND my Q1 AskUserQuestion listed "Tap-count-to-unlock (gesture-revealed)" as a from-scratch candidate when it was already half-built and unused-on-Settings. User picked Option B Recommended without knowing tap-unlock was the cohesion-aligned answer. **Surfaced honestly via second Rule A ask** with 3 options (revert env-var ship + switch to tap-unlock / keep env-var + delete tap-unlock / parallel mechanisms). User chose Recommended (revert + switch). Env-var ship reverted entirely: `src/lib/devAccess.ts` + `src/lib/devAccess.test.ts` deleted; 5 sub-pages re-edited to drop `useRouter` + `isDeveloperUser` + useEffect-redirect → adopt `useDeveloperUnlock` + scraper-audit-style render-time message; `settings/page.test.tsx` restored to mock `DeveloperUnlockContext` (which the original test correctly anticipated). Net diff vs. baseline (devAccess never landed; 7 files modified only). **Amendment 2 re-audited Pass 1+2+3+4 clean in plan file.**

  **Process lesson captured to memory:** Pass 3 sibling-sweep grep regex must cover **vocabulary breadth**, not just the obvious naming for the pattern you expect. When gating dev tools, search for `DeveloperUnlock`, `tapVersion`, `isUnlocked` alongside env-var patterns. The cost of a too-narrow regex = parallel-mechanism failure mode (the specific cohesion-by-aggregation drift CLAUDE.md 2026-05-14 locked against). Caught here pre-commit by luck (the stale test mock had the existing context's name in it); next time the regex should catch it deliberately.

  **Concurrent-chat fetch check (Phase 2 readiness gate item 9, locked 2026-05-17):** `git fetch origin main` ran twice — pre-edit (HEAD = origin/main) + pre-code-push (HEAD = origin/main). No collision. Third validation of the rule in 2 days running clean.

  **Phase 5 captures applied this chat (this entry + ROADMAP §3.12-pre status flip 🔵 → ✅ + §1 lead-line refresh per 2-paragraph cap + §3.12-pre count fix "4 sub-pages" → "3 sub-pages" + BUGS U27 new entry + Next session focus rewritten).** Single doc-only commit follows the code commit on push.

  **3 buckets close-out:**
  - **(a) Parked items added this chat:** None new beyond BUGS U27 (which is a captured ADJACENT, not parked).
  - **(b) Deferred audits / known follow-ups:**
    - **`4a8b04c` user phone-verification on prod** — pending Vercel deploy + her hands-on test.
    - **BUGS U27 `/api/developer/*` server-side gating** — defense-in-depth follow-up; separate security-hardening ship; user picks priority vs. §3.12 themes.
    - **Pass 3 sibling-sweep regex vocabulary-breadth rule** — captured to memory; consider promoting to CLAUDE.md plan-audit standard Pass 3 subsection as a structural fix.
    - **`scraper-audit` sibling now visible** — separate admin route, not on Settings menu, already self-gates correctly. No work needed unless user wants Settings to also link there (out of scope this chat).
  - **(c) Dogfood-style findings:**
    - **Pass 3 sibling-sweep regex narrowness is a recurring class of miss.** This is the second time in two chats Claude's sibling sweep was too narrow (the 2026-05-16 `pedantic-panini` chat caught its narrowness in the AGENTS.md split, this chat caught its narrowness in the dev-gate pattern). **Status: propose rule now** — flag for the user a CLAUDE.md Pass 3 amendment ("when planning a gating / unlock / role-based feature, ALWAYS grep for: `Unlock|Gate|Role|Permission|Allowlist|tapVersion|isUnlocked|isAllowed` in addition to the obvious naming you're searching for"). Surfaced separately for user decision below.
    - **Auto mode + plan-mode + Rule A re-ask interplay:** when auto mode is active and a substantive plan-amendment is needed mid-execution, the right pattern (which this chat demonstrated) is: stop, surface honestly, ask Rule A, get lock, then continue. Auto mode doesn't override CLAUDE.md's amendment-rigor rule. **Status: just-captured** (not a new rule, just confirmation the existing pattern works in auto mode).

  Plan file `C:\Users\marsh\.claude\plans\continue-from-prev-chat-quizzical-stardust.md` retains full audit trail + Amendment 1 (debug-log lacks auth pattern) + Amendment 2 (sibling-sweep miss revert) + 4-pass audits on each.

- **Persona-audit kickoff closed end-to-end (`binary-valley` chat, 2026-05-17). Flavor D skim-first walk completed. 8 surfaces × 5 personas = 40 cells walked; ~80 findings prioritized P0/P1/P2/P3. 10 cross-cutting themes surfaced + 2 Rule A locks shipped.** Non-build design chat per Chat Lifecycle Protocol; Phases 2-4 no-ops; Phase 1 (purpose + walk + concerns) + Phase 5 (capture into new §3.12-pre + §3.12 chunks) carried the load.

  **Method:** Each surface (Home / Vault / Garden / Calendar / Journal / Shed / Settings / FAB+modals) walked through 5 personas (Maya / Sydney / Walter / Aria / Sam — see [docs/PERSONAS.md](docs/PERSONAS.md) locked same day). Per-surface coherence preferred over per-persona coherence. 2-3 concerns per cell, prioritized P0 (app-store blocker) / P1 (high) / P2 (medium) / P3 (low). Full 8 × 5 walk preserved in plan file `C:\Users\marsh\.claude\plans\continue-from-prev-chat-binary-valley.md`.

  **10 cross-cutting themes (highest leverage):**
  1. **P0 — Developer tools gating** (Settings exposes dev tools to all users; APP-STORE BLOCKER; split into §3.12-pre)
  2. **P0 — Outdoor-garden assumption sweep** (Aria + Sam excluded by default framing across Home/Vault/Garden/Calendar/Shed/FAB)
  3. **P0 — Plain-language audit** (Walter + Sam jargon friction on Vault/Shed/Plantable/Annual roadmap/Monthly pulse/Polyculture/Grow instance/Vendor scorecard/dev-tool labels)
  4. **P0 — Visible-button fallback for swipe-only gestures** (Walter won't swipe; verify Calendar/Garden/Journal alternatives)
  5. **P0 — Empty-state cliff audit** (Sam day-1; per-surface guiding-not-scolding copy sweep)
  6. **P1 — Disclosure-by-data on FAB Menu + tabs** (6-option FAB always; 3-tab Vault always; Sam paradox of choice; Aria irrelevant options)
  7. **P1 — Cross-view consistency** (Sydney's primary pain; already VISION §11 parked)
  8. **P1 — Settings information architecture** (10+ flat items; Walter loses nav; Sam overwhelmed)
  9. **P1 — Memory-plane gaps** (Maya's primary value gap; chunk 3.6 territory)
  10. **P1 — Touch target sweep** (Walter; iPad-first audit across all surfaces)

  **2 Rule A locks (both Recommended picks):**
  - **Q1 → Theme #1 dev-tools gating splits into §3.12-pre chunk** (ships first; mechanical; independent of all other persona work; APP-STORE BLOCKER). Verified during plan-mode: [src/app/settings/page.tsx:7-17](src/app/settings/page.tsx) renders `SETTINGS_ITEMS` flat with no role/build/env gate — Developer / Import logs / Extract cache visible to every authed user; subtitle even says "Profile, data, and developer tools." Finding confirmed not inferred.
  - **Q2 → §3.12 = triage catalog form** (findings list + cross-cutting themes + per-surface notes; not build chunk). Per-theme planning chats spin off this catalog as user prioritizes; do not build directly from §3.12.

  **User pushback on persona-walk discipline validated retroactively (cross-link 2026-05-17 sharing-purpose chat decision 9):** Persona walk on existing app reinforced the persona-walk plan-audit subcategory locked in `competent-blackwell-5de2a2`. App-store distribution context (VISION §1, §3) made the gap visible: app was built to one user's needs; many surfaces work for Maya / Sydney but exclude Aria / Walter / Sam by default framing. The persona system is structurally necessary for app-store readiness.

  **Phase 5 captures applied this chat (all doc-only):**
  - **ROADMAP.md:** new §3.12-pre 🔵 chunk "App-store ship-readiness preflight — gate developer tools"; new §3.12 🟣 chunk "Persona-audit findings catalog"; §1 lead-line refresh (binary-valley para added, ecstatic-rubin para demoted to archive note per 2-para cap); Next session focus rewritten (§3.12-pre = next ship; Chat A still queued; per-theme planning chats listed); this §6 entry
  - No code touched; no VISION/CLAUDE/BUGS/BACKLOG/PERSONAS edits this chat (out of scope for Phase 5)

  **Concurrent-chat fetch check (Phase 2 readiness gate, locked 2026-05-17):** `git fetch origin main` ran pre-edit; HEAD = origin/main, no divergence. Second meta-validation of the rule in two days (2026-05-16 first; this chat second confirmation it's running cleanly).

  **Dependencies for future work:**
  - §3.12-pre (dev-tools gating) = independent + mechanical = ships first.
  - §3.12 catalog = triage register, not build chunk; do not build directly from §3.12.
  - Per-theme planning chats spin off when user prioritizes (see Next session focus subsection for theme effort + pairings).

  **3 buckets at close-out:**
  - **(a) Parked items added this chat:** None new beyond the catalog itself. §3.12 IS the parking surface for the 10 themes; per-theme planning chats will move them into active chunks.
  - **(b) Deferred audits / known follow-ups:**
    - §3.12 theme #4 (visible-button fallback) NEEDS VERIFICATION on real device — does mobile Calendar have button alternative to swipe-complete, or only desktop? Audit pre-empts any P0→P1 demotion.
    - §3.12 theme #1 (dev-tools gating) implementation pattern (build-flag / role-gated / env-based / combo) chosen at plan-time when §3.12-pre is built, not now.
    - Chat A's 9 SURFACE items still queued; persona-audit confirmed U12 + icon-density + HarvestModal end-state + Garden card spacing as persona-relevant (Walter / Maya impact named); does NOT invalidate any Chat A item.
    - Skim-depth walk surfaced ~80 findings; deeper per-persona dives possible later if a theme needs more concrete user-story detail before build. Optional.
  - **(c) Dogfood-style findings:**
    - **Status: just-captured.** User accepted both Recommended Rule A options without modification — strong signal the catalog-vs-build-chunk distinction reads cleanly (no "what's the difference?" pushback). Validation that the §3.12 shape is correct for this surface volume.
    - **Status: just-captured.** Plan-mode verification step (Reading [src/app/settings/page.tsx](src/app/settings/page.tsx) pre-ExitPlanMode) caught that the dev-tools finding was actually-verified rather than just-inferred. Sibling pattern to Pass 1 cite-by-path discipline; reinforces "don't ship a finding based on Explore agent characterization alone."
    - **Status: just-captured.** Skim-depth (~120s per surface, 2-3 concerns per cell) felt right for 8 × 5 walk — would have been overwhelming at deep-dive shape per cell, and per-persona-only walks (Flavors B / C in the counter-options) would have lost the cross-surface theme view. Flavor D shape validated.

  **Close-out shape:** Handoff shape — new chat starting. Plan file retained for retrospective. Doc-only push tier (only ROADMAP.md touched).

- **Persona-audit chat purpose queued + sequenced BEFORE Chat A (locked end-of-`competent-blackwell-5de2a2` via Rule A ask).** User's verbatim: *"should we review the rest of the app for any concerns or recommendations based on the users or under the concern that i want to sell? like walk through it as each user?"* Triggered by the same-chat lock of the persona system + app-store distribution context — natural next move to test existing app through the new lens. **Shape locked = Flavor D (skim-first kickoff):** one chat walks 5 personas (PERSONAS.md) × ~8 surfaces (Home/Vault/Garden/Calendar/Journal/Shed/Settings/FAB+modals); top 2-3 concerns per persona × surface intersection; triaged into new ROADMAP §3.12 chunk with prioritized findings; deep dives deferred to future chats. **Sequence locked = persona-audit FIRST, Chat A (aesthetic-decision sweep on 9 SURFACE items) AFTER.** Reasoning: persona-audit findings may re-prioritize or amend Chat A's items; locking aesthetic decisions before persona-audit risks redo work. Chat A's plan file (`fresh-chat-purpose-vision-sorted-clover.md`) preserved for resumption after persona-audit closes. **Recommended persona-audit chat opening prompt:** see end-of-chat handoff message for paste-able version. Counter-options surfaced but not picked: (A) comprehensive deep-dive in one chat (fatigue risk); (B) per-persona series of 5 chats (slower); (C) per-surface series of ~8 chats (longest total); parallel-to-Chat-A (faster but risk of overlap if persona-audit flags a Chat A item mid-stream); Chat A first then persona-audit (redo risk).

- **Sharing-purpose chat closed end-to-end (`competent-blackwell-5de2a2`, 2026-05-17). All 6 open-territory items (a)-(f) from VISION §11 resolved + 4 bonus locks (public boundary, sharing data model, persona system, app-store distribution context) shipped in same chat.** Non-build design chat per Chat Lifecycle Protocol; Phases 2-4 no-ops; Phase 1 (state purpose + surface conflicts + propose resolutions) + Phase 5 (capture decisions into VISION + new PERSONAS.md + CLAUDE.md) carried the load. **10 decisions locked:**

  1. **Public boundary** → 🕐 long-term in VISION §9. "Never public" stays as *current* boundary; not a forever-lock. Revisit post-MVP if user pull emerges. Reason: differentiator is private accumulated knowledge per VISION §2; opening public pulls into category (Greg, Planta, PictureThis) where leaders exist and moat collapses; infrastructure cost (moderation, abuse, content review) ≈ second app inside this one. User signal verbatim: *"maybe thats a long term pin? ... idk if its better to build the public side? or keep it internal with invite only 'communities'."* User-direction-contradicts-locked-rule path followed: surfaced explicitly, surfaced trade-off, recommendation locked direction.

  2. **Sharing data model = communication + transfer-at-time.** NO pre-marked "shareable" state on plants or items. Removes entire lifecycle-state-management complexity (greenhouse vs garden split, marked-then-planted contradiction, quantity-share-marking). Sharing happens via communication (browse → request → approve, OR board post → claim) + transfer at approval time. User surfaced the underlying complexity (sister's 180-seed-start scenario; *"what if she marks them as shareable, but then starts planting?"*) which forced the simplification.

  3. **Privacy controls = per-category opt-in** (not per-item). Default: nothing shared with circle. User opts in by CATEGORY (seed packets / active plants / harvests / etc.) per group. Manageable even at power-user scale (180+ items).

  4. **Trusted-circle internal structure = groups with mode.** Each group has: members, mode (browse OR board), name. Default "Friends & Family" browse-mode group auto-created on first circle member added. Members can be in multiple groups (e.g. sister in "Family" browse + "Vista Gardeners" board). User signal verbatim: *"should we have different layers to your members? like when you add someone, you can choose whether its open free for all lol or if its community the request board?"* Resolved as per-group-mode (not per-member-mode) so board surface coheres as a group-shared feed.

  5. **Club request-board flows (A/B/C) locked.** Flow A: Post an ASK → others respond/claim. Flow B: Post an OFFER → others claim. Flow C: Browse the board feed. Per-post opt-in (nothing exposed by default). Coordination on multi-responder claims TBD at build time.

  6. **Trade lifecycle = two shapes, both auto-log to Journal.** Shape 1 (direct give): long-press plant/packet → "Give / trade" → pick recipient from browse-mode group members → confirm → Journal entry. Shape 2 (broadcast offer): from board-mode group surface → "Post an offer" → someone claims → kicks off Shape 1 → Journal entry. No dedicated Trades tab. Button conditional-renders (hidden when user has no circle). Persona-walk confirmed: works for Maya / Sydney / Walter / Aria / Sam; solo users + Sam never see the button (no clutter).

  7. **Notifications differentiated by interaction type, not relationship layer.** Direct interactions (incoming request, approval, trade complete) → push by default. Ambient activity (board posts, others' general updates) → pull-only. Household coordination ("BF watered tomatoes 2hr ago") → opt-in light push (off by default). NO activity feed at any layer. New VISION §10 don't-touch entry locks "no activity feed."

  8. **Household attribution = auto-record + default-hide + filter-on-demand.** Data layer captures who did each action (already happens via `user_id`). Default UI shows no person ("watered 2hr ago"). One opt-in filter view ("show actions by [person]") surfaces attribution when wanted. Minimal MVP UX cost: one filter UI surface (likely on Journal or per-plant action history).

  9. **Persona system locked as new process tool.** 5 personas: Maya (Power Gardener) / Sydney (Spring-Planner Hobbyist) / Walter (Retiree Gardener) / Aria (Houseplant Urban User) / Sam (First-Time Beginner). Captured in new `docs/PERSONAS.md`. Referenced from CLAUDE.md plan-audit standard as Pass 2 + Pass 3 "persona walk" subcategory. User signal verbatim: *"would it be a good idea to mock up different users and use them as a 'thought review' as we go through our build?"* User was already applying the persona lens in real-time (pushed back on first trade-home recommendation as *"way too specific and is not applicable to every user if i put this app on the store"*). Formalizing makes the check consistent.

  10. **App-store / public-distribution context made explicit.** Updates VISION §1 boundaries (new line) + §3 first paragraph (now explicit "intended for public release / app-store distribution") + this ROADMAP §6 entry + CLAUDE.md "What this project is" section (refined from "single user has been testing personally" to "user is product owner; intended for public release"). User signal verbatim: *"do we need to note in our roadmap that im building this app for public use/selling/app store?"* Foundational context that should have been explicit from day one — was implicit ("home gardeners across the full skill range") but never named. Implication: every UX decision must generalize beyond user's specific scenario. Reinforces the persona-walk discipline.

  **Phase 5 captures applied this chat (all doc-only):**
  - **New:** `docs/PERSONAS.md` (5 personas + cross-scan table + maintenance notes)
  - **VISION.md:** §1 boundaries line; §3 first paragraph + Layer 2 (Household attribution note) + Layer 3 (full group-structure block); §9 new Public social-media stream 🕐 row + Level 3 trade row updated with two-shape note; §10 new no-activity-feed don't-touch; §11 sharing-UX entry replaced with resolved summary
  - **ROADMAP.md:** §6 this entry; §3.5 trade sub-item expanded; new §3.11 chunk "Trusted-circle infrastructure" 🟣
  - **CLAUDE.md:** "What this project is" section refined; plan-audit standard Pass 2 + Pass 3 gain "persona walk" subcategory; "Required reading before any task" gains PERSONAS.md as optional context

  **Dependencies for future build chunks:**
  - Trusted-circle infrastructure (new §3.11) depends on §3.5 batch-tracking for richer browse depth ("10 tomatoes, 4 planted, 6 in pots") but MVP sharing CAN ship before §3.5 lands (aggregate counts work for first version)
  - May inform §3.4 beds-as-first-class (bed-level sharing implications — communal plots in board-mode groups could share at bed level)
  - Notifications infrastructure not yet specified; UX is locked but build chunk needs scoping

  **3 buckets at close-out:**
  - **(a) Parked items added this chat:** Public social-media stream → VISION §9 row (🕐 long-term, post-MVP revisit). HarvestModal end-state still parked separately at §11 (not touched this chat).
  - **(b) Deferred audits / known follow-ups:** Notifications infrastructure build chunk needs scoping (decision is locked, build path is not); §3.11 new chunk needs dependencies + size estimate filled in during a future planning session; multi-responder coordination on board-claim flow TBD at build time; invite mechanics UX needs separate design pass.
  - **(c) Dogfood-style findings:** (1) User pushed back on "too specific" trade-home framing — validates persona-walk discipline retroactively (filed: just-captured, validation of decision 9). (2) User asked for plain-language re-explanation on Z3/Z4/attribution after jargon overshoot — captured as user-communication pattern: when introducing tech-shaped concepts to non-developer user, lead with concrete scenario + plain-language, not abstract framing (filed: just-captured). (3) User's tendency to surface big strategic context (app-store note, persona system) mid-design-discussion — capture meta-decisions as they emerge rather than deferring; this chat caught both because user surfaced them, but a future chat might miss them if Claude isn't actively listening for them (filed: just-captured).

  **Close-out shape:** Handoff shape — new chat starting. Plan file retained at `C:\Users\marsh\.claude\plans\continue-from-prev-chat-frolicking-token.md` for retrospective. Doc-only push.

- **2 uncovered-work items captured post-close (ecstatic-rubin-5f30c8 chat).** After ExitPlanMode + push of `5ca4bae`, user dropped two feedback items in auto-mode: (1) **U26 captured to BUGS.md** — web browser calendar needs arrows (sub-`xl:1280px` desktop viewports have no month navigation since `3de1c2c`'s `hidden xl:flex` hides arrows and swipe is touch-only). Strict bug per VISION §4 principle 3. XS fix candidate (recommended `pointer-coarse:hidden` for input-capability media-query). (2) **VISION §11 "Modal/menu placement on desktop" re-flagged** with cutoff observation + explicit user request for audit pass — *"at some point, we need to do an audit of our menus. some of them are too low and get cut off on my screen."* Audit scope expanded from 2026-05-08 list with grep-derived primitive inventory; bundles naturally with chunk 3.2 B4 + B5. **P3 rule self-tested before these doc captures:** `git fetch origin main` showed HEAD == origin/main (no divergence) — rule confirmed working as intended, no rebase needed. Doc-only push of these captures = second push of the chat (P4 settings allowlist tested again).

- **U25 ✅ verified clean on prod (`11eaf37`).** User phone-verified 2026-05-17 on prod: My Plants list per-row "Add journal entry" button renders pen-with-motion (`ICON_MAP.Edit`), matching Active Garden gallery + list. Cross-surface cohesion within Garden tabs confirmed end-to-end. Vault profile sites remain parked per ROADMAP §4 ("Cross-surface 'Add journal entry' icon cohesion — Vault profile tabs," parked 2026-05-16). BUGS U25 row marks `· ✅ Verified on prod 2026-05-17`. §5 "Verified clean" subsection gains `11eaf37` row. §5 "Still TBD verification" line for `3de1c2c` tightened to remove the Garden card pen icon portion (now implicitly verified via U25; calendar arrows hide on phone still TBD). U25 fully CLOSED — chunk closure gate cleared.

- **Concurrent-chat amendment fetch rule locked into CLAUDE.md Chat Lifecycle Protocol Phase 2 readiness gate (9th checklist item).** Pattern caught LIVE during this chat's pre-push diff: 4 commits landed on origin/main between this chat's plan-lock and pre-push — `f620e37` shipped a ROADMAP archive policy (§7) that OBSOLETED this chat's own P2 R10 ROADMAP-shortening park entry the moment the parallel chat landed. Reconciled mid-Phase-3 via `git stash + git pull --rebase origin main + drop stash + surgical re-apply on rebased state` (user picked Recommended path via Rule A ask). Rule (9th checklist item): "Before amending CLAUDE.md / VISION.md / ROADMAP.md (canonical docs), `git fetch origin main` + diff against HEAD; if origin/main has diverged, rebase or surface the divergence to user before continuing." Cost ~5s per canonical-doc amendment. Scoped to canonical-doc amendments only (code-edit collisions are caught by mechanical signals — build/test/merge — so this rule plugs the doc-edit gap where no mechanical signal exists). Sibling pattern to Pass 1 cite-by-path verification. **Meta-validation:** the rule being locked this chat would have caught this very chat's collision had it been in place during plan-mode draft. Second meta-validation in two consecutive days (2026-05-16 was the first; 2026-05-17 is the second). Cumulative pattern: concurrent-chat amendments on canonical docs are a recurring drift class on this project; explicit `git fetch` discipline is structurally necessary.

- **Push-classifier settings allowlist locked into `.claude/settings.json` + CLAUDE.md caveat amended (P4).** User pushback 2026-05-16 during U25 push: `AskUserQuestion` greenlit "push (Recommended)" but Claude Code's auto-mode push-classifier still surfaced a separate runtime permission prompt requiring a typed `push` token. *"You shouldn't have to wait for me to say so."* Two options surfaced via Rule A ask; user picked Recommended (settings-layer fix). **Changes shipped:** (i) `.claude/settings.json` (worktree copy; main repo copy syncs via `git pull` after this push lands) gains a top-level `permissions.allow` block with the narrow rule `"Bash(git push origin HEAD:main)"`; (ii) CLAUDE.md push-classifier caveat appended with note that `permissions.allow` allowlisting bypasses the runtime classifier prompt + names that rule enforcement ("code pushes need explicit yes-build") moves fully to plan-mode + AskUserQuestion discipline. **Trade-off (user heard before greenlight):** removes runtime backstop on this push form; mitigated by plan-mode review + explicit AskUserQuestion gating on code pushes. **Open verification (testable on this chat's push):** whether `permissions.allow` actually bypasses the auto-mode classifier; outcome captured in close-out.

- **Sharing-purpose chat scoped as own dedicated chat purpose, parallel to Chat A (locked 2026-05-17).** User raised mid-handoff in frosty-wright-d437a7: *"i also want to dig into the sharing functionality and the purpose of sharing within the app. i have a few conflicting ideas and id like to narrow down the use of 'sharing' and how it will integrate/appear in the app."* PM triage: scope-✅ per VISION §1/§3/§9 (sharing IS in the locked vision); not feature creep; closest existing roadmap chunk is §3.5 lifecycle (transfer/give-away/trade) which is 🟣 Phase 3-gated. **Bucket: Phase 2 (jobs-to-be-done) / Phase 3 (IA) style design discussion** — needs its own chat, not bundled into Chat A aesthetic sweep. Reasons: (a) different cognitive mode (vision-level design vs per-item aesthetic); (b) sharing is upstream of all 9 Chat A SURFACE items (single-user surfaces — calendar/journal/garden/icon/save-cancel/harvest-end — no sharing UX collisions); (c) Phase 2/3 design framework's existing slot for this. **Locked decisions surfaced for the new chat (don't re-litigate):** Self/Household/Trusted-circle 3-layer audience locked 2026-05-08; invite-only forever; never public/strangers; Level 3 trades ✅ / Level 4 paid ❌ / Level 5 public marketplace ❌. **Open territory captured to VISION §11** for the new chat to chew on. Doc-only ship; no decision pre-empted.

### 2026-05-16

- **ROADMAP shortening — Option A locked + executed in same chat as audit (doc-only).** User asked directly "should we shorten our roadmap doc? why so large. is that optimal or beneficial?" Honest PM answer: no, ~621 lines was too long — drift-prone, ate context budget on every session-start re-read (RULES CARD #1). Three numeric caps locked in §7 "Archive policy" subsection: §1 lead-line ≤ 2 paragraphs; §5 ~10 entries; §6 ~14 days inline → `docs/archive/ROADMAP_log_YYYY-MM.md` (archive file not yet created — project age 9 days as of 2026-05-16). Applied this turn: dropped 5 older §1 lead-line paragraphs (full content preserved in §6 2026-05-13 / 2026-05-14 / 2026-05-16 entries) + 20 older §5 entries (history in `git log`). ROADMAP ~621 → ~600 lines. **Counter-options considered:** Option B (AGENTS.md doc-architecture split — bigger structural fix, parked in VISION §11 as own chat purpose); Option C (queue for Chat D doc-hygiene); Option D (defer to AGENTS.md split entirely). User picked A (immediate). **Known follow-up surfaces (not part of this archive policy):** §1 has stale per-chat subsections ("Just shipped this session," "Verified clean," "Still TBD verification," "New feedback batch — triaged") that persist across chats as snapshots — flagged in §7 as a future doc-hygiene pass. **Meta-finding:** this is the second drift-shape this audit chat caught + actioned within itself (first was the parallel-chat collision discovery; this is the doc-length drift). The audit + the actions on it ran in the same chat — collapses what would have been Chat D scope.

- **Vision + Roadmap alignment audit ran (frosty-wright-d437a7 chat, read-only) — POST-AMENDMENT.** User-requested deliberate audit before continuing build. 4-pass against the audit itself (factual / concerns / sibling / lock) terminated clean. **7 cross-cutting findings:** (A) aesthetic-decision backlog is the rate-limiter — 4 chunks blocked on user input, 12+ parked items waiting; (B) two coalescence opportunities — icon/visual cohesion + card-primitive hierarchy; (C) U25 is the cleanest immediate ship — XS, no decisions; (D) chunk 3.2 desktop pass idle 5 days; (E) 3 missing destinations — journal flicker bug, migration audit RLS/RPC gap, U24 root-cause-class investigation; (F) 2 VISION §11 stale-wording entries; (G) BACKLOG.md stamp 9 days stale. **Original recommended 5-chat sequence:** A=U25 fix; B=aesthetic-decision sweep; C=build unblocked items; D=resume chunk 3.2 B3; E=chunk 3.10 doc-hygiene + 3 missing-destination captures + F/G wording fixes. **AMENDMENT (discovered via fetch mid-close-out):** parallel chat `pedantic-panini-9d963c` shipped U25 Garden tabs (`11eaf37`) + close-out (`241c29a`) BEFORE this audit completed. **Findings (B) and (C) substantially validated by parallel chat's discovery:** the parallel chat's U25 plan-readiness gate independently surfaced 3 additional Vault profile aria-label sites — exactly the icon/cohesion sibling pattern Finding B named as needing coalescence. The parallel chat parked those Vault sites via Rule A. Two coalescence opportunities (B1 icon-cohesion + B2 card-primitive hierarchy) remain valid; the icon-cohesion bundle now has the new Vault-sites entry to fold in. **Updated 4-chat sequence (collapsed):** A=aesthetic-decision sweep (9 SURFACE items per Rule C, was Chat B); B=build unblocked items (was Chat C); C=resume chunk 3.2 B3 (was Chat D); D=chunk 3.10 doc-hygiene scoping + 3 missing-destination captures + F/G wording fixes + BACKLOG.md refresh + NEW Vault profile aria-label cohesion decision (was Chat E). **Parked-item classification:** 9 SURFACE / 14+1=15 LEAVE-ALONE (the new Vault-sites entry joins LEAVE-ALONE since user already chose park) / 0 RETIRE + 2 wording-update flags. **Dogfood-findings status:** all 3 process themes (A/D/E recurrence shapes) tagged just-captured for monitoring. PLUS new dogfood meta-finding: parallel-chat collision itself validates that two independent paths converged on cross-surface cohesion discipline within hours — strong signal the user's icon-cohesion concern is structural, not a one-off. **Plan file:** `C:\Users\marsh\.claude\plans\fresh-chat-purpose-vision-sorted-clover.md` — full report + POST-AUDIT AMENDMENT block. Close-out per Chat Lifecycle Protocol Phase 5; 3 buckets enumerated; in-place close shape. Doc-only commit this turn.

- **U25 closed on Garden tabs (`11eaf37`).** Code fix shipped: inline 4-path notebook SVG at [MyPlantsView.tsx:893](src/components/MyPlantsView.tsx:893) swapped to `<ICON_MAP.Edit className="w-5 h-5" />` matching the `3de1c2c` (2026-05-12) ship sites at [ActiveGardenView.tsx:1112](src/components/ActiveGardenView.tsx:1112) + [:1260](src/components/ActiveGardenView.tsx:1260). New regression test at `src/app/garden/myPlantsIconCohesion.regression.test.ts` (3 it-blocks). Tests 401 → 404; build clean; Preview MCP skipped per auth-blocked condition. Bonus side-effect: also closes BUGS Post-Launch #3 stroke-weight drift on this site. Button shell untouched (rounded-lg + text-emerald-600 different from ActiveGardenView's rounded-md + text-emerald-900); shell cohesion across Garden tabs is a separate parked question, ICON-only is U25 scope.

- **Vault profile sites parked (3 additional `aria-label="Add journal entry"` sites).** During the U25 plan-readiness gate, the cross-surface aria-label grep surfaced 3 additional sibling surfaces missed by the handoff: [VaultProfileJournalTab.tsx:51](src/app/vault/[id]/VaultProfileJournalTab.tsx:51) + [:67](src/app/vault/[id]/VaultProfileJournalTab.tsx:67) (labeled CTA "Add journal" + `ICON_MAP.Journal` at `w-4 h-4`); [VaultProfilePlantingsTab.tsx:133](src/app/vault/[id]/VaultProfilePlantingsTab.tsx:133) (icon-only `ICON_MAP.Journal` at `w-4 h-4`). Surfaced via Rule A ask with recommended option = park. User chose park. Reasons: (a) U25's reported bug was within Garden tabs only; (b) Vault sites have a meaningfully different visual primitive (labeled CTA + smaller icon size) — substantive aesthetic decision; (c) ICON_MAP.Journal-on-Vault may legitimately be the right call because Vault profile context lacks the `care`-entry-type-marker collision that `3de1c2c` was originally resolving. Captured in ROADMAP §4 as "Cross-surface 'Add journal entry' icon cohesion — Vault profile tabs (parked 2026-05-16)" with full site list + reasoning. Re-surface during Phase 6 design-system pass, or sooner if a new Add-journal site is added. **Independently a Pass 3 sibling-sweep instance** (Pass 3 was locked in parallel chat `3d9a8a7` while this chat was in flight); see meta-finding below.

- **Migration-naming guard locked into "Things to never do without explicit user permission" (CLAUDE.md amendment via `3e71149`).** Appended as new bullet after the "Push code/feature changes" bullet: never create a migration file with a timestamp prefix matching an existing file in `supabase/migrations/`. Use unique `YYYYMMDDHHMMSS` 14-digit timestamps. Cites the `20250330000000_journal_entry_type_prune.sql` + `20250330000000_user_settings_onboarding_complete.sql` collision that contributed to U24 root cause (the second file was silently skipped during prod Dashboard application). Companion to ROADMAP §3.10 R9 audit (still queued — separate chat) which will rename existing duplicate-prefix collisions.

- **Cross-surface scope-check sub-clause superseded by Pass 3 sibling sweep (mid-rebase decision, locked 2026-05-16).** This chat (`pedantic-panini-9d963c`) drafted a "Cross-surface scope check" sub-clause for the Pass 2 cohesion-by-aggregation subcategory — addressing the same drift class as the parallel chat's new Pass 3 sibling sweep (`3d9a8a7`). Discovered on rebase that both rules cover the same territory. Decision per the new Role-lock rule ("Claude owns CLAUDE.md hygiene") + the user's "push whatever you need to" directive: **dropped the sub-clause; Pass 3 (broader, structurally cleaner, BLOCKING/ADJACENT/CONCERN rubric) supersedes**. The U25 drift example added to Pass 3's "Why this pass exists" section instead — proving Pass 3 catches visual cohesion drift (not just code-format drift like U24 Phase A). **Meta-finding (two chats converged on the same rule):** independent Pass 3 sibling sweep design + cross-surface aria-label grep both arrived at the same proactive-grep-for-peers discipline within hours of each other. Strong validation the pattern is a real need on this project.

- **CLAUDE.md amendment shipped (`3d9a8a7`) — 5 high-value framings from user's other-project skeleton + 1 new gap from 10-section checklist walk.** User flagged that her other project feels smoother on focus/decision-quality/positional-awareness; asked Claude to review the skeleton-CLAUDE.md the other Claude provided and incorporate as needed. **New locks (all 2026-05-16):** (1) **Authority precedence** — explicit doc-vs-doc winner per domain (VISION wins scope, CLAUDE wins process, ROADMAP wins current-state, WORKFLOW subordinate to CLAUDE, BUGS/BACKLOG are registers not authority). Replaces implicit-precedence behavior. (2) **Audit Pass 3 — Sibling pattern sweep** — proactive grep for codebase peers + bug-class shape elsewhere; outputs BLOCKING / ADJACENT / CONCERN. Renumbers Lock hygiene → Pass 4. Catches the same drift class as U24 Phase A console.error format inconsistency (caught post-build pre-push, would have been caught pre-build with this pass). Complements Pass 2 cohesion-by-aggregation (reactive); sibling sweep is proactive. (3) **Role lock subsection** — names "the trap" (enumerate-without-recommending, ask-permission-for-mechanical-decisions). User=visionary/customer; Claude=dev+PM+tech-lead. (4) **6-condition handoff readiness gate** — binary checklist (tests/build / pushed / docs current / memory matches / no dangling / chunk complete) before Claude proposes a fresh chat. Distinct from transition signals (when-to-consider). (5) **Mid-sprint blocking-vs-polish split** inside feedback-batch triage Step 1 — blocking fixes ship now, polish gets `[POLISH PHASE]` tag. **Sharpening adds:** RULES CARD #9 (capability honesty, now scanned per-turn) + #10 (role lock summary); lowercase-prose-as-confidence one-sentence add to "Idk / I trust you" pattern; "memory drifts; canon is the source" reminder on required reading. **Deferred:** AGENTS.md doc-architecture split → VISION §11 (would touch every cross-ref, deserves its own plan-audit + chat purpose). **Skipped:** "no shame UX" voice rule (project-specific), conventional-commit prefix (needs separate user decision), redundancy with existing RULES CARD #1/2/4/5/6/7. **Memory:** `audit_pass3_is_sibling_sweep.md` written + MEMORY.md index updated. **Process validation:** plan + 4-pass audit (1 factual / 2 concerns / 3 sibling / 4 lock — testing the new pass on my own changes) terminated clean at Pass 4 across initial plan + 1 amendment round (A0 added post-checklist walk). Plan at `C:\Users\marsh\.claude\plans\ive-been-workin-with-replicated-wind.md`. Doc-only ship.

- **U24 closed end-to-end via prod DDL apply (no code change needed in Phase B).** Phase A instrumentation (shipped `7207164` 2026-05-14) surfaced the root cause exactly as designed: 3× `PGRST204: Could not find the 'ended_at' column of 'grow_instances' in the schema cache` captured in debug log when user retried bulk-end with instrumented build. **Locked finding:** Phase A's plan-audit rigor (Pass 1 destructure + .select("id") + console.error) collapsed what would have been hours of speculation into minutes of diagnosis. The fix path was a Supabase Dashboard SQL Editor paste of the missing migration's DDL, NOT additional code work. User applied 3 SQL blocks: (a) narrow version of `20250207600000` (ALTER TABLE ADD ended_at + status CHECK with 3 values); (b) widening of CHECK to 5 values matching `20250211000000_phase1_schema_foundation.sql:140`; (c) missing column `onboarding_completed_at` from `20250330000000_user_settings_onboarding_complete.sql` (discovered via comprehensive column-dump diagnostic). Bulk-end retest on phone succeeded. U24 status: CLOSED.

- **Migration audit ran against prod Supabase via column-dump diagnostic.** Schema_migrations tracking table doesn't exist (user has been applying migrations manually via Dashboard SQL Editor, not via `supabase db push` CLI). Fallback: `SELECT table_name, column_name, data_type FROM information_schema.columns WHERE table_schema='public'` chunked in 3 alphabetical buckets due to Dashboard's 100-row UI cap. Cross-referenced against all ~100 migrations in `supabase/migrations/`. **Result:** only ONE additional column gap found beyond the U24 one — `user_settings.onboarding_completed_at`. All other column-additive migrations are applied. `journal_entries.entry_type` CHECK constraint verified current (11 values including `cold_stratify` from 2026-03-19 migration `20260319120000_journal_entry_type_cold_stratify.sql` — which means even 2026-dated migrations are reaching prod). **Out of audit scope:** RLS policies, RPC functions, triggers, indexes, storage policies. ~10 migrations affect those — could have separate gaps. Not investigated this turn.

- **Migration-naming process bug locked: duplicate timestamp prefix `20250330000000` caused silent migration skip.** Two migration files share the exact same timestamp prefix: `20250330000000_journal_entry_type_prune.sql` and `20250330000000_user_settings_onboarding_complete.sql`. Supabase migration ordering uses the prefix — when prefixes collide, ordering is undefined and one can be silently skipped by `supabase db push` or by Dashboard application order. Prune was applied to prod; onboarding_completed_at wasn't. **Same root cause likely produced the original U24 `ended_at` gap** (need to investigate timestamps around `20250207500000` / `20250207600000` for similar collisions — not done this turn). **Process fix proposed for future migrations:** use unique `YYYYMMDDHHMMSS` timestamps (14 digits with actual hour/minute/second) instead of `YYYYMMDD000000` (8 digits + 6 zeros). Captured to ROADMAP §3.10 as a new R-item.

- **U25 captured to BUGS — My Plants cross-surface icon mismatch with Active Garden.** Discovered during phone-verify of `3de1c2c` (chunk 3.9 #4 garden trigger icon swap): Active Garden gallery + list use `<ICON_MAP.Edit />` (pen-with-motion); My Plants list uses inline SVG of a notebook at [MyPlantsView.tsx:893](src/components/MyPlantsView.tsx:893). Same action ("Add journal entry") reads with two different icons. Strict bug per VISION §4 principle 3. Root cause: `3de1c2c` scoped to ActiveGardenView only — MyPlantsView wasn't named in scope. The cohesion-by-aggregation rule (locked 2026-05-14) would have caught this in Pass 2 had it existed at the time of the ship. Validates the rule's named failure mode in retrospect. Fix scoped: 2-line code change + 1 import + regression test extension + Preview MCP. Deferred to follow-up chat.

- **Tree-data feature asks parked to chunk 3.6 + §4 (chill hours + rootstock tracking).** User raised 2026-05-16: *"i need chill hours added to our profiles for trees. and i want to be able to track rootstocks for trees."* Step 0 search: chill hours = net-new; rootstock = pre-captured at `docs/FEEDBACK_AND_FEATURE_NOTES.md:67` (legacy pre-VISION-v4 doc with workaround pattern + "first-class later" note). PM triage: both scope-✅ per VISION §6 Theme 2 / chunk 3.6 BUT off-current-chunks (U24 Phase B + parallel onboarding chat + chunk 3.2 B3-B5 + chunk 3.9 #5/#7/#8/#9/#10 + §3.10 doc-hygiene). User chose park-to-3.6. **Locked attachment-point distinction:** chill hours = variety-level on `plant_profiles` (cultivar property: "Snow Queen nectarine = 200-300 hrs"); rootstock = instance-level on `grow_instances` (specimen property: "this apple was grafted onto M27 dwarf rootstock"). Distinction matters for Phase 3 IA work — the same field can't sit at both layers without a duplication question. Chill hours likely needs eventual micro-climate-triggers (Failure Mode #3) integration for "this variety won't perform at your location" alerts. Rootstock likely bundles with beds-as-first-class Phase 3 build (both extend `grow_instances` schema). Rootstock entry migrated from legacy doc; legacy entry marked SUPERSEDED 2026-05-16 to prevent dual-capture drift. Doc-only ship.

### 2026-05-14

- **U24 Phase A shipped (`7207164`).** Silent-failure pattern in bulk-end handlers fixed across 3 handlers per the diagnosis plan. Loop-with-hadError pattern mirrors existing sibling handlers in the same files (ActiveGardenView handleBulkDelete + moveSelectedToPermanentPlants; MyPlantsView handleBulkDelete) so the fix doesn't introduce a new control-flow shape. `.select("id")` row-count check at the direct-supabase site catches the `{error: null, data: []}` zero-row-match edge case. `console.error` instrumentation captured by the debug log page for Phase B retry. `showErrorToast` reused in GrowInstanceModal via existing `useToast` hook. Three sibling end-paths confirmed already-clean (no edits). One path-correction recorded in plan vs. diagnosis: `useVaultPlantingsHandlers` lives at `src/app/vault/[id]/`, not `src/hooks/`. Phase B awaits user retry on her 7 stuck plants with instrumented build live; debug-log page captures `ActiveGardenView.handleBulkEndBatch: update failed` console output for diagnosis.

- **Cohesion-by-aggregation rule locked into CLAUDE.md plan-audit standard.** User raised the concern mid-build: *"i request a feature and it gets implemented but you fill in gaps without thinking it fully through... You ask for X. Claude scopes X. Claude also makes a bunch of smaller decisions to actually implement X — what the swipe threshold is, whether swipe autocommits or confirms, what the row primitive shape is, what padding the day header uses... Each of those individual choices feels like an implementation detail at planning time, so Claude classifies it as 'engineering, decide silently' rather than 'aesthetic, ask first.' Then they aggregate — and a month later you look at the Calendar and feel that nothing reads right."* Existing CLAUDE.md had the spirit (RULES CARD #4 + VISION §4 principle 2 + anti-patterns) but no sharp rule for the "engineering-disguised-aesthetic that aggregates" failure mode. **Rule codified** as a dedicated Pass 2 subcategory in the plan-audit standard, plus a cross-reference clause appended to RULES CARD #4. Concrete enumerated categories (toast color, animation tech, threshold value, log-string format, padding token, icon weight, row-primitive shape, transition timing, copy frame, empty-state copy). Two-branch resolution at audit time: (a) cite existing pattern as anchor by path, or (b) ask. Failure-mode signal explicitly named: "Small enough to feel like engineering." **Specific drift this rule caught in U24 Phase A pre-push:** `console.error` format `[scope] description` (new) vs. `scope: description` (existing helpers) — fixed via commit amend BEFORE push to origin/main. Also surfaced F-cohesion-1 (toast color variance between `setQuickToast`-emerald and `showErrorToast`-amber across components) as pre-existing drift not introduced by this diff — parked for future VISION §11 cohesion pass.

- **U24 diagnosed — silent-failure pattern in bulk-end handler.** Strict bug U24 ("ended 7 plants still appear in Active Garden", reported 2026-05-13). Diagnostic chat via 2 Explore passes (1: trace read+write paths from `ActiveGardenView`; 2: find all 6 end-plant code paths) + direct code-read verification of the linchpin findings + 3 user-clarification asks (hard-refresh test, flow used, view mode). **Root cause:** bulk-end handler at [ActiveGardenView.tsx:749-773](src/components/ActiveGardenView.tsx:749). Line 757 — `await supabase.from("grow_instances").update({...}).eq("id", batch.id).eq("user_id", batchUserId)` — discards the `{data, error}` result. Whatever error Supabase returns (RLS edge, CHECK violation, network, trigger), the code continues to toast `Ended N plantings`, exit bulk mode, and `load()` (which refetches unchanged rows). User sees deceptive success + plants reappear. **All 6 end-plant code paths mapped** with mutation payloads + file:line: paths 1+3+5+6 use varied wrappers (`updateWithOfflineQueue` and direct Supabase); paths 2+4 are direct Supabase bulk-ends with error-discarded pattern. Every path writes BOTH `status` AND `ended_at` (rules out original triage causes a/b/d). **User-confirmed context** ruled out additional causes: persistent across pull-to-refresh + full app close+reopen (rules out cache/state cause c); flow used = long-press → FAB → End batch → confirm (= path #2); personal view + own plants (rules out RLS cause e). Remaining underlying-error candidates (i-iv) require instrumentation to diagnose. **Fix path scoped Phase A + Phase B:** Phase A (S-size, ships first) = add `const {error}` check + `.select('id')` row-count check at line 757 + audit 5 sibling paths for same gap + 1 new unit test on error path + Preview MCP screenshot. Phase B (depends on A) = user retries on her 7 stuck plants with instrumented build, surfaced error names the underlying cause, ship targeted fix or close as transient. Manual cleanup for the 7 stuck plants surfaces as Phase 5 ask (retry after A, OR one-off SQL update). Plan at `C:\Users\marsh\.claude\plans\three-things-to-handle-iterative-peach.md` retains both amendment rounds + 3-pass audit log. **Two amendments declared mid-chat:** (1) original recommended cause (Harvest-flow gap) disproven by user — re-audit triggered, plan revised; (2) family-view-RLS hypothesis disproven — re-audit triggered, plan revised. Audit terminated clean at Pass 3 on revised plan after both loops. Doc-only ship.

- **HarvestModal end-state realization parked as separate UX↔data gap.** Discovered mid-U24-diagnosis: [HarvestModal.tsx:99-110](src/components/HarvestModal.tsx:99) inserts a `journal_entries` row with `entry_type: 'harvest'` but does NOT mutate the `grow_instances` row. Per VISION §5 end-states, "Harvested out — ran its full course, retired (annual)" is supposed to be an end-state. Current code doesn't realize the end-state in data; only in journal. Not the cause of U24 (user confirmed she used bulk-end flow, not harvest), but is a real UX↔data gap surfaced this chat and worth fixing eventually. **Routed to VISION §11 parked items** (needs aesthetic decision: UX model A "auto-end checkbox on Harvest modal" vs model B "prompt after harvest log saves"). Should respect `is_permanent_planting` (perennial harvest = ongoing, not an end-state).

- **Plan-audit Pass 2 reframed as "Concerns / gaps / inconsistencies hunt."** User flagged 2026-05-13 mid-close-out that this chat's Pass 2 had run light — scoped to numbering / cross-refs / classifier-caveat consistency, not actively hunting for what could go wrong. Quote: *"do you not do a concerns, gaps, and inconsistencies pass? is it just wording audit? i want our audits to be relatively thorough so we are catching concerns and bugs before we implement any coding."* Diagnosis: CLAUDE.md Pass 2 description used code-shaped category names ("race conditions, async ordering, double-fire") which made the doc-batch Pass 2 unconsciously dial down to wording-check shape. **Fix locked:** (1) Renamed Pass 2 from "Semantic + edge" to "Concerns / gaps / inconsistencies hunt" in CLAUDE.md plan-audit standard. (2) Added explicit category lists per batch shape — code batch (state transitions, race conditions, async ordering, null/empty/many, missing query filters, error handling, missing imports/side effects, optimistic-UI vs refetch mismatch, RLS/auth assumptions, mobile-vs-desktop split, test coverage gaps) vs doc batch (internal contradictions, stale framing, broken cross-refs, missing destination routing, numbering/placement collisions, hierarchy gaps, dating/stamp drift). (3) Required "state hunt categories out loud before running Pass 2." Skipping the category-naming = the audit ran light. (4) Code ships specifically run more thoroughly than doc batches — production regression cost is higher than doc-drift fix cost. Saved to memory at [audit_pass2_is_concerns_hunt.md](C:\Users\marsh\.claude\projects\C--Users-marsh-OneDrive-Documents-Garden-Tracker\memory\audit_pass2_is_concerns_hunt.md) as a sister rule to `audit_passes_are_floor.md`. Doc-only ship.

- **U24 captured in BUGS.md — ended plants still showing in Active Garden.** Reported 2026-05-13 mid-close-out by user testing calendar / garden. Stamp refreshed to 2026-05-13. Step 0 search confirmed not previously captured (grepped BUGS.md / ROADMAP §3+§4 / VISION §11 / BACKLOG.md). Diagnosis deferred to next chat — read the Active Garden query path in `ActiveGardenView.tsx` + parent `garden/page.tsx`, trace where `grow_instances` are fetched + filtered. Five possible causes enumerated (not exhaustive): missing `ended_at IS NULL` filter; `status` mismatch with endPlant flow; optimistic UI; `deleted_at` vs `ended_at` conflation; household-scope mismatch. Doc-only ship.

- **Push-classifier caveat locked into CLAUDE.md push-tiers section.** During the 2026-05-13 `884cf9d` doc-only push, the auto-mode classifier (which gates every `git push origin main` regardless of file type) prompted 3 times with letter-coded reply options ("Reply `A`" / "Reply `B`") before a bare `push` landed it. The doc-only push tier rule says no per-push greenlight is needed — but the classifier may still surface a permission gate at runtime, separate from the rule. Caveat captured: extends the doc-only tier (doesn't contradict it); convention locked for future asks — frame reply concretely ("Reply `push` to proceed") so user can paste one word back, not letter codes. Doc-only ship `2cc313a`.

- **2026-05-13 calendar-testing feedback captured as ROADMAP §3.9 #9 + #10 (new 2026-05-13).** User dropped two new items while testing calendar functionality, explicitly framed as "table for later." Step 0 search: NOT directly captured but related to VISION §11 row-primitive parked decision + BUGS U13 hierarchy pattern. Both bucketed 🔵 current build, aesthetic-flagged. **#9 swipe-to-complete checkmark confirmation:** refines `cea21e0` (singleton row swipe ship 2026-05-08) + `f77507a` (consolidated row swipe ship 2026-05-10), both currently autocommit past 100px threshold + 8px direction lock. VISION §11 swipe-batch resolution direction (2026-05-08) named "left=complete, right=snooze" but didn't specify confirm-vs-autocommit — refinement of locked behavior, not contradiction. Needs user input on interaction model (reveal-checkmark? swipe-then-confirm? rubber-band-back?). **#10 restructure calendar task cards:** primary line = TO_DO verb, secondary = plant/variety, remove date except in Overdue section. Related to VISION §11 "Calendar task row primitive — bubbles vs. list" + BUGS U13 hierarchy pattern. Needs structure proposal + per-element treatment input. Numbering disambiguation: `(new 2026-05-13)` label suffix added because §4's parenthetical `(2026-05-12 #9)` / `#10` references the original 12-item 2026-05-12 batch's parked items (gallery-vs-table / growing-indicator-tags), NOT chunk 3.9 numbering — explicit suffix prevents future confusion. Doc-only ship `2cc313a`.

- **§3.9 pre-existing drift fix shipped inline.** Chunk 3.9 intro line said `**Scope (7 items):**` but listed items 1-8 (drift introduced when #8 was added). Adjacent to today's new #9 + #10 additions; fixed inline rather than leaving more broken. Updated to `**Scope (10 items: 1-8 from 2026-05-12 batch + 9-10 from 2026-05-13 calendar-testing additions):**`. Status line also refreshed to mark accurate shipped/parked/needs-input states for each item. Doc-only ship `2cc313a`. Amendment declared mid-execute + re-audited per Phase 3 amendment protocol; Pass 2 returned clean on as-written-now plan.

### 2026-05-13

- **Four minor rule items locked end-of-session.** After the rules-list provide and "are we clean" check-in, Claude flagged 4 minor items that could still be addressed; user chose to walk all 4 in this chat (per "finish in this chat" direction). All shipped: **(1) Dogfood-findings status tagging** — bucket (c) findings must be tagged just-captured / propose-rule-now / file-as-parked / file-as-bug-feature at close. Prevents finding accumulation without destination. Added to Phase 5 close-out. **(2) Capture-doc boundaries** — new top-level subsection with a reference table mapping signal-type → destination doc (VISION §9/§6/§10/§11, CLAUDE.md user-comms, ROADMAP §1/§3/§5/§6, BUGS.md, BACKLOG.md). Codifies implicit routing practice. **(3) Phase 1 trivial-Q&A short form** — 2-line declaration format added after the expanded 7-line format, with criteria for when to use which (fact-lookup/explanation/clarification + no edits + no decision). Prevents the heavy form from being skipped entirely on trivial chats. **(4) CLAUDE.md-contradiction rule** — central rule parallel to VISION.md contradiction rule, placed near "A note on session continuity." When user direction contradicts a locked CLAUDE.md rule: flag explicitly, surface trade-off, respect override if reaffirmed, amend CLAUDE.md if change is durable. No silent compliance.

- **Plan-audit cap-misreading fix locked.** User flagged 2026-05-13 (end-of-session) that "minimum 2 audit passes, 3 for state-touching" wording can be misread as a CAP on total pass-runs when intent is REQUIRED pass-TYPES. User: *"why are our audits limited to 3? i want it to continue until clean or immaterial pass."* Fix: Plan-audit standard paragraph rewritten to make explicit that 2/3 refers to pass-TYPES (Factual / Semantic+edge / Lock hygiene) required, NOT to total pass-runs. Each required pass-type runs iteratively per the iterative-loop + strict-clean-pass clauses — find concerns → revise plan → re-run THAT pass-type → loop until clean or immaterial. No cap on total pass-runs. Phase 1 protocol audit-reference line also tightened to match. Loop intent was always there (iterative-loop clarification, strict-clean-pass clause both said "loop until clean") but the "minimum 2/3" wording created surface ambiguity that contradicted the loop intent.

- **Four voyager_sanctuary adoptions locked into CLAUDE.md.** User shared the voyager_sanctuary project's rules card 2026-05-13 and asked which to adopt. Triaged 13 rules → 5 already aligned, 3 skipped (push-tier cue-drop, plan-required threshold, verbatim scope-creep template — all project-specific to voyager), 4 strong adoptions shipped, 1 modest mentioned but not yet asked. Adoptions: **(1) Strict clean-pass clause** added to Plan-audit standard's iterative-loop — "fixed inline + declared clean" doesn't satisfy the gate; pass must be re-run on revised plan from the top. **(2) Capability honesty subsection** added as new top-level CLAUDE.md section near Pre-push visual verification — Claude doesn't offer capabilities it doesn't have (no pulling mobile logs / no real-Android verify / no prod state access without WebFetch + URL). Examples tailored to GT (mobile-only PWA, Vercel deploy, debug log page workflow). **(3) Phase 1 declaration format expanded** — now includes In-scope / Out-of-scope / Success criteria fields (XS chats can use 1-liners or "n/a — Q&A only"). Explicit Out-of-scope is the counter-creep guard. **(4) 3-buckets + 2-shapes** Phase 5 close-out structure adopted alongside Rules A/B/C — every close enumerates Parked items / Deferred audits / Dogfood findings (empty bucket → "none this chat" explicitly); close-out picks Handoff shape (new chat) or In-place shape (sub-purpose mid-chat). Doc-only ship.

- **Rule C — Recommended option marker on every AskUserQuestion locked.** Every `AskUserQuestion` must mark Claude's recommended option first with `(Recommended)` appended. Applies to all `AskUserQuestion`s in chat, not just close-out. Placed in Phase 5 alongside Rules A + B. Reinforces RULES CARD #4 "Don't decide silently." User flagged inconsistency: some prior asks had the marker, others didn't.

- **Plan-audit iterative-loop language locked.** Plan-audit standard now explicitly says: audit is iterative, not one-shot; each pass that surfaces concerns triggers plan revision + re-audit of revised plan; loop runs until pass returns clean (no findings or only immaterial). Treats "minimum 2 audit passes" as the FLOOR per audit cycle, not the ceiling for total passes. Closes ambiguity that existed since 2026-05-12 plan-audit standard lock.

- **Chat Lifecycle Protocol Phase 5 amended — Rules A + B (close-out cleanliness) locked.** User flagged 2026-05-13 (same session as protocol-lock) that the just-shipped close-out was failing on the very loose-ends problem the protocol was meant to fix: three dangling recommendations from my handoff (merge strategy, UserPromptSubmit hook, next-chat purpose) described as text-mentions ("your call", "your option") rather than asked explicitly. User: *"i typically just copy/paste the handoff doc and move on at that point so i want it to clean up and tie out any loose ends."* **Rule A — Explicit-approval asks on recommendations:** any non-auto-shipped recommendation Claude surfaces must be converted to an explicit AskUserQuestion at close-out, with rule/option content **self-contained in the question text** (not just referenced by name). **Rule B — Tie-out-loose-ends gate:** Phase 5 doesn't complete until every loose end is either asked-with-answer OR queued in next-chat handoff with a clear ask attached. Vague directional language doesn't satisfy the gate. Both rules added directly inside the Phase 5 section of CLAUDE.md (alongside the uncovered-work register). Drift this session that motivated the self-contained-question-text addendum: my first AskUserQuestion referenced "Rule A" / "Rule B" by name without embedding the rule text in the question — user couldn't see the rules from the question UI view (different from chat scrollback). Now part of Rule A.

- **Chat Lifecycle Protocol locked.** Single 5-phase protocol added to CLAUDE.md stitching kickoff → plan readiness → execute (amendment = re-audit) → verification (definition-of-done) → close (uncovered-work register clean). Lowers plan-audit threshold from "non-trivial work" to every chat purpose; XS purposes get a 3-line plan + ~60-90s audit. Closes all four drift shapes the user named (purpose / plan-amendment / premature-done / lost-thread close-out). RULES CARD #3 reworded to point at new section. **Compliance-enforcement layer:** "Phase declarations" subsection requires Claude to output the active phase in text at three trigger points — chat open (3-line block), every phase transition (1-line), every plan amendment (1-line). Missing or wrong declaration = visibly skipped phase, catchable by user in seconds. Replaces the "user has to keep reminding Claude of the rules" failure mode with self-narration the user can verify pass-by-pass. Recovery clause: when the user does have to remind, Claude acknowledges briefly, declares the missed phase, executes the gate, moves on (no defensive apology). Override clause: trivial Q&A exempt from declarations; anything touching code/docs/commits/pushes must declare. Plan file at `.claude/plans/okay-im-running-into-delegated-boole.md` retains full 2-round audit trail (10 findings resolved across both rounds). Doc-only ship, no code touched.
- **§3.10 R1-R5 doc-drift batch shipped (`884cf9d`).** Single doc-only commit closing the five strict drift items the user-delivered audit flagged 2026-05-12. **Pattern variance:** BUGS.md U1+U3 closures used inline `✅ Fixed (verified date)` annotations matching U21-U23's in-file convention, rather than the audit's "move to Resolved (Closed)" recommendation. Reason: less structural churn, U-numbers stay anchored for historical reference, matches already-shipped pattern. Surface for any future audit that re-suggests the "move" approach.
- **R5 lock-hygiene note.** VISION §11 icon-density entry — only the factual count was edited (`~25+` → `42`, manually counted in `ICON_MAP` at `src/lib/styleDictionary.tsx:484-526`); the parked-decision substance (stroke 1.2 canonical recommended; `styleDictionary.tsx` as larger library; counter stroke 2.0; parked awaiting user) is untouched. No silent overstep on a parked decision. 3-pass plan-audit triggered Pass 3 specifically to gate this brush.
- **§5/§6 ROADMAP entries split into follow-up log commit.** Rather than including the §5 Recently-shipped + §6 Decision-log entries in the R1-R5 ship commit (which would have required a `<this commit>` placeholder for its own hash), split into a follow-up `docs:` log commit so the §5 entry can reference `884cf9d` directly. Mirrors the `3de1c2c` ship + `c744c8b` log pattern from 2026-05-12.

### 2026-05-12

- **User-delivered doc audit received and triaged into §3.10.** External audit (possibly run in another Claude session) flagged 7 🔴 strict drift items (test count 329 vs 387, WORKFLOW.md "Seed Vault" stale name, BUGS U1+U3 silently fixed, BUGS+VISION stale stamps, icon count ~25+ vs 42), 3 🟠 structural items (PROJECT_STATUS.md canonical-vs-archived ambiguity, WORKFLOW.md missing 5 procedural rules from 2026-05-11/12, BACKLOG.md `.cursor/plans/` ref), and 2 🟣 future items (~30 legacy docs disposition, optional `.claude/settings.json` setup). All cross-referenced — most are new captures. Strict fixes (R1-R5) ready to ship as one doc commit in next session; structural fixes (R6-R8) + future (D1-D2) need scoping decisions. Audit document itself is preserved in this session's transcript.
- **Chunk 3.9 #2 + #4 batch shipped (`3de1c2c`).** Two XS fixes bundled per WORKFLOW §"Batching small fixes." **#2 calendar arrows:** hidden below `xl:1280px` via `hidden xl:flex` matching B1/B2 family. Alternative breakpoints considered: `lg:1024px` (would split iPad-landscape inconsistently with the rest of the desktop pass), always-hidden (would require keyboard-arrow nav on desktop — out of scope). `xl:` won on cohesion grounds. **#4 garden trigger icon:** swapped to `ICON_MAP.Edit` (pen with motion). Alternatives `Journal` (lined paper), `Pencil`, and a new composite `Journal+Plus` overlay were considered; `Edit` won on (a) zero new icon work (already in styleDictionary), (b) strongest "log/write" semantic, (c) visually distinct from all 10 entry-type markers (no basket curve, no leaf, no droplet). Scope A locked over Scope B (also retag the `care` marker) and Scope C (full entry-type vocabulary audit) — user opted for minimal scope. The secondary cohesion question (Harvest basket + JournalCareHands cupped curve reading as same family within the entry-type set) is now visible and noted for future Phase 6 design-system pass.
- **ROADMAP §3.9 #3 (garden card spacing) parked for future batch.** User identified the empty-space-below-short-names issue, Claude diagnosed `min-h-[1.75rem]` on the h3 at `ActiveGardenView.tsx:1075` as the root cause, and 3 fix options were surfaced with trade-offs (remove min-h / reduce / move space via justify-end). User opted to think through the alignment-vs-tightness trade-off in its own batch rather than bundle with #2+#4. Parked entry in §4 preserves the full diagnosis + options so future-Claude doesn't re-discover them.
- **ROADMAP §3.9 #4 mis-triage learning.** During the 2026-05-12 12-item batch triage, the user's "bag icon" feedback was assumed to be the shopping-list icon in the mobile header. Actual surface was `JournalCareHands` on Garden plant cards (at small sizes the cupped-hands silhouette reads as a basket/bag). Mismatch surfaced when planning chunk 3.9 #4 — Claude initially walked through shopping-list icon candidates (cart/basket/checklist/clipboard); user corrected to the garden trigger surface. Reinforcement of the existing "search existing captures FIRST + clarify ambiguous items before locking triage" rule — when a vague visual cue ("bag") could plausibly map to multiple surfaces, ask user to clarify the surface during triage. Captured here for decision-log visibility, not in CLAUDE.md (rule already exists).
- **Calendar expand-all toggle shipped (`6d76c20`, chunk 3.9 #1).** First chunk-3.9 build item shipped after journal-search. Master state derived from existing `expandedDateGroups` Set (no new state shape). `allDateKeys` memo lists "overdue" (if any tasks) + every date in `byDate` + today if it has completed-only. Button hidden when `allDateKeys.length === 0`. Smooth content animation via `grid-template-rows: 0fr ↔ 1fr` transition — chosen over `max-height` hack (which requires arbitrary upper bound) because content height varies wildly per section. Animation applies to both master and per-section toggles for consistency — would have created jarring inconsistency to animate only one. Initial render at default-collapsed doesn't animate (CSS transitions skip first paint), so the page-load behavior from `a7dadb7` is unchanged. Pre-push mobile-viewport check via Preview MCP attempted per rule `aad2e56` but Calendar is behind `AuthGuard` → redirect loop → skipped per the rule's documented "auth-blocked" condition. Build error during execution (`ICON_MAP is not defined`) was caught by `npm run build` and fixed before push; surfaced the new plan-audit standard's Pass 1 factual rubric (`eea6a84`).
- **Plan-audit standard locked (`eea6a84`).** Three-pass taxonomy with concrete content per pass (Factual / Semantic+edge / Lock hygiene). Min 2 passes baseline; 3 passes for contexts / nav / locked-decision changes. Plan file required at ≥3 files or state-machine changes. Explore agent at >5 files. Mid-session scope additions need fresh Pass 1+2+3. User explicitly flagged this as the pattern she most often re-enforces; codifying the rubric removes ambiguity in "loop until clean."
- **Pre-push visual verification rule locked (`aad2e56`).** Preview MCP mobile-viewport check required on visual ships (`.tsx`/`.css` diffs). Honest about what it catches (general layout, console errors, overflow) vs what it doesn't (Android Chrome 100vh quirk, webkit pseudos, hardware back). Skip conditions documented (auth-blocked, cold start >60s, pure logic).
- **FAB UX fix batch shipped (`79836f4`, U22 + U23).** Two user-reported fixes bundled per WORKFLOW.md "3-5 small fixes" cadence and shared surface (FAB). **U22 — Android back closes FAB:** wired `useModalBackClose` on 4 pages where `skipPopOnNavigateRef` was orphaned (already declared and set on 28+ nav handlers but never read by a hook). Discovery: a prior session built half the infrastructure; this fix completed it. Vault unchanged (correct via umbrella pattern). **U23 — FAB viewport cutoff:** root cause was `top-1/2 -translate-y-1/2 max-h-[85vh]` on Chrome Android where `100vh` reflects the idealized URL-bar-collapsed viewport, not visible space. Switched to flex-wrapper + `max-h-[85svh]`. Animation/backdrop preserved. Trade-off recorded: Android back on add-plant sub-screen closes the whole FAB rather than going up one level — matches user's explicit "close FAB" ask; in-menu Back button still works for sub-level nav. Rejected alternative: putting `useModalBackClose` inside `UniversalAddMenu` itself — would double-pushState on Vault (umbrella pattern already covers `addMenuOpen`) and violate the existing regression-test rule "child must not duplicate parent's back-close" (`calendarBatchSelect.regression.test.ts:202-204`). 5-pass plan-audit terminated clean.
- **U21 fix shipped (`fb67e10`).** Only follow-up bug from `7482c59` journal-search ship now closed. CSS-only suppression via `[&::-webkit-search-cancel-button]:appearance-none` Tailwind arbitrary variant. Rejected alternatives: switching to `type="text"` (loses search semantics + history dropdown + mobile keyboard "Search" key) and removing the custom X (regresses iOS Safari which doesn't render the native button). Scope locked at journal (1 file, 1 line) after app-wide audit of 7 `type="search"` inputs confirmed only journal had a manual clear-X duplicate. Pattern documented inline; no shared utility extracted per "don't add abstractions beyond what the task requires." 4-pass plan-audit terminated clean.
- **CLAUDE.md restructure: top-of-doc Rules Card added.** Multiple drift events this session signaled that load-bearing rules were buried inside ~400 lines of CLAUDE.md and being missed during turn-by-turn responses. Added a 🪪 RULES CARD section at the top of CLAUDE.md as the first thing future-Claude reads each session. 8 bullets covering: required reading, feedback-batch Step 0 search, plan-audit-build cadence, aesthetic decisions, PM/feature-creep enforcement, push tiers, end-with-where-we-are, session-switch signals. Card links to detail sections below for "why" and "how." Plus: created `docs/CLAUDE_CODE_SETUP.md` with paste-ready `.claude/settings.json` snippet for an optional `UserPromptSubmit` hook that injects the rules card into every prompt — user-owned, user-enables. Structural fix for the "Claude keeps drifting from rules I locked yesterday" problem the user flagged in this session.
- **Debug log page shipped (`ed5441c`).** Built specifically so user can copy-paste captured console output into bug-report conversations (no screenshots of tiny phone text). Scope: 50-entry rolling buffer in sessionStorage, captures log/warn/error, page UI with Copy/Refresh/Clear. Explicitly DEVELOPER TOOLING, not user-facing feature — bounded scope (no filters, no persistence across reload, no network/render-warning capture, no enable-disable UI). Reusable for future bug-debugging. Per the PM/feature-creep rule the user locked earlier today: this was initially push-back'd as feature creep, then re-evaluated because (a) tooling for the build process ≠ user-facing product feature, (b) user pushed back on push-back with clear use-case (copy-paste workflow), (c) scope guardrails kept it bounded.
- **New procedural rule locked: PM enforcement on feature creep / off-track requests.** User explicitly framed Claude as "project manager and coder" and asked for enforcement when requests pull project off-track. Captured in CLAUDE.md "Feature creep enforcement" subsection. Behavior: push back plainly, propose deferral, surface scope, then respect override if user wants it anyway after hearing cost. Reinforces existing "Push back when needed" project-lead obligation.
- **Journal search shipped (`7482c59`).** First item shipped from chunk 3.9 (2026-05-12 batch). Search field above view-toggle row matching Vault/Garden pattern. Multi-token AND, fields: note, entry_type (underscore-stripped), plant_name + display_names, formatted date. All three view modes. Pure filter function in `src/lib/journalSearch.ts` (testable without React). 8 new vitest cases (378/378). 3-pass plan-audit + 2-pass self-audit, both clean.
- **Decision: multi-token AND match.** "tomato 4/21" only matches entries with BOTH "tomato" AND "4/21" somewhere in searchable fields. Conventional for filter-as-you-type search.
- **Decision: no derived-action-label in search haystack.** Could include "Planted"/"Harvest"/"Care" labels derived from `getActionFromNote`, but it'd duplicate that logic in the search helper. Instead, the raw `entry_type` (with underscores → spaces) is in the haystack, and most notes contain the action verb naturally. Edge case: entry with null note and entry_type "planting" won't match query "planted" (substring miss). Acceptable for v1; refine if user reports.
- **Decision: no clear-X button matches Vault... no wait, added clear-X.** Vault doesn't have a manual clear-X (relies on browser native for type="search"). Journal adds a manual clear-X for consistent UX on iOS Safari (where native clear doesn't appear). Minor deviation from Vault; can backport later if cross-app consistency matters more.
- **`a7dadb7` verified clean in prod.** User's phone screenshots show "Overdue (42 tasks)" and "Overdue (41 tasks)" collapsed at top of Calendar with Upcoming Tasks expanded below. Default-collapse rule shipping confirmed.
- **New procedural rule locked: "Handling feedback batches."** When user drops a multi-item batch in one message, Claude owns triage into 🔵 current build / 🟣 future phase / ❌ outside scope. Triage gets presented in text (not bundled into a single AskUserQuestion), aesthetic items flagged separately for user input, conflicts with VISION.md surfaced for clarification, ROADMAP + VISION updated immediately, and a single closing question on where-to-start. Captured in CLAUDE.md.
- **New procedural rule locked: "Roadmap maintenance."** ROADMAP.md is a living doc — mark sections done as work progresses, amend chunks when info arrives, move items between buckets. Added a new step **3.5 Final ROADMAP review** to close-out protocol: between mid-session roadmap updates and retrospective scan, walk the full roadmap one more time to catch drift. Captured in CLAUDE.md.
- **Feedback batch 2026-05-12 (12 items) triaged.** 7 items 🔵 current build (new chunk 3.9): Calendar expand-all toggle, phone month-arrow redundancy, garden card spacing, bag icon swap, journal card format, journal search, profile no-AI-data fallback. 5 items 🟣 future / parked: welcome instructions (⚠️ VISION conflict, needs clarification), gallery-vs-table-view discussion (Phase 5), journal growing-indicator tags (Phase 3 IA), vault metadata filters (Phase 3 IA), harvest calculator (memory plane / Failure Mode #2). Each parked item logged in §4 with bucket reasoning.
- **No double-park.** Items 9-12 in the parked list reference existing chunks (Phase 3 IA, Phase 5 page goals, §3.6 plant profile depth) rather than creating duplicate parked entries.

### 2026-05-11

- **Calendar default-collapse rules shipped (`a7dadb7`).** Overdue section now collapsed on Calendar load. Today still auto-expands. Other days remain collapsed (unchanged). Tap-grid-day still switches to single-day mode (preserved). Single-line removal (`if (overdueTasks.length > 0) next.add("overdue");`) from the init effect in `src/app/calendar/page.tsx`. 3-pass plan-audit was clean (Pass 1 flagged a pre-existing race condition involving the separate `completedTasksForMonth` fetch; documented as known follow-up, not in scope per "no while-I'm-at-it scope creep"). -1 LOC, 370/370 tests, clean build.
- **Default-collapse rule set locked (overdue=collapsed, today=open, others=collapsed; tap-grid-day preserves single-day mode; count badges on collapsed headers).** Future-days default was the swing decision: locked as **collapsed** because task fatigue is the failure mode being solved (VISION §6, §11) and count badges already preserve at-a-glance scan.
- **AskUserQuestion bundling pattern caught and corrected.** When introducing a multi-item rule set for the first time, do NOT offer a single "Lock it all (Recommended)" AskUserQuestion — user picks Recommended efficiently but skips per-item digest, which surfaces later as "what do you think?" + interrupts to ExitPlanMode. Either (a) discuss verbally and let her engage per piece, then bundle-lock once she's clearly seen each; or (b) ask separately per sub-decision. Captured in CLAUDE.md and VISION §12.
- **Sidebar icon density flagged (parked widening of FAB icon consistency).** User saw the issue in B1's sidebar: primary nav (24×24 @ 2.0), Shopping list (20×20 @ 1.2), Settings/Feedback (20×20 @ 2.0), Help (text glyph `?`) — four different rendering systems in one component. Same parked decision as VISION §11 "FAB icon style consistency" from 2026-05-08, widened to "App-wide icon density / canonical stroke weight." Awaiting user decision on canonical: stroke 1.2 (recommended — `styleDictionary.tsx` is the larger of the two libraries) or stroke 2.0.

### 2026-05-11 (earlier)

- **Phase 4+5 Desktop Layout Pass — B2 shipped (`8624c8d`).** Second milestone: Calendar page two-column layout at `xl:1280px`. Wrapped existing return in `xl:flex xl:gap-6 xl:items-start` with two child wrapper divs (left 640px sticky + right `flex-1 max-w-[720px]`). Mobile byte-identical. Single-file mechanical wrap of `src/app/calendar/page.tsx` (+9/-2).
- **Sub-decisions locked for B2 (D1–D4):** D1 = sticky left column on desktop (so grid stays visible while task list scrolls — reinforces "primary check-in surface" role from VISION §7). D2 = right-column `max-w-[720px]` (comfortable row line length at 1920+; whitespace to the right is parked space for Phase 5). D3 = `gap-6` (24px, matches existing card-level spacing). D4 = keep 640px left column per A1–A7 / ROADMAP §3.2 (each day cell ~88px).
- **B2 plan-audit ran 4 passes before clean.** Pass 2 surfaced three real misses: (a) worst-case viewport squeeze at 1280px + expanded sidebar → right col ~328px (narrower than mobile); (b) sticky-position smoothness analysis was hand-wavy until math added (header 44 + `pt-2` 8 = 52px natural vs `top-12` = 48px sticky → smooth 4px scroll-then-stick, no jump); (c) loading-state behavior in the empty right col wasn't documented. Pass 3 caught a wording error in Risk 4. Pass 4 clean. Mitigation for Risk 4 parked as fast-follow if prod testing shows cramping at 1280px viewport.
- **Process correction reinforced from session start.** Initial audit attempt stopped at one pass; user flagged that WORKFLOW.md requires looping until findings are clean or immaterial. Re-ran with full discipline. Captured behaviorally — multi-pass audit is the default, not an option.
- **Phase 4+5 Desktop Layout Pass — B1 shipped (`e61ffdc`).** First milestone of the pass: app shell + sidebar nav at `xl:1280px`. Bottom nav hides, left sidebar appears (240px ↔ 64px collapsible, state persisted to localStorage). Five primary nav items reused from BottomNav. New shared `navItems.tsx` module. Top header simplified at `xl:`.
- **Breakpoint locked at `xl:` (1280px), not `lg:` (1024px).** Reason: iPad-landscape stays on mobile-style layout — a sidebar at iPad widths would feel cramped, and the existing app already uses `lg:` for the swipe/inline split (different concern, can coexist).
- **Scope decision: this pass touches every page (sidebar everywhere), but right-column layout is Calendar-only.** Other pages stay single-column main column until Phase 5 designs them. Keeps B1 bounded.
- **Sidebar shape locked (A2 + S1–S5):** 240px expanded / 64px collapsed, chevron toggle at top-right, state persisted to localStorage, active state = emerald left-edge accent bar + `bg-emerald-50` tint, icons identical to BottomNav (DRY via new shared module), small "Garden Tracker" wordmark at top, "Shopping list / Help / Settings / Feedback" live in sidebar footer (utilities, not sections).
- **Top header at `xl:` simplifies, doesn't disappear.** Cloud-sync + page title + household toggle stay; everything else moves to sidebar. Header items get `xl:hidden`; sidebar surfaces them on desktop.
- **FAB stays viewport-anchored at `xl:`.** Doesn't offset for sidebar. FAB → popover treatment is a separate concern (milestone B3).
- **Milestone breakdown for the pass (B1–B5).** Insert as ROADMAP §3.2 (renumbering 3.2 → 3.3, 3.3 → 3.4, etc.). B2 = Calendar two-column, B3 = FAB popover, B4 = modals/sheets centered-with-backdrop, B5 = per-page audit pass.
- **Communication pattern observed and captured in CLAUDE.md:** "I am ok with your recommendations" + explicit-starting-point naming (e.g. "A1 and A7") = **blanket trust-transfer on the batch of recommendations**, not a partial accept. Treat the full slate as locked; start where named; revisit individual items only on later redirect. Reduces friction without being silent on aesthetic decisions.

### 2026-05-10

- **Calendar consolidated-row apply-all shipped (`f77507a`).** User flagged a cohesion gap: singleton overdue rows had inline `[Snooze][Done]`, consolidated rows had only a chevron. Same row primitive should read the same top-to-bottom. Resolved by adding apply-all actions to consolidated rows.
- **Layout decision: `[Snooze][Done][Chevron]` on the right.** Preserves singleton-row button positions; chevron tacked on at the end. Two ways to expand (title-body button + chevron button), both `aria-expanded`. Recommended over `[Chevron][Snooze][Done]` to keep button order identical to singleton rows for vertical scan rhythm.
- **Safety decision: confirmation step (Option B over A).** Apply-all actions confirm before applying — "Mark all N as done?" sheet for complete-all; "Snooze all N tasks" sheet (Tomorrow / In 3 days / Next week chips + date picker) for snooze-all. Singletons stay no-confirm (single-tap speed retained); confirmation differentiates "applying to a group" from "applying to one" and matches the existing long-press batch-select pattern. Compensates for parked undo-toast.
- **Swipe primitive extraction: `useRowSwipe` hook.** Swipe gesture logic moved from inline `CalendarTaskRow` useEffect into a reusable hook (`{ enabled, onSwipeLeft, onSwipeRight } → { rowRef, swipeOffsetX, isSwiping }`). Consumed by both `CalendarTaskRow` and new `ConsolidatedOverdueHeader`. Mobile swipe parity on consolidated rows (Principle 9) ships in the same commit as the desktop button cluster.
- **State separation: `groupAction` kept distinct from `selectMode`/`selectedIds`/`batchActionOpen`.** Two parallel "bulk action" patterns now coexist: long-press multi-select (for arbitrary task selection across the page) and consolidated-row apply-all (for a pre-defined group). Different intents; both stay around.
- **Bulk handler patterns mirror existing batch flows.** `handleCompleteAllInGroup` and `handleSnoozeAllInGroup` follow the optimistic-UI + Promise.all + single-debounced-refetch pattern from `handleBatchReschedule`. Single toast on bulk completion ("N tasks completed") rather than per-task. Transplant→harvest cascade preserved per-task inside the bulk snooze handler.
- **Row primitive ("bubbles vs. list") remains parked.** The cohesion-of-actions issue is resolved by this ship, but the underlying `rounded-xl + white bg + emerald border + drop shadow` styling question is a separate concern still parked in VISION.md §11. Re-evaluate after this batch + the 2026-05-08 swipe batch both have prod time.
- **Process correction reinforced.** "yes build" is a contract that follows a complete plan-audit; sub-decisions surfaced during the build also get plan-audit treatment when reasonable people could disagree on the solution. (User explicit reminder this session.)

### 2026-05-08

- **Vision statement v4 locked.** Multi-tenant home garden management app. Memory + active management planes. Four roles. Multi-circle private sharing. Cost-driven freemium.
- **Audience layers locked:** self / household / trusted circle (incl. clubs). Invite-only forever. Never public.
- **Skill spectrum:** disclosure-by-data, no skill toggle.
- **Beds become a first-class entity** — implementation deferred to Phase 3.
- **Three lifecycle paths** named: seed (primary depth), established (acquired), division (lighter weight).
- **Four failure modes** named: lifecycle gaps, shallow profiles, untrusted timing, disorientation.
- **Cross-account exchange:** Level 3 (trade with history) ✅; Level 4 (paid) ❌; Level 5 (public) ❌.
- **Recipes / meal planning** ❌ Not ever (out of domain).
- **Direct hardware integration** ❌ Not ever; generic data import 🕐.
- **Micro-climate triggers** ✅ — anchored to user's actual garden, not zone-level.
- **Cost-driven freemium** locked: features with fixed cost free; features with variable cost (AI, beyond-limits, multi-circle) paid.
- **Plant database moderation** — concern raised, Phase 3 problem; recommended hybrid model.
- **Project documentation:** VISION.md (canonical truth), CLAUDE.md (Claude orientation + project-lead behaviors), WORKFLOW.md (process), ROADMAP.md (this file — where we are / what's next).
- **Project-lead behaviors codified** in CLAUDE.md after user flagged Claude was acting as smart respondent rather than lead.
- **User communication patterns captured** in CLAUDE.md for future Claude sessions.
- **Session transition management framework** added to CLAUDE.md — defines when Claude should proactively suggest switching chats and the close-out protocol to run when switching. Goal: chat-to-chat handoffs are smooth without the user tracking chat length.
- **Calendar fatigue batch shipped (build chunk 3.1).** Overdue consolidation key: `(title, plant_profile_id, grow_instance_id, user_id)` — `user_id` in key prevents household-member task merging. Singletons render flat (no fake nesting). "Select all in group" reuses existing long-press batch-select rather than introducing parallel checkbox UI. Per-date header tint = `bg-emerald-50/40` mirrors Overdue's amber pattern. Adjacent-month cells = `bg-neutral-100` (Option A) for unambiguous "not part of month" cue. Undo toast deferred to future design-system pass.
- **Row primitive ("bubbles vs. list") parked** in VISION.md §11 — re-evaluate after Calendar fatigue batch deploys; consolidation may resolve reading problem before we touch the row primitive. Recommended approach if needed: drop shadow + replace card border with bottom-divider + drop rounding + keep snooze/complete actions inline. Pure checkboxes go too far (lose action affordances + category color signal).

### 2026-05-07

- **Plant placeholder is `/public/plant-placeholder.png`** (three-leaf PNG).
- **Placeholder container background is `bg-white`** — matches PNG, no seam.
- **FAB transition standard** locked: fade-scale (open/close), horizontal-slide (submenu), slide-up (target modal). 200ms in / 150ms out. **Amended 2026-05-20 (Path Y refactor, U7 close — `d49e1ae`):** all 5 add-button picks now classified as **submenus** (in-place horizontal slide via `animate-submenu-slide-forward` / `animate-submenu-slide-back`), not just Add Plant. The "target modal" classification (slide-up) now applies only to standalone modal mounts reached via non-FAB callers (onboarding "Observe Hillside" → NewTaskModal; vault Plant Again → QuickAddSeed; QuickLog "+ Add New Supply" cross-modal → QuickAddSupply; etc.) — those keep `animate-modal-slide-up`. The add-button menu container itself never unmounts during the FAB flow; only the inner sub-screen content swaps via the submenu-slide keyframes.
- **Don't make aesthetic decisions without asking** rule added to WORKFLOW.md.
- **Batch small fixes 3-5 at a time** rule added to WORKFLOW.md.
- **Plan-audit-build for non-trivial work** rule added to WORKFLOW.md.

---

## 7. How to update this file

At the end of any session that includes meaningful work:

1. Update **§1 Current focus** — what's the next session's starting point.
2. Update **§3 Build chunks** if any item changed status (🔵 → 🟡 → 🟢 → ✅).
3. Add to **§5 Recently shipped** any new commits, with hash and brief description.
4. Add to **§6 Decision log** any locked-in decisions made this session.
5. Update **§4 Parked items** if anything got newly parked or unparked.

If a session does NOT touch the roadmap meaningfully (small bug fix only), at minimum update §5 (recently shipped).

---

### Archive policy (locked 2026-05-16)

ROADMAP.md is read by every session per RULES CARD #1. Long doc → drift-prone (the 2026-05-16 alignment audit demonstrated exactly this). Keep this file lean via three numeric caps:

- **§1 lead-line cap = 2 paragraphs.** Current chat's lead-line + the immediately prior. Older dated paragraphs always have corresponding §6 entries — they're redundant. When adding a new lead-line, drop the third-oldest.
- **§5 cap = ~10 entries.** The "(last ~10)" header is the rule, not a suggestion. When adding entry 11, drop the oldest. Full commit history lives in `git log`; date stamps in §6 entries preserve the timeline.
- **§6 cap = ~14 days inline.** When entries older than 14 days exist, move them to `docs/archive/ROADMAP_log_YYYY-MM.md` (one file per month). No file needed yet (project started 2026-05-07; all entries within window as of 2026-05-16). When the archive file is created, add a `(see docs/archive/...)` pointer at the bottom of §6.

**Known follow-up surfaces (not part of this archive policy, but related drift):** §1 has stale per-chat subsections ("Just shipped this session," "Verified clean," "Still TBD verification," "New feedback batch — triaged") that persist across chats as snapshots. Consider draining them in a future doc-hygiene pass. The bigger structural fix is the AGENTS.md doc-architecture split (parked in VISION §11 2026-05-16).
