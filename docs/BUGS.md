# Known Issues & Bug Triage

**Last updated:** 2026-05-19 (`trusting-bassi-1cc9ff` chat — paperwork-gap closure: U5 + U26 marked ✅ Fixed retroactively. U5 actually fixed 2026-05-08 by `4536897` (`flex justify-center truncate` → `line-clamp-2` on Active Garden gallery card title); U26 functionally addressed 2026-05-17 by `efe588d` (`hidden xl:flex` → `hidden md:flex` on Calendar month-nav arrows). Both BUGS.md rows had been left unmarked; this chat re-investigated and confirmed via codebase grep + git log that both are resolved in code. The original `pointer-coarse:hidden` recommendation for U26 was overtaken by the 2026-05-17 Walter persona lock and noted in the row. No code changes this commit. Previous: 2026-05-17 (`flamboyant-wilson-34fc0c` chat — U27 captured as ADJACENT finding during §3.12-pre dev-tools UI gating ship `4a8b04c`. 4 `/api/developer/*` server-side routes accept any authed user — defense-in-depth gap; deferred to follow-up security-hardening ship per Q2 Rule A lock in `binary-valley`. Previous: 2026-05-16 (later) U25 ✅ CLOSED on Garden tabs via `11eaf37`; Vault profile sites parked per cross-surface scope-check sub-clause. Earlier 2026-05-16: U24 ✅ CLOSED — Phase A instrumented build surfaced PGRST204 on bulk-end; root cause = `ended_at` column missing from prod Supabase; user applied catch-up DDL via Dashboard SQL Editor; retry succeeded. Plus migration audit run: only ONE additional column gap found across all public tables — `user_settings.onboarding_completed_at` — also fixed via Dashboard. Root cause of both gaps: two migrations with identical timestamp prefix `20250330000000` → ordering ambiguity → silent skip. Captured as process bug.))
**Owner:** Part 5 (Bug Fix and Hardening)

All items are triaged as **must-fix before release**, **post-launch**, or **user-reported (2026-05-07)**. Must-fix items are tracked here until closed; post-launch items move to [BACKLOG.md](BACKLOG.md) when deferred.

For systemic / pattern-level analysis of these bugs, see [PRODUCT_AUDIT_2026-05-07.md](PRODUCT_AUDIT_2026-05-07.md). Several bugs below are likely the same underlying issue (e.g., truncation handling, modal layering).

---

## Must-Fix Before Release

| # | Area | Issue | Status | Notes |
|---|------|-------|--------|-------|
| 1 | Law 4 / Security | `save-hero-from-url` API route uploaded raw web images (5–10 MB) without Sharp compression | **Fixed** (2026-03-11) | Added Sharp `.resize(1200×1200).jpeg({ quality: 85 })` before upload, matching other server-side routes. |
| 2 | Auth / PKCE | Signup email verification froze; password reset redirected to wrong domain | **Fixed** (prior session) | Implemented `/auth/callback` PKCE exchange page; updated `signup`, `reset-password`, `update-password` pages; added `AuthGuard` exemption. |
| 3 | UX | Seed packet Notes and Location fields stripped spaces mid-typing | **Fixed** (prior session) | `updatePacketNotes` / `updatePacketStorageLocation` in `vault/[id]/page.tsx` now only trims on `onBlur` persist, not on live `onChange`. |

---

## Post-Launch (Deferred)

| # | Area | Issue | Effort | Source |
|---|------|-------|--------|--------|
| 1 | Scheduling | `plantingWindow.ts` does not use `user_settings.planting_zone` for zone-aware sow windows. Uses static month ranges only. | Medium | `plantingWindow.ts` TODO comment |
| 2 | Navigation | `?grow=` URL param from vault profile → Active Garden scrolls to batch, but only if the batch is currently growing. Ended/archived batches don't highlight. | Low | NAVIGATION_ISSUES_LOG |
| 3 | UX | Stroke weight inconsistency: inline SVGs outside `ICON_MAP` still use `strokeWidth="2"` or `"2.5"` in a few secondary locations (e.g. calendar, some edit modals). Core interactive icons are consistent. | Low | TECHNICAL_AUDIT_ACTION_CENTER_AND_EDIT |
| 4 | Feature | Plant → Harvest E2E test not written (sow + harvest automation). | Medium | Part 4.1 deferred |
| 5 | UX | Full-screen drawer (Edit Grow, EditPacketModal) has no `visualViewport` resize handler for virtual keyboard. Only needed if users report inputs hidden behind keyboard. | Low | TECHNICAL_AUDIT_ACTION_CENTER_AND_EDIT |

---

## Resolved (Closed)

| # | Area | Issue | Fix date |
|---|------|-------|----------|
| 1 | Security | `plant_varieties` table had no RLS — any user could read/update any plant by id | 2025-02-05 (migration `20250205600000_plant_varieties_rls.sql`) |
| 2 | Data | `plant_profiles` missing RLS `WITH CHECK` on INSERT | 2025-02-12 (migration `20250212200000_plant_profiles_seed_packets_rls_with_check.sql`) |
| 3 | UX | `AddPlantModal` wrote to both `purchase_vendor` and `purchase_nursery` | Fixed (prior audit wave) |
| 4 | UX | `QuickAddSeed` locked-in chip briefly empty when `preSelectedProfileId` loads async | Fixed — `lockedInVarietyLabel` now falls back to `profileDisplayName` prop while profiles load |
| 5 | UX | Edit Plant Profile modal did not show save failure message | Fixed (Part 3.3 audit wave) — error renders in modal footer |

---

## User-reported issues (2026-05-07 feedback session)

Concrete, reproducible issues raised during a live testing session. Each is contained — most are quick fixes. Patterns across these are analyzed in [PRODUCT_AUDIT_2026-05-07.md](PRODUCT_AUDIT_2026-05-07.md).

### Bugs (specific reproductions)

| # | Surface | Issue | Likely fix | Effort |
|---|---------|-------|------------|--------|
| U1 | Home — Quick Start | "Set up your zone" step persists after zone is set in `user_settings`. ✅ **Fixed (verified 2026-05-12)** — "Set up your zone" string absent from `src/app/page.tsx` and entire `src/`; widget removed. | Read `user_settings.planting_zone` and gate the step | ~1h |
| U2 | FAB sub-modals | Background visible & interactable behind sub-modal (e.g., open "Add Seed", parent menu still scrollable) | Audit modal scroll-lock + backdrop primitive globally — likely affects all modals (see PRODUCT_AUDIT Pattern 4) | ~2-4h (one-time, app-wide) |
| U3 | QuickAddSeed manual | Variety dropdown not alphabetical — hard to find entries. ✅ **Fixed (verified 2026-05-12)** — `src/components/QuickAddSeed.tsx:213` sorts via `localeCompare(a, b, undefined, { sensitivity: "base" })` before render. | Sort the array before render | ~10 min |
| U4 | Journal page header | "Journal" truncates to "Jou..." on small screens | Responsive header sizing or shorter label | ~30 min |
| U5 | Active Garden gallery cards | Plant names clip from the LEFT ("rtichoke", "umber", "mato"). ✅ **Fixed 2026-05-08 (`4536897`) · verified 2026-05-19** — root cause was `flex justify-center truncate` on the gallery card title, which center-aligns then clips with `text-overflow: ellipsis` (renders ellipsis only at the end, so a centered, ellipsised, single-line word loses both ends visually — looks like left-clip). Fix replaced `truncate` with `line-clamp-2` at [ActiveGardenView.tsx:1094](src/components/ActiveGardenView.tsx:1094); long names now wrap to 2 lines with no clipping. Codebase-wide grep 2026-05-19 (`justify-center.*truncate` + `text-center.*truncate`) confirmed no plant-name surface still uses the buggy pattern; the only remaining `text-center + truncate` is on the page-title h1 in [AuthGuard.tsx:205](src/components/AuthGuard.tsx:205) + [:307](src/components/AuthGuard.tsx:307), which is U4 territory (also fixed by `4536897` via removing the fixed-width Personal/Family pill). Re-flagged 2026-05-19 in `trusting-bassi-1cc9ff` chat as "still open per BUGS.md" — investigation confirmed BUGS row was a paperwork gap, not a live bug. | Switch to right-side ellipsis (`text-overflow: ellipsis; overflow: hidden; white-space: nowrap`) — original recommendation. Actual fix used `line-clamp-2` (2-line wrap) which is friendlier to long variety names than single-line `truncate` would have been. |
| U6 | FAB main vs submenus | Main menu icons styled differently from submenu icons | Unify under ICON_MAP — part of design system pass | Bundle with U10 |
| U7 | FAB transitions | "Add seed" smooth in-place transition; other 3 submenus close + reopen — feels glitchy. ✅ **Fixed 2026-05-20 (Path Y refactor — `bee5338` + `6652a25` + `b9cb40b` + `c812a5a` + `d49e1ae` on top of revert `143a07f`)** — all 5 add-button picks now use the in-place horizontal slide that Add Plant uses. Form bodies extracted as `<TaskForm>` / `<JournalEntryForm>` / `<SupplyForm>` / `<SeedPacketForm>` named exports; UniversalAddMenu hosts them via new `screen === "seed" | "shed" | "task" | "journal"` branches. Menu container never unmounts during the FAB flow. Standalone modal shells preserved for non-FAB callers (onboarding, deep-link, vault Plant Again, QuickLog cross-modal "+ Add New Supply"). Two accepted regressions vs prior standalone-modal behavior: (a) cross-modal "+ Add New Supply" from menu-internal JournalEntryForm closes the menu and does NOT auto-resume on journal sub-screen after QuickAddSupply closes; (b) pages don't auto-refresh after in-menu successful add (page-side `onJournalAdded`/`onSuccess` refetch hooks only fire on standalone-modal mount path now). Both per Q1 Option B locked at greenlight — minimize complexity. Followed prior misfire `e7d59d7` which used choreographed close-then-open coordination; reverted via `143a07f` before Path Y refactor landed. | Pick one model (in-place sub-screen for all) and align | ~10h actual |
| U8 | FAB submenu headers | Header positions / styling differ between submenus | Standardize | ~1-2h |
| U9 | Active Garden cards | Selection / highlight border looks visually off | Tune border weight / color (part of design system pass) | Bundle with U10 |
| U10 | Throughout | Seedling placeholder for missing hero is visually unpleasant | Design new placeholder (icon, illustration, or initials) — part of design system pass | ~2-4h |
| U11 | Active Garden cards | Quick Log button uses ICON_MAP.Journal which reads ambiguous at small sizes | Replace with clearer icon OR add label | ~30 min |
| U12 | Journal page | Two ways to add an entry ("+Entry" button + FAB → Add journal) — redundant *(re-flagged 2026-05-12 — user verifying journal page after `7482c59` search ship)* | UX coherence decision: keep both with differentiation, or remove one | Decision + ~30 min |
| U13 | Journal entry cards | Visual hierarchy poor — eyes dart between date, badge, note, pills; plant variety mingled with description *(re-flagged 2026-05-12 — user feedback batch item #6)* | Lead with note in heavier weight, demote date, group type+pills; structured header + body + tags | ~1-2h |
| U14 | Journal entry cards | Spacing between cards too tight | `space-y` increase OR adjust card padding | ~15 min |
| U15 | AddPlantModal | "Permanent / Seasonal" terminology inconsistent with "Active Garden / My Plants" tab labels | Pick one terminology and propagate (UX coherence — see Pattern 6) | ~1h + decision |
| U16 | AddPlantModal | Permanent/Seasonal toggle redundant when already chosen via FAB sub-screen | Hide toggle when `defaultProfileType` already set | ~30 min |
| U17 | Journal page | View-mode icons (Table / Gallery / Timeline) unlabeled | Add tooltips or visible labels | ~30 min |
| U18 | Calendar page | Header (May 2026 + arrows + Plantable banner) takes too much vertical space; calendar grid feels secondary | Compact month nav inline, demote Plantable to smaller link, reduce padding | ~2-3h |
| U19 | Home page | Bottom half (Shopping list, At a glance) lacks visual hierarchy compared to top half | Consistent card style; soften "Done" buttons; consolidate or restructure | ~2-3h |
| U20 | Home page | Sections load staggered on app open (top first, bottom pops in later) | Skeleton placeholders for all sections at first paint, OR batched data fetch | ~2-3h |
| U21 | Journal search input (`7482c59`) | Two clear-X buttons visible when query is non-empty on Android Chrome — one grey (custom React clear-X) and one blue (browser-native `type="search"` cancel button). Reported 2026-05-12 immediately after `7482c59` shipped. ✅ **Fixed 2026-05-12 (`fb67e10`)** | Hide native button via Tailwind arbitrary variant `[&::-webkit-search-cancel-button]:appearance-none` on the search input; custom X remains as the single control | ~5 min (1-line CSS) |
| U22 | Universal Add Menu (FAB) | Android back button does not close the FAB menu — only Escape (keyboard) and Cancel button (often unreachable per U23) work. Reported 2026-05-12. ✅ **Fixed 2026-05-12 (`79836f4`)** | Wire `useModalBackClose` for `addMenuOpen` on Home, Garden, Journal, Calendar (Vault already correct via `anyModalOpen` umbrella). `skipPopOnNavigateRef` was already declared + set on every nav handler; the hook was the missing link. | ~10 min (4 imports + 4 hook calls) |
| U23 | Universal Add Menu (FAB) | Menu cut off by browser chrome / OS nav bar on phone — 2 of 5 options + Cancel button unreachable behind nav. Reported 2026-05-12 (Garden page screenshot, but app-wide). ✅ **Fixed 2026-05-12 (`79836f4`)** | Switch container from absolute-center (`top-1/2 -translate-y-1/2 max-h-[85vh]`) to flex-wrapper with `max-h-[85svh]`. `svh` excludes browser chrome reliably; flex centering avoids transform-stack conflict with the entrance animation. | ~5 min (1 file, +3 -1 lines) |
| U28 | FAB menu — Add Seed Packet picker only shows Manual Entry | ✅ **Fixed 2026-05-24 (`4e9fb25`)** — threaded the 4 missing callback props (`onOpenBatch` / `onOpenLinkImport` / `onOpenPurchaseOrder` / `onStartManualImport`) from each page's `<UniversalAddMenu>` through to `<SeedPacketForm>` in the menu's `screen === "seed"` branch. Sibling-sweep also caught `<SupplyForm>` missing `onOpenPurchaseOrder` + `onOpenBatchPhotoImport` — fixed in same commit. 6 new optional props on `UniversalAddMenuProps`; each of the 5 pages wraps the same callbacks they already pass to standalone modal mounts, but uses `closeAll()` instead of `closeActiveModal()` since the callbacks now fire from the menu surface (not from a standalone modal). Reported 2026-05-23 by user during phone use: *"Add Seed Packet inside the FAB menu — only the Manual entry button shows. Previously there were 4 buttons (Manual entry / From Vault / Scan purchase order / Photo import)."* (User mis-named "From Vault" — actual missing buttons are Photo Import / Link Import / Purchase Order; "From Vault" exists on the Add Plant picker, a different surface.) **Strict bug per VISION §4 principle 3** (objective broken behavior — features regressed without user action). **Root cause:** Path Y Commit E (`d49e1ae`, 2026-05-19) wired `<SeedPacketForm>` to mount INSIDE `<UniversalAddMenu>` as the new `screen === "seed"` sub-screen, replacing the previous external-modal trigger pattern. The new wire-up at [UniversalAddMenu.tsx:254-258](src/components/UniversalAddMenu.tsx:254) passes only `onClose`, `onSuccess`, and `onBack` to `SeedPacketForm`. The 3 conditional callback props (`onOpenBatch` for Photo Import, `onOpenLinkImport` for Link Import, `onOpenPurchaseOrder` for Purchase Order) were NOT threaded through. `SeedPacketForm`'s choose step at [QuickAddSeed.tsx:533-582](src/components/QuickAddSeed.tsx:533) renders each non-Manual button only when its prop is set (`{onOpenBatch && (...)}` etc.); with all 3 undefined, only Manual Entry shows. Standalone `<QuickAddSeed>` callers (Vault Plant Again, QR-scan prefill, OnboardingDock, VaultPageContent autoload) still receive the props from their existing call sites, so this regression is FAB-menu-specific. | Thread the 3 callback props from `<UniversalAddMenu>` → `<SeedPacketForm>`. Sibling pattern already exists at UniversalAddMenu — `useUniversalAddModals()` is already consumed (per d49e1ae commit message, for the JournalEntryForm "+ Add New Supply" cross-modal). Either: (a) expose `openBatchImport / openLinkImport / openPurchaseOrder` from `useUniversalAddModals()` if not already, and pass to `<SeedPacketForm>` — restores prior 4-button behavior; OR (b) accept Manual-only-in-FAB-menu as deliberate and provide separate FAB main-menu picks for Photo Import / Link Import / Purchase Order. (a) restores prior behavior with smallest delta. **Sibling-sweep also warranted** on the other 3 form sub-components mounted in UniversalAddMenu (`SupplyForm` / `TaskForm` / `JournalEntryForm`) — same shape of regression possible. | ~30 min including sibling sweep. |
| U27 | `/api/developer/*` server-side routes — defense-in-depth gating gap | UI gate shipped `4a8b04c` (2026-05-17 `flamboyant-wilson-34fc0c` chat) hides Settings dev menu items + adds render-time "unlock required" gates on the 5 dev pages — but the 4 `/api/developer/*` route handlers ([backfill-cache/route.ts](src/app/api/developer/backfill-cache/route.ts), [backfill-plant-descriptions/route.ts](src/app/api/developer/backfill-plant-descriptions/route.ts), [usage/route.ts](src/app/api/developer/usage/route.ts), [usage/route.test.ts](src/app/api/developer/usage/route.test.ts)) check `Authorization: Bearer` but accept ANY authed user. A curl-savvy non-dev user could still hit these endpoints despite the UI being gated. **Captured as ADJACENT finding in Pass 3 sibling-sweep during `binary-valley` plan-mode (2026-05-17); Q2 Rule A lock chose UI-only scope for this ship to keep the persona-audit P0 (visible app-store blocker) decoupled from security hardening.** Different shape of work (server-side env check or context-equivalent in handler context vs. UI gating). Real but lower-urgency than the visible UI blocker — Sam-on-day-1 doesn't curl your API. **Recommended fix shape:** add a server-side dev check inside each handler — either (a) re-implement `useDeveloperUnlock` check on server (NOT possible, hook = client-side), or (b) add a separate server-side env-var allowlist (`DEV_USER_IDS` server-only), or (c) add an `is_developer` claim to Supabase auth JWT, or (d) accept the UI-only gating as sufficient (not "security" in the strict sense — these endpoints DO check auth, they just don't restrict by dev role). Most likely (b) or (d); (b) lets sister access dev tools if added to env; (d) is acceptable if the user concludes "dev tools are still tied to my data and the worst a curious user can do is repair/edit their OWN plant data, which they could do anyway via the UI." | Decision needed: gate via env-var server-side OR accept UI-only as sufficient. If gating: ~30 min (4 routes + server-side env helper). If accepting: 5 min doc note + close. |
| U26 | Calendar page — month-nav arrows on web browser | ✅ **Fixed 2026-05-17 (`efe588d`) · verified 2026-05-19** — original fix shape was `pointer-coarse:hidden` (input-capability media query). Reversed during the §3.12 theme #4 ship after the Walter persona was locked 2026-05-17 (iPad-primary, swipe-excluded). `pointer-coarse:hidden` would have *regressed* Walter — iPad reports a coarse pointer, so arrows would have hidden on iPad even though Walter explicitly needs them visible there. Actual fix: shifted the existing `hidden xl:flex` to `hidden md:flex` at [calendar/page.tsx:976+985](src/app/calendar/page.tsx:976) (plus 3 other Calendar surfaces in `efe588d`). Arrows now visible at ≥768px (iPad portrait, laptop, desktop browser at any viewport size). Phone portrait still uses swipe (no arrows below `md:`). Re-flagged 2026-05-19 in `trusting-bassi-1cc9ff` chat as "still open per BUGS.md recommendation" — investigation confirmed BUGS row was a paperwork gap (the recommendation was overtaken by the persona lock), not a live bug. Original report (2026-05-17 ecstatic-rubin-5f30c8 close-out): *"our web browser calendar is going to need arrows since no swipe action can be done on computer."* Resolved by the breakpoint shift — web browser at any width ≥768px now has arrows. Earlier history: `← May 2026 →` arrows originally hidden below `xl:1280px` via `hidden xl:flex` (shipped `3de1c2c` 2026-05-12 chunk 3.9 #2); that left sub-`xl` desktop viewports without month navigation. | Original recommendation `pointer-coarse:hidden`. Actual fix `hidden md:flex` (the §3.12 theme #4 ship) better serves Walter persona — see ROADMAP §1 lead-line for `efe588d`. |
| U25 | Garden — My Plants list (per-row "Add journal entry" trigger) | ✅ **Fixed 2026-05-16 (`11eaf37`) · ✅ Verified on prod 2026-05-17** — Garden tabs only. Vault profile Journal/Plantings tabs (3 additional sites at [VaultProfileJournalTab.tsx:51](src/app/vault/[id]/VaultProfileJournalTab.tsx:51) + [:67](src/app/vault/[id]/VaultProfileJournalTab.tsx:67) + [VaultProfilePlantingsTab.tsx:133](src/app/vault/[id]/VaultProfilePlantingsTab.tsx:133)) use `ICON_MAP.Journal` with a different visual primitive (labeled CTA at w-4 h-4 + "Add journal" text + icon-only w-4 h-4 vs. Garden's icon-only w-5 h-5) — explicitly parked for future cross-surface icon-cohesion pass per Pass 3 sibling sweep (locked in parallel chat `3d9a8a7`, ADJACENT classification); surfaced by Pass 2 iteration 1 audit and dispositioned via Rule A ask. See ROADMAP §4 "Cross-surface 'Add journal entry' icon cohesion — Vault profile tabs (parked 2026-05-16)" for full reasoning. Cross-surface icon mismatch with Active Garden. Active Garden gallery + list use `<ICON_MAP.Edit />` (pen-with-motion) at [ActiveGardenView.tsx:1110](src/components/ActiveGardenView.tsx:1110) + [:1258](src/components/ActiveGardenView.tsx:1258) — shipped via `3de1c2c` 2026-05-12 per chunk 3.9 #4. My Plants list uses an inline SVG of a notebook (book cover + horizontal page lines, `strokeWidth="2"`) at [MyPlantsView.tsx:893](src/components/MyPlantsView.tsx:893) — never updated. Same action ("Add journal entry") reads with two different icons across sibling Garden tabs. Reported 2026-05-16 by user phone-verifying `3de1c2c`: *"in garden, the add journal entry in active garden is pencil. the add journal entry in my plants is a paper."* **Strict bug per VISION §4 principle 3** (objective cross-surface inconsistency). **Root cause = cohesion-by-aggregation drift on the original `3de1c2c` ship.** Scope locked at "Garden card trigger icon swap" was applied to ActiveGardenView only — MyPlantsView wasn't named in scope despite hosting the same action with the same `aria-label="Add journal entry"`. CLAUDE.md's cohesion-by-aggregation rule (locked 2026-05-14, post-`3de1c2c`) would have caught this in Pass 2 had it been active at the time of the ship. Pre-existing inline-SVG stroke-weight drift also relevant: per BUGS Post-Launch #3 (stroke weight inconsistency app-wide), MyPlantsView line 893 uses `strokeWidth="2"` vs. ICON_MAP's canonical 1.5 — fixing this with `<ICON_MAP.Edit />` also resolves the stroke-weight drift on this site as a bonus. | Replace inline SVG at [MyPlantsView.tsx:893](src/components/MyPlantsView.tsx:893) with `<ICON_MAP.Edit className="w-5 h-5" />` + add `import { ICON_MAP } from "@/lib/styleDictionary";` to MyPlantsView imports (currently missing — verified). Extend `src/app/garden/activeGardenBulkEnd.regression.test.ts` (or new `myPlantsIconCohesion.regression.test.ts`) with positive assertion `expect(myPlantsView).toContain("ICON_MAP.Edit")` + negative assertion that the inline SVG path strings are gone (prevents revert). Preview MCP mobile-viewport visual check on My Plants list view. | XS (~10 min: 2-line code change + 1 import + 2 regression-test assertions + standard plan-audit) |
| U24 | Active Garden view (Garden page) | User ended 7 plants but they still appear in Active Garden. Reported 2026-05-13. **Diagnosed 2026-05-14:** silent-failure pattern in bulk-end handler at [ActiveGardenView.tsx:749-773](src/components/ActiveGardenView.tsx:749). Line 757 (`supabase.from("grow_instances").update({status: "archived", ended_at: now}).eq("id", batch.id).eq("user_id", batchUserId)`) discards the `{data, error}` result — neither destructured nor inspected. Whatever error Supabase returns (RLS, CHECK, network, trigger), the code continues to clear bulk selection, exit bulk mode, toast `Ended N plantings` (deceptive), and call `load()` which refetches the unchanged rows → user sees plants reappear immediately. **All 6 end-plant code paths verified:** every path writes both `status` AND `ended_at` (so causes a/b/d from original triage ruled out): #1 single-end [ActiveGardenView.tsx:639](src/components/ActiveGardenView.tsx:639) via `updateWithOfflineQueue`; #2 bulk-end [ActiveGardenView.tsx:757](src/components/ActiveGardenView.tsx:757) direct ⚠ error discarded; #3 My Plants single [MyPlantsView.tsx:389](src/components/MyPlantsView.tsx:389); #4 My Plants bulk [MyPlantsView.tsx:486](src/components/MyPlantsView.tsx:486); #5 Grow Instance Modal archive [GrowInstanceModal.tsx:416](src/components/GrowInstanceModal.tsx:416); #6 Vault Plantings end batch [useVaultPlantingsHandlers.ts:76](src/hooks/useVaultPlantingsHandlers.ts:76). **User-confirmed context:** persistent across pull-to-refresh AND full app close+reopen (rules out cache/state cause c); flow used = long-press → multi-select → FAB → End batch → confirm (= path #2 above); personal view + own plants (rules out RLS-denial cause e from path #2's `.eq("user_id", batchUserId)`). Underlying error candidates remaining: (i) `softDeleteTasksForGrowInstance` line 758 or `revertProfileStatusIfNoActiveGrows` line 761 throws after update (unlikely — would only affect plants 2..7); (ii) transient network/auth-token glitch (would explain all 7 if connection blip); (iii) DB trigger reverting `status='archived'` → `'growing'` (worth grepping `supabase/migrations/` for UPDATE triggers on `grow_instances`); (iv) CHECK constraint quirk (unlikely — `archived` is in constraint list). Diagnosis plan at `C:\Users\marsh\.claude\plans\three-things-to-handle-iterative-peach.md`. **Phase A ✅ Shipped 2026-05-14 (`7207164`):** 3 handlers patched (ActiveGardenView.handleBulkEndBatch line 757 — `{data, error}` destructure + `.select("id")` + loop-with-hadError; MyPlantsView.handleBulkEndBatch line 486 — `updateWithOfflineQueue` destructure + same loop pattern; GrowInstanceModal.handleArchive line 414 — added err else-branch via `showErrorToast`). 4 sibling bulk-handler error toasts updated to friendly+refresh wording for cohesion. New regression test file `src/app/garden/activeGardenBulkEnd.regression.test.ts` with 14 it-blocks (387 → 401 tests). 3 sibling end-paths confirmed already-clean (ActiveGardenView single-end line 639; MyPlantsView single-end line 389; useVaultPlantingsHandlers line 76 — path corrected from diagnosis plan's `src/hooks/` to actual `src/app/vault/[id]/`). **Phase B ✅ CLOSED 2026-05-16 (no code change needed):** user retried bulk-end with instrumented build; debug log captured 3× `PGRST204: Could not find the 'ended_at' column of 'grow_instances' in the schema cache` errors. **Underlying cause = missing prod database column.** Migration `20250207600000_grow_instances_archived_ended.sql` (which adds `ended_at` + widens status CHECK) was never applied to user's production Supabase. Local + tests + build passed because local Supabase had the migration; prod silently failed because the column didn't exist. User applied the migration's SQL directly via Supabase Dashboard → SQL Editor, then re-applied the wider CHECK constraint from `20250211000000_phase1_schema_foundation.sql:140` (5 status values). Re-tested bulk-end on phone — succeeded. **U24 closed.** Full diagnostic chain proved Phase A instrumentation worked exactly as designed: silent-failure pattern → instrumented destructure + console.error → captured error in debug log → user pasted error string → root cause identified in minutes (would have taken hours of speculation without instrumentation). Validates the plan-audit rigor end-to-end. | ✅ Closed 2026-05-16 |

### Systemic / structural issues (require workstream-scale work)

These were reported but are larger than single bugs. See [PRODUCT_AUDIT_2026-05-07.md](PRODUCT_AUDIT_2026-05-07.md) for analysis.

| # | Issue | Workstream |
|---|-------|------------|
| S1 | Hero-photo finding during import is slow | Import flow (#1) — defer hero search |
| S2 | Original uploaded photos / receipts are discarded after AI extraction | Source-image / receipt retention (#4) |
| S3 | AI autofill load-bearing because legacy DB data is dirty | Data quality remediation (#3) + AI autofill UX (#2) |
| S4 | Per-specimen care impossible (only variety-level care) | Per-specimen care (#8) |
| S5 | Calendar bounded recurrence missing — copper protocol case requires manual cleanup | Calendar recurrence model (#9) |
| S6 | Calendar chain-snooze missing — slipping day 1 doesn't shift days 2-7 | Calendar recurrence model (#9) |
| S7 | Grow instance "profile" is underdeveloped — should be the central hub for an individual plant | Grow instance hub buildout (#10) |
| S8 | Plant profile vs grow instance terminology / conceptual confusion | Resolved by #10 + UX coherence (#6) |
| S9 | Empty / sparse-data states neglected (Journal opaque to new users, Gallery view useless without photos, etc.) | New-user discoverability (#5) |
| S10 | Visual cohesion uneven across icons, placeholders, badges | Design system pass (#7) |

---

## Security Checklist (Part 5.2 — Completed 2026-03-11)

| Check | Result |
|-------|--------|
| **RLS on all user tables** | ✅ `plant_profiles`, `seed_packets`, `grow_instances`, `journal_entries`, `tasks`, `care_schedules`, `shopping_list`, `plant_varieties` all have RLS enabled with `auth.uid() = user_id` policies. |
| **`user_id` on all INSERTs** | ✅ Full sweep of 22 source files — no violations found. Law 1 upheld across entire insert surface. |
| **No secrets in client code** | ✅ All API keys (`GOOGLE_GENERATIVE_AI_API_KEY`, `OPENAI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `TAVILY_API_KEY`) are server-only env vars. Only `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` are public (by design). |
| **Image compression on all upload paths** | ✅ Client uploads use `compressImage()` from `src/lib/compressImage.ts`. Server-side routes (`batch-import`, `find-hero-photo`, `extract-from-photo`, `save-hero-from-url`) all use Sharp after this session's fix. |
| **Soft delete on all deletions** | ✅ `plant_profiles`, `seed_packets`, `grow_instances`, `journal_entries`, `tasks` use `update({ deleted_at })`. Hard `.delete()` is only used in Settings Trash for permanent purge. |
