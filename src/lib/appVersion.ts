/**
 * Single source of truth for the user-facing app version.
 * Keep in sync with package.json "version". Displayed in Settings → Profile
 * and attached to feedback submissions as device context.
 */
export const APP_VERSION = "0.1.0";
